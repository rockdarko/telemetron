# Ansible
*.retry
.ansible/
.vault_pass
roles/local/
ops/local/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.pytest_cache/

# Editors / OS
*.swp
*.swo
.idea/
.vscode/
.DS_Store
Thumbs.db

# Agentic / AI tooling - local-only, doesn't pertain to the project itself
.planning/
.claude/
.cursor/
CLAUDE.md
AGENTS.md
GEMINI.md
.cursorrules
.codex
.aider*

# --- Private inventories ---
# Only inventory/example-homelab/ (the shipped public reference) and the
# README ship in git. Other inventories (real homelab hosts, BYO) stay local.
inventory/*/
!inventory/example-homelab/
!inventory/example-homelab/**

# --- Ansible vault (OPS-02, Phase 1 D-15) ---
# Real vault files are NEVER committed (overrides the negations above for
# example-homelab/**). The .example template IS committed.
# Naming convention: vault_<role>_<purpose>.
**/vault.yml
.vault_password
ansible-vault-password.txt

# Secrets / local
*.env
.env.local
