---
phase: 08-garage-role-backend-retargeting
plan: "03"
type: execute
wave: 3
depends_on: ["08-01", "08-02"]
files_modified:
  - playbooks/deploy_docker.yml
  - inventory/example-homelab/group_vars/all/storage.yml
  - inventory/example-homelab/group_vars/all/garage.yml
  - roles/README.md
  - docs/architecture.md
  - docs/quickstart.md
  - docs/inventory.md
  - README.md
autonomous: true
requirements: [STORE-03]
must_haves:
  truths:
    - "playbooks/deploy_docker.yml references role: garage instead of role: minio"
    - "roles/minio/ directory does not exist"
    - "inventory/example-homelab/group_vars/all/minio.yml does not exist"
    - "inventory/example-homelab/group_vars/all/garage.yml exists with Garage operator knobs"
    - "storage.yml uses telemetron_garage_buckets instead of telemetron_minio_buckets"
    - "grep -r 'minio' roles/ playbooks/ inventory/example-homelab/ returns zero matches outside inline migration comments"
    - "All four documentation files reference Garage instead of MinIO"
    - "ansible-playbook --syntax-check playbooks/deploy_docker.yml exits 0"
  artifacts:
    - path: "playbooks/deploy_docker.yml"
      provides: "Garage in the storage slot"
      contains: "role: garage"
    - path: "inventory/example-homelab/group_vars/all/storage.yml"
      provides: "Garage bucket var name"
      contains: "telemetron_garage_buckets"
    - path: "inventory/example-homelab/group_vars/all/garage.yml"
      provides: "Garage operator knobs"
      contains: "garage_publish_host"
  key_links:
    - from: "playbooks/deploy_docker.yml"
      to: "roles/garage/tasks/main.yml"
      via: "role: garage"
      pattern: "role: garage"
    - from: "inventory/example-homelab/group_vars/all/storage.yml"
      to: "roles/garage/defaults/main.yml"
      via: "telemetron_garage_buckets"
      pattern: "telemetron_garage_buckets"
---

<objective>
Remove all MinIO artifacts from the codebase, wire Garage into the deploy playbook, rename inventory files and vars, and update all documentation to reflect Garage replacing MinIO.

Purpose: STORE-03 requires zero MinIO references remaining in roles/, playbooks/, and inventory/ (excluding inline migration-context comments). The deploy playbook must show garage in the storage slot. All four operator-facing docs must reference Garage with correct ports and var names.

Output: MinIO fully removed; Garage fully integrated into the codebase surface.
</objective>

<execution_context>
@/home/darko/.claude/get-shit-done/workflows/execute-plan.md
@/home/darko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/darko/git/rockdarko/telemetron/.planning/PROJECT.md
@/home/darko/git/rockdarko/telemetron/.planning/ROADMAP.md
@/home/darko/git/rockdarko/telemetron/.planning/STATE.md
@/home/darko/git/rockdarko/telemetron/.planning/phases/08-garage-role-backend-retargeting/08-CONTEXT.md
@/home/darko/git/rockdarko/telemetron/.planning/phases/08-garage-role-backend-retargeting/08-01-SUMMARY.md
@/home/darko/git/rockdarko/telemetron/.planning/phases/08-garage-role-backend-retargeting/08-02-SUMMARY.md

<interfaces>
<!-- Current state of files being modified. -->

From playbooks/deploy_docker.yml line 36:
    - role: minio
      tags:
        - minio
  Comment at line 74: "minio -> loki -> tempo -> ..."

From inventory/example-homelab/group_vars/all/minio.yml:
  minio_publish_host: false
  minio_container_name: telemetron-minio

From inventory/example-homelab/group_vars/all/storage.yml lines 22-27:
  telemetron_minio_buckets: [loki-chunks, tempo-traces, mimir-blocks, mimir-ruler, mimir-alerts]

From roles/README.md:
  Line 16: minio role in the component table
  Line 39: mention of minio role port as Phase 1 template
  Line 59: roles/minio/README.md as canonical README template reference

From docs/architecture.md (MinIO references at multiple lines):
  Component table, dependency graph, ASCII art, port allocation table, storage details

From docs/quickstart.md:
  Lines 73, 89-90, 93: MinIO flags, credentials, S3 alias explanation

From docs/inventory.md:
  Lines 20, 24, 54, 73-80, 90-92: MinIO file, var names, credentials, alias explanation

From README.md line 41:
  MinIO in the component table
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove MinIO role, swap playbook, rename inventory files</name>
  <files>
    playbooks/deploy_docker.yml
    inventory/example-homelab/group_vars/all/storage.yml
    inventory/example-homelab/group_vars/all/garage.yml
  </files>
  <read_first>
    playbooks/deploy_docker.yml
    inventory/example-homelab/group_vars/all/storage.yml
    inventory/example-homelab/group_vars/all/minio.yml
    roles/garage/defaults/main.yml
    .planning/phases/08-garage-role-backend-retargeting/08-CONTEXT.md
  </read_first>
  <action>
    Three code changes plus one directory deletion and one file rename.

    Step A: Delete roles/minio/ directory entirely (git rm -r roles/minio/). This removes defaults, tasks, templates, handlers, meta, vars, and README. Per STORE-03 this directory must not exist.

    Step B: Rename inventory file (git mv inventory/example-homelab/group_vars/all/minio.yml inventory/example-homelab/group_vars/all/garage.yml per D-120). Then rewrite the content:
    - Header comment: "Telemetron -- Garage operator knobs (Phase 8, D-120)"
    - garage_publish_host: false with comment about S3 API :3900 and admin :3903 not exposed on host
    - garage_container_name: telemetron-garage with comment about garage network alias
    - Match the structure shown in PATTERNS.md garage.yml translation

    Step C: Edit playbooks/deploy_docker.yml:
    - Line 36: change "role: minio" to "role: garage" and tags from "minio" to "garage"
    - Line 10: update the --tags example comment from "--tags minio" to "--tags garage"
    - Line 74 (deploy order comment): change "minio -> loki -> tempo -> ..." to "garage -> loki -> tempo -> ..."

    Step D: Edit inventory/example-homelab/group_vars/all/storage.yml (D-119):
    - Rename telemetron_minio_buckets to telemetron_garage_buckets
    - Update all comments in that block from "MinIO"/"minio" to "Garage"/"garage"
    - Update the file header comment from "MinIO bucket names" to "Garage bucket names"

    Step E: Run ansible-playbook --syntax-check playbooks/deploy_docker.yml to confirm the playbook parses correctly with the role swap.
  </action>
  <verify>
    <automated>! test -d roles/minio && test -f inventory/example-homelab/group_vars/all/garage.yml && ! test -f inventory/example-homelab/group_vars/all/minio.yml && grep -q 'role: garage' playbooks/deploy_docker.yml && ! grep -q 'role: minio' playbooks/deploy_docker.yml && grep -q 'telemetron_garage_buckets' inventory/example-homelab/group_vars/all/storage.yml && ! grep -q 'telemetron_minio_buckets' inventory/example-homelab/group_vars/all/storage.yml && cd /home/darko/git/rockdarko/telemetron && ansible-playbook --syntax-check playbooks/deploy_docker.yml 2>&1 | tail -1 | grep -q 'Syntax.*OK\|playbook.*syntax is ok' && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - roles/minio/ directory does not exist
    - inventory/example-homelab/group_vars/all/minio.yml does not exist
    - inventory/example-homelab/group_vars/all/garage.yml exists with garage_publish_host: false and garage_container_name: telemetron-garage
    - playbooks/deploy_docker.yml line 36 area has role: garage with tags: garage
    - Deploy order comment says "garage -> loki -> tempo -> ..."
    - storage.yml contains telemetron_garage_buckets (not telemetron_minio_buckets)
    - ansible-playbook --syntax-check playbooks/deploy_docker.yml exits 0
  </acceptance_criteria>
  <done>MinIO role directory deleted; playbook references Garage; inventory files renamed and retargeted; syntax check passes</done>
</task>

<task type="auto">
  <name>Task 2: Update all documentation (roles/README, docs/, top-level README)</name>
  <files>
    roles/README.md
    docs/architecture.md
    docs/quickstart.md
    docs/inventory.md
    README.md
  </files>
  <read_first>
    roles/README.md
    docs/architecture.md
    docs/quickstart.md
    docs/inventory.md
    README.md
    roles/garage/defaults/main.yml
    .planning/phases/08-garage-role-backend-retargeting/08-CONTEXT.md
  </read_first>
  <action>
    Five documentation files updated to replace MinIO with Garage references.

    roles/README.md:
    - Line 16: change "minio" to "garage" in the component table and description from "S3-compatible object storage" to "S3-compatible object storage (Garage)" or similar
    - Line 39: change the "minio role port" reference to "garage role" (the canonical template reference is now the garage role since minio is deleted)
    - Line 59: change "roles/minio/README.md" to "roles/garage/README.md" as the canonical template reference

    docs/architecture.md — multiple edits:
    - Component table: replace MinIO row (image, ports, description) with Garage row: dxflrs/garage:v2.3.0, ports 3900/3901/3903, single-node, S3-compatible object storage replacing MinIO
    - ASCII art diagrams: replace "MinIO" labels with "Garage"
    - Port allocation table: replace MinIO API 9000/9000 and MinIO Console 9001/9001 rows with Garage S3 API 3900/3900, Garage Admin 3903/3903, Garage RPC 3901 (internal only)
    - Storage dependency sections: change "MinIO loki-chunks bucket" to "Garage loki-chunks bucket" etc.
    - Storage table: change "MinIO" column entries to "Garage"
    - Known limitations section: REPLACE the "MinIO community edition is archived" note with a note that Garage v2.3.0 has replaced MinIO as of v1.1.0. Keep the note concise — one sentence about the migration.

    docs/quickstart.md:
    - Line 73 area: update the "edit files" list from "MinIO retention" context to "Garage" context if applicable
    - Lines 89-90: replace minio_root_user and minio_root_password with the four Garage secret keys (garage_admin_token, garage_rpc_secret, garage_s3_access_key_id, garage_s3_secret_key) in the secrets documentation
    - Line 93: update the S3 alias explanation from MinIO root to Garage shared key
    - Update any container count references (telemetron_minio -> telemetron-garage, port 9000 -> 3900)

    docs/inventory.md:
    - Line 20: change "MinIO bucket names" to "Garage bucket names" in the storage.yml description
    - Line 24: change "minio.yml" to "garage.yml" in the file listing
    - Line 54: change telemetron_minio_buckets to telemetron_garage_buckets in the variables table
    - Lines 73-80: replace minio_root_user/minio_root_password table entries with garage_admin_token, garage_rpc_secret, garage_s3_access_key_id, garage_s3_secret_key
    - Lines 75-80: update the per-backend alias explanation from minio_root_user to garage_s3_access_key_id
    - Line 90-92: update the "future hardening" note from "split MinIO into per-backend users" to "split Garage into per-backend keys"

    README.md (top-level):
    - Line 41: replace MinIO row in component table with Garage: dxflrs/garage:v2.3.0, "S3-compatible object storage"

    For ALL documentation edits: ensure zero "MinIO" references remain in the operational/current context. Historical references (like "replaces MinIO" or "migrated from MinIO") are acceptable as one-time migration context but should be brief.
  </action>
  <verify>
    <automated>grep -c -i 'minio' roles/README.md | xargs test 0 -eq && grep -q 'garage' roles/README.md && grep -q 'dxflrs/garage' docs/architecture.md && grep -q 'garage_admin_token' docs/quickstart.md && grep -q 'telemetron_garage_buckets' docs/inventory.md && grep -q 'garage' README.md && ! grep -qi 'minio' README.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - roles/README.md component table shows "garage" in the storage slot, not "minio"
    - roles/README.md canonical README reference points to roles/garage/README.md
    - docs/architecture.md component table has Garage row with dxflrs/garage:v2.3.0 and ports 3900/3903
    - docs/architecture.md port allocation table has Garage S3 API 3900 and Garage Admin 3903 (no MinIO 9000/9001)
    - docs/architecture.md known limitations section no longer says "MinIO community edition is archived" as a current concern
    - docs/quickstart.md secrets section lists garage_admin_token, garage_rpc_secret, garage_s3_access_key_id, garage_s3_secret_key
    - docs/inventory.md file listing shows garage.yml not minio.yml
    - docs/inventory.md variables table shows telemetron_garage_buckets
    - docs/inventory.md secrets table shows garage_* vars not minio_root_*
    - README.md component table shows Garage not MinIO
    - grep -ri 'minio' roles/README.md docs/ README.md returns zero matches (excluding intentional historical/migration notes)
  </acceptance_criteria>
  <done>All five documentation files updated; zero operational MinIO references; Garage ports, images, and vars documented correctly across the full operator-facing documentation surface</done>
</task>

<task type="auto">
  <name>Task 3: Final codebase-wide MinIO sweep and CLAUDE.md update</name>
  <files>
    CLAUDE.md
  </files>
  <read_first>
    CLAUDE.md
  </read_first>
  <action>
    Two-part final verification and cleanup.

    Part A: Run a comprehensive grep sweep to confirm STORE-03 completion:
    - grep -ri 'minio' roles/ playbooks/ inventory/example-homelab/ docs/ README.md
    - The ONLY acceptable remaining matches are:
      (a) Inline comments that document the migration context (e.g., "replaces MinIO" or "migrated from MinIO") — these are acceptable
      (b) Zero matches is ideal
    - If any operational references (variable names, image names, endpoint URLs, container names) remain, fix them

    Part B: Update CLAUDE.md Technology Stack section:
    - In the "TL;DR Per-Role Recommendation Sheet" table: replace the minio row with garage (dxflrs/garage:v2.3.0, HIGH confidence, Ship)
    - In the "Three Decisions That Block Everything Else" section 1 (MinIO crisis): add a resolution note at the top indicating this was resolved in v1.1.0 Phase 8 by replacing MinIO with Garage v2.3.0
    - In the "Recommended Stack -- Detailed / Datastores" table: replace the MinIO row with Garage
    - In the "What NOT to Use" table: update the MinIO row to note it has been replaced by Garage (not just deferred)
    - In the port allocation snapshot: replace MinIO API 9000/9000 and MinIO Console 9001/9001 with Garage S3 API 3900, Garage Admin 3903, Garage RPC 3901 (internal only)
    - In the Docker Compose vs systemd-unit table: if MinIO is mentioned, update to Garage
    - Keep the information about the MinIO archival as historical context in the "Three Decisions" section (it explains WHY Garage was chosen)
  </action>
  <verify>
    <automated>MINIO_HITS=$(grep -ri 'minio' roles/ playbooks/ inventory/example-homelab/ 2>/dev/null | grep -v '^Binary' | grep -vi 'replaces\|replaced\|migrated\|migration\|formerly\|was minio\|archived' | wc -l); test "$MINIO_HITS" -eq 0 && grep -q 'dxflrs/garage' CLAUDE.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - grep -ri 'minio' roles/ playbooks/ inventory/example-homelab/ returns zero operational matches (only migration-context comments if any)
    - CLAUDE.md Technology Stack TL;DR table contains garage row with dxflrs/garage:v2.3.0
    - CLAUDE.md port allocation snapshot contains Garage S3 API 3900 and Garage Admin 3903
    - CLAUDE.md Three Decisions section 1 has a resolution note about Garage v2.3.0
    - roles/minio/ directory does not exist (confirmed in Task 1 but re-verified here)
  </acceptance_criteria>
  <done>Codebase-wide MinIO sweep complete with zero operational references remaining; CLAUDE.md technology stack updated to reflect Garage as the shipped storage backend</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git repository | Removing roles/minio/ and minio.yml; ensuring no secrets leak into committed files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09 | Information Disclosure | git history of roles/minio/ | accept | MinIO role contained no secrets (credentials were in gitignored secrets.yml); role deletion via git rm is safe; historical commits remain in git log as expected |
| T-08-10 | Tampering | Documentation accuracy | mitigate | Final grep sweep ensures no stale MinIO references remain that could mislead operators into configuring a non-existent MinIO endpoint |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs in this plan; all changes are file deletions, renames, and content edits |
</threat_model>

<verification>
- roles/minio/ does not exist
- inventory/example-homelab/group_vars/all/minio.yml does not exist
- grep -ri 'minio' roles/ playbooks/ inventory/example-homelab/ returns only migration-context comments (or zero matches)
- grep -q 'role: garage' playbooks/deploy_docker.yml
- grep -q 'telemetron_garage_buckets' inventory/example-homelab/group_vars/all/storage.yml
- ansible-playbook --syntax-check playbooks/deploy_docker.yml exits 0
- All five doc files reference Garage not MinIO in their component/port/var tables
</verification>

<success_criteria>
The MinIO role, inventory file, and all operational references are completely removed from the codebase. Garage is wired into deploy_docker.yml as the storage role. All four operator-facing docs and the top-level README reference Garage with correct ports (3900/3903), image (dxflrs/garage:v2.3.0), and var names (garage_*). CLAUDE.md technology stack reflects the shipped state. A comprehensive codebase grep for "minio" returns only historical migration context, not any operational reference.
</success_criteria>

<output>
Create `.planning/phases/08-garage-role-backend-retargeting/08-03-SUMMARY.md` when done
</output>
