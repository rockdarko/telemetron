---
phase: 08-garage-role-backend-retargeting
plan: "02"
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - roles/loki/defaults/main.yml
  - roles/loki/templates/loki.yaml.j2
  - roles/loki/tasks/verify.yml
  - roles/tempo/defaults/main.yml
  - roles/tempo/templates/tempo.yaml.j2
  - roles/tempo/tasks/verify.yml
  - roles/mimir/defaults/main.yml
  - roles/mimir/templates/mimir.yaml.j2
  - roles/mimir/tasks/verify.yml
  - roles/prometheus/defaults/main.yml
  - roles/prometheus/templates/prometheus.yml.j2
  - inventory/example-homelab/group_vars/all/secrets.yml.example
autonomous: true
requirements: [STORE-02, OPS-01]
must_haves:
  truths:
    - "Loki S3 endpoint default points to http://garage:3900 (scheme prefix preserved)"
    - "Tempo S3 endpoint default points to garage:3900 (no scheme prefix)"
    - "Mimir S3 endpoint default points to garage:3900 (no scheme prefix)"
    - "Loki template uses object_store: aws per STORE-02"
    - "Tempo and Mimir templates have MinIO inline comments updated to Garage"
    - "All three backend verify.yml files use docker_container_exec against the Garage container for bucket verification"
    - "Tempo has a new bucket verify step (D-118)"
    - "Prometheus scrape config has a 4th hardcoded job targeting garage:3903/metrics with bearer auth"
    - "secrets.yml.example has garage_admin_token, garage_rpc_secret replacing minio_root_user/minio_root_password"
    - "secrets.yml.example does NOT have garage_s3_access_key_id or garage_s3_secret_key (D-112: auto-generated)"
    - "Per-backend S3 credential aliases default to dynamically loaded garage vars (set_fact from D-112 host file)"
    - "No mc image vars remain in loki or mimir defaults"
  artifacts:
    - path: "roles/loki/defaults/main.yml"
      provides: "Retargeted S3 endpoint to Garage"
      contains: "http://garage:3900"
    - path: "roles/tempo/defaults/main.yml"
      provides: "Retargeted S3 endpoint to Garage"
      contains: "garage:3900"
    - path: "roles/mimir/defaults/main.yml"
      provides: "Retargeted S3 endpoint to Garage"
      contains: "garage:3900"
    - path: "roles/loki/tasks/verify.yml"
      provides: "Garage-based bucket verification replacing mc one-shot"
      contains: "docker_container_exec"
    - path: "roles/tempo/tasks/verify.yml"
      provides: "New Garage bucket verify step (D-118)"
      contains: "garage bucket info"
    - path: "roles/mimir/tasks/verify.yml"
      provides: "Garage-based bucket verification replacing mc one-shot"
      contains: "docker_container_exec"
    - path: "roles/tempo/templates/tempo.yaml.j2"
      provides: "MinIO inline comments updated to Garage"
      contains: "Garage"
    - path: "roles/mimir/templates/mimir.yaml.j2"
      provides: "MinIO inline comments updated to Garage"
      contains: "Garage"
    - path: "roles/prometheus/templates/prometheus.yml.j2"
      provides: "4th scrape job for Garage metrics"
      contains: "job_name: garage"
    - path: "inventory/example-homelab/group_vars/all/secrets.yml.example"
      provides: "Garage credential placeholders replacing MinIO"
      contains: "garage_admin_token"
  key_links:
    - from: "roles/loki/tasks/verify.yml"
      to: "roles/garage/defaults/main.yml"
      via: "garage_container_name cross-role var"
      pattern: "garage_container_name"
    - from: "roles/prometheus/templates/prometheus.yml.j2"
      to: "roles/garage/defaults/main.yml"
      via: "garage_admin_token cross-role var"
      pattern: "garage_admin_token"
---

<objective>
Retarget all three backend roles (Loki, Tempo, Mimir) from MinIO to Garage S3 endpoints, migrate verify tasks from minio/mc one-shot containers to docker_container_exec against Garage, update MinIO inline comments in Tempo and Mimir templates, add Garage self-metrics scrape job to Prometheus, and update the secrets.yml.example credential surface.

Purpose: STORE-02 requires all three backends to read/write through Garage's S3 API. OPS-01 requires Garage self-metrics scraped with bearer auth. The verify migration (D-117/D-118) is bundled here because it touches the same verify.yml files as the endpoint retargeting. Template comment updates for tempo.yaml.j2 and mimir.yaml.j2 are included per PATTERNS.md to eliminate stale MinIO references.

Output: 12 modified files completing the S3 retargeting and metrics integration.
</objective>

<execution_context>
@/home/darko/.claude/get-shit-done/workflows/execute-plan.md
@/home/darko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/darko/git/rockdarko/telemetron/.planning/PROJECT.md
@/home/darko/git/rockdarko/telemetron/.planning/ROADMAP.md
@/home/darko/git/rockdarko/telemetron/.planning/STATE.md
@/home/darko/git/rockdarko/telemetron/.planning/phases/08-garage-role-backend-retargeting/08-CONTEXT.md
@/home/darko/git/rockdarko/telemetron/.planning/phases/08-garage-role-backend-retargeting/08-RESEARCH.md
@/home/darko/git/rockdarko/telemetron/.planning/phases/08-garage-role-backend-retargeting/08-PATTERNS.md
@/home/darko/git/rockdarko/telemetron/.planning/phases/08-garage-role-backend-retargeting/08-01-SUMMARY.md

<interfaces>
<!-- Existing patterns the executor needs. -->

From roles/loki/defaults/main.yml (S3 block, lines 48-56):
  loki_s3_endpoint: "http://minio:9000"  # Loki format: http:// prefix
  loki_s3_bucket: loki-chunks
  loki_s3_region: us-east-1
  loki_s3_force_path_style: true
  loki_s3_insecure: true

From roles/loki/defaults/main.yml (mc vars to remove, lines 129-132):
  loki_mc_image: minio/mc
  loki_mc_image_tag: "RELEASE.2025-04-16T18-13-26Z"

From roles/tempo/defaults/main.yml (S3 block, lines 52-60):
  tempo_s3_endpoint: "minio:9000"  # Tempo format: NO http:// prefix
  No mc vars to remove.

From roles/mimir/defaults/main.yml (S3 block, lines 43-50):
  mimir_s3_endpoint: "minio:9000"  # Mimir format: NO http:// prefix
  mimir_mc_image/mimir_mc_image_tag at lines 138-142 to remove.

From roles/loki/templates/loki.yaml.j2 line 40:
  object_store: s3  # STORE-02 requires changing to: aws

From roles/tempo/templates/tempo.yaml.j2:
  S3 config block (lines 35-43) with MinIO inline comments to update to Garage

From roles/mimir/templates/mimir.yaml.j2:
  S3 config block (lines 25-57) with MinIO inline comments to update to Garage

From roles/prometheus/defaults/main.yml (target pattern):
  prometheus_otel_self_target: "otel:8888"
  prometheus_otel_metrics_target: "otel:8889"
  prometheus_node_exporter_target: "node-exporter:9100"

From roles/prometheus/templates/prometheus.yml.j2:
  Three existing scrape jobs (otel_self, otel_metrics, node_exporter) each with
  static_configs, labels, and metric_relabel_configs. The new garage job adds
  authorization.credentials (new pattern in this template).

From inventory/example-homelab/group_vars/all/secrets.yml.example:
  minio_root_user/minio_root_password at lines 32-33
  loki/tempo/mimir aliases at lines 41-56 pointing to minio_root_*
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retarget Loki, Tempo, Mimir S3 defaults and templates to Garage</name>
  <files>
    roles/loki/defaults/main.yml
    roles/loki/templates/loki.yaml.j2
    roles/tempo/defaults/main.yml
    roles/tempo/templates/tempo.yaml.j2
    roles/mimir/defaults/main.yml
    roles/mimir/templates/mimir.yaml.j2
  </files>
  <read_first>
    roles/loki/defaults/main.yml
    roles/loki/templates/loki.yaml.j2
    roles/tempo/defaults/main.yml
    roles/tempo/templates/tempo.yaml.j2
    roles/mimir/defaults/main.yml
    roles/mimir/templates/mimir.yaml.j2
    .planning/phases/08-garage-role-backend-retargeting/08-PATTERNS.md
  </read_first>
  <action>
    Six file edits -- each a surgical change to the S3 endpoint default, template comments, and cleanup of mc image vars.

    roles/loki/defaults/main.yml:
    - Change loki_s3_endpoint from "http://minio:9000" to "http://garage:3900" (preserve http:// scheme prefix per RESEARCH Pitfall 7)
    - Update the inline comment block (lines 48-51) to reference Garage instead of MinIO. Keep the note about Loki's http:// prefix format requirement.
    - DELETE the mc image block entirely (lines ~129-132): loki_mc_image and loki_mc_image_tag plus their comment. These are no longer needed (STORE-03 -- mc image removal).

    roles/loki/templates/loki.yaml.j2:
    - Change line 40 from object_store: s3 to object_store: aws (per STORE-02 requirement "Loki uses object_store: aws" -- defensive G-6 mitigation for Garage compatibility)
    - Update inline comments in the aws: config block (lines 50-57) from "MinIO" references to "Garage"

    roles/tempo/defaults/main.yml:
    - Change tempo_s3_endpoint from "minio:9000" to "garage:3900" (NO scheme prefix -- Tempo format per Pitfall 7)
    - Update the inline comment block (lines 52-55) to reference Garage instead of MinIO
    - Tempo has no mc image vars -- no deletions needed

    roles/tempo/templates/tempo.yaml.j2:
    - Update any MinIO inline comments in the S3 config block (lines 35-43) to reference Garage instead of MinIO. Do NOT change the S3 config keys themselves -- only the human-readable comments.

    roles/mimir/defaults/main.yml:
    - Change mimir_s3_endpoint from "minio:9000" to "garage:3900" (NO scheme prefix -- Mimir format per Pitfall 7)
    - Update the inline comment block (lines 43-46) to reference Garage instead of MinIO
    - DELETE the mc image block (lines ~138-142): mimir_mc_image and mimir_mc_image_tag plus their comment (STORE-03)

    roles/mimir/templates/mimir.yaml.j2:
    - Update any MinIO inline comments in the S3 config block (lines 25-57) to reference Garage instead of MinIO. Do NOT change the S3 config keys themselves -- only the human-readable comments.
  </action>
  <verify>
    <automated>grep -q 'http://garage:3900' roles/loki/defaults/main.yml && grep -v '^#' roles/loki/defaults/main.yml | grep -qv 'minio' && grep -q 'object_store: aws' roles/loki/templates/loki.yaml.j2 && grep -q '"garage:3900"' roles/tempo/defaults/main.yml && grep -q '"garage:3900"' roles/mimir/defaults/main.yml && ! grep -q 'mc_image' roles/loki/defaults/main.yml && ! grep -q 'mc_image' roles/mimir/defaults/main.yml && ! grep -qi 'minio' roles/tempo/templates/tempo.yaml.j2 && ! grep -qi 'minio' roles/mimir/templates/mimir.yaml.j2 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - roles/loki/defaults/main.yml contains loki_s3_endpoint: "http://garage:3900" (with http:// scheme)
    - roles/loki/defaults/main.yml contains zero loki_mc_image or loki_mc_image_tag lines
    - roles/loki/templates/loki.yaml.j2 line 40 contains object_store: aws (not s3)
    - roles/tempo/defaults/main.yml contains tempo_s3_endpoint: "garage:3900" (no scheme)
    - roles/tempo/templates/tempo.yaml.j2 contains zero MinIO references in inline comments
    - roles/mimir/defaults/main.yml contains mimir_s3_endpoint: "garage:3900" (no scheme)
    - roles/mimir/defaults/main.yml contains zero mimir_mc_image or mimir_mc_image_tag lines
    - roles/mimir/templates/mimir.yaml.j2 contains zero MinIO references in inline comments
    - grep -r 'minio:9000' roles/loki/ roles/tempo/ roles/mimir/ returns zero matches
  </acceptance_criteria>
  <done>All three backend S3 endpoints point to garage:3900 with correct per-backend scheme format; Loki object_store is aws; all mc image vars removed; Tempo and Mimir template comments updated from MinIO to Garage</done>
</task>

<task type="auto">
  <name>Task 2: Migrate verify tasks to docker_container_exec + add Prometheus Garage scrape + update secrets</name>
  <files>
    roles/loki/tasks/verify.yml
    roles/tempo/tasks/verify.yml
    roles/mimir/tasks/verify.yml
    roles/prometheus/defaults/main.yml
    roles/prometheus/templates/prometheus.yml.j2
    inventory/example-homelab/group_vars/all/secrets.yml.example
  </files>
  <read_first>
    roles/loki/tasks/verify.yml
    roles/tempo/tasks/verify.yml
    roles/mimir/tasks/verify.yml
    roles/prometheus/defaults/main.yml
    roles/prometheus/templates/prometheus.yml.j2
    inventory/example-homelab/group_vars/all/secrets.yml.example
    .planning/phases/08-garage-role-backend-retargeting/08-PATTERNS.md
    .planning/phases/08-garage-role-backend-retargeting/08-RESEARCH.md
  </read_first>
  <action>
    Six file edits -- verify task migration, Prometheus scrape job addition, and secrets surface update.

    roles/loki/tasks/verify.yml (D-117):
    - REPLACE Step 3 entirely (lines 63-98: the mc one-shot "Assert loki-chunks bucket received an object" block) with a docker_container_exec block:
      Name: "Verify loki-chunks bucket exists in Garage"
      Module: community.docker.docker_container_exec
      container: "{{ garage_container_name }}"
      command: /garage bucket info {{ loki_s3_bucket }}
      changed_when: false
      register: loki_verify_bucket
      failed_when: loki_verify_bucket.rc is defined and loki_verify_bucket.rc != 0
      tags: loki, loki-verify
    - Update the file header comment (line 3) from "assert an object key landed in MinIO's loki-chunks bucket via a one-shot mc container" to reference Garage bucket verification via docker_container_exec
    - Update Step 3 comment to explain D-117 migration from mc to docker_container_exec

    roles/mimir/tasks/verify.yml (D-117):
    - REPLACE Step 2 entirely (lines 104-130: the mc one-shot "Assert all three required Mimir buckets are reachable" block) with a docker_container_exec loop:
      Name: "Verify all three required Mimir buckets exist in Garage"
      Module: community.docker.docker_container_exec
      container: "{{ garage_container_name }}"
      command: /garage bucket info {{ item }}
      loop: ["{{ mimir_blocks_bucket }}", "{{ mimir_ruler_bucket }}", "{{ mimir_alerts_bucket }}"]
      changed_when: false
      register: mimir_verify_buckets
      failed_when: mimir_verify_buckets.rc is defined and mimir_verify_buckets.rc != 0
      tags: mimir, mimir-verify
    - Update the file header comment (lines 1-5) from MinIO references to Garage
    - Update Step 2 comment to explain D-117 migration

    roles/tempo/tasks/verify.yml (D-118 -- NEW step):
    - REPLACE the Step 3 intentional-omission comment block (lines 136-144) with an active docker_container_exec verification step:
      Comment: "Step 3 (D-118): assert tempo-traces bucket exists in Garage. Added in Phase 8 alongside the migration from minio/mc to docker_container_exec. All three backends now have consistent bucket verification."
      Name: "Verify tempo-traces bucket exists in Garage"
      Module: community.docker.docker_container_exec
      container: "{{ garage_container_name }}"
      command: /garage bucket info {{ tempo_s3_bucket }}
      changed_when: false
      register: tempo_verify_bucket
      failed_when: tempo_verify_bucket.rc is defined and tempo_verify_bucket.rc != 0
      tags: tempo, tempo-verify
    - Update the file header comment (lines 1-7) from MinIO references to Garage

    roles/prometheus/defaults/main.yml (D-114):
    - Add new default var: prometheus_garage_target: "garage:3903" (following the existing prometheus_*_target naming pattern, placed after prometheus_node_exporter_target)

    roles/prometheus/templates/prometheus.yml.j2 (D-114, D-115, D-116):
    - Add a 4th hardcoded scrape job block AFTER the node_exporter job block and BEFORE the extra_scrape_configs loop (line ~82):
      job_name: garage with a D-114 inline comment
      static_configs targeting {{ prometheus_garage_target }} with labels service: garage
      authorization.credentials: {{ garage_admin_token }} (D-115 -- bearer token inline, new pattern in this template)
      Same metric_relabel_configs as the other three jobs (prometheus_default_relabel_drop_regex and prometheus_default_relabel_id_catchall_regex)
    - This is the first job in this template with an authorization: block -- add an inline comment noting this is bearer auth for the Garage admin API metrics endpoint

    inventory/example-homelab/group_vars/all/secrets.yml.example (D-110, D-111, D-112, D-113):
    - REPLACE the Phase 1 MinIO block (lines 26-33: minio_root_user and minio_root_password) with a Phase 8 Garage block containing: garage_admin_token: CHANGE_ME, garage_rpc_secret: CHANGE_ME. Add comments noting D-110 for admin_token, generate rpc_secret with openssl rand -hex 32. NOTE per D-112: do NOT add garage_s3_access_key_id or garage_s3_secret_key here -- S3 credentials are auto-generated by bootstrap and persisted to the host file, not operator-supplied in secrets.yml.
    - UPDATE the Phase 2 Loki/Tempo/Mimir alias blocks (lines 35-56): per D-113, the per-backend S3 credential aliases should default to the dynamically loaded garage vars. Update comments to note that garage_s3_access_key_id and garage_s3_secret_key are set_fact'd by the garage role bootstrap from the D-112 host file, and these aliases reference those facts. Change loki_s3_access_key to "{{ garage_s3_access_key_id }}", loki_s3_secret_key to "{{ garage_s3_secret_key }}", and same for tempo_* and mimir_* aliases.
  </action>
  <verify>
    <automated>grep -q 'docker_container_exec' roles/loki/tasks/verify.yml && grep -q 'garage bucket info' roles/loki/tasks/verify.yml && grep -q 'docker_container_exec' roles/mimir/tasks/verify.yml && grep -q 'garage bucket info' roles/mimir/tasks/verify.yml && grep -q 'docker_container_exec' roles/tempo/tasks/verify.yml && grep -q 'garage bucket info' roles/tempo/tasks/verify.yml && grep -q 'prometheus_garage_target' roles/prometheus/defaults/main.yml && grep -q 'job_name: garage' roles/prometheus/templates/prometheus.yml.j2 && grep -q 'authorization' roles/prometheus/templates/prometheus.yml.j2 && grep -q 'garage_admin_token' inventory/example-homelab/group_vars/all/secrets.yml.example && ! grep -q 'garage_s3_access_key_id' inventory/example-homelab/group_vars/all/secrets.yml.example && ! grep -q 'minio_root' inventory/example-homelab/group_vars/all/secrets.yml.example && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - roles/loki/tasks/verify.yml Step 3 uses community.docker.docker_container_exec with /garage bucket info, NOT minio/mc one-shot
    - roles/mimir/tasks/verify.yml Step 2 uses community.docker.docker_container_exec loop with /garage bucket info for 3 buckets, NOT minio/mc one-shot
    - roles/tempo/tasks/verify.yml has a new Step 3 using community.docker.docker_container_exec with /garage bucket info tempo_s3_bucket
    - grep -r 'mc_image\|minio/mc\|MC_HOST_local' roles/loki/tasks/ roles/mimir/tasks/ roles/tempo/tasks/ returns zero matches
    - roles/prometheus/defaults/main.yml contains prometheus_garage_target: "garage:3903"
    - roles/prometheus/templates/prometheus.yml.j2 contains job_name: garage block with authorization.credentials
    - secrets.yml.example contains garage_admin_token: CHANGE_ME and garage_rpc_secret: CHANGE_ME
    - secrets.yml.example does NOT contain garage_s3_access_key_id or garage_s3_secret_key (D-112: auto-generated)
    - secrets.yml.example contains zero minio_root_user or minio_root_password references
    - Per-backend alias vars (loki_s3_access_key, etc.) reference garage_s3_access_key_id/garage_s3_secret_key (D-113, dynamically loaded by garage bootstrap)
  </acceptance_criteria>
  <done>All three backend verify tasks use docker_container_exec against Garage; Prometheus has a 4th scrape job with bearer auth; secrets.yml.example has Garage admin credentials replacing MinIO (S3 creds auto-generated per D-112); all mc image references removed from verify files</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prometheus -> Garage admin API | Bearer token (garage_admin_token) transmitted in plaintext over Docker bridge to :3903/metrics |
| Backend verify tasks -> Garage container | docker_container_exec runs /garage CLI with no credential args (bucket info is read-only and authenticated by the admin token in the TOML config) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Information Disclosure | prometheus.yml.j2 bearer token | mitigate | Token rendered in prometheus.yml at /opt/telemetron/prometheus/prometheus.yml with mode 0600 (existing template pattern); file bind-mounted read-only; not published to host; Docker bridge traffic only |
| T-08-07 | Information Disclosure | secrets.yml.example CHANGE_ME placeholders | accept | Placeholders are safe to commit; .gitignore blocks populated secrets.yml; documented in file header |
| T-08-08 | Repudiation | Verify tasks via docker_container_exec | accept | Verify tasks are read-only (bucket info query); no state mutation; changed_when: false ensures no ansible audit trail ambiguity |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs in this plan; all changes are config edits |
</threat_model>

<verification>
- grep -r 'minio:9000' roles/ returns zero matches (all endpoints retargeted)
- grep -r 'mc_image' roles/ returns zero matches (all mc image vars removed)
- grep -r 'minio/mc' roles/ returns zero matches (all mc image references gone)
- grep -r 'MC_HOST_local' roles/ returns zero matches (all mc env patterns gone)
- grep -q 'job_name: garage' roles/prometheus/templates/prometheus.yml.j2 (scrape job exists)
- grep -q 'garage_admin_token: CHANGE_ME' inventory/example-homelab/group_vars/all/secrets.yml.example (credential placeholder exists)
- grep -qi 'minio' roles/tempo/templates/tempo.yaml.j2 returns zero matches (comments updated)
- grep -qi 'minio' roles/mimir/templates/mimir.yaml.j2 returns zero matches (comments updated)
- ansible-playbook --syntax-check playbooks/deploy_docker.yml exits 0
</verification>

<success_criteria>
Loki, Tempo, and Mimir all have their S3 endpoints pointing to garage:3900 with correct per-backend format. All three verify tasks use docker_container_exec instead of minio/mc one-shot containers. Tempo and Mimir templates have MinIO comments updated to Garage. Prometheus has a 4th scrape job targeting garage:3903/metrics with bearer auth. The secrets surface is fully migrated from MinIO to Garage credentials with S3 keys auto-generated per D-112.
</success_criteria>

<output>
Create `.planning/phases/08-garage-role-backend-retargeting/08-02-SUMMARY.md` when done
</output>
