---
phase: 06-opt-in-orchestration-docs-smoke-test
plan: 03
type: execute
wave: 3
depends_on:
  - "06-02"
files_modified:
  - docs/architecture.md
  - docs/quickstart.md
  - docs/inventory.md
autonomous: false
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03
must_haves:
  truths:
    - "`docs/architecture.md` exists and contains an Overview, Signal Flow (ASCII diagram), Components table, Port Allocation table, Monolithic Mode bullets, Storage Dependencies, and Known Debt sections"
    - "`docs/quickstart.md` exists with the canonical 8-step path: Prerequisites -> Clone -> Edit hosts -> Copy secrets -> (Optional) Vault encrypt -> Deploy -> Smoke test -> Open Grafana, plus Troubleshooting + Production Hardening subsections"
    - "`docs/inventory.md` exists with: Directory shape, group_vars/all conventions per Phase, full secrets contract enumerating the 8 keys (minio_root_user, minio_root_password, loki_s3_access_key, loki_s3_secret_key, tempo_s3_access_key, tempo_s3_secret_key, mimir_s3_access_key, mimir_s3_secret_key, grafana_admin_password), host_vars patterns, symlink-from-outside-repo pattern, v2 multi-host callout"
    - "Operator runs `docs/quickstart.md` verbatim on a fresh clone targeting leviathan and the walkthrough succeeds end-to-end (DOCS-02 acceptance)"
    - "Public docs (docs/*.md) contain ZERO D-XX references, ZERO `.planning/` paths, ZERO INSPQ heritage details beyond a one-line attribution"
    - "Public docs reference `playbooks/smoke_test.yml` (created in 06-02) as the canonical smoke step in quickstart"
  artifacts:
    - path: "docs/architecture.md"
      provides: "Public-facing terse architecture reference: components, signal flow, port matrix, monolithic-mode tradeoffs, storage deps, known debt"
      contains: "## Signal Flow"
    - path: "docs/quickstart.md"
      provides: "Zero-to-dashboards teaching walkthrough with full commands + expected outputs + troubleshooting"
      contains: "## Prerequisites"
    - path: "docs/inventory.md"
      provides: "Deep-dive inventory model: directory shape, group_vars schema, secrets contract, symlink-out-of-repo pattern, multi-host v2 callout"
      contains: "## Directory shape"
  key_links:
    - from: "docs/quickstart.md"
      to: "docs/inventory.md"
      via: "Link in 'Building your own inventory' subsection"
      pattern: "inventory.md"
    - from: "docs/quickstart.md"
      to: "playbooks/smoke_test.yml"
      via: "Step 7 'Smoke test' references the playbook by path"
      pattern: "smoke_test.yml"
    - from: "docs/quickstart.md"
      to: "roles/grafana/README.md"
      via: "Production hardening subsection cross-link to Reverse proxy section"
      pattern: "roles/grafana/README.md"
    - from: "docs/architecture.md"
      to: "CLAUDE.md Port-allocation snapshot"
      via: "Direct reproduction of the port table (distillation, no D-XX refs)"
      pattern: "## Port Allocation"
---

<objective>
Author the three M1 documentation files (`docs/architecture.md`, `docs/quickstart.md`, `docs/inventory.md`) against the fully-deployed and smoke-verified stack from Plans 06-01 and 06-02. Delivers DOCS-01, DOCS-02, DOCS-03.

Purpose: ROADMAP SC4 promises three docs "authored against a stack that actually booted." This plan synthesizes the public-facing distillation from `.planning/research/ARCHITECTURE.md` + `CLAUDE.md` (port matrix, version table) + per-role READMEs (component descriptions). Per D-103, public docs strip internal navigation (D-XX refs, `.planning/` paths, INSPQ heritage details beyond a one-line attribution).

Per D-102 voice split: `architecture.md` and `inventory.md` are terse reference (3-4 pages and 2-3 pages); `quickstart.md` is a teaching walkthrough (6-8 pages) with every command, expected output, and a troubleshooting subsection.

DOCS-02 "verified by an operator running it verbatim on a fresh target" acceptance is enforced via the live UAT on leviathan in Task 4 (D-109).

Output: 3 new Markdown files under `docs/`. All ASCII. No D-XX refs. Verified live on leviathan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-opt-in-orchestration-docs-smoke-test/06-CONTEXT.md
@.planning/phases/06-opt-in-orchestration-docs-smoke-test/06-RESEARCH.md
@.planning/phases/06-opt-in-orchestration-docs-smoke-test/06-01-SUMMARY.md
@.planning/phases/06-opt-in-orchestration-docs-smoke-test/06-02-SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@CLAUDE.md
@inventory/example-homelab/group_vars/all/secrets.yml.example
@inventory/example-homelab/group_vars/all/network.yml
@inventory/example-homelab/group_vars/all/storage.yml
@inventory/README.md

<interfaces>
<!-- Source-of-truth contracts the docs distill from. -->

From `CLAUDE.md` "Port-allocation snapshot (homelab single-host)" table:
- Grafana 3000, Loki 3100, Tempo 3200, OTel OTLP gRPC 4317, OTel OTLP HTTP 4318, Tempo OTLP gRPC internal 14317, Tempo OTLP HTTP internal 14318, Prometheus 9090, Alertmanager 9093, PromLens 8081, Karma 8082, Mimir 9009, MinIO API 9000, MinIO Console 9001, Postgres 5432, NFS 2049.

From `CLAUDE.md` "TL;DR Per-Role Recommendation Sheet":
- alertmanager: quay.io/prometheus/alertmanager:v0.32.1
- fluentbit: fluent/fluent-bit:4.2.3
- grafana: grafana/grafana-oss:13.0.1
- karma: ghcr.io/prymitive/karma:v0.130
- loki: grafana/loki:3.7.2
- mimir: grafana/mimir:3.0.6
- minio: minio/minio:RELEASE.2025-04-22T22-12-26Z
- node_exporter: quay.io/prometheus/node-exporter:v1.11.1
- opentelemetry: otel/opentelemetry-collector-contrib:0.152.0
- prometheus: prom/prometheus:v3.11.3
- promlens: prom/promlens:v0.3.0
- tempo: grafana/tempo:2.10.5

From `inventory/example-homelab/group_vars/all/secrets.yml.example` (8 keys):
- minio_root_user, minio_root_password (Phase 1)
- loki_s3_access_key, loki_s3_secret_key (Phase 2 -- alias minio root)
- tempo_s3_access_key, tempo_s3_secret_key (Phase 2 -- alias minio root)
- mimir_s3_access_key, mimir_s3_secret_key (Phase 2 -- alias minio root)
- grafana_admin_password (Phase 5)

From `inventory/example-homelab/group_vars/all/network.yml`:
- telemetron_network: telemetron
- telemetron_publish_default: false
- telemetron_tz: Etc/UTC

From `playbooks/smoke_test.yml` (created in Plan 06-02):
- Run command: `ansible-playbook -i inventory/example-homelab playbooks/smoke_test.yml --ask-vault-pass`
- Tag-scoped: `--tags log`, `--tags metric`, `--tags trace`

From `.planning/research/ARCHITECTURE.md` (source for signal flow diagram):
- Contains the canonical ASCII diagram already authored for internal planning. Distill into a 60-80-character-wide ASCII diagram for public consumption -- strip internal annotations.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author docs/architecture.md (DOCS-01) -- terse public-facing reference</name>
  <files>docs/architecture.md</files>
  <read_first>
    - .planning/research/ARCHITECTURE.md (PRIMARY source: signal flow ASCII diagram, dependency graph, port table, components table)
    - .planning/research/STACK.md (per-role recommendation table, Three Decisions for "Known Debt" section)
    - CLAUDE.md (port-allocation snapshot, version compatibility matrix, component table)
    - .planning/phases/06-opt-in-orchestration-docs-smoke-test/06-CONTEXT.md (D-102 voice split: terse reference; D-103 synthesis from research+CLAUDE.md; no D-XX refs in public docs)
    - .planning/phases/06-opt-in-orchestration-docs-smoke-test/06-RESEARCH.md (Finding I: synthesis pattern + target structure; Finding F: ASCII diagram conventions)
  </read_first>
  <action>
Create `docs/architecture.md` with this EXACT structure. Voice: terse, reference-only. Audience: technical homelab operator who already knows what observability is. Length target: 3-4 pages rendered. ASCII only. Lines under 80 chars where practical.

**Hard rules (RESEARCH Finding I2 + D-103):**
- Zero D-XX references.
- Zero `.planning/` paths.
- Zero `inspq`, `qc.ca`, `montreal`, `québec`, `francais`, `french`, `/srv/nfs/inspq`, `vault_inspq` matches (Gate 1).
- Zero non-ASCII characters.
- Reference upstream INSPQ heritage in ONE line at the top of "Overview" only -- not throughout.

Use this skeleton (fill in the specifics from the source files listed in `read_first`):

```markdown
# Telemetron architecture

Telemetron is a single-host Docker observability stack deployed via
Ansible. It runs Prometheus + Mimir for metrics, Loki for logs, Tempo for
traces, Grafana for visualization, Alertmanager + Karma for alerts, and
Fluent Bit + OTel Collector for ingestion. The stack is a clean-slate
fork of an internal Quebec-government deployment, normalized to English
and stripped of org-specific assumptions.

## Overview

Single-host Docker deployment. All components run as containers on one
user-defined bridge network (`telemetron`). The OTel Collector is the
single ingress for new instrumentation; Prometheus retains its scrape
model for infrastructure and self-metrics. The three Grafana-stack
backends (Loki, Mimir, Tempo) run in monolithic mode against a single
MinIO S3-compatible object store. Grafana is provisioned with explicit
datasource UIDs (`prometheus`, `loki`, `tempo`, `mimir`) and 5-10
starter dashboards.

## Signal Flow

[ASCII diagram showing the data path. Reproduce the diagram from
.planning/research/ARCHITECTURE.md verbatim, cleaned of internal
annotations. Width <= 80 chars. Show:
- Applications -> OTel Collector (OTLP gRPC :4317 / HTTP :4318)
- Fluent Bit tails Docker container logs -> OTel Collector (OTLP/HTTP)
- OTel Collector fans out to Loki :3100 (logs), Tempo :14317/:14318
  internal (traces), Mimir :9009 (metrics via prometheusremotewrite)
  AND exposes :8889 for Prometheus scraping
- Prometheus :9090 scrapes node_exporter :9100, OTel self-metrics :8888,
  OTel app-metrics :8889 + remote_writes to Mimir :9009
- node_exporter :9100 -> Prometheus
- Loki / Tempo / Mimir all -> MinIO :9000 (S3)
- Prometheus -> Alertmanager :9093
- Grafana :3000 queries Loki :3100, Prometheus :9090, Tempo :3200,
  Mimir :9009
- Karma :8082 reads from Alertmanager :9093
- PromLens :8081 queries Prometheus :9090
- nfsd (opt-in, host-package) -> /srv/telemetron-nfs/<hostname>/*.log
  -> Fluent Bit tail input -> OTel Collector -> Loki]

## Components

| Component | Image | Port (host) | Mode | Purpose |
|-----------|-------|------------:|------|---------|
| MinIO | minio/minio:RELEASE.2025-04-22T22-12-26Z | 9000 / 9001 | single-node | S3-compatible object storage for Loki/Tempo/Mimir |
| Loki | grafana/loki:3.7.2 | 3100 | monolithic (-target=all) | Log backend |
| Tempo | grafana/tempo:2.10.5 | 3200 (HTTP) / 14317-14318 (OTLP internal) | monolithic (-target=all) | Trace backend |
| Mimir | grafana/mimir:3.0.6 | 9009 | monolithic (-target=all) | Long-term metrics |
| OTel Collector | otel/opentelemetry-collector-contrib:0.152.0 | 4317 / 4318 | gateway | OTLP ingest + fan-out |
| Prometheus | prom/prometheus:v3.11.3 | 9090 | scrape + remote_write | Short-term metrics + alerting eval |
| node_exporter | quay.io/prometheus/node-exporter:v1.11.1 | 9100 | host exporter | Host CPU/mem/disk/net/FS |
| Fluent Bit | fluent/fluent-bit:4.2.3 | 2020 (HTTP API) | tail | Docker container log shipping |
| Alertmanager | quay.io/prometheus/alertmanager:v0.32.1 | 9093 | single | Alert routing (null receiver default) |
| Grafana | grafana/grafana-oss:13.0.1 | 3000 | OSS | Dashboards + Explore |
| Karma | ghcr.io/prymitive/karma:v0.130 | 8082 | UI | Alert triage UI |
| PromLens | prom/promlens:v0.3.0 | 8081 | UI (deprecation candidate) | PromQL editor |
| nfsd | host package (`nfs-utils` / `nfs-kernel-server`) | 2049 (opt-in) | systemd | Optional NFS server for legacy log ingestion |

## Port Allocation

| Service | Container Port | Host Port |
|---------|---------------:|----------:|
| Grafana | 3000 | 3000 |
| Loki HTTP | 3100 | 3100 |
| Tempo HTTP | 3200 | 3200 |
| OTel OTLP gRPC | 4317 | 4317 |
| OTel OTLP HTTP | 4318 | 4318 |
| Tempo OTLP gRPC (internal) | 14317 | none |
| Tempo OTLP HTTP (internal) | 14318 | none |
| Prometheus | 9090 | 9090 |
| Alertmanager | 9093 | 9093 |
| PromLens | 8080 | 8081 |
| Karma | 8080 | 8082 |
| Mimir | 9009 | 9009 |
| MinIO API | 9000 | 9000 |
| MinIO Console | 9001 | 9001 |
| node_exporter | 9100 | 9100 |
| Fluent Bit HTTP | 2020 | none (bridge only) |
| NFS (if enabled) | 2049 | 2049 |

## Monolithic Mode

The three Grafana-stack backends run as single binaries with `-target=all`:

- **Loki**: distributor, ingester, querier, query-frontend, ruler, compactor, index-gateway in one process; TSDB schema v13; S3 backend via MinIO `loki-chunks` bucket.
- **Tempo**: ingester + compactor + querier in one process; OTLP receivers on internal-only :14317/:14318 to avoid clash with OTel Collector; S3 backend via MinIO `tempo-traces` bucket.
- **Mimir**: distributor + ingester + querier + compactor + ruler + alertmanager-store in one process; multitenancy disabled; THREE distinct S3 buckets (`mimir-blocks`, `mimir-ruler`, `mimir-alerts`) -- Mimir refuses to start sharing buckets across stores.

Monolithic mode trades horizontal scalability for operational simplicity.
HA / distributed mode (HAProxy in front of multiple replicas) is a v2
milestone.

## Storage Dependencies

| Backend | Object store | Bucket(s) | On-disk state |
|---------|--------------|-----------|---------------|
| Loki | MinIO | loki-chunks | `/loki/compactor/markers/` (marker-file persistence) |
| Tempo | MinIO | tempo-traces | `/var/tempo/generator/wal` (metrics-generator WAL) |
| Mimir | MinIO | mimir-blocks, mimir-ruler, mimir-alerts | ingester memory only |
| Prometheus | n/a (TSDB local) | n/a | `/prometheus/data` (15-day retention) |
| Grafana | embedded SQLite | n/a | `telemetron_grafana_data` volume |
| Alertmanager | n/a | n/a | `telemetron_alertmanager_data` volume |
| MinIO | itself | (server) | `telemetron_minio_data` volume |

## Known Debt

- **MinIO community edition is archived.** The pinned tag (`RELEASE.2025-04-22T22-12-26Z`) is the last published community release. A future milestone replaces MinIO with Garage or SeaweedFS. Loki / Tempo / Mimir configurations all target the S3 API, so the replacement is contained to the `minio` role.
- **PromLens is frozen.** Last release v0.3.0 (Dec 2022). The Prometheus 3 UI absorbs its tree-view feature. The role README marks it as a deprecation candidate; new deployments may pass `--skip-tags promlens`.
- **Hook router is deferred.** The Alertmanager -> CI webhook bridge is a future-milestone item. M1 ships Alertmanager with a `null` default receiver; alerts are visible in Karma but not dispatched automatically.
- **MongoDB is dropped.** Was Graylog's metadata store; Graylog is not in scope for Telemetron.

## Further reading

- `docs/quickstart.md` -- zero-to-dashboards walkthrough.
- `docs/inventory.md` -- inventory model in depth.
- `roles/<name>/README.md` -- per-component documentation.
```

Replace the `[ASCII diagram...]` placeholder with the actual ASCII diagram distilled from `.planning/research/ARCHITECTURE.md`. Keep it under 80 characters wide. Use `+---+` / `|` / `-->` (ASCII arrows) for portability across terminals and GitHub raw view. Label arrows with protocol/port (e.g. `--OTLP/HTTP:4318-->`).

Hard rules check after writing:
```bash
grep -E "D-[0-9]{2,}" docs/architecture.md       # must return zero
grep -E "\.planning/" docs/architecture.md        # must return zero (paths)
grep -riE 'inspq|qc\.ca|montreal|québec|francais|french|/srv/nfs/inspq|vault_inspq' docs/architecture.md
grep -rPn '[^\x00-\x7F]' docs/architecture.md     # must return zero
```
All four must return zero matches.
  </action>
  <verify>
    <automated>
      cd /home/darko/git/rockdarko/telemetron && \
      test -f docs/architecture.md && \
      grep -q "^# Telemetron architecture" docs/architecture.md && \
      grep -q "^## Overview" docs/architecture.md && \
      grep -q "^## Signal Flow" docs/architecture.md && \
      grep -q "^## Components" docs/architecture.md && \
      grep -q "^## Port Allocation" docs/architecture.md && \
      grep -q "^## Monolithic Mode" docs/architecture.md && \
      grep -q "^## Storage Dependencies" docs/architecture.md && \
      grep -q "^## Known Debt" docs/architecture.md && \
      grep -q "grafana/loki:3.7.2" docs/architecture.md && \
      grep -q "grafana/tempo:2.10.5" docs/architecture.md && \
      grep -q "grafana/mimir:3.0.6" docs/architecture.md && \
      grep -q "otel/opentelemetry-collector-contrib:0.152.0" docs/architecture.md && \
      grep -q "prom/prometheus:v3.11.3" docs/architecture.md && \
      grep -q "fluent/fluent-bit:4.2.3" docs/architecture.md && \
      grep -q "ghcr.io/prymitive/karma:v0.130" docs/architecture.md && \
      grep -q "loki-chunks" docs/architecture.md && \
      grep -q "tempo-traces" docs/architecture.md && \
      grep -q "mimir-blocks" docs/architecture.md && \
      grep -q "mimir-ruler" docs/architecture.md && \
      grep -q "mimir-alerts" docs/architecture.md && \
      ! grep -qE "D-[0-9]{2,}" docs/architecture.md && \
      ! grep -qE "\.planning/" docs/architecture.md && \
      ! grep -qiE 'inspq|qc\.ca|montreal|québec|francais|french|/srv/nfs/inspq|vault_inspq' docs/architecture.md && \
      ! grep -rPn '[^\x00-\x7F]' docs/architecture.md
    </automated>
  </verify>
  <acceptance_criteria>
    - File `docs/architecture.md` exists.
    - Starts with `# Telemetron architecture`.
    - Contains all 8 required section headers in order: `## Overview`, `## Signal Flow`, `## Components`, `## Port Allocation`, `## Monolithic Mode`, `## Storage Dependencies`, `## Known Debt`, `## Further reading`.
    - Components table contains all 13 component rows (MinIO, Loki, Tempo, Mimir, OTel, Prometheus, node_exporter, Fluent Bit, Alertmanager, Grafana, Karma, PromLens, nfsd) and the EXACT pinned tags from CLAUDE.md.
    - Port Allocation table contains at least 14 rows.
    - Signal Flow section contains at least one literal arrow string `-->` or `--OTLP` (ASCII diagram present).
    - Known Debt section contains literal `MinIO community edition is archived` and `PromLens is frozen` and `Hook router is deferred`.
    - `grep -E "D-[0-9]{2,}" docs/architecture.md` returns exit 1 (zero D-XX references).
    - `grep -E "\.planning/" docs/architecture.md` returns exit 1.
    - `grep -iE 'inspq|qc\.ca|montreal|québec|francais|french|/srv/nfs/inspq|vault_inspq' docs/architecture.md` returns exit 1.
    - `grep -rPn '[^\x00-\x7F]' docs/architecture.md` returns exit 1 (zero non-ASCII).
  </acceptance_criteria>
  <done>
docs/architecture.md is a 3-4-page terse reference with all 8 required sections, all 13 components with pinned tags, the full port allocation table, an ASCII signal-flow diagram, monolithic-mode tradeoff bullets, storage dependency table, and 4 known-debt callouts. ZERO D-XX refs, ZERO .planning/ paths, ZERO INSPQ heritage details beyond the one-line opener.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author docs/quickstart.md (DOCS-02) -- teaching walkthrough</name>
  <files>docs/quickstart.md</files>
  <read_first>
    - .planning/phases/06-opt-in-orchestration-docs-smoke-test/06-CONTEXT.md (D-104 quickstart structure 8 steps; D-102 voice; D-103 no D-XX refs in public docs)
    - .planning/phases/06-opt-in-orchestration-docs-smoke-test/06-RESEARCH.md (Finding F: copy-pasteable commands + expected output snippets; Finding G: README anti-patterns; Pitfall 5 vars_files for smoke)
    - playbooks/deploy_docker.yml (the deploy command being documented)
    - playbooks/smoke_test.yml (the smoke step in Step 7)
    - playbooks/smoke_test/README.md (operator-facing smoke doc -- cross-link)
    - inventory/example-homelab/group_vars/all/secrets.yml.example (the secrets keys quickstart shows)
    - inventory/README.md (existing top-level inventory orientation -- cross-link target)
    - roles/grafana/README.md (Reverse proxy section -- cross-link for production hardening per CONTEXT Claude's Discretion)
  </read_first>
  <action>
Create `docs/quickstart.md` as a teaching walkthrough -- LONGER and more verbose than architecture.md/inventory.md (D-102 voice split). Length target: 6-8 pages rendered. ASCII only. Include EVERY command operator types, EVERY expected output snippet, and a Troubleshooting subsection.

**Hard rules:**
- Zero D-XX references.
- Zero `.planning/` paths.
- Zero `vault_` prefix usage (D-90).
- Zero `inspq` / `qc.ca` / `montreal` / etc.
- ASCII only.

Use this EXACT structure with these section headers:

```markdown
# Telemetron quickstart

Bring up the full Telemetron observability stack on a single Docker host
in one playbook run, then verify it with the included smoke test.

By the end of this walkthrough you will have:

- 12 healthy containers running on a `telemetron` Docker bridge network.
- Prometheus + Mimir for metrics, Loki for logs, Tempo for traces.
- Grafana on `:3000` with four provisioned datasources and starter dashboards.
- Alertmanager + Karma reachable.
- A synthetic log + metric + trace pushed through the stack and visible in Grafana.

Estimated time: 15-20 minutes (most of it waiting for Docker pulls).

## Prerequisites

You need:

| Item | Version | How to check |
|------|---------|--------------|
| Ansible | 2.15 or later | `ansible --version` |
| `community.docker` collection | 4.x or later | `ansible-galaxy collection list community.docker` |
| Docker on the target host | 24+ recommended; 27+ for full feature parity | `ssh <target> docker version` |
| SSH key-based access to the target host | passwordless `sudo` if applicable | `ssh <target> hostname` |
| Free ports on the target host | 3000, 3100, 3200, 4317, 4318, 9000, 9001, 9009, 9090, 9093, 9100, 8081, 8082, 2020 | `ssh <target> ss -tln` |

If `community.docker` is not installed:

```bash
ansible-galaxy collection install community.docker
```

## Step 1 -- Clone

```bash
git clone https://github.com/rockdarko/telemetron.git
cd telemetron
```

## Step 2 -- Edit the example inventory

Open `inventory/example-homelab/example-homelab.hosts` and change the
hostname + SSH user pair to point at your target Docker host. The file
ships with a single placeholder host -- replace it.

```bash
# Expected: one hosts file with [telemetron] section + one host line.
cat inventory/example-homelab/example-homelab.hosts
```

If you need to override defaults (per-role tunables, host port publish
flags, MinIO retention, etc.), edit files under
`inventory/example-homelab/group_vars/all/`. See `docs/inventory.md` for
the full schema.

## Step 3 -- Copy the secrets template

```bash
cp inventory/example-homelab/group_vars/all/secrets.yml.example \
   inventory/example-homelab/group_vars/all/secrets.yml
```

Open `secrets.yml` and replace every `CHANGE_ME` placeholder with a real
value. There are 9 keys; the first 8 reference each other so you only
need to set distinct values for two of them:

- `minio_root_user` (>= 3 chars)
- `minio_root_password` (>= 8 chars; recommend 32+)
- `grafana_admin_password` (any strong password)

The Loki / Tempo / Mimir S3 access keys default to the MinIO root
credentials -- no separate value needed for those.

## Step 4 -- Optional: encrypt secrets with ansible-vault

If you want to use ansible-vault (one of several options -- sops,
env-vars, external secret managers, and chmod 600 are also valid):

```bash
ansible-vault encrypt inventory/example-homelab/group_vars/all/secrets.yml
```

You will be prompted for a vault password. Save it somewhere safe; you
will need it on every playbook run.

## Step 5 -- Deploy

```bash
ansible-playbook -i inventory/example-homelab playbooks/deploy_docker.yml \
  --ask-vault-pass
```

This pulls all the Docker images on the first run (~3-5 minutes on a
typical home connection). Subsequent runs are much faster.

Expected last lines:

```
PLAY RECAP *********************************************************************
<your-host>      : ok=N  changed=N  failed=0  unreachable=0  skipped=N  rescued=0  ignored=0
```

`failed=0` is the gate. If anything failed, see the Troubleshooting
section below.

Run the playbook a SECOND time -- Telemetron is idempotent, so the
second run should report `changed=0`:

```bash
ansible-playbook -i inventory/example-homelab playbooks/deploy_docker.yml \
  --ask-vault-pass
```

Expected:
```
<your-host>      : ok=N  changed=0  failed=0  ...
```

## Step 6 -- Confirm containers are healthy

```bash
ssh <your-host> docker ps --filter name=telemetron- --format \
  'table {{.Names}}\t{{.Status}}'
```

Expected: 12 lines, all containing `Up ... (healthy)` (Phase 4-5 verify
gates establish HEALTHCHECKs on every container).

If `nfsd` was enabled via `enable_nfsd: true`, you also see
`systemctl is-active nfs-server.service` returning `active` on the host
(nfsd is a host-package, not a container).

## Step 7 -- Smoke test

```bash
ansible-playbook -i inventory/example-homelab playbooks/smoke_test.yml \
  --ask-vault-pass
```

This pushes a synthetic log + metric + trace via OTLP/HTTP to the OTel
Collector and verifies all four datasources (Loki, Prometheus, Mimir,
Tempo) return the data within a 60-second budget per signal.

Expected last lines:
```
TASK [Smoke summary -- all signals verified] ***********************************
ok: [<your-host>] => {
    "msg": [
        "Telemetron M1 smoke test PASSED",
        "trace_id: <hex>",
        "run_id:   <epoch>",
        "loki:     ok",
        "prom:     ok",
        "mimir:    ok",
        "tempo:    ok"
    ]
}
```

If any signal fails: see `playbooks/smoke_test/README.md` for a
troubleshooting table covering Loki / Prometheus / Mimir / Tempo failure
modes.

To exercise a single signal independently:

```bash
ansible-playbook -i inventory/example-homelab playbooks/smoke_test.yml \
  --tags log    --ask-vault-pass
ansible-playbook -i inventory/example-homelab playbooks/smoke_test.yml \
  --tags metric --ask-vault-pass
ansible-playbook -i inventory/example-homelab playbooks/smoke_test.yml \
  --tags trace  --ask-vault-pass
```

## Step 8 -- Open Grafana

Open `http://<your-host>:3000` in a browser. Log in:

- Username: `admin`
- Password: the value you set for `grafana_admin_password` in Step 3.

Click **Dashboards** in the left sidebar. You should see a curated
starter set including:

- **Host health** -- node_exporter metrics (CPU, memory, disk, network).
- **Loki Explore landing** -- click a log line to drill into structured fields.
- **Tempo Explore landing** -- click a trace span; the "Logs for this span" link uses Tempo `tracesToLogsV2` to navigate to Loki with the `trace_id` pre-filtered.
- **OTel Collector self-metrics** -- pipeline health for the gateway.
- **Backend self-metrics** -- Loki / Mimir / Tempo internal health.

Click **Explore** in the left sidebar, select the **Loki** datasource,
and run the query `{service_name="telemetron-smoke"}`. The synthetic
smoke-test log from Step 7 should be there.

Click **Karma** -- not in Grafana, but at `http://<your-host>:8082` --
to see the Alertmanager queue.

## Troubleshooting

| Symptom | Cause | Fix |
|---------|-------|-----|
| `ssh: Could not resolve hostname` during playbook run | Hostname in `example-homelab.hosts` is wrong | Edit `inventory/example-homelab/example-homelab.hosts`; verify with `ssh <host> hostname` |
| `Failed to connect to docker daemon` | Docker not installed on target | Install Docker (`curl -fsSL https://get.docker.com | sh` on the target) |
| `Bind for 0.0.0.0:3100 failed: port is already allocated` | Loki port collision | `ssh <host> ss -tlnp \| grep 3100`; stop the conflicting service |
| Smoke test "Loki asserter exhausts retries" | OTel Collector not forwarding logs | `ssh <host> docker logs telemetron-opentelemetry` for export errors |
| Smoke test "Tempo asserter retries with 404" | Trace stuck in WAL or OTel trace export failing | `ssh <host> docker logs telemetron-tempo` for ingester errors |
| `vault password mismatch` | Wrong password supplied to `--ask-vault-pass` | Provide the password you used in Step 4 |
| Second `deploy_docker.yml` run shows `changed > 0` | Often a `community.docker` collection version older than 4.5.2 | Upgrade with `ansible-galaxy collection install -U community.docker` |

## Production hardening (brief)

For homelab single-LAN deployments the defaults are reasonable. For
production-adjacent use cases, consider:

- **Reverse proxy + TLS termination in front of Grafana**: see the
  "Reverse proxy" section in `roles/grafana/README.md` for sample
  configurations (Nginx, Caddy, Traefik).
- **Anonymous viewer mode in Grafana**: disabled by default; enable via
  `grafana_anonymous_viewer: true` in inventory if you want unauthenticated
  read-only access (e.g. NOC TV dashboards).
- **Secrets rotation**: Grafana admin password is first-boot-only. After
  initial deploy, rotate via
  `docker exec telemetron-grafana grafana-cli admin reset-admin-password '<new>'`
  and update `secrets.yml` to match.
- **Multi-host / HA deployment**: out of M1 scope; a future milestone
  ships distributed-mode role variants behind an HAProxy.

## Next steps

- `docs/architecture.md` -- component-level architecture reference.
- `docs/inventory.md` -- how to build your own inventory beyond the example.
- `roles/<name>/README.md` -- per-component documentation including tunables, modes, and secrets.
```

Hard-check after writing:
```bash
grep -E "D-[0-9]{2,}" docs/quickstart.md          # must return zero
grep -E "\.planning/" docs/quickstart.md          # must return zero
grep -E "vault_" docs/quickstart.md               # must return zero
grep -iE 'inspq|qc\.ca|montreal' docs/quickstart.md  # must return zero
grep -rPn '[^\x00-\x7F]' docs/quickstart.md       # must return zero
```
All five must return zero matches.
  </action>
  <verify>
    <automated>
      cd /home/darko/git/rockdarko/telemetron && \
      test -f docs/quickstart.md && \
      grep -q "^# Telemetron quickstart" docs/quickstart.md && \
      grep -q "^## Prerequisites" docs/quickstart.md && \
      grep -q "^## Step 1 -- Clone" docs/quickstart.md && \
      grep -q "^## Step 2 -- Edit the example inventory" docs/quickstart.md && \
      grep -q "^## Step 3 -- Copy the secrets template" docs/quickstart.md && \
      grep -q "^## Step 4 -- Optional: encrypt secrets with ansible-vault" docs/quickstart.md && \
      grep -q "^## Step 5 -- Deploy" docs/quickstart.md && \
      grep -q "^## Step 6 -- Confirm containers are healthy" docs/quickstart.md && \
      grep -q "^## Step 7 -- Smoke test" docs/quickstart.md && \
      grep -q "^## Step 8 -- Open Grafana" docs/quickstart.md && \
      grep -q "^## Troubleshooting" docs/quickstart.md && \
      grep -q "^## Production hardening" docs/quickstart.md && \
      grep -q "playbooks/deploy_docker.yml" docs/quickstart.md && \
      grep -q "playbooks/smoke_test.yml" docs/quickstart.md && \
      grep -q "playbooks/smoke_test/README.md" docs/quickstart.md && \
      grep -q "docs/inventory.md" docs/quickstart.md && \
      grep -q "docs/architecture.md" docs/quickstart.md && \
      grep -q "roles/grafana/README.md" docs/quickstart.md && \
      grep -q -- "--tags log" docs/quickstart.md && \
      grep -q "secrets.yml.example" docs/quickstart.md && \
      grep -q "grafana_admin_password" docs/quickstart.md && \
      grep -q "minio_root_user" docs/quickstart.md && \
      ! grep -qE "D-[0-9]{2,}" docs/quickstart.md && \
      ! grep -qE "\.planning/" docs/quickstart.md && \
      ! grep -qE "vault_" docs/quickstart.md && \
      ! grep -qiE 'inspq|qc\.ca|montreal|québec|francais|french' docs/quickstart.md && \
      ! grep -rPn '[^\x00-\x7F]' docs/quickstart.md
    </automated>
  </verify>
  <acceptance_criteria>
    - File `docs/quickstart.md` exists.
    - Starts with `# Telemetron quickstart`.
    - Contains all 8 required Step headers AND Prerequisites, Troubleshooting, Production hardening, Next steps headers (12 headers total).
    - References `playbooks/deploy_docker.yml`, `playbooks/smoke_test.yml`, `playbooks/smoke_test/README.md`, `docs/inventory.md`, `docs/architecture.md`, `roles/grafana/README.md`.
    - References tag-scoped smoke runs (`--tags log`).
    - References `secrets.yml.example` + `grafana_admin_password` + `minio_root_user`.
    - Troubleshooting section has a table with at least 4 rows.
    - `grep -E "D-[0-9]{2,}" docs/quickstart.md` returns exit 1 (zero D-XX refs).
    - `grep -E "\.planning/" docs/quickstart.md` returns exit 1.
    - `grep -E "vault_" docs/quickstart.md` returns exit 1.
    - `grep -iE 'inspq|qc\.ca|montreal|québec|francais|french' docs/quickstart.md` returns exit 1.
    - `grep -rPn '[^\x00-\x7F]' docs/quickstart.md` returns exit 1.
  </acceptance_criteria>
  <done>
docs/quickstart.md is a 6-8-page teaching walkthrough with the canonical 8-step path, Prerequisites table, Troubleshooting table, brief Production hardening subsection cross-linking roles/grafana/README.md, and Next steps. Every command is copy-pasteable; expected output snippets follow each major command. ZERO D-XX refs, ZERO `.planning/` paths, ZERO `vault_` prefix.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author docs/inventory.md (DOCS-03) -- deep-dive inventory model</name>
  <files>docs/inventory.md</files>
  <read_first>
    - .planning/phases/06-opt-in-orchestration-docs-smoke-test/06-CONTEXT.md (D-105 inventory.md scope: directory shape, group_vars conventions, secrets contract with full key list, host_vars patterns, symlink-from-outside-repo, multi-host preview as v2)
    - inventory/README.md (existing top-level orientation -- cross-link FROM inventory.md TO this for quick start)
    - inventory/example-homelab/group_vars/all/secrets.yml.example (8-key authoritative secrets contract)
    - inventory/example-homelab/group_vars/all/ (full list of 15 var files for the group_vars schema documentation)
    - .planning/phases/06-opt-in-orchestration-docs-smoke-test/06-RESEARCH.md (Finding F: Markdown conventions)
  </read_first>
  <action>
Create `docs/inventory.md` as a terse reference deep-dive. Length target: 2-3 pages rendered. ASCII only.

**Hard rules** (same as architecture.md): no D-XX, no `.planning/`, no `vault_` prefix, no INSPQ refs, ASCII only.

Structure (use these exact section headers in this order):

```markdown
# Telemetron inventory model

Telemetron is inventory-agnostic. Any environment can target any host;
the example inventory at `inventory/example-homelab/` is one worked
example. This document covers how to build your own.

For a quick start using the example inventory, see `inventory/README.md`
and the walkthrough in `docs/quickstart.md`. For per-component tunables,
see each role's README under `roles/<name>/README.md`.

## Directory shape

```
inventory/
  <env-name>/
    <env-name>.hosts         # the inventory file (INI or YAML format)
    group_vars/
      all/
        network.yml          # telemetron_network, port publish defaults
        storage.yml          # volume prefix, MinIO bucket names
        secrets.yml          # operator-supplied; .gitignored
        secrets.yml.example  # template; committed
        nfsd.yml             # enable_nfsd opt-in knob
        minio.yml            # per-role tunables (one file per role)
        loki.yml
        tempo.yml
        mimir.yml
        prometheus.yml
        opentelemetry.yml
        fluentbit.yml
        node_exporter.yml
        alertmanager.yml
        grafana.yml
        karma.yml
        promlens.yml
    host_vars/
      <hostname>.yml         # optional per-host overrides
```

## group_vars/all conventions

Telemetron uses `group_vars/all/<role>.yml` rather than playbook-level
`vars:` so that operators override defaults without forking the role.
Every role's `defaults/main.yml` declares the tunables; `group_vars/all/<role>.yml`
in the operator's inventory wins.

Cross-cutting files (not tied to one role):

| File | Purpose | Key vars |
|------|---------|----------|
| `network.yml` | Docker bridge network + per-role host port publish defaults | `telemetron_network`, `telemetron_publish_default`, `telemetron_tz` |
| `storage.yml` | Volume prefix, MinIO bucket names, retention defaults | `telemetron_volume_prefix`, `telemetron_config_root` |
| `secrets.yml` | Sensitive variables (see Secrets contract below) | 9 keys |
| `nfsd.yml` | Opt-in NFS server | `enable_nfsd`, `nfsd_exports`, `nfsd_share_root` |

Per-role files (one per deployed role) follow the same convention:
declare overrides for any variable named in `roles/<name>/defaults/main.yml`.

## Secrets contract

The example secrets file (`secrets.yml.example`) declares the full set
of M1 sensitive variables. Operators copy it to `secrets.yml`, fill in
real values, and choose any protection mechanism (ansible-vault, sops,
external secret manager, or chmod 600 on a homelab).

| Key | Type | Consumed by | Notes |
|-----|------|-------------|-------|
| `minio_root_user` | string | roles/minio | MinIO root username (>= 3 chars) |
| `minio_root_password` | string | roles/minio | MinIO root password (>= 8 chars; recommend 32+) |
| `loki_s3_access_key` | string | roles/loki | Defaults to `{{ minio_root_user }}` |
| `loki_s3_secret_key` | string | roles/loki | Defaults to `{{ minio_root_password }}` |
| `tempo_s3_access_key` | string | roles/tempo | Defaults to `{{ minio_root_user }}` |
| `tempo_s3_secret_key` | string | roles/tempo | Defaults to `{{ minio_root_password }}` |
| `mimir_s3_access_key` | string | roles/mimir | Defaults to `{{ minio_root_user }}` |
| `mimir_s3_secret_key` | string | roles/mimir | Defaults to `{{ minio_root_password }}` |
| `grafana_admin_password` | string | roles/grafana | First-boot-only; rotate post-deploy via `grafana-cli admin reset-admin-password` |

Naming convention: `<role>_<purpose>`. No prefix decoration. Each role's
README "Secrets" section enumerates the keys it expects.

`secrets.yml` is in `.gitignore`; `secrets.yml.example` is committed
with `CHANGE_ME` placeholders.

## host_vars patterns

Use `host_vars/<hostname>.yml` for per-target overrides that should not
apply to the whole environment. Common patterns:

- Per-host port re-mapping when 2049 (NFS) collides on a target.
- Per-host disk paths when `telemetron_volume_prefix` differs.
- Per-host SSH user when one target accepts a different account than the
  rest.

`host_vars/` is optional; an inventory with a single host typically
keeps everything in `group_vars/all/`.

## Symlinking an out-of-tree inventory

If you maintain inventory elsewhere (a separate repo, an Ansible vault,
a private fork), symlink it in:

```bash
ln -s /path/to/your/inventory inventory/myenv
```

Then run playbooks with `-i inventory/myenv`. Add the symlink to
`.git/info/exclude` if you don't want it tracked locally:

```bash
echo inventory/myenv >> .git/info/exclude
```

This keeps your inventory cleanly separated from the upstream repo's
example inventory.

## Multi-host extension

The example inventory ships as a single-host shape -- one target host
under the `[telemetron]` group. Telemetron's M1 deploy is designed for
a single Docker host.

Multi-host (HA / federation) deployments require:

- Distributed-mode role variants for Loki / Tempo / Mimir (microservices target).
- HAProxy in front of read/write paths.
- An inventory layout that splits backends across multiple hosts.

This is a future milestone, not part of M1.

## Validation

Once your inventory is in place, validate it without deploying:

```bash
ansible-playbook --syntax-check -i inventory/<your-env> \
  playbooks/deploy_docker.yml
ansible-inventory -i inventory/<your-env> --list
```

The first command catches YAML/Ansible-syntax errors; the second prints
the resolved host + group + variable graph so you can confirm
overrides land where you expect.

## See also

- `inventory/README.md` -- top-level inventory orientation.
- `inventory/example-homelab/README.md` -- the worked example.
- `docs/quickstart.md` -- zero-to-dashboards walkthrough using this example.
- `roles/<name>/README.md` -- per-role tunable documentation.
```

Hard-check after writing:
```bash
grep -E "D-[0-9]{2,}" docs/inventory.md           # must return zero
grep -E "\.planning/" docs/inventory.md           # must return zero
grep -E "vault_" docs/inventory.md                # must return zero
grep -iE 'inspq|qc\.ca|montreal' docs/inventory.md # must return zero
grep -rPn '[^\x00-\x7F]' docs/inventory.md        # must return zero
```
All five must return zero matches.
  </action>
  <verify>
    <automated>
      cd /home/darko/git/rockdarko/telemetron && \
      test -f docs/inventory.md && \
      grep -q "^# Telemetron inventory model" docs/inventory.md && \
      grep -q "^## Directory shape" docs/inventory.md && \
      grep -q "^## group_vars/all conventions" docs/inventory.md && \
      grep -q "^## Secrets contract" docs/inventory.md && \
      grep -q "^## host_vars patterns" docs/inventory.md && \
      grep -q "^## Symlinking an out-of-tree inventory" docs/inventory.md && \
      grep -q "^## Multi-host extension" docs/inventory.md && \
      grep -q "^## Validation" docs/inventory.md && \
      grep -q "minio_root_user" docs/inventory.md && \
      grep -q "minio_root_password" docs/inventory.md && \
      grep -q "loki_s3_access_key" docs/inventory.md && \
      grep -q "loki_s3_secret_key" docs/inventory.md && \
      grep -q "tempo_s3_access_key" docs/inventory.md && \
      grep -q "tempo_s3_secret_key" docs/inventory.md && \
      grep -q "mimir_s3_access_key" docs/inventory.md && \
      grep -q "mimir_s3_secret_key" docs/inventory.md && \
      grep -q "grafana_admin_password" docs/inventory.md && \
      grep -q "secrets.yml.example" docs/inventory.md && \
      grep -q "telemetron_network" docs/inventory.md && \
      grep -q "telemetron_publish_default" docs/inventory.md && \
      grep -q "enable_nfsd" docs/inventory.md && \
      grep -q "host_vars/" docs/inventory.md && \
      grep -q "inventory/README.md" docs/inventory.md && \
      grep -q "future milestone" docs/inventory.md && \
      ! grep -qE "D-[0-9]{2,}" docs/inventory.md && \
      ! grep -qE "\.planning/" docs/inventory.md && \
      ! grep -qE "vault_" docs/inventory.md && \
      ! grep -qiE 'inspq|qc\.ca|montreal|québec|francais|french' docs/inventory.md && \
      ! grep -rPn '[^\x00-\x7F]' docs/inventory.md
    </automated>
  </verify>
  <acceptance_criteria>
    - File `docs/inventory.md` exists.
    - Starts with `# Telemetron inventory model`.
    - Contains all 8 required section headers: `## Directory shape`, `## group_vars/all conventions`, `## Secrets contract`, `## host_vars patterns`, `## Symlinking an out-of-tree inventory`, `## Multi-host extension`, `## Validation`, `## See also`.
    - Secrets contract section lists ALL 9 keys: `minio_root_user`, `minio_root_password`, `loki_s3_access_key`, `loki_s3_secret_key`, `tempo_s3_access_key`, `tempo_s3_secret_key`, `mimir_s3_access_key`, `mimir_s3_secret_key`, `grafana_admin_password`.
    - Multi-host extension section explicitly calls out v2 / future-milestone status.
    - Symlink section contains literal `ln -s` command + `.git/info/exclude` reference.
    - References `inventory/README.md`, `docs/quickstart.md`.
    - `grep -E "D-[0-9]{2,}" docs/inventory.md` returns exit 1.
    - `grep -E "\.planning/" docs/inventory.md` returns exit 1.
    - `grep -E "vault_" docs/inventory.md` returns exit 1.
    - `grep -iE 'inspq|qc\.ca|montreal|québec|francais|french' docs/inventory.md` returns exit 1.
    - `grep -rPn '[^\x00-\x7F]' docs/inventory.md` returns exit 1.
  </acceptance_criteria>
  <done>
docs/inventory.md is a 2-3-page deep-dive covering directory shape, group_vars/all per-Phase schema, the full 9-key secrets contract, host_vars patterns, symlink-from-outside-repo with the `.git/info/exclude` trick, a multi-host v2 callout, and a Validation subsection. Cross-links to inventory/README.md and quickstart.md.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Live UAT on leviathan -- operator runs docs/quickstart.md verbatim (DOCS-02 acceptance per D-109)</name>
  <files>(verification only -- appends quickstart-verbatim-walkthrough outcomes to .planning/phases/06-opt-in-orchestration-docs-smoke-test/06-HUMAN-UAT.md; auto-fixes any doc factual inaccuracies discovered)</files>
  <read_first>
    - .planning/phases/06-opt-in-orchestration-docs-smoke-test/06-CONTEXT.md (D-104 quickstart structure; D-109 leviathan UAT mandatory)
    - .planning/phases/06-opt-in-orchestration-docs-smoke-test/06-RESEARCH.md (validation architecture: manual UAT on leviathan is the M1 test framework)
    - [[project_leviathan_uat_host]] user memory
  </read_first>
        <action>
**What this task verifies (built by prior tasks in this plan):**

- docs/architecture.md: terse public-facing architecture reference (3-4 pages).
    - docs/quickstart.md: 8-step teaching walkthrough (6-8 pages) with prerequisites, expected outputs, troubleshooting, production hardening.
    - docs/inventory.md: 2-3-page inventory deep-dive with full 9-key secrets contract + symlink pattern.

**Verification procedure (run on leviathan per D-109):**

**The DOCS-02 acceptance criterion is: "verified by an operator running it verbatim on a fresh target." This task IS that test.**

**Step 1 -- Fresh clone:**

```bash
cd /tmp
rm -rf telemetron-quickstart-uat
git clone https://github.com/rockdarko/telemetron.git telemetron-quickstart-uat
cd telemetron-quickstart-uat
```

If the working repo has uncommitted Phase 6 changes, fall back to:

```bash
cd /tmp && rm -rf telemetron-quickstart-uat && \
cp -r /home/darko/git/rockdarko/telemetron telemetron-quickstart-uat && \
cd telemetron-quickstart-uat && git stash || true
```

**Step 2 -- Follow `docs/quickstart.md` step-by-step:**

Open `docs/quickstart.md` in another terminal. Execute EVERY command shown
exactly as written, substituting `leviathan` for `<your-host>` where the
doc has placeholders. DO NOT skip steps. DO NOT use knowledge outside the
doc.

For each step, record:
- Did the command run as documented? (yes/no)
- Did the output match what the doc said to expect? (yes/no/partial)
- Any deviation? (capture exact text)

Steps to execute on leviathan:
1. Prerequisites: verify each row of the Prerequisites table.
2. Step 1 Clone: skip (already cloned above as a test artifact).
3. Step 2 Edit hosts: point at leviathan.
4. Step 3 Copy secrets template: cp + fill in values.
5. Step 4 (optional) ansible-vault encrypt: SKIP unless testing the encrypt path explicitly.
6. Step 5 Deploy: `ansible-playbook -i inventory/example-homelab playbooks/deploy_docker.yml --ask-vault-pass`.
7. Step 5 idempotency: re-run; confirm changed=0.
8. Step 6 docker ps: confirm 12 healthy containers.
9. Step 7 Smoke: `ansible-playbook -i inventory/example-homelab playbooks/smoke_test.yml --ask-vault-pass`; confirm "Telemetron M1 smoke test PASSED" line.
10. Step 7 Single-signal: try `--tags log`, `--tags metric`, `--tags trace`.
11. Step 8 Open Grafana: browser to http://leviathan:3000, log in with grafana_admin_password, verify the dashboards are visible AND the smoke-test log query `{service_name="telemetron-smoke"}` returns results.

**Step 3 -- "Click in Grafana" UI step (per D-109):**

This is the UI verification CONTEXT.md flags as "the click in Grafana
step that 05-HUMAN-UAT was unable to automate":

- Click **Dashboards** -- count the visible dashboards (expect 5-10).
- Open any one dashboard -- panels render real data (no "Datasource not found" errors).
- Click **Explore** -> **Loki** -> run `{service_name="telemetron-smoke"}` -> see the smoke log.
- Click **Explore** -> **Tempo** -> if a trace ID was captured from the smoke output, paste it and verify the span graph renders.
- From the Tempo trace span detail, click the "Logs for this span" link -> confirm it navigates to Loki Explore with `trace_id` pre-filtered (UI-04 trace-to-logs wiring).

**Step 4 -- Cross-link audit:**

From `docs/quickstart.md`, click through every internal link. Confirm:
- `docs/inventory.md` exists and is the right doc.
- `docs/architecture.md` exists and is the right doc.
- `playbooks/smoke_test/README.md` exists and is the right doc.
- `roles/grafana/README.md` exists and has a "Reverse proxy" section to link to.

Then from `docs/inventory.md` and `docs/architecture.md`, click the same.

**Step 5 -- Record outcomes in `.planning/phases/06-opt-in-orchestration-docs-smoke-test/06-HUMAN-UAT.md`:**

Append a new section "Plan 06-03 quickstart-walkthrough UAT" with:
- Per-step pass/fail (1-11 above).
- UI verification result (Step 3 dashboards + trace-to-logs).
- Any docs typo / inaccuracy / missing-context discovered; fix immediately as a same-class auto-fix and re-run the affected step (per Rule 1 from Phase 4-5).
- Cross-link audit result (Step 4).

If `docs/quickstart.md` has any FACTUAL inaccuracy (wrong command syntax,
wrong expected-output snippet, wrong port), fix the doc and re-run from
the affected step. DOCS-02 acceptance requires that an operator can
follow it verbatim and succeed.

If any step requires knowledge OUTSIDE the doc to succeed, that is a
DOCS-02 failure -- fix the doc and re-run.

**Resume signal contract:** Type "approved -- 06-03 UAT clean; quickstart works verbatim" if Steps 1-5 pass and 06-HUMAN-UAT.md is updated. Type "issues: ..." with the specific doc deficiency if any step requires outside knowledge.
  </action>
  <verify>
    <automated>echo "MANUAL UAT CHECKPOINT -- this task is a human-verify gate. Run the verification procedure documented in the action block on leviathan, append outcomes to .planning/phases/06-opt-in-orchestration-docs-smoke-test/06-HUMAN-UAT.md, then provide the resume signal documented in the action block. The execute-phase orchestrator pauses on checkpoint:human-verify tasks until the user provides the resume signal."</automated>
  </verify>
  <done>Operator has executed the verification procedure on leviathan, all listed pass-criteria are met, outcomes are recorded in 06-HUMAN-UAT.md, and the resume signal has been provided to the orchestrator. Failure: any unaddressed pass-criteria blocks proceeding to the next plan.</done>
</task>

</tasks>

<verification>
- `docs/architecture.md`, `docs/quickstart.md`, `docs/inventory.md` all exist.
- All three pass the Gate 1 scrub (zero D-XX refs, zero `.planning/` paths, zero `inspq|qc.ca|montreal` strings, zero non-ASCII characters, zero `vault_` prefix).
- `docs/quickstart.md` contains all 8 step headers + Prerequisites + Troubleshooting + Production hardening.
- `docs/inventory.md` lists all 9 secrets contract keys.
- `docs/architecture.md` contains the 8 required sections + ASCII signal flow diagram + full port allocation table + all 13 components with pinned tags.
- Leviathan UAT: operator runs quickstart.md verbatim and succeeds end-to-end including the UI "click in Grafana" step.
</verification>

<success_criteria>
- DOCS-01 acceptance: `docs/architecture.md` documents components, monolithic-mode tradeoffs, signal flow, port-allocation cheat sheet.
- DOCS-02 acceptance: `docs/quickstart.md` is verified by an operator running it verbatim on leviathan, end-to-end.
- DOCS-03 acceptance: `docs/inventory.md` covers the inventory model in depth beyond the existing `inventory/README.md` stub, including the secrets contract and symlink pattern.
- All three docs are public-facing (no D-XX, no `.planning/`, no INSPQ heritage beyond a one-line attribution).
- Live UAT recorded on leviathan per D-109.
</success_criteria>

<output>
After completion, create `.planning/phases/06-opt-in-orchestration-docs-smoke-test/06-03-SUMMARY.md` capturing:
- Files created (3 docs).
- Distillation sources used (CLAUDE.md port matrix, .planning/research/ARCHITECTURE.md signal flow, secrets.yml.example for the secrets contract).
- Any doc typos / inaccuracies discovered and auto-fixed during leviathan UAT.
- DOCS-02 verbatim-acceptance evidence from Task 4.
- Forward note: Plan 06-04 README rewrite can NOW link to docs/quickstart.md as canonical "how to run this."
</output>
