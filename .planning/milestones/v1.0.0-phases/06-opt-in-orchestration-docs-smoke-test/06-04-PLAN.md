---
phase: 06-opt-in-orchestration-docs-smoke-test
plan: 04
type: execute
wave: 4
depends_on:
  - "06-03"
files_modified:
  - README.md
  - docs/README.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - DOCS-04
  - INV-01
  - INV-03
must_haves:
  truths:
    - "Top-level README.md is rewritten per D-106: opens with the value prop 'self-hosted observability in one playbook'; contains a 3-line Quick start code block linking to docs/quickstart.md; component table drops hook_router + HAProxy from shipped (with one-line v2 callouts); Origin section retains the INSPQ attribution; 'Status: early.' is DELETED ENTIRELY; Layout section is refreshed"
    - "docs/README.md planned-docs table ticks `architecture.md`, `quickstart.md`, `inventory.md` as shipped in M1; marks `metrics.md`, `alerts.md`, `mimir-retention.md`, `fluentbit-timestamps.md`, `hook-router.md`, `instrumentation-otel.md`, `migration-from-inspq.md` as v2-deferred per DOCS-V2-01..07"
    - "Two back-to-back runs of `ansible-playbook playbooks/deploy_docker.yml` on leviathan with `enable_nfsd: true` (full 13-role surface) both report `changed=0` (OPS-04 close-out)"
    - ".planning/PROJECT.md Active section is updated: '0 remaining roles; M1 complete'"
    - ".planning/ROADMAP.md Phase 6 progress row is updated to '4/4 plans complete'"
    - "INV-01 acceptance: operator clones repo, edits ONE hostname + SSH-user pair, supplies vault password, runs single ansible-playbook command, and the full M1 stack comes up end-to-end on a fresh Docker host -- demonstrated on leviathan"
  artifacts:
    - path: "README.md"
      provides: "Rewritten top-level README -- M1 'self-hosted observability in one playbook' value prop, 3-line quick start, accurate component table"
      contains: "self-hosted observability"
    - path: "docs/README.md"
      provides: "Updated planned-docs table -- 3 M1 docs ticked; 7 v2 docs marked deferred"
      contains: "architecture.md"
    - path: ".planning/PROJECT.md"
      provides: "Active section updated to reflect M1 close-out"
      contains: "M1 complete"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 6 progress row updated"
      contains: "4/4"
    - path: ".planning/phases/06-opt-in-orchestration-docs-smoke-test/06-HUMAN-UAT.md"
      provides: "Per-plan UAT outcomes recorded (Plans 06-01..04 leviathan runs)"
      contains: "Plan 06-04"
  key_links:
    - from: "README.md"
      to: "docs/quickstart.md"
      via: "Quick start section link"
      pattern: "docs/quickstart.md"
    - from: "docs/README.md"
      to: "REQUIREMENTS.md DOCS-V2-01..07"
      via: "v2-deferred annotation in planned-docs table"
      pattern: "v2"
    - from: ".planning/ROADMAP.md Phase 6 row"
      to: ".planning/phases/06-opt-in-orchestration-docs-smoke-test/"
      via: "Plans list ticked"
      pattern: "4/4"
---

<objective>
Close out M1 with three coordinated changes:

1. **Rewrite top-level `README.md`** per D-106: pivot from "Status: early. Coming soon..." to "self-hosted observability in one playbook" with an honest, accurate component table (drop hook_router + HAProxy from "shipped"), a 3-line quick start linking to `docs/quickstart.md`, and an Origin section retaining INSPQ attribution.

2. **Update `docs/README.md`** planned-docs table to tick the 3 M1 docs shipped in Plan 06-03 and mark the 7 v2-deferred docs against `REQUIREMENTS.md DOCS-V2-01..07`.

3. **Revalidate end-to-end idempotency on leviathan** (OPS-04 close-out): two back-to-back deploys with `enable_nfsd: true` both report `changed=0` across the full 13-role surface, and update `.planning/PROJECT.md` Active section + `.planning/ROADMAP.md` Phase 6 progress row to declare M1 complete.

This is the milestone-close plan. After it lands and the leviathan validation succeeds, Telemetron M1 is feature-complete.

Output: rewritten `README.md` + refreshed `docs/README.md` + bookkeeping updates in `.planning/PROJECT.md` + `.planning/ROADMAP.md` + final entries in `.planning/phases/06-opt-in-orchestration-docs-smoke-test/06-HUMAN-UAT.md`. Verified live on leviathan per D-109.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-opt-in-orchestration-docs-smoke-test/06-CONTEXT.md
@.planning/phases/06-opt-in-orchestration-docs-smoke-test/06-RESEARCH.md
@.planning/phases/06-opt-in-orchestration-docs-smoke-test/06-01-SUMMARY.md
@.planning/phases/06-opt-in-orchestration-docs-smoke-test/06-02-SUMMARY.md
@.planning/phases/06-opt-in-orchestration-docs-smoke-test/06-03-SUMMARY.md
@CLAUDE.md
@README.md
@docs/README.md
@docs/quickstart.md
@docs/architecture.md
@docs/inventory.md

<interfaces>
<!-- The CURRENT shape of files this plan rewrites. -->

From CURRENT `README.md` (45 lines, must be rewritten per D-106):
- Para 1: "**One-stop self-hosted observability stack** -- driven by Ansible, targeting Docker/VM or Kubernetes/OpenShift." -- KEEP value-prop intent, REWRITE per D-106.
- Para 2: aspirational component bullet list including HAProxy and Hook Router -- DROP both from shipped per D-106; one-line v2 callouts only.
- "**Status: early.** This is a clean-slate fork of an internal Quebec-government deployment originally authored at INSPQ..." -- DELETE the "Status: early" sentence ENTIRELY per D-106. KEEP the INSPQ attribution but move to the Origin section.
- "## Quick start" -- currently "Coming soon as roles land in `roles/`." REPLACE with 3-line code block + link to docs/quickstart.md.
- "## Layout" -- currently lists `hooks/` which is deferred to v2. REFRESH to match the actual repo tree (drop `hooks/`).
- "## Inventory model" + "## License" + "## Author" -- KEEP, light refresh.

From CURRENT `docs/README.md`:
- "Documentation" header + "Planned" table with 10 rows (`architecture.md`, `quickstart.md`, `inventory.md`, `metrics.md`, `alerts.md`, `mimir-retention.md`, `fluentbit-timestamps.md`, `hook-router.md`, `instrumentation-otel.md`, `migration-from-inspq.md`).
- All 10 are currently "Planned." Plan 06-03 shipped the first 3 -- they need to be ticked as M1 shipped, and the remaining 7 must be marked as v2 deferred against DOCS-V2-01..07.

From `.planning/PROJECT.md` (Active section -- currently reflects Phase 5 / Phase 6 in-progress per STATE.md):
- Update Active line to "0 remaining roles; M1 complete" per D-110 + CONTEXT `<decisions>` D-107 fourth bullet "this plan also closes the milestone."

From `.planning/ROADMAP.md` Phase 6 row in the Progress table:
- Currently: `| 6. Opt-in, Orchestration, Docs & Smoke Test | 0/TBD | Not started | - |`
- Will become: `| 6. Opt-in, Orchestration, Docs & Smoke Test | 4/4 | Complete (verified) | <YYYY-MM-DD> |`
- AND the Phase 6 Plans list at the bottom of the Phase 6 detail section needs all 4 plans ticked.

CRITICAL: Per CLAUDE.md / D-90 / [[feedback_no_decorative_convention_prefixes]], the rewritten README MUST NOT use `vault_` prefix. The OPS-02 secrets-discipline language uses the role-namespaced `<role>_<purpose>` convention.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite top-level README.md per D-106</name>
  <files>README.md</files>
  <read_first>
    - README.md (CURRENT shape -- understand what's being replaced and what's being kept)
    - .planning/phases/06-opt-in-orchestration-docs-smoke-test/06-CONTEXT.md (D-106 full structure; D-110 light doc cascade per plan)
    - .planning/phases/06-opt-in-orchestration-docs-smoke-test/06-RESEARCH.md (Finding G: README anti-patterns + G2 effective structure for technical homelab projects + G4 "Status: early" replacement = delete entirely)
    - docs/quickstart.md (created in Plan 06-03 -- the 3-line quickstart links to it)
    - docs/architecture.md (cross-link target)
    - docs/inventory.md (cross-link target)
    - CLAUDE.md (TL;DR per-role table -- source for the component table that drops hook_router + HAProxy)
    - .planning/REQUIREMENTS.md (DOCS-V2-04 hook_router-doc deferral; STORAGE-01 MinIO replacement deferral; ALERT-V2-01..05 hook_router code deferral)
  </read_first>
  <action>
REPLACE the entire content of `README.md` with the EXACT text below. ASCII only. No `vault_` prefix. No "Status: early." text. Origin section retains INSPQ attribution but is one paragraph at the bottom, not in the opener.

```markdown
# Telemetron

**Self-hosted observability in one playbook.** Bring up Prometheus + Loki +
Tempo + Grafana + Alertmanager on your own hardware with one Ansible
command. Single-host Docker. Homelab-friendly.

## Quick start

```bash
git clone https://github.com/rockdarko/telemetron.git && cd telemetron
# edit inventory/example-homelab/example-homelab.hosts (one hostname + SSH user)
# copy + fill secrets.yml from inventory/example-homelab/group_vars/all/secrets.yml.example
ansible-playbook -i inventory/example-homelab playbooks/deploy_docker.yml \
  --ask-vault-pass
```

Full walkthrough with prerequisites, expected outputs, and troubleshooting:
[`docs/quickstart.md`](docs/quickstart.md).

After the deploy, validate with the included smoke test:

```bash
ansible-playbook -i inventory/example-homelab playbooks/smoke_test.yml \
  --ask-vault-pass
```

The smoke test pushes a synthetic log + metric + trace through the stack
and confirms visibility in Grafana within 60 seconds.

## What's included

| Component | Image | Purpose |
|-----------|-------|---------|
| OpenTelemetry Collector | `otel/opentelemetry-collector-contrib:0.152.0` | OTLP ingest and signal fan-out |
| Loki | `grafana/loki:3.7.2` | Log backend (monolithic mode) |
| Tempo | `grafana/tempo:2.10.5` | Trace backend (monolithic mode) |
| Mimir | `grafana/mimir:3.0.6` | Long-term metrics (monolithic mode) |
| Prometheus | `prom/prometheus:v3.11.3` | Short-term metrics + alert evaluation |
| node_exporter | `quay.io/prometheus/node-exporter:v1.11.1` | Host metrics |
| Fluent Bit | `fluent/fluent-bit:4.2.3` | Container log shipping |
| MinIO | `minio/minio:RELEASE.2025-04-22T22-12-26Z` | S3-compatible object storage |
| Alertmanager | `quay.io/prometheus/alertmanager:v0.32.1` | Alert routing (null receiver default) |
| Grafana | `grafana/grafana-oss:13.0.1` | Dashboards + Explore |
| Karma | `ghcr.io/prymitive/karma:v0.130` | Alert triage UI |
| PromLens | `prom/promlens:v0.3.0` | PromQL editor (deprecation candidate) |
| nfsd (opt-in) | host package (`nfs-utils` / `nfs-kernel-server`) | Optional NFS server for legacy log ingestion |

Architecture detail: [`docs/architecture.md`](docs/architecture.md).

### Not in M1

- **Hook router** (Alertmanager -> CI/automation webhook bridge) is deferred to a future milestone. Alerts are visible in Karma but not auto-dispatched.
- **HAProxy** (load balancing for distributed-mode backends) is only useful when Loki / Mimir / Tempo run in microservices mode, which is a future-milestone deployment topology.
- **Kubernetes / OpenShift deployment path** is a future milestone; M1 ships the Docker path only.
- **Multi-host inventory** is a future milestone; M1 ships a single-host example.

## Requirements

- Ansible 2.15+
- `community.docker` collection 4.x+
- Docker 24+ on the target host (27+ recommended)
- SSH key-based access; passwordless `sudo` if applicable
- One Docker host (homelab box, VM, or bare metal)

## Layout

```
roles/        Ansible roles, one directory per component
playbooks/    deploy_docker.yml (orchestrator) + smoke_test.yml (acceptance)
inventory/    one subdirectory per environment; symlink yours in
docs/         architecture, quickstart, inventory model
```

## Inventory model

Each environment is a directory under `inventory/`. The shipped example
is `inventory/example-homelab/`. Build your own from scratch or symlink
an out-of-tree inventory in: see [`docs/inventory.md`](docs/inventory.md).

## Origin

Telemetron is a clean-slate fork of an internal observability stack
originally authored at INSPQ (Quebec public health institute). The fork
has been normalized to English, stripped of org-specific assumptions
(vault paths, internal domains, NFS share roots, cert chains), and
re-licensed MIT. The component selection, monolithic-mode deployment
shape, and Ansible-driven orchestration are inherited; the role
implementations are rewritten against current upstream image versions
and homelab-first defaults.

## License

MIT -- see [LICENSE](LICENSE).

## Author

Rock Martel-Langlois ([@rockdarko](https://github.com/rockdarko)).
```

After writing the new README, run the hard checks:
```bash
! grep -q "Status: early" README.md
! grep -q "Coming soon" README.md
! grep -q "HAProxy" README.md || grep -A1 "Hook router" README.md | head -5  # HAProxy only mentioned in "Not in M1" section
! grep -E "vault_" README.md
! grep -iE 'qc\.ca|montreal|québec|francais|french' README.md  # INSPQ is allowed (one-line attribution) but NOT in opener
! grep -rPn '[^\x00-\x7F]' README.md
grep -q "docs/quickstart.md" README.md
grep -q "docs/architecture.md" README.md
grep -q "docs/inventory.md" README.md
```

All checks must pass (the grep negations must return exit 1; the affirmative greps must return exit 0).
  </action>
  <verify>
    <automated>
      cd /home/darko/git/rockdarko/telemetron && \
      test -f README.md && \
      grep -q "^# Telemetron" README.md && \
      grep -q "Self-hosted observability in one playbook" README.md && \
      grep -q "## Quick start" README.md && \
      grep -q "## What's included" README.md && \
      grep -q "### Not in M1" README.md && \
      grep -q "## Requirements" README.md && \
      grep -q "## Layout" README.md && \
      grep -q "## Inventory model" README.md && \
      grep -q "## Origin" README.md && \
      grep -q "## License" README.md && \
      grep -q "## Author" README.md && \
      grep -q "docs/quickstart.md" README.md && \
      grep -q "docs/architecture.md" README.md && \
      grep -q "docs/inventory.md" README.md && \
      grep -q "playbooks/deploy_docker.yml" README.md && \
      grep -q "playbooks/smoke_test.yml" README.md && \
      grep -q "grafana/loki:3.7.2" README.md && \
      grep -q "grafana/tempo:2.10.5" README.md && \
      grep -q "Hook router" README.md && \
      grep -q "HAProxy" README.md && \
      ! grep -q "Status: early" README.md && \
      ! grep -q "Coming soon" README.md && \
      ! grep -qE "vault_" README.md && \
      ! grep -qiE 'qc\.ca|montreal|québec|francais|french' README.md && \
      ! grep -rPn '[^\x00-\x7F]' README.md
    </automated>
  </verify>
  <acceptance_criteria>
    - File `README.md` exists.
    - File contains literal `Self-hosted observability in one playbook` (D-106 value prop).
    - File contains all 9 required section headers: `## Quick start`, `## What's included`, `### Not in M1`, `## Requirements`, `## Layout`, `## Inventory model`, `## Origin`, `## License`, `## Author`.
    - File contains link to `docs/quickstart.md`, `docs/architecture.md`, `docs/inventory.md`.
    - File contains references to `playbooks/deploy_docker.yml` AND `playbooks/smoke_test.yml`.
    - File component table contains EXACT pinned tags for all 12 shipped components (loki:3.7.2, tempo:2.10.5, mimir:3.0.6, otel 0.152.0, prom v3.11.3, node-exporter v1.11.1, fluent-bit 4.2.3, minio RELEASE..., alertmanager v0.32.1, grafana-oss:13.0.1, karma v0.130, promlens v0.3.0).
    - File "Not in M1" section explicitly mentions Hook router, HAProxy, Kubernetes, Multi-host.
    - File does NOT contain literal `Status: early` (DELETED per D-106 + RESEARCH §G4).
    - File does NOT contain literal `Coming soon` (the placeholder is gone).
    - File does NOT contain `vault_` (D-90).
    - File does NOT contain `qc.ca`, `montreal`, `québec`, `francais`, `french` (Gate 1). `INSPQ` IS allowed (one-line attribution in Origin section).
    - `grep -rPn '[^\x00-\x7F]' README.md` returns exit 1 (zero non-ASCII).
  </acceptance_criteria>
  <done>
README.md is rewritten to lead with the value prop "Self-hosted observability in one playbook", has a 3-line copy-pasteable quick start linking to docs/quickstart.md, an accurate 13-row component table (12 containers + 1 host-package nfsd), a "Not in M1" subsection explicitly callouting hook_router + HAProxy + Kubernetes + multi-host deferrals, the requirements list, an updated Layout section (drops `hooks/`), an Origin section retaining the INSPQ attribution as one paragraph at the bottom, and the License + Author sections preserved. ZERO `Status: early`, ZERO `vault_`, ZERO non-ASCII.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update docs/README.md planned-docs table (tick 3 M1 shipped + mark 7 v2-deferred)</name>
  <files>docs/README.md</files>
  <read_first>
    - docs/README.md (CURRENT shape -- 10-row "Planned" table)
    - .planning/REQUIREMENTS.md v2 section (DOCS-V2-01..07 -- the 7 deferred docs)
    - .planning/phases/06-opt-in-orchestration-docs-smoke-test/06-CONTEXT.md (D-110 light doc cascade per plan; D-106 docs/README.md planned-docs table updated)
  </read_first>
  <action>
REPLACE the entire content of `docs/README.md` with the EXACT text below. ASCII only. No `vault_` prefix.

```markdown
# Documentation

Reference docs for Telemetron M1.

## Shipped (M1)

| File | Content |
|---|---|
| [`architecture.md`](architecture.md) | Components, monolithic-mode tradeoffs, signal flow, port matrix |
| [`quickstart.md`](quickstart.md) | Zero-to-dashboards walkthrough with prerequisites, expected outputs, troubleshooting |
| [`inventory.md`](inventory.md) | Inventory model, group_vars schema, secrets contract, multi-host extension preview |

See also the per-component documentation under
[`roles/<name>/README.md`](../roles/) and the smoke-test operator notes
at [`playbooks/smoke_test/README.md`](../playbooks/smoke_test/README.md).

## Deferred (v2)

The following docs are deferred to a future milestone. Tracked in
`.planning/REQUIREMENTS.md` under DOCS-V2-01..07.

| File | Content | Status |
|---|---|---|
| `alerts.md` | Bundled Prometheus alerts catalog | v2 (DOCS-V2-01) |
| `mimir-retention.md` | Two-tier retention strategy | v2 (DOCS-V2-02) |
| `fluentbit-timestamps.md` | DST + timezone handling for log shipping | v2 (DOCS-V2-03) |
| `hook-router.md` | Alertmanager -> CI bridge architecture (hook router itself is deferred -- see ALERT-V2-01..05) | v2 (DOCS-V2-04) |
| `instrumentation-otel.md` | Instrumenting your apps for OTLP | v2 (DOCS-V2-05) |
| `migration-from-inspq.md` | Port notes for anyone forking the original INSPQ stack | v2 (DOCS-V2-06) |
| `metrics.md` | Health metrics catalog with thresholds | v2 (DOCS-V2-07) |
```

After writing:
```bash
grep -q "## Shipped (M1)" docs/README.md
grep -q "## Deferred (v2)" docs/README.md
grep -q "architecture.md" docs/README.md
grep -q "DOCS-V2-01" docs/README.md
grep -q "DOCS-V2-07" docs/README.md
! grep -rPn '[^\x00-\x7F]' docs/README.md
```

All commands must succeed (last one returns exit 1).
  </action>
  <verify>
    <automated>
      cd /home/darko/git/rockdarko/telemetron && \
      test -f docs/README.md && \
      grep -q "^# Documentation" docs/README.md && \
      grep -q "^## Shipped (M1)" docs/README.md && \
      grep -q "^## Deferred (v2)" docs/README.md && \
      grep -q "architecture.md" docs/README.md && \
      grep -q "quickstart.md" docs/README.md && \
      grep -q "inventory.md" docs/README.md && \
      grep -q "DOCS-V2-01" docs/README.md && \
      grep -q "DOCS-V2-02" docs/README.md && \
      grep -q "DOCS-V2-03" docs/README.md && \
      grep -q "DOCS-V2-04" docs/README.md && \
      grep -q "DOCS-V2-05" docs/README.md && \
      grep -q "DOCS-V2-06" docs/README.md && \
      grep -q "DOCS-V2-07" docs/README.md && \
      grep -q "alerts.md" docs/README.md && \
      grep -q "mimir-retention.md" docs/README.md && \
      grep -q "fluentbit-timestamps.md" docs/README.md && \
      grep -q "hook-router.md" docs/README.md && \
      grep -q "instrumentation-otel.md" docs/README.md && \
      grep -q "migration-from-inspq.md" docs/README.md && \
      grep -q "metrics.md" docs/README.md && \
      ! grep -qE "vault_" docs/README.md && \
      ! grep -rPn '[^\x00-\x7F]' docs/README.md
    </automated>
  </verify>
  <acceptance_criteria>
    - File `docs/README.md` contains `## Shipped (M1)` AND `## Deferred (v2)` section headers.
    - "Shipped" table contains links to all 3 M1 docs (`architecture.md`, `quickstart.md`, `inventory.md`).
    - "Deferred" table contains all 7 v2 doc filenames (`alerts.md`, `mimir-retention.md`, `fluentbit-timestamps.md`, `hook-router.md`, `instrumentation-otel.md`, `migration-from-inspq.md`, `metrics.md`).
    - "Deferred" table references all 7 v2 IDs (DOCS-V2-01 through DOCS-V2-07).
    - File does NOT contain `vault_`.
    - `grep -rPn '[^\x00-\x7F]' docs/README.md` returns exit 1.
  </acceptance_criteria>
  <done>
docs/README.md is updated: M1-shipped docs ticked in their own table; v2-deferred docs are catalogued with their DOCS-V2-XX IDs against REQUIREMENTS.md.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update .planning/PROJECT.md + .planning/ROADMAP.md bookkeeping (M1 close-out)</name>
  <files>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/PROJECT.md (CURRENT Active section text -- locate the line that mentions remaining roles / current focus)
    - .planning/ROADMAP.md (CURRENT Phase 6 row in Progress table + Phase 6 Plans subsection at end of Phase 6 detail; ALSO the Phase 6 Goal text and SC1 text -- both contain stale "14 roles"/"14-role stack" language that must be corrected to "13 deployed + nfsd opt-in (hook_router v2-deferred)")
    - .planning/REQUIREMENTS.md (CURRENT INV-03 entry around line 69 -- the dependency-order list still includes `hook_router` paired with `alertmanager`; annotate as v2-deferred per ALERT-V2-01..05)
    - .planning/phases/06-opt-in-orchestration-docs-smoke-test/06-CONTEXT.md (D-107 fourth bullet "this plan also closes the milestone"; D-110 light doc cascade)
  </read_first>
  <action>
**File 1: `.planning/PROJECT.md`**

Locate the section that tracks active work / remaining roles / current focus. Read the file and find the line(s) declaring the M1 progress (search for "remaining roles" or "Active" or "Current focus"). UPDATE that line to:

```
0 remaining roles; M1 complete.
```

If multiple lines reference progress, update all of them coherently. For example:
- If there is a "Current focus" line that says "Phase 06 -- opt-in, orchestration, docs & smoke test", change it to "M1 complete; awaiting v2 milestone planning."
- If there is a remaining-roles count, change it to 0.

Do NOT rewrite the entire PROJECT.md -- only update the milestone-progress lines. Preserve the existing Key Decisions table, Out of Scope section, and historical content.

**File 2: `.planning/ROADMAP.md`**

Locate the Phase 6 row in the `| Phase | Plans Complete | Status | Completed |` Progress table. Currently:

```
| 6. Opt-in, Orchestration, Docs & Smoke Test | 0/TBD | Not started | - |
```

Replace with (use today's date as found via `date +%Y-%m-%d` on the host):

```
| 6. Opt-in, Orchestration, Docs & Smoke Test | 4/4 | Complete (verified) | <YYYY-MM-DD> |
```

Also locate the Phase 6 detail section (`### Phase 6: Opt-in, Orchestration, Docs & Smoke Test`) and update the `**Plans**: TBD` line at the end to:

```
**Plans**: 4 plans
Plans:
- [x] 06-01-PLAN.md -- nfsd role port (LEGACY-01) + FB tail-input integration; Wave 1
- [x] 06-02-PLAN.md -- playbooks/smoke_test.yml + 3 OTLP/HTTP producers + 4 Grafana-proxy asserters (OPS-07); Wave 2
- [x] 06-03-PLAN.md -- docs/architecture.md + docs/quickstart.md + docs/inventory.md (DOCS-01, DOCS-02, DOCS-03); Wave 3
- [x] 06-04-PLAN.md -- README.md rewrite (DOCS-04) + INV-01/INV-03 close-out + idempotency revalidation; Wave 4
```

Mark the Phase 6 line at the top of the file (in the `Phases` section) as complete by changing `- [ ] **Phase 6:** ...` to `- [x] **Phase 6:** ...`.

**ROADMAP Phase 6 Goal + SC1 cleanup (resolves stale "14 roles" language):**

In the Phase 6 Goal paragraph (the line beginning `**Goal**: Operator can clone the repo...`), find the substring:

```
`playbooks/deploy_docker.yml` orchestrates all 14 roles in correct dependency order
```

Replace with:

```
`playbooks/deploy_docker.yml` orchestrates 13 deployed roles + nfsd as the 14th opt-in slot (hook_router deferred to v2 per ALERT-V2-01..05) in correct dependency order
```

In Phase 6 SC1 (the line beginning `1. Operator clones the repo, edits a single hostname + SSH-user`), find the substring:

```
the full 14-role stack comes up end-to-end on a fresh Docker host
```

Replace with:

```
the full 13-role stack comes up end-to-end (nfsd is the 14th slot, opt-in default-off; hook_router deferred to v2) on a fresh Docker host
```

In the top-of-file Phase 6 bullet (around line 20, the `- [ ] **Phase 6: ...`), find the substring `orchestrating all 14 roles in dependency order` and replace with `orchestrating 13 deployed roles + nfsd as the 14th opt-in slot (hook_router deferred to v2) in dependency order` -- this keeps the top-of-file overview consistent with the Goal text correction below.

These three edits eliminate every "14 roles" / "14-role stack" literal in the Phase 6 section so post-M1 the ROADMAP doesn't internally contradict what shipped.

**File 3: `.planning/REQUIREMENTS.md`**

Locate INV-03 (around line 69, inside the `### Inventory and orchestration (INV)` block). The current dependency-order list includes `(`alertmanager`, `hook_router`)`. Replace the INV-03 paragraph with one that strikes `hook_router` from the active chain and adds a footnote noting it was deferred to v2 per ALERT-V2-01..05.

Specifically, find the existing INV-03 paragraph that begins with `- [ ] **INV-03**: ` and ends with `targeted re-runs.` -- preserve the actual arrow characters used in the source file (Unicode right-arrow U+2192, not ASCII `->`). Replace with a new INV-03 paragraph that:

1. Opens with `- [ ] **INV-03**: ` (same checkbox).
2. Changes the opener phrase from `orchestrates the 14 roles in dependency order` to `orchestrates 13 deployed roles in dependency order with \`nfsd\` as the 14th opt-in slot`.
3. Replaces the `(\`alertmanager\`, \`hook_router\`)` cluster with just `\`alertmanager\`[^hook-router-deferred]` (a Markdown footnote reference).
4. Preserves the rest of the dependency chain byte-for-byte from the alertmanager position onward.
5. Adds a blank line, then the footnote definition: `[^hook-router-deferred]: \`hook_router\` was originally paired with \`alertmanager\` here; deferred to v2 per ALERT-V2-01..05 during the Phase 4 scope reshape. M1 ships Alertmanager with a \`null\` default receiver.`

If `.planning/REQUIREMENTS.md` already contains a `[^hook-router-deferred]` definition from a prior edit pass (the file may have been pre-modified during plan revision), do not duplicate the definition -- one definition is sufficient for Markdown footnotes.

After all three files are updated:
```bash
grep -q "M1 complete" .planning/PROJECT.md
grep -q "4/4" .planning/ROADMAP.md
grep -q "06-01-PLAN.md" .planning/ROADMAP.md
grep -q "06-04-PLAN.md" .planning/ROADMAP.md
grep -q "\[x\] \*\*Phase 6" .planning/ROADMAP.md
# Phase 6 "14 roles" cleanup gates (scoped to Phase 6 section only):
test "$(awk '/### Phase 6:/,/^### /' .planning/ROADMAP.md | grep -c '14 roles')" = "0"
test "$(awk '/### Phase 6:/,/^### /' .planning/ROADMAP.md | grep -c '14-role stack')" = "0"
# REQUIREMENTS.md hook_router-deferred annotation:
grep -E 'hook_router.*(deferred|v2)' .planning/REQUIREMENTS.md
```
All gates must return exit 0.
  </action>
  <verify>
    <automated>
      cd /home/darko/git/rockdarko/telemetron && \
      grep -q "M1 complete" .planning/PROJECT.md && \
      grep -q "4/4" .planning/ROADMAP.md && \
      grep -q "06-01-PLAN.md" .planning/ROADMAP.md && \
      grep -q "06-02-PLAN.md" .planning/ROADMAP.md && \
      grep -q "06-03-PLAN.md" .planning/ROADMAP.md && \
      grep -q "06-04-PLAN.md" .planning/ROADMAP.md && \
      grep -q "\[x\] \*\*Phase 6" .planning/ROADMAP.md && \
      grep -q "Complete (verified)" .planning/ROADMAP.md && \
      test "$(awk '/### Phase 6:/,/^### /' .planning/ROADMAP.md | grep -c '14 roles')" = "0" && \
      test "$(awk '/### Phase 6:/,/^### /' .planning/ROADMAP.md | grep -c '14-role stack')" = "0" && \
      grep -qE 'hook_router.*(deferred|v2)' .planning/REQUIREMENTS.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `.planning/PROJECT.md` contains literal string `M1 complete`.
    - `.planning/ROADMAP.md` Phase 6 row in the progress table shows `4/4` and `Complete (verified)`.
    - `.planning/ROADMAP.md` Phase 6 detail section lists all 4 Plans (06-01-PLAN.md through 06-04-PLAN.md), each prefixed with `[x]`.
    - `.planning/ROADMAP.md` top-of-file Phase 6 line is `- [x] **Phase 6:** ...` (ticked).
    - The Phase 6 section in `.planning/ROADMAP.md` no longer contains the literal phrase `14 roles` or `14-role stack` -- both have been replaced with the "13 deployed + nfsd opt-in (hook_router v2-deferred)" wording. Verified via `awk '/### Phase 6:/,/^### /' .planning/ROADMAP.md | grep -c '14 roles'` returning 0 and the same with `'14-role stack'` returning 0.
    - `.planning/REQUIREMENTS.md` INV-03 entry annotates `hook_router` as deferred to v2 (footnote or inline strike). Verified via `grep -E 'hook_router.*(deferred|v2)' .planning/REQUIREMENTS.md` returning at least 1 match line.
  </acceptance_criteria>
  <done>
.planning/PROJECT.md Active section declares M1 complete. .planning/ROADMAP.md Phase 6 progress row shows 4/4 + Complete (verified) + today's date. Phase 6 detail section lists all 4 plans as ticked. Top-of-file Phase 6 checkbox is `[x]`. Phase 6 Goal text and SC1 corrected from "14 roles"/"14-role stack" to "13 deployed + nfsd opt-in (hook_router v2-deferred)". .planning/REQUIREMENTS.md INV-03 entry annotates `hook_router` as deferred to v2 via footnote.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Live UAT on leviathan -- INV-01 acceptance + OPS-04 close-out + INV-03 13-role tags audit (D-109)</name>
  <files>.planning/phases/06-opt-in-orchestration-docs-smoke-test/06-HUMAN-UAT.md (append M1 close-out section)</files>
  <read_first>
    - .planning/phases/06-opt-in-orchestration-docs-smoke-test/06-CONTEXT.md (D-107 fourth bullet "this plan also closes the milestone"; D-109 leviathan UAT)
    - .planning/phases/06-opt-in-orchestration-docs-smoke-test/06-RESEARCH.md (Finding H idempotency contract; Open Question 3 community.docker version check on leviathan; Finding H4 nfsd-specific idempotency risks)
    - .planning/REQUIREMENTS.md (INV-01, INV-03, OPS-04 acceptance text)
    - [[project_leviathan_uat_host]] user memory
    - .planning/phases/06-opt-in-orchestration-docs-smoke-test/06-HUMAN-UAT.md (previous Plan 06-01/02/03 UAT entries to append below)
  </read_first>
        <action>
**What this task verifies (built by prior tasks in this plan):**

- README.md rewritten per D-106 (value prop pivot; component table accurate; INSPQ moved to Origin; Status: early deleted).
    - docs/README.md updated with 3-shipped + 7-v2-deferred docs tables.
    - .planning/PROJECT.md Active section updated to declare M1 complete.
    - .planning/ROADMAP.md Phase 6 row marked 4/4 + Complete (verified) + Phase 6 detail lists all 4 plans ticked.

**Verification procedure (run on leviathan per D-109):**

**This is the M1 close-out UAT. After it passes, M1 ships.**

**Step 1 -- community.docker version check (Open Question 3 from RESEARCH):**

```bash
ansible-galaxy collection list community.docker
```

If the controller version is `< 4.5.2`, upgrade:

```bash
ansible-galaxy collection install -U community.docker
```

(Docker 29 + community.docker < 4.5.2 causes false-changed reports on
port-range containers per RESEARCH §H2 finding 2; this upgrade clears
that pitfall before the idempotency gate run.)

**Step 2 -- Fresh clone walkthrough on leviathan (INV-01 acceptance):**

```bash
cd /tmp && rm -rf telemetron-m1-uat && \
git clone https://github.com/rockdarko/telemetron.git telemetron-m1-uat && \
cd telemetron-m1-uat
```

(If the working repo has uncommitted Phase 6 changes, fall back to a
copy-from-working-tree as in Plan 06-03 Task 4. The INV-01 acceptance
gate is "operator clones repo" semantics; the actual git clone vs. copy
distinction is a CI/CD concern for v2, not an INV-01 blocker.)

Follow `docs/quickstart.md` Steps 1-7 verbatim on leviathan:
1. Verify prerequisites (Ansible 2.15+, community.docker 4.5.2+ from Step 1 above, Docker on leviathan, SSH access, ports free).
2. Step 1 Clone (already done above).
3. Step 2 Edit `inventory/example-homelab/example-homelab.hosts` -- point at `leviathan`.
4. Step 3 Copy `secrets.yml.example` -> `secrets.yml`; fill in `minio_root_user`, `minio_root_password`, `grafana_admin_password`.
5. Step 5 Deploy -- record PLAY RECAP.
6. Step 5 idempotency re-run -- record PLAY RECAP `changed=0`.
7. Step 6 docker ps -- record 12 healthy containers.
8. Step 7 Smoke test -- record `Telemetron M1 smoke test PASSED`.

PASS criteria: every step succeeds without referring to anything outside
docs/quickstart.md.

**Step 3 -- INV-03 per-role tag audit:**

For every one of the 13 roles in the deploy_docker.yml roles list, run a
targeted re-deploy with `--tags <role>` and confirm it executes:

```bash
for role in minio loki tempo mimir node_exporter opentelemetry prometheus fluentbit alertmanager grafana karma promlens nfsd; do
  echo "=== Re-deploy --tags $role ==="
  ansible-playbook -i inventory/leviathan playbooks/deploy_docker.yml \
    --tags "$role" --ask-vault-pass
  echo "Exit code: $?"
done
```

PASS criteria:
- Each of the 13 invocations returns exit code 0.
- For roles other than nfsd: PLAY RECAP shows `changed=0` (idempotency holds at per-role granularity).
- For nfsd: PLAY RECAP shows `changed=0` IF `enable_nfsd: true` was set during the initial deploy (the role evaluated and was idempotent). If `enable_nfsd: false`, PLAY RECAP shows `changed=0, skipped=N` (the role was no-op'd by the when guard).

**Step 4 -- OPS-04 close-out: two back-to-back full deploys both changed=0:**

Set `enable_nfsd: true` in leviathan inventory (with a placeholder
`nfsd_exports` to exercise the full role surface), then:

```bash
# First run -- changes expected (nfsd flip from previous false state if applicable)
ansible-playbook -i inventory/leviathan playbooks/deploy_docker.yml \
  --ask-vault-pass | tee /tmp/m1-deploy-run1.log

# Second run -- MUST be changed=0
ansible-playbook -i inventory/leviathan playbooks/deploy_docker.yml \
  --ask-vault-pass | tee /tmp/m1-deploy-run2.log

# Verify second run
grep "PLAY RECAP" -A1 /tmp/m1-deploy-run2.log
```

PASS criteria: the PLAY RECAP for the second run shows `changed=0
failed=0` for the leviathan host.

If `changed > 0` on the second run: identify the offending task(s) from
the diff output, apply the same-class fix per RESEARCH §H2 (likely a
template iteration ordering issue or a known false-changed source),
re-run, and document the auto-fix.

**Step 5 -- Cross-doc audit (M1 close-out hygiene):**

```bash
cd /home/darko/git/rockdarko/telemetron
grep -E "Status: early" README.md      # must return exit 1
grep -E "Coming soon" README.md        # must return exit 1
grep -E "vault_" README.md             # must return exit 1
grep -E "vault_" docs/quickstart.md    # must return exit 1
grep -E "vault_" docs/architecture.md  # must return exit 1
grep -E "vault_" docs/inventory.md     # must return exit 1
grep -E "vault_" docs/README.md        # must return exit 1
grep -riE 'inspq|qc\.ca|montreal|québec|francais|french' docs/  # only inspq allowed in docs/quickstart.md if any (likely zero)
grep -riE '/srv/nfs/inspq|vault_inspq' .                          # must return exit 1 across the whole repo
```

All must pass.

**Step 6 -- Record outcomes in `.planning/phases/06-opt-in-orchestration-docs-smoke-test/06-HUMAN-UAT.md`:**

Append a final section "Plan 06-04 M1 close-out UAT" with:
- Step 1 result (community.docker version + any upgrade applied).
- Step 2 result (INV-01 fresh-clone walkthrough -- pass/fail per step).
- Step 3 result (INV-03 13-role tag audit -- one line per role with exit code + changed count).
- Step 4 result (OPS-04 two-deploy idempotency -- PLAY RECAP snippets).
- Step 5 result (cross-doc audit -- list any violation found and fix applied).
- Final M1 close-out declaration: "M1 SHIPPED on leviathan <YYYY-MM-DD>."

If ANY step fails: stop, capture the failure mode, attempt a same-class
auto-fix, and re-run. If unfixable, the milestone does NOT close --
report failure for human triage.

**Resume signal contract:** Type "approved -- M1 SHIPPED" if all 6 steps pass and 06-HUMAN-UAT.md final section is written. Type "issues: ..." with the specific blocker if any step fails.
  </action>
  <verify>
    <automated>echo "MANUAL UAT CHECKPOINT -- this task is a human-verify gate. Run the verification procedure documented in the action block on leviathan, append outcomes to .planning/phases/06-opt-in-orchestration-docs-smoke-test/06-HUMAN-UAT.md, then provide the resume signal documented in the action block. The execute-phase orchestrator pauses on checkpoint:human-verify tasks until the user provides the resume signal."</automated>
  </verify>
  <done>Operator has executed the verification procedure on leviathan, all listed pass-criteria are met, outcomes are recorded in 06-HUMAN-UAT.md, and the resume signal has been provided to the orchestrator. Failure: any unaddressed pass-criteria blocks proceeding to the next plan.</done>
</task>

</tasks>

<verification>
- README.md is rewritten with the D-106 structure (value prop, quick start, component table, Not in M1, Requirements, Layout, Inventory model, Origin, License, Author).
- README.md has ZERO `Status: early`, ZERO `Coming soon`, ZERO `vault_`, ZERO non-ASCII characters.
- docs/README.md has 3 M1-shipped + 7 v2-deferred docs tables with DOCS-V2-01..07 IDs.
- .planning/PROJECT.md Active section declares `M1 complete`.
- .planning/ROADMAP.md Phase 6 row is `4/4` `Complete (verified)`; Phase 6 detail lists all 4 plans ticked; top-of-file Phase 6 checkbox is `[x]`.
- Leviathan UAT: INV-01 fresh-clone walkthrough passes; INV-03 13-role tag audit passes; OPS-04 two-deploys-both-changed=0 passes; cross-doc audit passes.
</verification>

<success_criteria>
- DOCS-04 acceptance: top-level README.md reflects what's actually shipped (no "early"/"skeleton only" language) and points at docs/quickstart.md.
- INV-01 acceptance: operator clones repo, edits single hostname + SSH-user pair, supplies vault password, runs single ansible-playbook command, and the full M1 stack comes up end-to-end on a fresh Docker host (demonstrated on leviathan).
- INV-03 acceptance: deploy_docker.yml orchestrates all 13 deployed roles + nfsd opt-in in dependency order; every role has a tag so `--tags <role>` works (audited on leviathan).
- OPS-04 close-out: second back-to-back deploy reports `changed=0` (audited on leviathan).
- .planning/PROJECT.md + .planning/ROADMAP.md bookkeeping reflects M1 close-out.
- Live UAT recorded on leviathan per D-109.
</success_criteria>

<output>
After completion, create `.planning/phases/06-opt-in-orchestration-docs-smoke-test/06-04-SUMMARY.md` capturing:
- Files modified (README.md, docs/README.md, .planning/PROJECT.md, .planning/ROADMAP.md).
- Hard-check audit results (all grep gates pass on README.md and docs/).
- community.docker version on leviathan post-upgrade (Open Question 3 close-out).
- INV-01 fresh-clone walkthrough result.
- INV-03 13-role tag audit result.
- OPS-04 two-back-to-back-deploys idempotency result.
- M1 SHIPPED declaration with date.
- Forward note: next planning is v2 milestone scoping (Garage migration / multi-host inventory / Kubernetes path / hook router).
</output>
