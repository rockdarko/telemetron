---
phase: 02-telemetry-backends
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - roles/loki/defaults/main.yml
  - roles/loki/meta/main.yml
  - roles/loki/handlers/main.yml
  - roles/loki/tasks/main.yml
  - roles/loki/tasks/verify.yml
  - roles/loki/templates/loki.yaml.j2
  - roles/loki/README.md
  - inventory/example-homelab/group_vars/all/loki.yml
  - inventory/example-homelab/group_vars/all/vault.yml.example
  - playbooks/deploy_docker.yml
  - roles/README.md
autonomous: true
requirements:
  - BACK-01
  - BACK-02
user_setup:
  - service: docker
    why: "Loki container + one-shot curl verify container both require a working Docker daemon on the target host."
    env_vars: []
    dashboard_config:
      - task: "Extend inventory/example-homelab/group_vars/all/vault.yml with vault_loki_s3_access_key and vault_loki_s3_secret_key (aliases to vault_minio_root_user / vault_minio_root_password per D-27); re-encrypt with ansible-vault"
        location: "Ansible control host shell (operator-side; copy values from the Phase-1 minio vault block)"
must_haves:
  truths:
    - "Operator runs `ansible-playbook -i inventory/example-homelab playbooks/deploy_docker.yml --tags loki --ask-vault-pass` and the loki container starts on the telemetron network, reaches MinIO at http://minio:9000, and reports HEALTHCHECK status `healthy` within ~60s"
    - "An in-network curl one-shot POSTs a synthetic log line to http://loki:3100/loki/api/v1/push and Loki accepts it (HTTP 204); within ~10s a fresh object key lands under MinIO's loki-chunks bucket (verified via `mc ls --json local/loki-chunks`)"
    - "Operator overrides `loki_retention_period: 7d` in inventory/example-homelab/group_vars/all/loki.yml, re-runs the role, and `curl -s http://loki:3100/config` (inside the telemetron network) reflects retention_period: 7d under limits_config"
    - "The compactor markers directory `/loki/compactor/markers/` is covered by the persistent named volume `telemetron_loki_data` mounted at `/loki` (Pitfall 12 mitigation)"
    - "Loki gRPC port is explicitly 9095 in the rendered config; Loki HTTP port is 3100; neither port is published to the host by default (loki_publish_host: false)"
    - "Running the playbook a second time on the converged host reports `changed=0` (OPS-04)"
    - "Grep gates pass: zero INSPQ leftovers and zero non-ASCII characters in roles/loki/"
    - "Image-pin gate passes: roles/loki/ contains `grafana/loki:3.7.2` and zero `:latest` references"
  artifacts:
    - path: "roles/loki/defaults/main.yml"
      provides: "All Loki role tunables (image pin grafana/loki:3.7.2, ports, volume, retention knob, D-37 limits, D-39 schema/compactor specifics, healthcheck timing, restart policy)"
      contains: "grafana/loki"
    - path: "roles/loki/templates/loki.yaml.j2"
      provides: "Full Loki monolithic config: auth_enabled:false (D-26), server ports (D-28/D-31), TSDB schema_config (D-39), storage_config.aws against MinIO with s3forcepathstyle:true (D-39), compactor with working_directory:/loki/compactor + retention (D-33 + Pitfall 12), limits_config with D-37 knobs"
      contains: "auth_enabled: false"
    - path: "roles/loki/tasks/main.yml"
      provides: "Render config + ensure volume + pull image + run container with HEALTHCHECK + restart policy + no host publish (default); includes verify.yml as final task"
      contains: "community.docker.docker_container"
    - path: "roles/loki/tasks/verify.yml"
      provides: "D-10a HEALTHCHECK poll via docker_container_info, in-network one-shot curl POST to /loki/api/v1/push (W8 synthetic verify), in-network mc one-shot asserting loki-chunks has objects"
      contains: "docker_container_info"
    - path: "roles/loki/handlers/main.yml"
      provides: "Single handler running `docker restart {{ loki_container_name }}` on config change (W6, D-19); never state: restarted"
      contains: "docker restart"
    - path: "roles/loki/meta/main.yml"
      provides: "Galaxy metadata + community.docker collection dependency"
      contains: "community.docker"
    - path: "roles/loki/README.md"
      provides: "Per-role README following the canonical roles/minio/README.md schema; includes Variables / Vault keys / Tags / Modes (monolithic only, auth_enabled:false) / Volumes / Healthcheck (Loki -health binary) / Security model (no host publish, ssh -L 3100 for operator access) / Idempotency / Port-acceptance gates / Operator access / Deviations from upstream INSPQ (D-25 — INSPQ-isms dropped + Pitfall 12 guard added + D-37 limits added) / Deprecation notes / Bring your own bucket name"
      min_lines: 100
    - path: "inventory/example-homelab/group_vars/all/loki.yml"
      provides: "Operator-facing Loki knobs: loki_publish_host:false (D-30) and loki_retention_period (aliased to telemetron_default_log_retention) — the BACK-02 knob path"
      contains: "loki_publish_host"
    - path: "inventory/example-homelab/group_vars/all/vault.yml.example"
      provides: "Extended with two new keys: vault_loki_s3_access_key and vault_loki_s3_secret_key (alias values per D-27)"
      contains: "vault_loki_s3_access_key"
    - path: "playbooks/deploy_docker.yml"
      provides: "Now includes loki role after minio with tag [loki]"
      contains: "role: loki"
    - path: "roles/README.md"
      provides: "Status table row for `loki` flipped to ported (☑); deviations summary referenced from role README"
      contains: "loki"
  key_links:
    - from: "roles/loki/tasks/main.yml (final task)"
      to: "roles/loki/tasks/verify.yml"
      via: "ansible.builtin.include_tasks at end of tasks/main.yml — verify is part of the role, mirrors roles/minio/tasks/bootstrap.yml inclusion pattern"
      pattern: "include_tasks:.*verify"
    - from: "roles/loki/tasks/verify.yml (pre-poll)"
      to: "Loki container Docker HEALTHCHECK status"
      via: "community.docker.docker_container_info polling State.Health.Status until healthy (D-10a — Loki 3.7.2 ships native /usr/bin/loki -health binary per RESEARCH Finding 6)"
      pattern: "docker_container_info"
    - from: "roles/loki/tasks/verify.yml (synthetic push)"
      to: "MinIO loki-chunks bucket"
      via: "one-shot curlimages/curl:8.10.1 container POSTs to http://loki:3100/loki/api/v1/push; then one-shot minio/mc container runs `mc ls --json local/loki-chunks` and asserts >=1 object key (W8)"
      pattern: "loki/api/v1/push"
    - from: "roles/loki/templates/loki.yaml.j2"
      to: "MinIO at http://minio:9000 with bucket loki-chunks"
      via: "storage_config.aws.endpoint (with http:// scheme — Loki format per RESEARCH); s3forcepathstyle:true; vault_loki_s3_access_key / vault_loki_s3_secret_key (D-27)"
      pattern: "storage_config:|http://minio:9000"
    - from: "roles/loki/defaults/main.yml + inventory/example-homelab/group_vars/all/loki.yml"
      to: "templates/loki.yaml.j2 limits_config.retention_period"
      via: "loki_retention_period knob (default 14d via telemetron_default_log_retention) rendered into the Jinja template — BACK-02 single-knob retention path"
      pattern: "loki_retention_period"
    - from: "playbooks/deploy_docker.yml roles: list"
      to: "roles/loki/"
      via: "- role: loki tags: [loki] appended after minio entry (D-23)"
      pattern: "role: loki"
---

<objective>
Port the Loki role from the upstream INSPQ stack into `roles/loki/`, configured for Loki 3.7.2 monolithic mode (`-target=all`) against the MinIO `loki-chunks` bucket bootstrapped in Phase 1. Wire the role into `playbooks/deploy_docker.yml` after `minio` (per D-23) so the playbook stays runnable at every commit (minio → loki).

This plan mirrors the Phase-1 `roles/minio/` canonical template verbatim for layout, handler discipline, healthcheck pre-poll, in-network verify pattern, and README schema. The Loki-specific work is: the monolithic config template (auth_enabled:false per D-26; TSDB schema; S3 storage_config against MinIO with `s3forcepathstyle:true`; compactor with `working_directory:/loki/compactor` and `retention_enabled:true` per D-33 + Pitfall 12; `limits_config` with D-37 label-discipline knobs and the `retention_period` BACK-02 knob); the explicit gRPC port pin `9095` (D-28); the Loki-native HEALTHCHECK using `/usr/bin/loki -health` (distroless image, no shell — research Finding 6).

Purpose:
1. Deliver BACK-01 (Loki running monolithic against `loki-chunks`, persistent volume covering `/loki/compactor/markers/`) and BACK-02 (`loki_retention_period` group_vars knob, documented in README).
2. Carry forward every Phase-1 cross-cutting gate (OPS-01..06) to a second role port — proves the canonical template is reusable.
3. Land an opinionated improvement pass over the INSPQ upstream per D-25 — drop `latest` tag, France-locale timezone, French task names, distributed-mode dead code (memberlist, bloom-filter, pattern-ingester), table-manager retention (replaced by compactor retention in Loki 3.x), and INSPQ bucket name `loki` (replaced with `loki-chunks`); ADD Pitfall-12 compactor working_directory guard, D-37 label-discipline limits, and explicit gRPC port pin.

Output: 7 files in `roles/loki/` (defaults, meta, handlers, tasks/main.yml + tasks/verify.yml, templates/loki.yaml.j2, README.md) + 1 new inventory file (`group_vars/all/loki.yml`) + extension to `vault.yml.example` + wiring in `playbooks/deploy_docker.yml` + status table flip in `roles/README.md`. Total: 11 modified files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/02-telemetry-backends/02-CONTEXT.md
@.planning/phases/02-telemetry-backends/02-RESEARCH.md
@.planning/phases/01-foundation-storage/01-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/research/STACK.md
@roles/minio/defaults/main.yml
@roles/minio/meta/main.yml
@roles/minio/handlers/main.yml
@roles/minio/tasks/main.yml
@roles/minio/tasks/bootstrap.yml
@roles/minio/templates/minio.env.j2
@roles/minio/README.md
@roles/README.md
@inventory/example-homelab/group_vars/all/network.yml
@inventory/example-homelab/group_vars/all/storage.yml
@inventory/example-homelab/group_vars/all/vault.yml.example
@inventory/example-homelab/group_vars/all/minio.yml
@playbooks/deploy_docker.yml
</context>

<interfaces>
<!-- Key contracts an executor needs without exploring the codebase. -->

From inventory/example-homelab/group_vars/all/network.yml:
- `telemetron_network: telemetron`  (network name Loki attaches to)
- `telemetron_publish_default: false` (default for *_publish_host knobs)
- `telemetron_tz: Etc/UTC` (container TZ; Loki defaults consume via fallback)

From inventory/example-homelab/group_vars/all/storage.yml:
- `telemetron_volume_prefix: telemetron`  (Loki data volume name = `telemetron_loki_data`)
- `telemetron_config_root: /opt/telemetron` (Loki config dir = `/opt/telemetron/loki`)
- `telemetron_minio_buckets` includes `loki-chunks` — Loki S3 config consumes this bucket name
- `telemetron_default_log_retention: 14d` — Loki retention default surface

From roles/minio (canonical patterns to mirror):
- defaults/main.yml shape: `<role>_image` + `<role>_image_tag` separated; `<role>_container_name`; `<role>_publish_host` + per-port `<role>_*_port`; `<role>_data_volume` + `<role>_data_path`; `<role>_config_dir`; `<role>_health_retries`/`<role>_health_delay`; `<role>_healthcheck_*` timing; `<role>_restart_policy`; `<role>_memory_limit`; `<role>_network`; `<role>_tz`
- handlers/main.yml shape: ONE handler named "Docker restart <role>" using `ansible.builtin.command: cmd: "docker restart {{ <role>_container_name }}"`, `changed_when: true`, `listen: restart <role>` — no `state: restarted` anywhere
- tasks/main.yml shape: 6 tasks in order — (1) ensure config dir, (2) render template (notify restart handler), (3) ensure named volume, (4) pull image, (5) run docker_container with healthcheck+restart_policy+no-host-publish-default, (6) include_tasks: verify.yml as final blocking task
- tasks/verify.yml shape: (Step 1) docker_container_info poll until State.Health.Status == 'healthy' with changed_when:false; (Step 2) one-shot synthetic push container with detach:false + auto_remove:true + changed_when:false + failed_when status!=0; (Step 3) one-shot mc/jq-style assertion against MinIO bucket with same flags
- Vault key reference convention: `{{ vault_<role>_<purpose> }}`

Loki 3.7.2 image entrypoint (RESEARCH Finding 9): `/usr/bin/loki`
Loki 3.7.2 HEALTHCHECK (RESEARCH Finding 6 + Pitfall A): `CMD ["/usr/bin/loki", "-health"]` — native binary flag; CMD form (not CMD-SHELL); image has no shell.
Loki 3.7.2 startup command: `-config.file=/etc/loki/loki.yaml -target=all`
Loki 3.7.2 in-container config path (RESEARCH Finding 2): `/etc/loki/loki.yaml`
Loki 3.7.2 in-container data root (RESEARCH Finding 2 + 7): `/loki` (covers tsdb-index, tsdb-cache, chunks, compactor/ with markers/)
Loki S3 endpoint format (RESEARCH Finding 2 + Storage gotcha): `endpoint: http://minio:9000` — WITH `http://` prefix in `storage_config.aws.endpoint` (Loki-specific; Tempo/Mimir omit the scheme).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create roles/loki/ skeleton — defaults, meta, handlers, templates, README + inventory file + vault.yml.example extension</name>
  <read_first>
    - .planning/phases/02-telemetry-backends/02-CONTEXT.md (D-25, D-26, D-27, D-28, D-30, D-31, D-33, D-37, D-39 — every Loki-shaped decision)
    - .planning/phases/02-telemetry-backends/02-RESEARCH.md §"Finding 2: Loki 3.7.2 Config Shape" (full config block — copy verbatim into the template with documented edits) AND §"Finding 6: Healthcheck Endpoints" (Loki -health binary) AND §"Finding 7: Volume Layout" (one-volume rule for Loki) AND §"INSPQ Upstream Audit — Loki INSPQ-isms Found" (the D-25 deviation table — every row drives a write decision)
    - .planning/research/PITFALLS.md §"Pitfall 4: Loki label discipline" (backs D-37 limits) AND §"Pitfall 12: Loki compactor marker-file loss on container recreate" (backs working_directory + one-volume) AND §"Performance Traps" (Loki chunk_target_size guidance — Claude's Discretion D-39)
    - roles/minio/defaults/main.yml (canonical structure to mirror — read fully; every variable section in the Loki defaults file mirrors a section here)
    - roles/minio/meta/main.yml (mirror galaxy_info + collections; only role_name and description change)
    - roles/minio/handlers/main.yml (mirror one-handler-only shape per W6; only the listen + container name change)
    - roles/minio/README.md (the canonical README schema — Loki README mirrors section-for-section, ASCII-only)
    - roles/minio/templates/minio.env.j2 (template-style precedent: `{{ ansible_managed }}` header + DO NOT EDIT comment)
    - inventory/example-homelab/group_vars/all/minio.yml (the per-role inventory file shape — Loki version mirrors this)
    - inventory/example-homelab/group_vars/all/vault.yml.example (the per-role vault keys append shape — already has Phase-1 section delimited by `# --- Phase 1: MinIO ---`; Phase-2 Loki section is appended below)
  </read_first>
  <files>roles/loki/defaults/main.yml, roles/loki/meta/main.yml, roles/loki/handlers/main.yml, roles/loki/templates/loki.yaml.j2, roles/loki/README.md, inventory/example-homelab/group_vars/all/loki.yml, inventory/example-homelab/group_vars/all/vault.yml.example</files>
  <action>
    Create the directory structure:
    ```bash
    mkdir -p roles/loki/{defaults,meta,handlers,tasks,templates,vars}
    ```

    Then write each file. **All files ASCII-only.** Use plain `--` (two hyphens), no em dashes, no smart quotes, no accented characters. The non-ASCII grep gate `grep -rPn '[^\x00-\x7F]' roles/loki/` MUST return zero matches when this task completes.

    ---

    **File 1: `roles/loki/defaults/main.yml`**

    Mirror the section-by-section shape of `roles/minio/defaults/main.yml`. Write exactly:

    ```yaml
    ---
    # roles/loki/defaults/main.yml
    # Loki role tunables. Override per-environment in
    # inventory/<env>/group_vars/all/loki.yml.
    #
    # Loki 3.7.2 monolithic mode (-target=all). Storage backend: MinIO S3
    # (loki-chunks bucket bootstrapped by roles/minio in Phase 1). Single
    # tenant only (auth_enabled: false per CONTEXT.md D-26).

    # --- Image pin (OPS-01: pin every image tag explicitly) ---
    # Loki 3.7.2 (2026-05-13) -- latest stable; TSDB schema default.
    # See: https://github.com/grafana/loki/releases/tag/v3.7.2
    loki_image: grafana/loki
    loki_image_tag: "3.7.2"

    # --- Container identity ---
    # Container name on the telemetron network. Other roles reach Loki at
    # http://{{ loki_container_name }}:3100. Future phases (Grafana, OTel,
    # Fluent Bit) consume this DNS name -- coordinate any change.
    loki_container_name: loki

    # --- Host port publishing (D-30, D-12, D-14) ---
    # Default: zero host publish. Inter-component traffic stays on the
    # telemetron Docker bridge network. Operator access via SSH local-forward:
    #   ssh -L 3100:localhost:3100 <host>
    # Override to `true` (publishes to 0.0.0.0) or `127.0.0.1` (loopback only)
    # in inventory/<env>/group_vars/all/loki.yml.
    loki_publish_host: "{{ loki_publish_host | default(false) }}"

    # --- Port matrix (D-28, D-31) ---
    # Loki HTTP API on :3100. Loki gRPC on :9095 (explicitly pinned per D-28;
    # Tempo moves to 9096, Mimir to 9097 -- all three default 9095 and would
    # collide on a single host).
    loki_http_port: 3100
    loki_grpc_port: 9095

    # --- Storage layout (D-16, D-17, D-18, Pitfall 12) ---
    # One named Docker volume covers MinIO chunks-related local state:
    # tsdb-index, tsdb-cache, and -- critically -- compactor/markers/.
    # See PITFALLS.md Pitfall 12 (marker file loss on container recreate).
    loki_data_volume: "{{ telemetron_volume_prefix | default('telemetron') }}_loki_data"
    loki_data_path: /loki

    # Host-side config directory (D-18 flat layout). Rendered Jinja config
    # lives here; bind-mounted read-only into the container.
    loki_config_dir: "{{ telemetron_config_root | default('/opt/telemetron') }}/loki"

    # --- S3 backend (D-39, RESEARCH Finding 2) ---
    # MinIO endpoint on the telemetron bridge -- Loki's storage_config.aws.endpoint
    # uses the http:// prefix (Loki-specific format; Tempo/Mimir omit scheme).
    # MinIO requires s3forcepathstyle: true; insecure: true keeps plain HTTP.
    loki_s3_endpoint: "http://minio:9000"
    loki_s3_bucket: loki-chunks
    loki_s3_region: us-east-1
    loki_s3_force_path_style: true
    loki_s3_insecure: true

    # --- Retention (D-33, BACK-02) ---
    # Compactor-driven retention (Loki 3.x; table_manager is dead code).
    # The single operator-facing knob; default ships at 14d via
    # telemetron_default_log_retention. Surfaced again in the inventory
    # loki.yml so operators see the override path without grep-hunting.
    loki_retention_period: "{{ telemetron_default_log_retention | default('14d') }}"
    loki_compactor_retention_enabled: true
    loki_compactor_retention_delete_delay: 2h
    loki_compactor_retention_delete_worker_count: 150
    loki_compactor_working_directory: /loki/compactor

    # --- Limits (D-37, PITFALLS Pitfall 4) ---
    # Loki rejects loudly when label discipline breaks. Fluent Bit's
    # complementary source-side allowlist ships in Phase 3.
    loki_max_streams_per_user: 5000
    loki_max_label_value_length: 2048
    loki_max_label_names_per_series: 15
    loki_reject_old_samples_max_age: 168h
    loki_max_entries_limit_per_query: 1000

    # --- Ingester chunking (Claude's Discretion per D-39; PITFALLS Performance Traps) ---
    loki_chunk_idle_period: 30m
    loki_chunk_target_size: 1572864
    loki_chunk_retain_period: 30s
    loki_max_chunk_age: 2h

    # --- Schema (D-39, RESEARCH Finding 2) ---
    # TSDB schema (Loki 2.8+ default; supersedes BoltDB-shipper).
    # `from` is an arbitrary past date -- v13 is the active TSDB schema.
    loki_schema_from: "2023-01-01"
    loki_schema_version: v13
    loki_schema_index_prefix: tsdb_index_

    # --- Verify pre-poll (D-10a, RESEARCH Finding 6 Loki -health binary) ---
    # The verify one-shot only runs after Loki's Docker HEALTHCHECK reports
    # `healthy` (polled via community.docker.docker_container_info).
    # 60s total budget = 30 retries x 2s.
    loki_health_retries: 30
    loki_health_delay: 2

    # --- Healthcheck (OPS-06, RESEARCH Finding 6) ---
    # Loki 3.7.2 ships /usr/bin/loki -health (binary-native; the image is
    # distroless -- no shell, so CMD-SHELL wget patterns from older guides
    # do NOT work). See PITFALLS Pitfall A in RESEARCH.md.
    loki_healthcheck_test: ["CMD", "/usr/bin/loki", "-health"]
    loki_healthcheck_interval: 10s
    loki_healthcheck_timeout: 5s
    loki_healthcheck_retries: 5
    loki_healthcheck_start_period: 30s

    # --- Restart policy (OPS-06) ---
    loki_restart_policy: unless-stopped

    # --- Resource limits (homelab-safe; tune per environment) ---
    # Loki ingester is the memory hog; 1G fits a homelab single-tenant.
    loki_memory_limit: 1g

    # --- Container identity follow-ups ---
    loki_container_user: "1000:0"

    # --- Network ---
    # Created by playbooks/deploy_docker.yml pre_tasks; this role attaches.
    loki_network: "{{ telemetron_network | default('telemetron') }}"

    # --- Timezone (OPS-06; PITFALLS Pitfall 6 Etc/UTC default) ---
    loki_tz: "{{ telemetron_tz | default('Etc/UTC') }}"
    ```

    ---

    **File 2: `roles/loki/meta/main.yml`**

    Content (mirrors roles/minio/meta/main.yml; only role_name and description change):

    ```yaml
    ---
    galaxy_info:
      role_name: loki
      author: Rock Martel-Langlois
      description: >
        Deploys Grafana Loki 3.7.2 in monolithic mode against MinIO S3
        (loki-chunks bucket). Log backend for Telemetron's observability
        plane; consumed by Grafana (Phase 5) and Fluent Bit / OTel (Phase 3).
      license: MIT
      min_ansible_version: "2.15"
      platforms:
        - name: Ubuntu
          versions: [jammy, noble]
        - name: Debian
          versions: [bookworm]
      galaxy_tags:
        - logs
        - loki
        - grafana
        - observability
        - telemetron

    dependencies: []

    collections:
      - community.docker
      - ansible.builtin
    ```

    ---

    **File 3: `roles/loki/handlers/main.yml`**

    Single handler per W6; mirrors the minio handler verbatim with name + listen + container name swapped:

    ```yaml
    ---
    # roles/loki/handlers/main.yml
    # Per D-19 / Pitfall 8: container restarts on config change use
    # `docker restart <name>` (preserves container state). The module-level
    # `state` parameter is intentionally never used for restarts -- force-recreate
    # is non-idempotent.
    #
    # Loki config changes require a container restart (no SIGHUP reload
    # path for storage_config or limits_config changes), so the config-file
    # template notifies this handler.

    - name: Docker restart loki
      ansible.builtin.command:
        cmd: "docker restart {{ loki_container_name }}"
      changed_when: true
      listen: restart loki
    ```

    ---

    **File 4: `roles/loki/templates/loki.yaml.j2`**

    The full monolithic config from RESEARCH Finding 2, with every `{{ var }}` pulled from `roles/loki/defaults/main.yml`. Use sorted-keys Jinja iteration ONLY where iterating over dicts (D-20); fixed sections are emitted verbatim. Inline-comment Pitfall references per D-36/D-37 contract.

    ```jinja
    # {{ ansible_managed }}
    # roles/loki/templates/loki.yaml.j2 -- rendered by roles/loki.
    # DO NOT EDIT BY HAND -- changes here are overwritten by ansible-playbook.
    #
    # Loki 3.7.2 monolithic mode (-target=all). See RESEARCH.md Finding 2 for
    # the verified config shape. Pitfall references inline -- do not strip
    # the comments; they are the load-bearing documentation for future
    # maintenance edits.

    auth_enabled: false   # CONTEXT.md D-26 -- single tenant; no X-Scope-OrgID

    server:
      http_listen_port: {{ loki_http_port }}
      grpc_listen_port: {{ loki_grpc_port }}   # CONTEXT.md D-28 -- explicit pin
      grpc_server_max_recv_msg_size: 10485760
      grpc_server_max_send_msg_size: 10485760

    common:
      path_prefix: {{ loki_data_path }}

    ingester:
      lifecycler:
        address: 127.0.0.1
        ring:
          kvstore:
            store: inmemory
          replication_factor: 1
        final_sleep: 0s
      max_chunk_age: {{ loki_max_chunk_age }}
      chunk_idle_period: {{ loki_chunk_idle_period }}
      chunk_target_size: {{ loki_chunk_target_size }}
      chunk_retain_period: {{ loki_chunk_retain_period }}
      wal:
        enabled: false   # monolithic; no distributed WAL needed

    schema_config:
      configs:
        - from: "{{ loki_schema_from }}"
          store: tsdb
          object_store: s3
          schema: {{ loki_schema_version }}
          index:
            prefix: {{ loki_schema_index_prefix }}
            period: 24h   # 24h is required for compactor retention support

    storage_config:
      tsdb_shipper:
        active_index_directory: {{ loki_data_path }}/tsdb-index
        cache_location: {{ loki_data_path }}/tsdb-cache
      aws:
        endpoint: {{ loki_s3_endpoint }}   # Loki format: http:// prefix included
        bucketnames: {{ loki_s3_bucket }}
        access_key_id: "{{ '{{' }} vault_loki_s3_access_key {{ '}}' }}"
        secret_access_key: "{{ '{{' }} vault_loki_s3_secret_key {{ '}}' }}"
        region: {{ loki_s3_region }}
        s3forcepathstyle: {{ loki_s3_force_path_style | string | lower }}
        insecure: {{ loki_s3_insecure | string | lower }}

    compactor:
      working_directory: {{ loki_compactor_working_directory }}   # PITFALLS Pitfall 12 -- on persistent volume
      delete_request_store: s3   # required for S3 backend in Loki 3.x
      retention_enabled: {{ loki_compactor_retention_enabled | string | lower }}
      retention_delete_delay: {{ loki_compactor_retention_delete_delay }}
      retention_delete_worker_count: {{ loki_compactor_retention_delete_worker_count }}

    limits_config:
      reject_old_samples: true
      reject_old_samples_max_age: {{ loki_reject_old_samples_max_age }}
      allow_structured_metadata: true
      max_entries_limit_per_query: {{ loki_max_entries_limit_per_query }}
      volume_enabled: true
      retention_period: {{ loki_retention_period }}   # BACK-02 single knob (CONTEXT.md D-33)
      # D-37 label discipline (PITFALLS Pitfall 4):
      max_streams_per_user: {{ loki_max_streams_per_user }}
      max_label_value_length: {{ loki_max_label_value_length }}
      max_label_names_per_series: {{ loki_max_label_names_per_series }}
    ```

    **IMPORTANT — Jinja escaping for vault references:** The template above uses `{{ '{{' }} vault_loki_s3_access_key {{ '}}' }}` because the Jinja rendering happens at *Ansible variable expansion time*, but the resulting `{{ vault_loki_s3_access_key }}` should resolve at *task execution time* (Ansible re-resolves variables in the rendered file? NO — actually Ansible does NOT re-evaluate Jinja in files after template rendering). The vault values must be substituted AT TEMPLATE RENDER TIME. **Correction:** simply write `{{ vault_loki_s3_access_key }}` directly in the template — Ansible's `template:` task expands all `{{ }}` references using the variable context (which includes vault keys) and writes the result to disk. The escape pattern above is WRONG. Rewrite those two lines as:

    ```jinja
          access_key_id: "{{ vault_loki_s3_access_key }}"
          secret_access_key: "{{ vault_loki_s3_secret_key }}"
    ```

    (Plain Jinja substitution; the rendered loki.yaml contains the actual secret values. The file is rendered with mode 0600 in tasks/main.yml to limit on-disk exposure. Document this in the role README's Security model section, mirroring the minio README's D-09/I12 note.)

    ---

    **File 5: `roles/loki/README.md`**

    Mirror the canonical schema from `roles/minio/README.md` section-by-section. Minimum 100 lines, ASCII-only.

    ```markdown
    # roles/loki

    Deploys [Grafana Loki](https://github.com/grafana/loki) 3.7.2 in
    monolithic mode (`-target=all`) on a single Docker host. Loki is the
    log backend for Telemetron's observability plane; logs land via OTel
    Collector / Fluent Bit (Phase 3) and are queried via Grafana (Phase 5).

    Storage backend: MinIO S3 (the `loki-chunks` bucket bootstrapped by
    `roles/minio` in Phase 1).

    Mirrors the canonical role template established by `roles/minio` --
    same defaults layout, same handler discipline (W6 single handler),
    same Docker HEALTHCHECK pre-poll (D-10a), same in-network verify
    pattern (W8).

    ## What this role does

    1. Renders `/opt/telemetron/loki/loki.yaml` from `templates/loki.yaml.j2`
       with MinIO S3 credentials pulled from Ansible vault.
    2. Ensures the named Docker volume `telemetron_loki_data` exists.
    3. Starts the `loki` container on the `telemetron` Docker bridge
       network, with a Docker `HEALTHCHECK` (`/usr/bin/loki -health`) and
       `restart: unless-stopped`. Container port `3100` is NOT published
       to the host by default (`loki_publish_host: false`).
    4. **As a blocking final task**, polls Loki's Docker HEALTHCHECK via
       `community.docker.docker_container_info` until `healthy`, then
       pushes a synthetic log line via a one-shot curl container and
       asserts that an object key landed in `loki-chunks` via a one-shot
       `mc` container.

    ## Variables

    See `defaults/main.yml` for the full list. Operators commonly only
    touch `loki_retention_period` (default `14d`) and `loki_publish_host`
    (default `false`).

    | Variable | Default | Purpose |
    |----------|---------|---------|
    | `loki_image` | `grafana/loki` | Image (do not change) |
    | `loki_image_tag` | `3.7.2` | Pinned tag |
    | `loki_container_name` | `loki` | DNS name on the `telemetron` network |
    | `loki_publish_host` | `false` | Publish :3100 to host. `false`/`true`/`127.0.0.1` |
    | `loki_http_port` | `3100` | HTTP API port |
    | `loki_grpc_port` | `9095` | gRPC port (D-28 explicit pin) |
    | `loki_data_volume` | `telemetron_loki_data` | Named Docker volume |
    | `loki_data_path` | `/loki` | In-container data root (covers compactor/markers/) |
    | `loki_config_dir` | `/opt/telemetron/loki` | Host config bind-mount source |
    | `loki_retention_period` | `14d` (via telemetron_default_log_retention) | **BACK-02** retention knob |
    | `loki_compactor_working_directory` | `/loki/compactor` | Pitfall 12 -- on persistent volume |
    | `loki_max_streams_per_user` | `5000` | D-37 / Pitfall 4 |
    | `loki_max_label_value_length` | `2048` | D-37 / Pitfall 4 |
    | `loki_max_label_names_per_series` | `15` | D-37 / Pitfall 4 |
    | `loki_s3_endpoint` | `http://minio:9000` | MinIO endpoint (note: scheme included for Loki) |
    | `loki_s3_bucket` | `loki-chunks` | MinIO bucket |
    | `loki_healthcheck_test` | `["CMD", "/usr/bin/loki", "-health"]` | Native binary; image is distroless |
    | `loki_restart_policy` | `unless-stopped` | Container restart policy |
    | `loki_memory_limit` | `1g` | Container memory limit |
    | `loki_network` | `telemetron` | Docker network |
    | `loki_tz` | `Etc/UTC` | Container timezone |

    ## Vault keys

    Required keys in `inventory/<env>/group_vars/all/vault.yml`:

    | Key | Purpose |
    |-----|---------|
    | `vault_loki_s3_access_key` | MinIO access key for the `loki-chunks` bucket |
    | `vault_loki_s3_secret_key` | MinIO secret key for the `loki-chunks` bucket |

    Per CONTEXT.md D-27, both keys are aliased to `vault_minio_root_user` /
    `vault_minio_root_password` in `vault.yml.example` for now. A future
    hardening milestone will split MinIO into per-backend users with
    bucket-scoped IAM policies; only `vault.yml` changes -- the role
    templates do not.

    ## Tags

    - `loki` -- runs the whole role (D-24 single tag per role)

    ## Modes

    Single mode only in M1: monolithic (`-target=all`). Single-tenant
    (`auth_enabled: false` per D-26 -- no `X-Scope-OrgID` header needed
    anywhere; all data lives in tenant `fake`). Distributed Loki and
    multi-tenant operation are deferred to a future milestone.

    ## Volumes

    | Volume | Mount | Purpose |
    |--------|-------|---------|
    | `telemetron_loki_data` (named) | `/loki` | Loki data root -- covers tsdb-index, tsdb-cache, chunks, compactor/ (incl. markers/). Persistent across container recreates. Pitfall 12 mitigation -- marker files survive recreate. |
    | `/opt/telemetron/loki/loki.yaml` (bind) | `/etc/loki/loki.yaml` (ro) | Rendered config |

    ## Healthcheck

    The Loki image is distroless (no shell). The Docker HEALTHCHECK uses
    Loki's native `-health` binary flag introduced in Loki 3.6+:

    ```
    HEALTHCHECK: ["CMD", "/usr/bin/loki", "-health"]
    ```

    Verify with:

    ```bash
    docker inspect loki --format '{{.State.Health.Status}}'   # expects: healthy
    ```

    The verify subtask polls this same HEALTHCHECK status via
    `community.docker.docker_container_info` (reading
    `result.container.State.Health.Status`) before pushing the synthetic
    payload, so first-deploy timing races are eliminated without
    requiring host port publishing.

    ## Operator access (no host publish by default per D-30)

    Inter-component traffic on the `telemetron` bridge reaches Loki at
    `http://loki:3100`. Operator access from a workstation uses SSH
    local-forward:

    ```bash
    ssh -L 3100:localhost:3100 <homelab-host>
    # then browse to http://localhost:3100/ready -- expects: ready
    ```

    ## Security model

    - **Default: no host port publish.** Operator access via SSH local
      forward.
    - **S3 credentials from vault.** The rendered `/opt/telemetron/loki/loki.yaml`
      is written with mode `0600` and contains the resolved vault values
      at rest. If you trust the host's filesystem permissions but not
      the host itself, this is the same posture as the minio role's
      env file. Future hardening: per-backend MinIO users with bucket-scoped
      IAM (deferred from M1 per D-27).
    - **Inter-component traffic on the `telemetron` bridge only.** Loki
      reaches MinIO at `http://minio:9000` -- never via the host.

    ## Idempotency

    Per OPS-04: running the playbook twice in a row reports `changed=0`.
    Verify with:

    ```bash
    ansible-playbook -i inventory/example-homelab \
                     playbooks/deploy_docker.yml --tags loki --ask-vault-pass
    # ...first run: changed=N
    ansible-playbook -i inventory/example-homelab \
                     playbooks/deploy_docker.yml --tags loki --ask-vault-pass
    # ...second run: changed=0
    ```

    Container restarts on config change use a handler that runs
    `docker restart loki` -- the module-level state parameter is never
    used for restarts (Pitfall 8).

    ## Port-acceptance gates

    This role passes all five gates documented in `roles/README.md`:

    - **Grep gates (Pitfall 9):** zero matches for INSPQ leftovers; zero
      non-ASCII characters in `roles/loki/`.
    - **Image-pin gate:** zero floating-tag references.
    - **Vault-discipline gate:** every `{{ vault_* }}` reference has a
      matching key in `vault.yml.example`.
    - **Idempotency gate:** twice-in-a-row playbook run reports `changed=0`.
    - **Healthcheck + restart-policy gate:** `docker inspect` returns
      `healthy` and `unless-stopped`.

    ## Deviations from upstream INSPQ (D-25)

    Per CONTEXT.md D-25, each Phase-2 role port is an opinionated improvement
    pass over the upstream INSPQ role -- not a mirror translate. Items dropped,
    replaced, or added vs. `~/git/inspq/ansible/loki/`:

    **Dropped (INSPQ-isms beyond grep gate):**

    - `loki_image_version: "latest"` -- replaced with explicit pin `3.7.2` per OPS-01.
    - `loki_container_env.TZ: "America/Toronto"` -- replaced with `Etc/UTC` per OPS-06 + Pitfall 6 (DST avoidance).
    - `loki_tenant_id: inspq` -- dropped; `auth_enabled: false` means no tenant header.
    - `loki_alertmanager_url: "http://localhost:9093"` -- omitted from M1; Phase 4 lands the alertmanager URL.
    - `loki_mode: "monolithic"` conditional logic -- simplified; M1 ships monolithic only.
    - `loki_container_restart_policy: "always"` -- replaced with `unless-stopped` per OPS-06.
    - LVM tasks (`loki_lvm`, `loki_vg`, `loki_lv_name`) -- dropped; named Docker volume covers storage.
    - `loki_memberlist_join_members` -- dropped; monolithic doesn't need memberlist.
    - Bloom filter / pattern ingester / distributed component vars -- dropped from M1 defaults.
    - `loki_network_mode: bridge` + custom network logic -- simplified; always uses the `telemetron` bridge per D-04.
    - INSPQ table_manager retention vars -- dropped (Loki 3.x uses compactor retention).
    - INSPQ S3 bucket name `loki` -- replaced with Phase-1 `loki-chunks` (Pitfall F).
    - French task names throughout -- replaced with English per CLAUDE.md.

    **Replaced with better defaults:**

    - INSPQ used `schema_config.configs[0].from: 2023-01-01` for v13 TSDB -- kept (reasonable past date).
    - INSPQ used `loki_container_user: "1000:0"` -- kept (Loki image's default UID).

    **Added (missing pitfall guards in upstream):**

    - **Pitfall 12 (compactor marker-file loss):** added `loki_compactor_working_directory: /loki/compactor` so markers live on the persistent volume.
    - **Pitfall 4 (label discipline):** added D-37 limits (`max_streams_per_user`, `max_label_value_length`, `max_label_names_per_series`).
    - **D-28 (gRPC port clash):** added explicit `loki_grpc_port: 9095` rather than relying on the implicit default.

    ## Bring your own bucket name

    Override `loki_s3_bucket` (or the upstream `telemetron_minio_buckets`
    list in `storage.yml`) to rename the bucket. Renaming MUST be paired
    with renaming in `roles/minio/defaults/main.yml` (or
    `telemetron_minio_buckets`) -- both ends speak the same name.

    ## Deprecation notes

    None for the loki role itself. Loki is the locked log backend per
    CLAUDE.md tech-stack constraints; alternatives (Quickwit, OpenObserve)
    are explicitly out of M1 scope.
    ```

    ---

    **File 6: `inventory/example-homelab/group_vars/all/loki.yml`**

    Mirror the shape of `inventory/example-homelab/group_vars/all/minio.yml`:

    ```yaml
    ---
    # Telemetron -- Loki operator knobs (Phase 2, D-30, D-33)
    # Role-specific overrides for the `loki` role.
    # See roles/loki/defaults/main.yml for the full default surface.

    # Default: zero host port publish. Inter-component traffic stays on
    # the telemetron Docker bridge network (Grafana / OTel / Fluent Bit
    # in later phases reach Loki at http://loki:3100). Operator access
    # via SSH local-forward:
    #   ssh -L 3100:localhost:3100 <host>
    # Override to `true` (publishes to 0.0.0.0) or `127.0.0.1` (loopback only).
    loki_publish_host: false

    # Log retention (BACK-02). Surfaced again here so operators see the
    # override path without grep-hunting defaults/main.yml. The role default
    # aliases to telemetron_default_log_retention (14d) from storage.yml.
    # Override directly here for non-default retention:
    #   loki_retention_period: 7d
    loki_retention_period: "{{ telemetron_default_log_retention | default('14d') }}"
    ```

    ---

    **File 7: Extension to `inventory/example-homelab/group_vars/all/vault.yml.example`**

    Read the current file first (it has a `# --- Phase 2+ keys ...` section near the bottom commented out). Append a new Phase 2 Loki section ABOVE that commented-out hint block (or replace the hint block with the real Phase 2 sections; the cleanest path is: keep the Phase 1 block intact, replace the "Phase 2+ keys will be added here" comment region with the real Phase 2 Loki block).

    The exact text to add (per D-27 — values alias to the Phase-1 MinIO root credentials):

    ```yaml
    # --- Phase 2: Loki S3 credentials (D-27) ---
    # Consumed by: roles/loki (S3 access to the loki-chunks bucket).
    # Values alias to the Phase-1 MinIO root credentials for now. A future
    # hardening milestone will split MinIO into per-backend users with
    # bucket-scoped IAM policies; at that point only vault.yml changes --
    # role templates do not (cheap forward-compat per D-27).
    vault_loki_s3_access_key: "{{ vault_minio_root_user }}"
    vault_loki_s3_secret_key: "{{ vault_minio_root_password }}"
    ```

    Do NOT touch the Phase 1 section. Do NOT touch keys for Phase 3+ (those land in their own plans).

    ---

    **All files ASCII-only.** No accented characters, smart quotes, em dashes (use `--`), or non-ASCII anywhere. The non-ASCII grep gate `grep -rPn '[^\x00-\x7F]' roles/loki/ inventory/example-homelab/group_vars/all/loki.yml inventory/example-homelab/group_vars/all/vault.yml.example` MUST report zero non-zero file counts after this task.
  </action>
  <verify>
    <automated>test -d roles/loki/defaults && test -d roles/loki/meta && test -d roles/loki/handlers && test -d roles/loki/templates && echo OK_dirs ; test -f roles/loki/defaults/main.yml && test -f roles/loki/meta/main.yml && test -f roles/loki/handlers/main.yml && test -f roles/loki/templates/loki.yaml.j2 && test -f roles/loki/README.md && test -f inventory/example-homelab/group_vars/all/loki.yml && echo OK_files ; grep -c 'grafana/loki' roles/loki/defaults/main.yml ; grep -c '"3.7.2"' roles/loki/defaults/main.yml ; grep -rcE ':latest' roles/loki/ ; grep -c 'auth_enabled: false' roles/loki/templates/loki.yaml.j2 ; grep -c 'grpc_listen_port:' roles/loki/templates/loki.yaml.j2 ; grep -c 'http://minio:9000' roles/loki/defaults/main.yml ; grep -c 's3forcepathstyle:' roles/loki/templates/loki.yaml.j2 ; grep -c 'retention_enabled:' roles/loki/templates/loki.yaml.j2 ; grep -c 'working_directory:' roles/loki/templates/loki.yaml.j2 ; grep -c '/loki/compactor' roles/loki/templates/loki.yaml.j2 ; grep -c 'max_streams_per_user:' roles/loki/templates/loki.yaml.j2 ; grep -c 'max_label_value_length:' roles/loki/templates/loki.yaml.j2 ; grep -c 'max_label_names_per_series:' roles/loki/templates/loki.yaml.j2 ; grep -c 'PITFALLS Pitfall 12\|Pitfall 12' roles/loki/templates/loki.yaml.j2 ; grep -c 'Pitfall 4\|D-37' roles/loki/templates/loki.yaml.j2 ; grep -c 'CONTEXT.md D-28\|D-28' roles/loki/templates/loki.yaml.j2 ; grep -c 'vault_loki_s3_access_key' roles/loki/templates/loki.yaml.j2 ; grep -c 'vault_loki_s3_secret_key' roles/loki/templates/loki.yaml.j2 ; grep -c 'community.docker' roles/loki/meta/main.yml ; grep -cE '^- name:' roles/loki/handlers/main.yml ; grep -c 'docker restart' roles/loki/handlers/main.yml ; grep -ciE 'state: restarted' roles/loki/handlers/main.yml roles/loki/defaults/main.yml ; grep -c 'loki_publish_host: false' inventory/example-homelab/group_vars/all/loki.yml ; grep -c 'loki_retention_period' inventory/example-homelab/group_vars/all/loki.yml ; grep -c 'vault_loki_s3_access_key' inventory/example-homelab/group_vars/all/vault.yml.example ; grep -c 'vault_loki_s3_secret_key' inventory/example-homelab/group_vars/all/vault.yml.example ; grep -c 'vault_minio_root_user' inventory/example-homelab/group_vars/all/vault.yml.example ; wc -l roles/loki/README.md ; grep -c '## Deviations from upstream INSPQ' roles/loki/README.md ; grep -c 'BACK-02' roles/loki/README.md ; grep -c 'Pitfall 12' roles/loki/README.md ; grep -riEc 'inspq|qc\.ca|montreal|québec|francais|french|/srv/nfs/inspq|vault_inspq' roles/loki/ ; grep -rPnc '[^\x00-\x7F]' roles/loki/ | grep -v ':0$' | wc -l ; grep -rPnc '[^\x00-\x7F]' inventory/example-homelab/group_vars/all/loki.yml inventory/example-homelab/group_vars/all/vault.yml.example | grep -v ':0$' | wc -l ; python3 -c "import yaml; yaml.safe_load(open('roles/loki/defaults/main.yml')); yaml.safe_load(open('roles/loki/meta/main.yml')); yaml.safe_load(open('roles/loki/handlers/main.yml')); yaml.safe_load(open('inventory/example-homelab/group_vars/all/loki.yml'))" && echo OK_yaml_parses</automated>
  </verify>
  <acceptance_criteria>
    - All 7 files exist (defaults/main.yml, meta/main.yml, handlers/main.yml, templates/loki.yaml.j2, README.md, inventory loki.yml, vault.yml.example extension visible)
    - `grep -c 'grafana/loki' roles/loki/defaults/main.yml` returns at least `1`
    - `grep -c '"3.7.2"' roles/loki/defaults/main.yml` returns `1` (exact pin)
    - **Image-pin gate (OPS-01):** `grep -rcE ':latest' roles/loki/` returns 0 (no `:latest` anywhere)
    - **D-26 verified:** `grep -c 'auth_enabled: false' roles/loki/templates/loki.yaml.j2` returns `1`
    - **D-28 verified:** `grep -c 'grpc_listen_port:' roles/loki/templates/loki.yaml.j2` returns `1` AND the line resolves to `9095` via the rendered `{{ loki_grpc_port }}` default
    - **D-39 / RESEARCH Finding 2 — S3 endpoint format:** `grep -c 'http://minio:9000' roles/loki/defaults/main.yml` returns `1` (Loki uses scheme-included endpoint)
    - **D-39 verified:** `grep -c 's3forcepathstyle:' roles/loki/templates/loki.yaml.j2` returns `1`
    - **D-33 + Pitfall 12 verified:** `grep -c 'retention_enabled:' roles/loki/templates/loki.yaml.j2` returns `1` AND `grep -c 'working_directory:' roles/loki/templates/loki.yaml.j2` returns `1` AND `grep -c '/loki/compactor' roles/loki/templates/loki.yaml.j2` returns at least `1`
    - **D-37 verified:** all three label-discipline knobs in template — `max_streams_per_user`, `max_label_value_length`, `max_label_names_per_series` each return `1` via grep
    - **Inline pitfall citation per D-36/D-37 contract:** `grep -c 'Pitfall 12' roles/loki/templates/loki.yaml.j2` returns at least `1` AND `grep -cE 'Pitfall 4|D-37' roles/loki/templates/loki.yaml.j2` returns at least `1`
    - **D-27 vault references:** `grep -c 'vault_loki_s3_access_key' roles/loki/templates/loki.yaml.j2` returns `1` AND `grep -c 'vault_loki_s3_secret_key' roles/loki/templates/loki.yaml.j2` returns `1` (and they appear as plain `{{ vault_loki_s3_access_key }}` Jinja substitutions, NOT escaped)
    - `grep -c 'community.docker' roles/loki/meta/main.yml` returns at least `1`
    - **W6 single-handler:** `grep -cE '^- name:' roles/loki/handlers/main.yml` returns `1` (exactly one handler)
    - **Pitfall 8 / D-19:** `grep -ciE 'state: restarted' roles/loki/handlers/main.yml roles/loki/defaults/main.yml` returns 0
    - **D-30:** `grep -c 'loki_publish_host: false' inventory/example-homelab/group_vars/all/loki.yml` returns `1`
    - **BACK-02:** `grep -c 'loki_retention_period' inventory/example-homelab/group_vars/all/loki.yml` returns at least `1`
    - **D-27 vault.yml.example extension:** `grep -c 'vault_loki_s3_access_key' inventory/example-homelab/group_vars/all/vault.yml.example` returns `1` AND `grep -c 'vault_loki_s3_secret_key' inventory/example-homelab/group_vars/all/vault.yml.example` returns `1` AND `grep -c 'vault_minio_root_user' inventory/example-homelab/group_vars/all/vault.yml.example` returns at least `2` (Phase 1 declaration + Phase 2 alias reference)
    - README is at least 100 lines: `wc -l roles/loki/README.md` returns >= 100
    - README documents the D-25 deviation table: `grep -c '## Deviations from upstream INSPQ' roles/loki/README.md` returns `1`
    - README documents BACK-02: `grep -c 'BACK-02' roles/loki/README.md` returns at least `1`
    - README mentions Pitfall 12 mitigation: `grep -c 'Pitfall 12' roles/loki/README.md` returns at least `1`
    - **D-21 grep gate (INSPQ):** `grep -riEc 'inspq|qc\.ca|montreal|québec|francais|french|/srv/nfs/inspq|vault_inspq' roles/loki/` returns 0
    - **D-21 non-ASCII gate:** `grep -rPnc '[^\x00-\x7F]' roles/loki/` reports no files with non-zero matches AND the same check on the inventory files reports zero non-zero files
    - All YAML parses with PyYAML: defaults/main.yml, meta/main.yml, handlers/main.yml, inventory loki.yml all load successfully
  </acceptance_criteria>
  <done>Role skeleton complete: defaults declare image pin (`grafana/loki:3.7.2`), all tunables including D-37 limits, D-39 schema, S3 endpoint with `http://` prefix, and D-33 retention. Meta declares community.docker. Handlers has ONE handler running `docker restart loki` only (W6). Config template renders the full monolithic Loki 3.7.2 config from RESEARCH Finding 2 with auth_enabled:false (D-26), gRPC port 9095 (D-28), s3forcepathstyle:true (D-39), compactor working_directory + retention_enabled:true (D-33 + Pitfall 12), and the three D-37 / Pitfall 4 limits with inline pitfall citations. README mirrors the minio README schema, includes the D-25 deviation-from-upstream-INSPQ table, and documents BACK-02 retention. Inventory loki.yml surfaces `loki_publish_host: false` (D-30) and the `loki_retention_period` (BACK-02) override path. vault.yml.example extended with the two D-27 alias keys. All grep + non-ASCII gates pass.</done>
</task>

<task type="auto">
  <name>Task 2: Write roles/loki/tasks/main.yml + tasks/verify.yml (container deploy + D-10a HEALTHCHECK poll + W8 synthetic push + mc bucket assert)</name>
  <read_first>
    - roles/loki/defaults/main.yml (Task 1 just created — your tasks reference every variable)
    - roles/loki/templates/loki.yaml.j2 (Task 1 just created — `template:` task renders this to /opt/telemetron/loki/loki.yaml)
    - roles/loki/handlers/main.yml (Task 1 just created — your tasks notify `restart loki` when the config file changes)
    - roles/minio/tasks/main.yml (canonical 6-task shape to mirror; only image/ports/data path/healthcheck differ)
    - roles/minio/tasks/bootstrap.yml (canonical pre-poll + one-shot push + mc assert shape — verify.yml mirrors this verbatim with curl-push instead of mc-mb, and mc-ls instead of mc-ls-with-diff)
    - .planning/phases/02-telemetry-backends/02-CONTEXT.md (D-32 verify pattern; D-10a HEALTHCHECK poll; D-19 handler-not-restart)
    - .planning/phases/02-telemetry-backends/02-RESEARCH.md §"Finding 6: Healthcheck Endpoints (Loki)" (CMD form with /usr/bin/loki -health; image is distroless) AND §"Finding 8: In-Network Verify Payload Shapes — Loki Verify" (curl POST shape) AND §"Finding 9: Image Entrypoints" (Loki entrypoint /usr/bin/loki, command `-config.file=/etc/loki/loki.yaml -target=all`)
    - .planning/research/PITFALLS.md §"Pitfall 8: Ansible role idempotency cascades" (state:started + recreate:false + handler-notify pattern)
  </read_first>
  <files>roles/loki/tasks/main.yml, roles/loki/tasks/verify.yml</files>
  <action>
    Two files. **All ASCII-only.**

    ---

    **File 1: `roles/loki/tasks/main.yml`**

    Mirror the 6-task shape of `roles/minio/tasks/main.yml` exactly. The Loki-specific differences: image name, command line, in-container config path (`/etc/loki/loki.yaml`), data path (`/loki`), single port (`3100`), and the CMD-form healthcheck.

    ```yaml
    ---
    # roles/loki/tasks/main.yml
    # Loki monolithic deploy + in-network verify.
    # Mirrors the canonical roles/minio/tasks/main.yml structure (D-32, OPS-06).
    # Per D-23: this role is wired into playbooks/deploy_docker.yml after minio.

    - name: Ensure Loki config directory exists on host
      ansible.builtin.file:
        path: "{{ loki_config_dir }}"
        state: directory
        mode: "0755"
      tags:
        - loki
        - loki-config

    - name: Render Loki config
      ansible.builtin.template:
        src: loki.yaml.j2
        dest: "{{ loki_config_dir }}/loki.yaml"
        mode: "0600"
      notify: restart loki
      tags:
        - loki
        - loki-config

    - name: Ensure Loki data volume exists
      community.docker.docker_volume:
        name: "{{ loki_data_volume }}"
        state: present
      tags:
        - loki

    - name: Pull Loki image
      community.docker.docker_image:
        name: "{{ loki_image }}:{{ loki_image_tag }}"
        source: pull
        force_source: false
      tags:
        - loki

    - name: Run Loki container
      community.docker.docker_container:
        name: "{{ loki_container_name }}"
        image: "{{ loki_image }}:{{ loki_image_tag }}"
        state: started
        recreate: false
        restart_policy: "{{ loki_restart_policy }}"
        memory: "{{ loki_memory_limit }}"
        user: "{{ loki_container_user }}"
        command:
          - "-config.file=/etc/loki/loki.yaml"
          - "-target=all"
        networks:
          - name: "{{ loki_network }}"
            aliases:
              - "{{ loki_container_name }}"
        # Per D-30 / D-12: zero host publish by default. Only publish if
        # the operator explicitly sets loki_publish_host to true or 127.0.0.1.
        published_ports: >-
          {{
            (
              [ loki_http_port | string + ':' + loki_http_port | string ]
              if loki_publish_host is sameas true else
              [ '127.0.0.1:' + loki_http_port | string + ':' + loki_http_port | string ]
              if loki_publish_host == '127.0.0.1' else
              []
            )
          }}
        mounts:
          - source: "{{ loki_data_volume }}"
            target: "{{ loki_data_path }}"
            type: volume
        volumes:
          - "{{ loki_config_dir }}/loki.yaml:/etc/loki/loki.yaml:ro"
        # CMD form -- the image is distroless (no shell). Loki 3.7.2 ships
        # /usr/bin/loki -health (binary-native). See RESEARCH.md Finding 6
        # and PITFALLS Pitfall A.
        healthcheck:
          test: "{{ loki_healthcheck_test }}"
          interval: "{{ loki_healthcheck_interval }}"
          timeout: "{{ loki_healthcheck_timeout }}"
          retries: "{{ loki_healthcheck_retries }}"
          start_period: "{{ loki_healthcheck_start_period }}"
        env:
          TZ: "{{ loki_tz }}"
      tags:
        - loki
        - loki-container

    # Verify MUST be the final task (D-32). Mirrors roles/minio/tasks/main.yml
    # pattern -- verify is included from main; not a separate playbook.
    - name: Verify Loki end-to-end (synthetic push + bucket assert)
      ansible.builtin.include_tasks: verify.yml
      tags:
        - loki
        - loki-verify
    ```

    ---

    **File 2: `roles/loki/tasks/verify.yml`**

    Three steps mirroring `roles/minio/tasks/bootstrap.yml`: (1) D-10a HEALTHCHECK poll via docker_container_info; (2) W8 synthetic push via one-shot curl; (3) MinIO bucket assertion via one-shot mc. All three with `changed_when: false`, `auto_remove: true`, `detach: false`, and `failed_when` on non-zero exit per D-32.

    ```yaml
    ---
    # roles/loki/tasks/verify.yml
    # Pre-poll Loki's Docker HEALTHCHECK, push a synthetic log line via
    # in-network curl one-shot, then assert an object key landed in MinIO's
    # loki-chunks bucket via a one-shot mc container.
    #
    # Mirrors the canonical roles/minio/tasks/bootstrap.yml pattern (D-10a +
    # W7 + W8 + D-32). Failure of any step fails the playbook.

    # Step 1: poll Loki's Docker HEALTHCHECK status until `healthy`.
    # Per D-10a: read State.Health.Status via docker_container_info -- the
    # control host cannot reach :3100 directly (D-30 no host publish).
    - name: Wait for Loki container HEALTHCHECK to report healthy
      community.docker.docker_container_info:
        name: "{{ loki_container_name }}"
      register: loki_health_check
      until: >-
        loki_health_check.container is defined
        and loki_health_check.container.State is defined
        and loki_health_check.container.State.Health is defined
        and loki_health_check.container.State.Health.Status == 'healthy'
      retries: "{{ loki_health_retries }}"
      delay: "{{ loki_health_delay }}"
      changed_when: false
      tags:
        - loki
        - loki-verify

    # Step 2: synthetic log push via one-shot curl container.
    # Mirrors W8 (one-shot push + assert) from minio/tasks/bootstrap.yml.
    # curlimages/curl is pinned to a recent release (no :latest per OPS-01).
    # Per W7: changed_when: false -- this is a read-shaped verification,
    # not a state mutation worth reporting as changed.
    # Per D-32: failed_when on non-zero exit fails the playbook.
    - name: Push synthetic log line to Loki (in-network verify)
      community.docker.docker_container:
        name: "{{ loki_container_name }}-verify-push"
        image: "curlimages/curl:8.10.1"
        state: started
        detach: false
        auto_remove: true
        recreate: true
        cleanup: true
        networks:
          - name: "{{ loki_network }}"
        entrypoint: ["/bin/sh", "-c"]
        command:
          - >-
            TS=$(date +%s%N) ;
            curl -sS -o /dev/null -w "%{http_code}\n"
            -X POST http://{{ loki_container_name }}:{{ loki_http_port }}/loki/api/v1/push
            -H "Content-Type: application/json"
            --data-raw "{\"streams\":[{\"stream\":{\"job\":\"telemetron-verify\",\"host\":\"verify\"},\"values\":[[\"$TS\",\"telemetron loki verify OK\"]]}]}"
            | grep -qE '^(204|200)$'
      register: loki_verify_push
      failed_when: loki_verify_push.status is defined and loki_verify_push.status != 0
      changed_when: false
      tags:
        - loki
        - loki-verify

    # Step 3: assert an object key landed in minio/loki-chunks within ~15s.
    # Loki flushes chunks on idle period or chunk_target_size; the verify
    # one-shot retries with a sleep to bound the wait. The mc container is
    # the same pinned image used by roles/minio/tasks/bootstrap.yml.
    # Per W8: mc ls --json is the documented stable interface.
    - name: Assert loki-chunks bucket received an object
      community.docker.docker_container:
        name: "{{ loki_container_name }}-verify-bucket"
        image: "{{ minio_mc_image | default('minio/mc') }}:{{ minio_mc_image_tag | default('RELEASE.2025-04-22T16-23-26Z') }}"
        state: started
        detach: false
        auto_remove: true
        recreate: true
        cleanup: true
        networks:
          - name: "{{ loki_network }}"
        env:
          MC_HOST_local: "http://{{ vault_loki_s3_access_key }}:{{ vault_loki_s3_secret_key }}@minio:9000"
        entrypoint: ["/bin/sh", "-c"]
        command:
          - >-
            i=0 ;
            while [ $i -lt 15 ] ; do
              COUNT=$(mc ls --json --recursive local/{{ loki_s3_bucket }} 2>/dev/null | wc -l) ;
              if [ "$COUNT" -gt 0 ] ; then echo "objects_found=$COUNT" ; exit 0 ; fi ;
              i=$((i+1)) ;
              sleep 2 ;
            done ;
            echo "no objects landed in {{ loki_s3_bucket }} within 30s" ;
            exit 1
      register: loki_verify_bucket
      failed_when: loki_verify_bucket.status is defined and loki_verify_bucket.status != 0
      changed_when: false
      tags:
        - loki
        - loki-verify
    ```

    **Implementation notes for the executor:**

    - The verify push uses `curlimages/curl:8.10.1` (a stable, narrow image with a shell — needed for the `$(date +%s%N)` substitution). If `curlimages/curl:8.10.1` is unreachable on the target host, fall back to `curlimages/curl:8.11.0` (test with `docker manifest inspect` first). Document the actual tag chosen in the SUMMARY.
    - Step 2's `grep -qE '^(204|200)$'` accepts either Loki return code (204 is canonical for `/push` success; 200 is also documented in some Loki versions). If both fail, the pipeline returns 1, `failed_when` kicks, playbook fails.
    - Step 3's retry loop waits up to 30s (15 iterations × 2s) for the first chunk to flush — Loki's default `chunk_idle_period: 30m` is shortened by ingester flushing on shutdown / size threshold, but for a single synthetic push the first object can take up to a minute. **Note for executor:** if Step 3 consistently times out on first deploy, this means the chunk hasn't flushed yet. The acceptance criterion for "first run" can be relaxed by reducing `loki_chunk_idle_period` in `defaults/main.yml` to 1m or 30s for verify purposes, OR by using a shorter `chunk_target_size`. Document any tuning made during execute-phase in the SUMMARY.
  </action>
  <verify>
    <automated>test -f roles/loki/tasks/main.yml && test -f roles/loki/tasks/verify.yml && echo OK_files ; grep -c 'include_tasks: verify.yml' roles/loki/tasks/main.yml ; grep -c 'community.docker.docker_container' roles/loki/tasks/main.yml ; grep -c 'community.docker.docker_volume' roles/loki/tasks/main.yml ; grep -c 'community.docker.docker_image' roles/loki/tasks/main.yml ; grep -c '/usr/bin/loki' roles/loki/tasks/main.yml roles/loki/defaults/main.yml ; grep -c '/etc/loki/loki.yaml' roles/loki/tasks/main.yml ; grep -c -- '-target=all' roles/loki/tasks/main.yml ; grep -c 'notify: restart loki' roles/loki/tasks/main.yml ; grep -ciE 'state: restarted' roles/loki/tasks/ ; grep -c 'community.docker.docker_container_info' roles/loki/tasks/verify.yml ; grep -c 'State.Health.Status' roles/loki/tasks/verify.yml ; grep -c 'loki/api/v1/push' roles/loki/tasks/verify.yml ; grep -c 'mc ls --json' roles/loki/tasks/verify.yml ; grep -c 'loki-chunks\|loki_s3_bucket' roles/loki/tasks/verify.yml ; grep -c 'auto_remove: true' roles/loki/tasks/verify.yml ; grep -c 'detach: false' roles/loki/tasks/verify.yml ; grep -c 'failed_when:' roles/loki/tasks/verify.yml ; grep -c 'changed_when: false' roles/loki/tasks/verify.yml ; grep -c 'vault_loki_s3_access_key' roles/loki/tasks/verify.yml ; grep -c 'tags:' roles/loki/tasks/main.yml ; grep -riEc 'inspq|qc\.ca|montreal|québec|francais|french|/srv/nfs/inspq|vault_inspq' roles/loki/tasks/ ; grep -rPnc '[^\x00-\x7F]' roles/loki/tasks/ | grep -v ':0$' | wc -l ; python3 -c "import yaml; yaml.safe_load(open('roles/loki/tasks/main.yml')); yaml.safe_load(open('roles/loki/tasks/verify.yml'))" && echo OK_yaml_parses ; (command -v ansible-lint >/dev/null && ansible-lint roles/loki/ 2>&1 | tail -10) || echo "ansible-lint not installed -- skipping"</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist
    - `grep -c 'include_tasks: verify.yml' roles/loki/tasks/main.yml` returns `1` (verify is included as final task per D-32)
    - `grep -c 'community.docker.docker_container' roles/loki/tasks/main.yml` returns at least `1` (server container task)
    - `grep -c 'community.docker.docker_volume' roles/loki/tasks/main.yml` returns `1` (volume ensure task)
    - `grep -c 'community.docker.docker_image' roles/loki/tasks/main.yml` returns `1` (image pull task)
    - **Loki command line — RESEARCH Finding 9:** `grep -c '/etc/loki/loki.yaml' roles/loki/tasks/main.yml` returns at least `1` AND `grep -c -- '-target=all' roles/loki/tasks/main.yml` returns at least `1`
    - **Notify handler wired:** `grep -c 'notify: restart loki' roles/loki/tasks/main.yml` returns `1` (template task notifies the W6 handler)
    - **Pitfall 8 gate:** `grep -ciE 'state: restarted' roles/loki/tasks/` returns 0
    - **D-10a HEALTHCHECK poll:** `grep -c 'community.docker.docker_container_info' roles/loki/tasks/verify.yml` returns `1` AND `grep -c 'State.Health.Status' roles/loki/tasks/verify.yml` returns at least `1`
    - **D-32 W8 synthetic push:** `grep -c 'loki/api/v1/push' roles/loki/tasks/verify.yml` returns `1`
    - **W8 MinIO bucket assert:** `grep -c 'mc ls --json' roles/loki/tasks/verify.yml` returns `1` AND `grep -cE 'loki-chunks|loki_s3_bucket' roles/loki/tasks/verify.yml` returns at least `1`
    - **D-32 verify-container hygiene:** `grep -c 'auto_remove: true' roles/loki/tasks/verify.yml` returns at least `2` (push + assert containers) AND `grep -c 'detach: false' roles/loki/tasks/verify.yml` returns at least `2` AND `grep -c 'failed_when:' roles/loki/tasks/verify.yml` returns at least `2`
    - **W7 idempotency:** `grep -c 'changed_when: false' roles/loki/tasks/verify.yml` returns at least `3` (poll + push + assert)
    - **D-27 vault aliases consumed:** `grep -c 'vault_loki_s3_access_key' roles/loki/tasks/verify.yml` returns at least `1` (mc env uses the per-backend key, NOT vault_minio_root_user directly)
    - Tags present: every task in main.yml has `tags:` including `loki`
    - **D-21 grep gate (INSPQ):** `grep -riEc 'inspq|qc\.ca|montreal|québec|francais|french|/srv/nfs/inspq|vault_inspq' roles/loki/tasks/` returns 0
    - **D-21 non-ASCII gate:** `grep -rPnc '[^\x00-\x7F]' roles/loki/tasks/` reports no files with non-zero matches
    - Both YAML files parse with PyYAML
    - If `ansible-lint` is installed: runs against `roles/loki/` with no `production` profile blocker errors (warnings acceptable per Claude's Discretion in CONTEXT.md)
  </acceptance_criteria>
  <done>tasks/main.yml mirrors the minio role's 6-task shape: ensure config dir, render template (notify handler), ensure volume, pull image, run server container (CMD-form healthcheck pointing at /usr/bin/loki -health, no host publish default, command line `-config.file=/etc/loki/loki.yaml -target=all`, TZ env), include verify.yml as final task. tasks/verify.yml mirrors the minio bootstrap.yml shape: (1) D-10a HEALTHCHECK poll via docker_container_info; (2) one-shot curl container POSTs synthetic JSON to /loki/api/v1/push and checks HTTP 204/200; (3) one-shot mc container retries `mc ls --json local/loki-chunks` for 30s and asserts at least one object key. All three steps with `changed_when: false`, `auto_remove: true`, `detach: false`, `failed_when` non-zero. All grep + non-ASCII gates pass. No `state: restarted` anywhere.</done>
</task>

<task type="auto">
  <name>Task 3: Wire roles/loki into playbooks/deploy_docker.yml; flip roles/README.md status row; verify all port-acceptance gates</name>
  <read_first>
    - playbooks/deploy_docker.yml (currently has minio wired; this task appends loki after minio per D-23)
    - roles/README.md (status table — Plan 01's flip pattern for minio row applies here for loki row)
    - roles/loki/ (Tasks 1+2 just created the full role — confirm presence before wiring)
    - .planning/phases/02-telemetry-backends/02-CONTEXT.md (D-23 wire-after-minio; D-24 single tag per role)
  </read_first>
  <files>playbooks/deploy_docker.yml, roles/README.md</files>
  <action>
    Two file edits. No new files.

    ---

    **Edit 1: `playbooks/deploy_docker.yml`**

    Read the current file. It currently looks like:

    ```yaml
      roles:
        - role: minio
          tags:
            - minio
      # Subsequent phase plans extend this list in dependency order
      # (D-05 dependency tree):
      #   Phase 2: loki, tempo, mimir (after minio -- depend on its buckets)
      #   Phase 3: prometheus, opentelemetry, fluentbit, node_exporter
      ...
    ```

    Append the `loki` role entry to the `roles:` list, AFTER the `minio` entry. The updated `roles:` block becomes:

    ```yaml
      roles:
        - role: minio
          tags:
            - minio
        - role: loki
          tags:
            - loki
      # Subsequent phase plans extend this list in dependency order
      # (D-05 dependency tree):
      #   Phase 2: tempo, mimir (after loki -- same wave; share no files)
      #   Phase 3: prometheus, opentelemetry, fluentbit, node_exporter
      #   Phase 4: alertmanager, hook_router
      #   Phase 5: grafana, karma, promlens
      #   Phase 6: nfsd (opt-in via enable_nfsd flag)
    ```

    Keep the header comment block, hosts, gather_facts, become, collections, and pre_tasks UNTOUCHED. The pre_task for `telemetron` network creation remains the first task in pre_tasks.

    ---

    **Edit 2: `roles/README.md`**

    The status table currently has `| `loki` | log backend (monolithic mode) | ☐ |`. Flip it to ☑ (checked checkbox). The exact line to change:

    Before:
    ```
    | `loki`                   | log backend (monolithic mode)                     | ☐ |
    ```

    After:
    ```
    | `loki`                   | log backend (monolithic mode)                     | ☑ |
    ```

    **CRITICAL non-ASCII note:** The ☐ and ☑ characters are themselves non-ASCII. They were committed by Phase 1 already (they live in `roles/README.md` at the project root, NOT inside `roles/<role>/`), so the D-21 grep gate's scope is `roles/loki/` — it does NOT apply to `roles/README.md`. **Verify before editing:** run `grep -rPn '[^\x00-\x7F]' roles/README.md` — if Phase 1 already used ☐ for the minio row, that's the precedent we mirror with ☑. Do NOT change those characters. If a non-ASCII gate at the project root is later enforced (Phase 6), that's a follow-up concern; Phase 2 mirrors the Phase 1 precedent.

    If the ☐ character is not present in `roles/README.md` (Phase 1 used a different status indicator), use the same indicator Phase 1 used. Read the file first, mirror the precedent. Do not invent a new status indicator.

    ---

    **Verification phase (after both edits):**

    1. `ansible-playbook --syntax-check -i inventory/example-homelab playbooks/deploy_docker.yml` MUST pass (no syntax errors with the loki role wired).

    2. If Docker is available on the executor's host AND the operator has set up a real `inventory/example-homelab/group_vars/all/vault.yml` with `vault_minio_root_user` + `vault_minio_root_password` (and the Phase 1 minio role has already been deployed successfully, leaving the `telemetron` network + MinIO + 5 buckets in place), run the end-to-end test:

       ```bash
       # First run -- loki should come up healthy and verify should succeed
       ansible-playbook -i inventory/example-homelab playbooks/deploy_docker.yml --tags loki --vault-password-file /tmp/.vp

       # Verify HEALTHCHECK status
       docker inspect loki --format '{{.State.Health.Status}}'   # expects: healthy

       # Verify gRPC port pin
       docker exec loki ss -tlnp 2>/dev/null | grep :9095 || \
         docker exec loki netstat -tlnp 2>/dev/null | grep :9095
       # (Loki distroless image may have neither ss nor netstat -- if so,
       # skip and trust the rendered config; an alternative is to run
       # `docker exec loki cat /etc/loki/loki.yaml | grep grpc_listen_port` -- expects: 9095)

       # Verify retention knob propagates
       docker run --rm --network telemetron curlimages/curl:8.10.1 \
         curl -s http://loki:3100/config 2>/dev/null | grep -E 'retention_period:.*14d' || \
         echo "retention_period override path -- override to 7d in inventory/example-homelab/group_vars/all/loki.yml and re-run to confirm propagation"

       # Verify mc shows objects in loki-chunks (note: may be empty on first run if no chunk has flushed yet)
       docker run --rm --network telemetron \
         -e MC_HOST_local="http://$(grep vault_minio_root_user .../vault.yml | cut -d: -f2 | tr -d ' '):$(grep vault_minio_root_password .../vault.yml | cut -d: -f2 | tr -d ' ')@minio:9000" \
         minio/mc:RELEASE.2025-04-22T16-23-26Z mc ls --json --recursive local/loki-chunks

       # Second run -- MUST report changed=0 in PLAY RECAP
       ansible-playbook -i inventory/example-homelab playbooks/deploy_docker.yml --tags loki --vault-password-file /tmp/.vp 2>&1 | grep 'PLAY RECAP' -A 5 | grep -E 'changed=0'
       ```

    3. If Docker is NOT available, document the human-run sequence in the SUMMARY (as Plan 01-03 did) with exact copy-pasteable commands.
  </action>
  <verify>
    <automated>grep -c '- role: loki' playbooks/deploy_docker.yml ; grep -c '- role: minio' playbooks/deploy_docker.yml ; grep -cE '^\s+- loki$' playbooks/deploy_docker.yml ; grep -c 'community.docker.docker_network' playbooks/deploy_docker.yml ; python3 -c "import yaml; yaml.safe_load(open('playbooks/deploy_docker.yml'))" && echo OK_yaml_parses ; ansible-playbook --syntax-check -i inventory/example-homelab playbooks/deploy_docker.yml 2>&1 | tail -3 ; grep -c '| \`loki\`' roles/README.md ; awk '/\| `loki`/{print}' roles/README.md ; grep -rPnc '[^\x00-\x7F]' playbooks/deploy_docker.yml ; grep -riEc 'inspq|qc\.ca|montreal|québec|francais|french|/srv/nfs/inspq|vault_inspq' roles/loki/ playbooks/deploy_docker.yml ; grep -rcE ':latest' roles/loki/ ; echo "--- Full grep-gate roll-up for Plan 02-01 ---" ; grep -riE 'inspq|qc\.ca|montreal|québec|francais|french|/srv/nfs/inspq|vault_inspq' roles/loki/ inventory/example-homelab/group_vars/all/loki.yml playbooks/deploy_docker.yml ; grep -rPnc '[^\x00-\x7F]' roles/loki/ inventory/example-homelab/group_vars/all/loki.yml | grep -v ':0$' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - **D-23 role wired:** `grep -c '- role: loki' playbooks/deploy_docker.yml` returns `1`
    - **Order preserved (minio first, loki second):** `grep -nE '- role: (minio|loki)' playbooks/deploy_docker.yml` shows minio on a lower line number than loki
    - **D-24 one-tag-per-role:** `grep -cE '^\s+- loki$' playbooks/deploy_docker.yml` returns `1`
    - **Pre-task untouched:** `grep -c 'community.docker.docker_network' playbooks/deploy_docker.yml` returns `1`
    - **YAML parses:** `python3 -c "import yaml; yaml.safe_load(open('playbooks/deploy_docker.yml'))"` exits 0
    - **Ansible syntax check passes:** `ansible-playbook --syntax-check -i inventory/example-homelab playbooks/deploy_docker.yml` exits 0
    - **roles/README.md status flipped:** the `| `loki`` row contains the checked indicator matching the Phase 1 minio precedent (if Phase 1 used ☑, this row uses ☑; if Phase 1 used another character, mirror it). The status indicator on the loki row is NOT ☐ anymore.
    - **D-21 grep gate (INSPQ) over all Plan 02-01 modified files:** `grep -riE 'inspq|qc\.ca|montreal|québec|francais|french|/srv/nfs/inspq|vault_inspq' roles/loki/ inventory/example-homelab/group_vars/all/loki.yml playbooks/deploy_docker.yml` returns 0 matches
    - **D-21 non-ASCII gate on roles/loki/ + inventory file:** `grep -rPnc '[^\x00-\x7F]' roles/loki/ inventory/example-homelab/group_vars/all/loki.yml` reports zero files with non-zero matches. (playbooks/deploy_docker.yml is permitted to retain the `--` em-dash-style ASCII content from Phase 1; the non-ASCII gate applies to role and per-role inventory files.)
    - **OPS-01 image-pin gate:** `grep -rcE ':latest' roles/loki/` returns 0
    - **If Docker available:** the end-to-end test in the action section succeeds: container is healthy, gRPC port 9095 is bound (verified via rendered config), second playbook run reports `changed=0`. If Docker is not available, the SUMMARY explicitly documents the human-run sequence.
  </acceptance_criteria>
  <done>The `loki` role is wired into `playbooks/deploy_docker.yml` with tag `[loki]`, appended after `minio` per D-23. The playbook stays runnable at this commit (minio → loki). `roles/README.md` status table flipped to mark loki as ported (mirroring the Phase 1 minio precedent for the checkbox character). All five port-acceptance gates pass on `roles/loki/`: image-pin, vault-discipline, grep (INSPQ), non-ASCII, no-state-restarted. Ansible syntax check passes. End-to-end Docker test either ran successfully or is documented in the SUMMARY for human-run validation.</done>
</task>

</tasks>

<verification>
After all three tasks complete, run the full Phase 2 Plan 01 acceptance gate:

```bash
# 1. Role files all exist with expected structure
test -f roles/loki/defaults/main.yml
test -f roles/loki/meta/main.yml
test -f roles/loki/handlers/main.yml
test -f roles/loki/tasks/main.yml
test -f roles/loki/tasks/verify.yml
test -f roles/loki/templates/loki.yaml.j2
test -f roles/loki/README.md
test -f inventory/example-homelab/group_vars/all/loki.yml

# 2. Image-pin gate (OPS-01)
grep -rcE ':latest' roles/loki/   # = 0

# 3. Image is the locked pin
grep -c 'grafana/loki' roles/loki/defaults/main.yml             # >= 1
grep -c '"3.7.2"' roles/loki/defaults/main.yml                  # = 1

# 4. Grep gate (OPS-05 / Pitfall 9)
grep -riE 'inspq|qc\.ca|montreal|québec|francais|french|/srv/nfs/inspq|vault_inspq' roles/loki/   # 0 matches

# 5. Non-ASCII gate (OPS-05)
grep -rPn '[^\x00-\x7F]' roles/loki/   # 0 matches

# 6. Idempotency gate (OPS-04)
grep -rciE 'state: restarted' roles/loki/   # = 0

# 7. Single-handler check (W6)
grep -cE '^- name:' roles/loki/handlers/main.yml   # = 1

# 8. D-10a HEALTHCHECK poll
grep -c 'community.docker.docker_container_info' roles/loki/tasks/verify.yml   # = 1
grep -c 'State.Health.Status' roles/loki/tasks/verify.yml                     # >= 1

# 9. W8 in-network verify push + bucket assert
grep -c 'loki/api/v1/push' roles/loki/tasks/verify.yml   # = 1
grep -c 'mc ls --json' roles/loki/tasks/verify.yml       # = 1

# 10. D-32 verify hygiene (auto_remove + detach + failed_when + changed_when)
grep -c 'auto_remove: true' roles/loki/tasks/verify.yml   # >= 2
grep -c 'detach: false' roles/loki/tasks/verify.yml       # >= 2
grep -c 'failed_when:' roles/loki/tasks/verify.yml        # >= 2
grep -c 'changed_when: false' roles/loki/tasks/verify.yml # >= 3

# 11. D-26 multitenancy off
grep -c 'auth_enabled: false' roles/loki/templates/loki.yaml.j2   # = 1

# 12. D-28 gRPC port pin
grep -c 'grpc_listen_port' roles/loki/templates/loki.yaml.j2      # = 1
grep -c 'loki_grpc_port: 9095' roles/loki/defaults/main.yml       # = 1

# 13. D-33 retention + Pitfall 12 working_directory
grep -c 'retention_enabled' roles/loki/templates/loki.yaml.j2          # = 1
grep -c 'working_directory' roles/loki/templates/loki.yaml.j2          # = 1
grep -c '/loki/compactor' roles/loki/templates/loki.yaml.j2            # >= 1

# 14. D-37 / Pitfall 4 limits
grep -c 'max_streams_per_user' roles/loki/templates/loki.yaml.j2       # = 1
grep -c 'max_label_value_length' roles/loki/templates/loki.yaml.j2     # = 1
grep -c 'max_label_names_per_series' roles/loki/templates/loki.yaml.j2 # = 1

# 15. D-39 S3 endpoint format (Loki uses http:// prefix)
grep -c 'http://minio:9000' roles/loki/defaults/main.yml               # = 1
grep -c 's3forcepathstyle' roles/loki/templates/loki.yaml.j2           # = 1

# 16. D-27 vault aliases declared + consumed
grep -c 'vault_loki_s3_access_key' inventory/example-homelab/group_vars/all/vault.yml.example   # = 1
grep -c 'vault_loki_s3_secret_key' inventory/example-homelab/group_vars/all/vault.yml.example   # = 1
grep -c 'vault_loki_s3_access_key' roles/loki/templates/loki.yaml.j2     # = 1

# 17. D-30 no host publish
grep -c 'loki_publish_host: false' inventory/example-homelab/group_vars/all/loki.yml   # = 1

# 18. BACK-02 surfaced in inventory
grep -c 'loki_retention_period' inventory/example-homelab/group_vars/all/loki.yml      # >= 1

# 19. README schema
wc -l roles/loki/README.md                                                       # >= 100
grep -c '## Deviations from upstream INSPQ' roles/loki/README.md                 # = 1
grep -c '## Port-acceptance gates' roles/loki/README.md                          # = 1

# 20. Playbook wiring + syntax
grep -c '- role: loki' playbooks/deploy_docker.yml                               # = 1
ansible-playbook --syntax-check -i inventory/example-homelab playbooks/deploy_docker.yml   # exit 0

# 21. (If Docker available) end-to-end test
#     ansible-playbook -i inventory/example-homelab playbooks/deploy_docker.yml --tags loki --ask-vault-pass
#     docker inspect loki --format '{{.State.Health.Status}}'    # = healthy
#     # synthetic push verified by verify.yml's own assertion -- if the playbook exited 0, the assertion passed
#     # Re-run: PLAY RECAP must show changed=0
```
</verification>

<success_criteria>
- `roles/loki/` is a complete role mirroring the canonical `roles/minio/` shape: defaults (image pin `grafana/loki:3.7.2`, D-28/D-31 ports, D-33/D-37/D-39 knobs, healthcheck timing, restart policy), meta (community.docker), handlers (single `docker restart loki` per W6), tasks/main.yml (6-task shape with config template + volume + image pull + container + verify include), tasks/verify.yml (D-10a HEALTHCHECK poll + W8 synthetic push to /loki/api/v1/push + mc ls --json bucket assertion), templates/loki.yaml.j2 (full monolithic Loki 3.7.2 config from RESEARCH Finding 2 with auth_enabled:false, gRPC 9095, MinIO S3 with s3forcepathstyle:true, compactor with /loki/compactor working_directory, D-37 limits), README.md (canonical schema with Deviations-from-upstream-INSPQ section per D-25 and BACK-02 retention documentation).
- `inventory/example-homelab/group_vars/all/loki.yml` surfaces `loki_publish_host: false` (D-30) and `loki_retention_period` (BACK-02 single-knob path).
- `inventory/example-homelab/group_vars/all/vault.yml.example` extended with two D-27 alias keys (`vault_loki_s3_access_key`, `vault_loki_s3_secret_key`) aliased to `vault_minio_root_user` / `vault_minio_root_password`.
- `playbooks/deploy_docker.yml` wires `loki` role with tag `[loki]` after `minio` per D-23; pre_task for `telemetron` network untouched.
- `roles/README.md` status table flipped for `loki` (mirroring the Phase 1 minio precedent character).
- All five port-acceptance gates pass on `roles/loki/`: image-pin (OPS-01), vault-discipline (OPS-02), grep (OPS-05 / Pitfall 9), non-ASCII (OPS-05), no-state-restarted (OPS-04 / Pitfall 8).
- BACK-01 deliverable: Loki monolithic against MinIO `loki-chunks`, persistent volume covers `/loki/compactor/markers/`.
- BACK-02 deliverable: `loki_retention_period` knob in `group_vars/all/loki.yml`, documented in README.
- Ansible syntax check passes against the example inventory.
- If Docker available: end-to-end test succeeds (container healthy, synthetic push lands a chunk object in `loki-chunks` within 30s, second run reports `changed=0`). If not: SUMMARY documents the human-run validation sequence.
</success_criteria>

<output>
After completion, create `.planning/phases/02-telemetry-backends/02-telemetry-backends-01-SUMMARY.md` documenting:
- Files created: 7 in roles/loki/ + 1 inventory file (loki.yml)
- Files modified: 3 (vault.yml.example, playbooks/deploy_docker.yml, roles/README.md)
- The D-25 deviation findings actually applied from RESEARCH §"INSPQ Upstream Audit — Loki INSPQ-isms Found"
- Pitfall-guards added that the upstream INSPQ did NOT have (Pitfall 12 compactor working_directory; Pitfall 4 / D-37 limits; D-28 explicit gRPC port pin)
- The exact Loki config knobs operators can tune (full default surface + BACK-02 retention path documented twice — defaults and inventory)
- D-10a HEALTHCHECK poll + D-32 in-network verify pattern carried forward from minio
- Whether the in-network verify was actually executed (Docker available) or documented for human run (Docker unavailable)
- Any tuning made during execute-phase (e.g. shortened `loki_chunk_idle_period` for verify, fallback curl image tag)
- Any deviations from this plan with rationale
</output>
