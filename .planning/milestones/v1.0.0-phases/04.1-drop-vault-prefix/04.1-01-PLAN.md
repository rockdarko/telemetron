---
phase: 04.1-drop-vault-prefix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - roles/minio/templates/minio.env.j2
  - roles/minio/tasks/bootstrap.yml
  - roles/minio/README.md
  - roles/loki/templates/loki.yaml.j2
  - roles/loki/tasks/verify.yml
  - roles/loki/README.md
  - roles/tempo/templates/tempo.yaml.j2
  - roles/tempo/README.md
  - roles/mimir/templates/mimir.yaml.j2
  - roles/mimir/tasks/verify.yml
  - roles/mimir/defaults/main.yml
  - roles/mimir/README.md
  - inventory/example-homelab/group_vars/all/vault.yml.example
  - inventory/example-homelab/group_vars/all/secrets.yml.example
  - inventory/example-homelab/README.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - roles/README.md
  - .planning/phases/01-foundation-storage/01-CONTEXT.md
  - .planning/phases/02-telemetry-backends/02-CONTEXT.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "grep -rE 'vault_(minio|loki|tempo|mimir)' roles/ inventory/ returns zero matches"
    - "inventory/example-homelab/group_vars/all/secrets.yml.example exists with 8 unprefixed keys; vault.yml.example no longer exists"
    - "PROJECT.md Key Decisions table has a D-90 row + .planning/REQUIREMENTS.md OPS-02 acceptance text + roles/README.md Gate 3 wording all reflect role-namespaced sensitive variables (no `vault_<role>_<purpose>` convention language)"
    - "All four affected roles (minio, loki, tempo, mimir) pass Gates 1-8 after rename"
    - "ansible-playbook against leviathan converges with renamed keys; synthetic log to Loki succeeds (push+query roundtrip); Mimir credential path validated via Prometheus remote_write -> Mimir -> query roundtrip (or direct synthetic push if implemented)"
  artifacts:
    - path: "roles/minio/templates/minio.env.j2"
      provides: "MinIO env injection without vault_ prefix"
      contains: "{{ minio_root_user }}"
    - path: "roles/minio/tasks/bootstrap.yml"
      provides: "MinIO bootstrap mc container env without vault_ prefix"
      contains: "{{ minio_root_user }}"
    - path: "roles/loki/templates/loki.yaml.j2"
      provides: "Loki S3 credentials without vault_ prefix"
      contains: "{{ loki_s3_access_key }}"
    - path: "roles/loki/tasks/verify.yml"
      provides: "Loki verify mc env without vault_ prefix"
      contains: "{{ loki_s3_access_key }}"
    - path: "roles/tempo/templates/tempo.yaml.j2"
      provides: "Tempo S3 credentials without vault_ prefix"
      contains: "{{ tempo_s3_access_key }}"
    - path: "roles/mimir/templates/mimir.yaml.j2"
      provides: "Mimir S3 credentials without vault_ prefix"
      contains: "{{ mimir_s3_access_key }}"
    - path: "roles/mimir/tasks/verify.yml"
      provides: "Mimir verify mc env without vault_ prefix"
      contains: "{{ mimir_s3_access_key }}"
    - path: "inventory/example-homelab/group_vars/all/secrets.yml.example"
      provides: "8 unprefixed secret keys for operator vault/sops/env"
      contains: "minio_root_user:"
    - path: ".planning/PROJECT.md"
      provides: "Key Decisions table has a D-90 row reflecting role-namespaced sensitive variables"
    - path: ".planning/REQUIREMENTS.md"
      provides: "OPS-02 acceptance text using role-namespaced sensitive variables"
    - path: "roles/README.md"
      provides: "Gate 3 renamed Vault-discipline -> Secrets-discipline; body uses role-namespaced naming"
  key_links:
    - from: "roles/minio/templates/minio.env.j2"
      to: "inventory/example-homelab/group_vars/all/secrets.yml.example"
      via: "Jinja variable resolution at runtime"
      pattern: "minio_root_(user|password)"
    - from: "roles/{loki,tempo,mimir}/templates/*.yaml.j2"
      to: "inventory/example-homelab/group_vars/all/secrets.yml.example"
      via: "Jinja variable resolution at runtime; loki_s3_*/tempo_s3_*/mimir_s3_* alias to minio_root_*"
      pattern: "(loki|tempo|mimir)_s3_(access|secret)_key"
    - from: "git mv vault.yml.example -> secrets.yml.example"
      to: "git history"
      via: "git mv (rename detection, not delete+add)"
      pattern: "R[0-9]+\\s+inventory/example-homelab/group_vars/all/vault.yml.example\\s+inventory/example-homelab/group_vars/all/secrets.yml.example"
---

<objective>
Drop the `vault_` prefix from every sensitive variable across the four affected roles
(`minio`, `loki`, `tempo`, `mimir`), rename `vault.yml.example` -> `secrets.yml.example`
with all 8 keys renamed inside, and cascade the convention change through PROJECT.md
(new D-90 Key Decisions row), REQUIREMENTS.md OPS-02 acceptance text, roles/README.md
Gate 3 (`Vault-discipline gate` -> `Secrets-discipline gate`), and Phase 1+2 CONTEXT.md
historical references (annotated inline `(superseded by D-90)` -- NOT rewritten). Re-run
gates 1-8 on the four affected roles, then smoke-test on leviathan with a synthetic log
push to Loki and synthetic metric push to Mimir to prove the credential plumbing still
works end-to-end after the rename.

Purpose: D-90 (`.planning/phases/05-ui-plane/05-CONTEXT.md`) plus user memory
[[feedback-no-decorative-convention-prefixes]] -- the `vault_*` prefix added no value
(role-namespace + suffix carry the same meaning), steered operators toward Ansible vault
specifically when vaulting is one option among many (sops, env vars, external secret
managers, plaintext-chmod-600 in a homelab), and implied tooling enforcement Ansible
doesn't actually provide. Phase 5 plan 05-01 names its first new secret `grafana_admin_password`
with no prefix from day 1; this phase brings the existing 8 keys + 4 roles in line BEFORE
Phase 5 lands, so the codebase isn't half-converted.

Output: 4 roles with renamed Jinja references + renamed inventory example file with 8
unprefixed keys + 4 doc files updated (PROJECT.md Key Decisions, REQUIREMENTS.md OPS-02,
roles/README.md Gate 3, inventory/example-homelab/README.md) + 4 role READMEs updated +
2 historical CONTEXT files annotated + Gates 1-8 re-green on all 4 roles + leviathan smoke
test passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-ui-plane/05-CONTEXT.md
@roles/README.md

<interfaces>
<!--
The 8-row rename table (source of truth). Every Jinja reference, every YAML key in
secrets.yml.example, and every README "Required vault keys" row MUST use the NEW
name. Verbatim from Phase 04.1 ROADMAP entry + 05-CONTEXT.md D-90.
-->

The 8 renames (NON-NEGOTIABLE — copy verbatim into each task action):

| Old (vault_-prefixed)              | New (role-namespaced)        |
|------------------------------------|------------------------------|
| `vault_minio_root_user`            | `minio_root_user`            |
| `vault_minio_root_password`        | `minio_root_password`        |
| `vault_loki_s3_access_key`         | `loki_s3_access_key`         |
| `vault_loki_s3_secret_key`         | `loki_s3_secret_key`         |
| `vault_tempo_s3_access_key`        | `tempo_s3_access_key`        |
| `vault_tempo_s3_secret_key`        | `tempo_s3_secret_key`        |
| `vault_mimir_s3_access_key`        | `mimir_s3_access_key`        |
| `vault_mimir_s3_secret_key`        | `mimir_s3_secret_key`        |

Canonical acceptance grep (must return zero across roles/ and inventory/):
```bash
grep -rE 'vault_(minio|loki|tempo|mimir)' roles/ inventory/
```

Per-role gates that re-run after rename (per roles/README.md):
  Gate 1 — grep clean (INSPQ leftovers + non-ASCII)
  Gate 2 — image pin (no :latest)
  Gate 3 — Secrets-discipline (renamed from Vault-discipline) using NEW naming
  Gate 4 — idempotency (changed=0 on second run)
  Gate 5 — healthcheck + restart-policy
  Gate 6 — README schema
  Gate 7 — telemetron label stamp
  Gate 8 — parent-directory bind-mount

Inventory layout (already established):
  inventory/example-homelab/group_vars/all/
    network.yml, storage.yml, minio.yml, loki.yml, tempo.yml, mimir.yml,
    prometheus.yml, opentelemetry.yml, fluentbit.yml, node_exporter.yml,
    alertmanager.yml, vault.yml.example  <-- to be renamed to secrets.yml.example

Leviathan inventory (Rock's personal, gitignored — lives on leviathan, NOT in this
working copy):
  inventory/leviathan/host_vars/leviathan/vault.yml  (operator-local)
  -> Per D-90 (locked source-of-truth in .planning/phases/05-ui-plane/05-CONTEXT.md
     lines 126-146): "The corresponding `vault.yml` (gitignored, operator-local)
     becomes `secrets.yml`." Task 7 implements this verbatim via ssh leviathan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename vault_ prefix in role templates and tasks (4 roles)</name>
  <files>
    roles/minio/templates/minio.env.j2,
    roles/minio/tasks/bootstrap.yml,
    roles/loki/templates/loki.yaml.j2,
    roles/loki/tasks/verify.yml,
    roles/tempo/templates/tempo.yaml.j2,
    roles/mimir/templates/mimir.yaml.j2,
    roles/mimir/tasks/verify.yml,
    roles/mimir/defaults/main.yml
  </files>
  <read_first>
    - roles/minio/templates/minio.env.j2
    - roles/minio/tasks/bootstrap.yml
    - roles/loki/templates/loki.yaml.j2
    - roles/loki/tasks/verify.yml
    - roles/tempo/templates/tempo.yaml.j2
    - roles/mimir/templates/mimir.yaml.j2
    - roles/mimir/tasks/verify.yml
    - roles/mimir/defaults/main.yml
    - .planning/phases/05-ui-plane/05-CONTEXT.md  (D-90 — source of truth for the rename)
    - .planning/ROADMAP.md  (Phase 04.1 entry — canonical 8-row rename table + success criteria)
    - roles/README.md  (Gate 3 — what "Secrets-discipline" means after rename)
  </read_first>
  <action>
    Mechanical rename of all `vault_(minio|loki|tempo|mimir)*` Jinja references and YAML
    comments in the 4 affected roles. Apply the 8-row rename table verbatim:

    | Old                              | New                          |
    |----------------------------------|------------------------------|
    | `vault_minio_root_user`          | `minio_root_user`            |
    | `vault_minio_root_password`      | `minio_root_password`        |
    | `vault_loki_s3_access_key`       | `loki_s3_access_key`         |
    | `vault_loki_s3_secret_key`       | `loki_s3_secret_key`         |
    | `vault_tempo_s3_access_key`      | `tempo_s3_access_key`        |
    | `vault_tempo_s3_secret_key`      | `tempo_s3_secret_key`        |
    | `vault_mimir_s3_access_key`      | `mimir_s3_access_key`        |
    | `vault_mimir_s3_secret_key`      | `mimir_s3_secret_key`        |

    Exact file-by-file changes:

    1. `roles/minio/templates/minio.env.j2` (lines 6-7):
       OLD:
         MINIO_ROOT_USER={{ vault_minio_root_user }}
         MINIO_ROOT_PASSWORD={{ vault_minio_root_password }}
       NEW:
         MINIO_ROOT_USER={{ minio_root_user }}
         MINIO_ROOT_PASSWORD={{ minio_root_password }}

    2. `roles/minio/tasks/bootstrap.yml` (lines 42, 68, 101):
       Line 42 comment OLD: `# required mc credential format. The URL embeds vault credentials and`
       Line 42 comment NEW: `# required mc credential format. The URL embeds secret credentials and`
       Lines 68 + 101 (MC_HOST_local URL):
         OLD: "http://{{ vault_minio_root_user }}:{{ vault_minio_root_password }}@{{ minio_container_name }}:{{ minio_api_port }}"
         NEW: "http://{{ minio_root_user }}:{{ minio_root_password }}@{{ minio_container_name }}:{{ minio_api_port }}"

    3. `roles/loki/templates/loki.yaml.j2` (lines 53-54):
       OLD:
         access_key_id: "{{ vault_loki_s3_access_key }}"
         secret_access_key: "{{ vault_loki_s3_secret_key }}"
       NEW:
         access_key_id: "{{ loki_s3_access_key }}"
         secret_access_key: "{{ loki_s3_secret_key }}"

    4. `roles/loki/tasks/verify.yml` (line 80):
       OLD: "http://{{ vault_loki_s3_access_key }}:{{ vault_loki_s3_secret_key }}@minio:9000"
       NEW: "http://{{ loki_s3_access_key }}:{{ loki_s3_secret_key }}@minio:9000"

    5. `roles/tempo/templates/tempo.yaml.j2` (lines 39-40):
       OLD:
         access_key: "{{ vault_tempo_s3_access_key }}"
         secret_key: "{{ vault_tempo_s3_secret_key }}"
       NEW:
         access_key: "{{ tempo_s3_access_key }}"
         secret_key: "{{ tempo_s3_secret_key }}"

    6. `roles/mimir/templates/mimir.yaml.j2` (lines 28-29):
       OLD:
         access_key_id: "{{ vault_mimir_s3_access_key }}"
         secret_access_key: "{{ vault_mimir_s3_secret_key }}"
       NEW:
         access_key_id: "{{ mimir_s3_access_key }}"
         secret_access_key: "{{ mimir_s3_secret_key }}"

    7. `roles/mimir/tasks/verify.yml`:
       Line 93 comment OLD: `# Mimir vault credentials (which alias to MinIO root creds per D-27).`
       Line 93 comment NEW: `# Mimir secret credentials (which alias to MinIO root creds per D-27, superseded by D-90).`
       Line 116:
         OLD: "http://{{ vault_mimir_s3_access_key }}:{{ vault_mimir_s3_secret_key }}@minio:9000"
         NEW: "http://{{ mimir_s3_access_key }}:{{ mimir_s3_secret_key }}@minio:9000"

    8. `roles/mimir/defaults/main.yml` (line 140 comment):
       OLD: `# reachable via the D-27-aliased mimir vault credentials.`
       NEW: `# reachable via the D-27-aliased mimir secret credentials (D-27 superseded by D-90).`

    Ordering rationale (per ROADMAP Phase 04.1 ordering constraint): rename Jinja
    references in templates/tasks FIRST so the codebase is internally consistent
    before the inventory example file is renamed in Task 2. After this task the
    role templates expect the NEW names; Task 2 supplies them.

    DO NOT touch any other `vault*` token in these files (`ansible-vault encrypt`,
    `--ask-vault-pass`, `.vault_pass`, "vault discipline" doc-prose strings, etc.
    are NOT part of this rename — only the 8 specific variable names + their
    inline doc-prose comments above).
  </action>
  <verify>
    <automated>! grep -rE 'vault_(minio|loki|tempo|mimir)' roles/minio/templates/ roles/minio/tasks/ roles/loki/templates/ roles/loki/tasks/ roles/tempo/templates/ roles/mimir/templates/ roles/mimir/tasks/ roles/mimir/defaults/</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -rE 'vault_(minio|loki|tempo|mimir)' roles/minio/templates/ roles/minio/tasks/ roles/loki/templates/ roles/loki/tasks/ roles/tempo/templates/ roles/mimir/templates/ roles/mimir/tasks/ roles/mimir/defaults/` exits 0 (negation: succeeds when grep finds nothing).
    - `grep -F '{{ minio_root_user }}' roles/minio/templates/minio.env.j2` returns 1 line.
    - `grep -F '{{ minio_root_password }}' roles/minio/templates/minio.env.j2` returns 1 line.
    - `grep -cF '{{ minio_root_user }}' roles/minio/tasks/bootstrap.yml` returns 2 (both MC_HOST_local URLs).
    - `grep -F '{{ loki_s3_access_key }}' roles/loki/templates/loki.yaml.j2` returns 1 line.
    - `grep -F '{{ loki_s3_secret_key }}' roles/loki/templates/loki.yaml.j2` returns 1 line.
    - `grep -F '{{ loki_s3_access_key }}' roles/loki/tasks/verify.yml` returns 1 line.
    - `grep -F '{{ tempo_s3_access_key }}' roles/tempo/templates/tempo.yaml.j2` returns 1 line.
    - `grep -F '{{ tempo_s3_secret_key }}' roles/tempo/templates/tempo.yaml.j2` returns 1 line.
    - `grep -F '{{ mimir_s3_access_key }}' roles/mimir/templates/mimir.yaml.j2` returns 1 line.
    - `grep -F '{{ mimir_s3_secret_key }}' roles/mimir/templates/mimir.yaml.j2` returns 1 line.
    - `grep -F '{{ mimir_s3_access_key }}' roles/mimir/tasks/verify.yml` returns 1 line.
    - `ansible-playbook -i inventory/example-homelab playbooks/deploy_docker.yml --syntax-check` exits 0 (templates parse).
  </acceptance_criteria>
  <done>
    All 8 vault-prefixed Jinja references in the 4 affected roles' templates and tasks
    are renamed to the unprefixed form per the 8-row table. Inline doc-prose comments
    that referenced "vault credentials" in the role tasks/defaults are updated to
    "secret credentials (D-27 superseded by D-90)" where applicable.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename vault.yml.example -> secrets.yml.example via git mv with 8 unprefixed keys inside</name>
  <files>
    inventory/example-homelab/group_vars/all/vault.yml.example,
    inventory/example-homelab/group_vars/all/secrets.yml.example,
    inventory/example-homelab/README.md
  </files>
  <read_first>
    - inventory/example-homelab/group_vars/all/vault.yml.example
    - inventory/example-homelab/README.md
    - .planning/phases/05-ui-plane/05-CONTEXT.md  (D-90 — file rename + key rename rationale)
    - .planning/ROADMAP.md  (Phase 04.1 — secrets.yml.example acceptance text)
  </read_first>
  <action>
    Use `git mv` (NOT cp + rm) so git detects the rename and preserves history.
    Then edit the renamed file in place to drop the `vault_` prefix from all 8 keys
    and update the in-file usage block.

    Step 1: `git mv inventory/example-homelab/group_vars/all/vault.yml.example inventory/example-homelab/group_vars/all/secrets.yml.example`

    Step 2: Edit `inventory/example-homelab/group_vars/all/secrets.yml.example` (the renamed file).
    Replace the entire content with:

    ```yaml
    ---
    # Telemetron -- example secrets file
    #
    # USAGE:
    #   1. Copy this file:
    #        cp inventory/example-homelab/group_vars/all/secrets.yml.example \
    #           inventory/example-homelab/group_vars/all/secrets.yml
    #   2. Replace every CHANGE_ME with a strong random value (32+ chars).
    #   3. Protect the file. Operator's choice: ansible-vault, sops, env-var
    #      injection, an external secret manager, or chmod 600 on a homelab.
    #      Telemetron does not impose a vaulting tool -- the contract is just
    #      "these variable names must resolve to real values at playbook time."
    #      The ansible-vault path (matches the old default):
    #        ansible-vault encrypt inventory/example-homelab/group_vars/all/secrets.yml
    #      Then supply the vault password via --ask-vault-pass,
    #      --vault-password-file <path>, or ANSIBLE_VAULT_PASSWORD_FILE env var.
    #
    # NAMING CONVENTION: role-namespaced -- <role>_<purpose>. No `vault_` prefix
    # (per D-90 / [[feedback-no-decorative-convention-prefixes]]); the role
    # namespace + descriptive suffix carry the meaning. Each role's README
    # "Secrets" section documents the keys it expects.
    #
    # GIT HYGIENE: this .example file IS committed (placeholders only).
    # The real secrets.yml is in .gitignore -- DO NOT commit a populated file.

    # --- Phase 1: MinIO ---
    # Consumed by: roles/minio (server root credentials + bucket-bootstrap mc container)
    # Both keys must be set; both are injected as env vars on the MinIO server
    # container and the one-shot mc bootstrap container. Per D-09 these root
    # credentials are reused by Loki/Tempo/Mimir for now (per-backend access-key
    # topology is deferred to a hardening milestone).
    minio_root_user: CHANGE_ME       # MinIO root username (>= 3 chars)
    minio_root_password: CHANGE_ME   # MinIO root password (>= 8 chars, recommend 32+)

    # --- Phase 2: Loki S3 credentials (D-27, superseded by D-90 naming) ---
    # Consumed by: roles/loki (S3 access to the loki-chunks bucket).
    # Values alias to the Phase-1 MinIO root credentials for now. A future
    # hardening milestone will split MinIO into per-backend users with
    # bucket-scoped IAM policies; at that point only secrets.yml changes --
    # role templates do not (cheap forward-compat per D-27).
    loki_s3_access_key: "{{ minio_root_user }}"
    loki_s3_secret_key: "{{ minio_root_password }}"

    # --- Phase 2: Tempo S3 credentials (D-27, superseded by D-90 naming) ---
    # Consumed by: roles/tempo (S3 access to the tempo-traces bucket).
    # Values alias to the Phase-1 MinIO root credentials for now (D-27).
    tempo_s3_access_key: "{{ minio_root_user }}"
    tempo_s3_secret_key: "{{ minio_root_password }}"

    # --- Phase 2: Mimir S3 credentials (D-27, superseded by D-90 naming) ---
    # Consumed by: roles/mimir (S3 access to all three mimir-* buckets:
    # mimir-blocks, mimir-ruler, mimir-alerts -- Mimir refuses to start
    # sharing buckets across these stores, per BACK-04 / Pitfall G).
    # Values alias to the Phase-1 MinIO root credentials for now (D-27).
    mimir_s3_access_key: "{{ minio_root_user }}"
    mimir_s3_secret_key: "{{ minio_root_password }}"

    # --- Phase 2+ additional keys will be added here as their roles land ---
    # (Examples for future planning visibility -- do NOT add real values yet:)
    # grafana_admin_password: CHANGE_ME   # Phase 5 -- Grafana admin login
    #
    # Note: Phase 4 (alertmanager) adds NO secret keys (D-66). The null
    # default receiver has no outbound destination; the single-host bridge
    # trust boundary covers inbound. The previously-planned hook_router
    # secret keys (jenkins_api_token, shared-secret) moved with the
    # hook-router deferral to a future milestone -- see REQUIREMENTS.md
    # ALERT-V2-01..05.
    ```

    Step 3: Update `inventory/example-homelab/README.md` to reflect the rename. Edit lines
    ~16, 44-50, 72-80:

    Replace line 16:
      OLD:  `    |       `-- vault.yml.example              # vault placeholders (copy to vault.yml, fill in, encrypt)`
      NEW:  `    |       `-- secrets.yml.example            # secret placeholders (copy to secrets.yml, fill in, protect with your tool of choice)`

    Replace the "Set up the vault file" block (lines 41-52). Replace with:
    ```
    3. **Set up the secrets file:**

       ```bash
       cp inventory/example-homelab/group_vars/all/secrets.yml.example \
          inventory/example-homelab/group_vars/all/secrets.yml

       # Edit secrets.yml -- replace every CHANGE_ME with a strong random value.
       # secrets.yml is gitignored; secrets.yml.example is committed.

       # Protect the file. Operator's choice -- ansible-vault is the path that
       # matches the rest of this README:
       ansible-vault encrypt inventory/example-homelab/group_vars/all/secrets.yml
       # Choose a vault password when prompted; remember it.
       ```
    ```

    Replace the "## Vault discipline (OPS-02)" section heading + body (lines 72-80) with:
    ```
    ## Secrets discipline (OPS-02)

    - Naming convention: role-namespaced — `<role>_<purpose>` (e.g. `minio_root_password`).
      No `vault_` prefix; the role namespace + descriptive suffix carry the meaning
      (per D-90 / [[feedback-no-decorative-convention-prefixes]]).
    - Every sensitive `{{ <role>_<purpose> }}` reference in a role MUST have a matching
      key in `secrets.yml.example` with a `CHANGE_ME` placeholder and a comment naming
      the consuming role.
    - The real `secrets.yml` is in `.gitignore`; the `.example` file ships in-repo.
    - Protection mechanism is the operator's choice: ansible-vault (use
      `--ask-vault-pass`, `--vault-password-file <path>`, or
      `ANSIBLE_VAULT_PASSWORD_FILE` env var), sops, environment-variable injection,
      an external secret manager, or chmod 600 on a homelab. Telemetron documents
      keys, not mechanism.
    ```

    Also update the "What's in here" tree-list line for `minio.yml` etc. is unaffected;
    only the `vault.yml.example` line needs the rename.

    DO NOT also rename `.vault_pass` (gitignored, not relevant) or change the
    `ansible-vault encrypt` command itself (that's still the recommended tool).
  </action>
  <verify>
    <automated>test -f inventory/example-homelab/group_vars/all/secrets.yml.example && test ! -f inventory/example-homelab/group_vars/all/vault.yml.example && [ "$(grep -cE '^(minio_root_user|minio_root_password|loki_s3_access_key|loki_s3_secret_key|tempo_s3_access_key|tempo_s3_secret_key|mimir_s3_access_key|mimir_s3_secret_key):' inventory/example-homelab/group_vars/all/secrets.yml.example)" -eq 8 ]</automated>
  </verify>
  <acceptance_criteria>
    - `test -f inventory/example-homelab/group_vars/all/secrets.yml.example` exits 0.
    - `test ! -f inventory/example-homelab/group_vars/all/vault.yml.example` exits 0.
    - `git status --porcelain inventory/example-homelab/group_vars/all/` shows a rename entry (status `R`) for `vault.yml.example -> secrets.yml.example`, NOT a delete + add.
    - `grep -cE '^(minio_root_user|minio_root_password|loki_s3_access_key|loki_s3_secret_key|tempo_s3_access_key|tempo_s3_secret_key|mimir_s3_access_key|mimir_s3_secret_key):' inventory/example-homelab/group_vars/all/secrets.yml.example` returns exactly 8.
    - `grep -E '^vault_' inventory/example-homelab/group_vars/all/secrets.yml.example` returns zero lines.
    - `grep -F 'role-namespaced' inventory/example-homelab/group_vars/all/secrets.yml.example` returns at least 1 match (in-file usage prose updated; supersedes the old `vault_<role>_<purpose>` comment).
    - `grep -F 'vault_<role>_<purpose>' inventory/example-homelab/group_vars/all/secrets.yml.example` returns 0 matches (old convention prose gone).
    - `grep -F 'secrets.yml.example' inventory/example-homelab/README.md` returns at least 2 matches.
    - `grep -F 'Secrets discipline (OPS-02)' inventory/example-homelab/README.md` returns 1 match.
    - `grep -F 'Vault discipline (OPS-02)' inventory/example-homelab/README.md` returns 0 matches (old heading is gone).
    - `ansible-playbook -i inventory/example-homelab playbooks/deploy_docker.yml --syntax-check` exits 0.
  </acceptance_criteria>
  <done>
    `vault.yml.example` renamed to `secrets.yml.example` via `git mv` (history preserved);
    8 keys inside renamed to the unprefixed form; in-file usage block updated to remove
    the old `vault_<role>_<purpose>` convention prose and instead reference
    `role-namespaced`; `inventory/example-homelab/README.md` updated to point at
    `secrets.yml.example` and the "Vault discipline" section renamed to
    "Secrets discipline" with role-namespaced naming language.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update 4 role READMEs to use new naming + rename Secrets-discipline gate references</name>
  <files>
    roles/minio/README.md,
    roles/loki/README.md,
    roles/tempo/README.md,
    roles/mimir/README.md
  </files>
  <read_first>
    - roles/minio/README.md
    - roles/loki/README.md
    - roles/tempo/README.md
    - roles/mimir/README.md
    - roles/README.md  (Gate 3 — current "Vault-discipline gate" wording that Task 4 will flip)
    - .planning/phases/05-ui-plane/05-CONTEXT.md  (D-90 — README "Secrets" subsection contract)
  </read_first>
  <action>
    Update each of the 4 role READMEs so the "Required vault keys" tables list the
    unprefixed key names, the prose around them references "secret credentials" (not
    "vault credentials"), the D-27 alias references say the new names, and the
    per-role gate-discipline reference at the bottom flips "Vault-discipline gate" to
    "Secrets-discipline gate".

    Do NOT remove the recommendation to use ansible-vault from the per-role `--tags`
    invocation examples (`--ask-vault-pass` etc.); ansible-vault is still the
    canonical tool, just no longer hard-coded into the variable name. Do NOT touch
    `inventory/example-homelab/group_vars/all/vault.yml` path-prose if any -- replace
    with `secrets.yml`.

    CRITICAL: For loki/tempo/mimir READMEs the substitutions in the D-27 alias prose
    paragraph MUST cover the FULL trailing prose (no `...` ellipsis truncation). The
    loki README ends that paragraph with `only `vault.yml` changes -- the role /
    templates do not change.`; tempo + mimir end with `(vault values change; role
    templates do not change). Cheap forward-compat per D-27.`. Each MUST be replaced
    in full so no stale `vault.yml` / `vault values` token survives.

    File-by-file changes:

    === roles/minio/README.md ===

    Line 72:
      OLD: `Required keys in `inventory/<env>/group_vars/all/vault.yml`:`
      NEW: `Required keys in `inventory/<env>/group_vars/all/secrets.yml`:`

    Lines 76-77 (table rows):
      OLD:
        | `vault_minio_root_user` | MinIO root username (>= 3 chars) |
        | `vault_minio_root_password` | MinIO root password (>= 8 chars; 32+ recommended) |
      NEW:
        | `minio_root_user` | MinIO root username (>= 3 chars) |
        | `minio_root_password` | MinIO root password (>= 8 chars; 32+ recommended) |

    Line 79:
      OLD: `Placeholders ship in `inventory/example-homelab/group_vars/all/vault.yml.example`.`
      NEW: `Placeholders ship in `inventory/example-homelab/group_vars/all/secrets.yml.example`.`

    Lines 118-120 prose:
      OLD:
        - **Root credentials from vault.** No `minioadmin/minioadmin` default
          anywhere; the role refuses to render the env file if vault keys
          are missing (Ansible errors out on the undefined vault variable).
      NEW:
        - **Root credentials from operator-supplied secrets.** No
          `minioadmin/minioadmin` default anywhere; the role refuses to render the
          env file if the secret keys are missing (Ansible errors out on the
          undefined variable). Mechanism is the operator's choice
          (ansible-vault / sops / env / external manager).

    Lines 164-165 (gate reference):
      OLD:
        - **Vault-discipline gate:** every `{{ vault_* }}` reference has a
          matching key in `vault.yml.example` with the consuming-role comment.
      NEW:
        - **Secrets-discipline gate:** every sensitive `{{ <role>_<purpose> }}`
          reference has a matching key in `secrets.yml.example` with the
          consuming-role comment.

    === roles/loki/README.md ===

    Line 63:
      OLD: `Required keys in `inventory/<env>/group_vars/all/vault.yml`:`
      NEW: `Required keys in `inventory/<env>/group_vars/all/secrets.yml`:`

    Lines 67-68:
      OLD:
        | `vault_loki_s3_access_key` | MinIO access key for the `loki-chunks` bucket |
        | `vault_loki_s3_secret_key` | MinIO secret key for the `loki-chunks` bucket |
      NEW:
        | `loki_s3_access_key` | MinIO access key for the `loki-chunks` bucket |
        | `loki_s3_secret_key` | MinIO secret key for the `loki-chunks` bucket |

    Lines 70-74 (FULL paragraph substitution — DO NOT truncate; the original tail
    contains `only `vault.yml` changes -- the role templates do not.` which MUST be
    replaced verbatim):
      OLD (verbatim, lines 70-74):
        Per CONTEXT.md D-27, both keys are aliased to `vault_minio_root_user` /
        `vault_minio_root_password` in `vault.yml.example` for now. A future
        hardening milestone will split MinIO into per-backend users with
        bucket-scoped IAM policies; only `vault.yml` changes -- the role
        templates do not.
      NEW (verbatim, replaces all 5 lines):
        Per CONTEXT.md D-27 (superseded by D-90 naming), both keys are aliased to
        `minio_root_user` / `minio_root_password` in `secrets.yml.example` for now.
        A future hardening milestone will split MinIO into per-backend users with
        bucket-scoped IAM policies; only `secrets.yml` changes -- the role
        templates do not.

    Lines 130-131:
      OLD: - **S3 credentials from vault.** The rendered `/opt/telemetron/loki/loki.yaml`
             is written with mode `0600` and contains the resolved vault values
      NEW: - **S3 credentials from operator-supplied secrets.** The rendered
             `/opt/telemetron/loki/loki.yaml` is written with mode `0600` and contains
             the resolved secret values

    Lines 164-165:
      OLD: - **Vault-discipline gate:** every `{{ vault_* }}` reference has a
             matching key in `vault.yml.example`.
      NEW: - **Secrets-discipline gate:** every sensitive `{{ <role>_<purpose> }}`
             reference has a matching key in `secrets.yml.example`.

    === roles/tempo/README.md ===

    Line 126:
      OLD: `Required keys in `inventory/<env>/group_vars/all/vault.yml`:`
      NEW: `Required keys in `inventory/<env>/group_vars/all/secrets.yml`:`

    Lines 130-131:
      OLD:
        | `vault_tempo_s3_access_key` | MinIO access key for the `tempo-traces` bucket |
        | `vault_tempo_s3_secret_key` | MinIO secret key for the `tempo-traces` bucket |
      NEW:
        | `tempo_s3_access_key` | MinIO access key for the `tempo-traces` bucket |
        | `tempo_s3_secret_key` | MinIO secret key for the `tempo-traces` bucket |

    Lines 133-137 (FULL paragraph substitution — DO NOT truncate; the trailing
    parenthetical `(vault values change; role templates do not change)` MUST be
    rewritten to `(secret values change; role templates do not change)`):
      OLD (verbatim, lines 133-137):
        Per CONTEXT.md D-27, both alias to `vault_minio_root_user` /
        `vault_minio_root_password` in `vault.yml.example` for now. A future
        hardening milestone splits MinIO into per-backend users with
        bucket-scoped IAM policies (vault values change; role templates do
        not). Cheap forward-compat per D-27.
      NEW (verbatim, replaces all 5 lines):
        Per CONTEXT.md D-27 (superseded by D-90 naming), both alias to
        `minio_root_user` / `minio_root_password` in `secrets.yml.example` for
        now. A future hardening milestone splits MinIO into per-backend users
        with bucket-scoped IAM policies (secret values change; role templates do
        not). Cheap forward-compat per D-27.

    Line 204:
      OLD: - **S3 credentials from vault.** The rendered `/opt/telemetron/tempo/tempo.yaml`
      NEW: - **S3 credentials from operator-supplied secrets.** The rendered
            `/opt/telemetron/tempo/tempo.yaml`

    Lines 236-237:
      OLD: - **Vault-discipline (OPS-02):** every `{{ vault_* }}` reference has
             a matching key in `vault.yml.example`.
      NEW: - **Secrets-discipline (OPS-02):** every sensitive `{{ <role>_<purpose> }}`
             reference has a matching key in `secrets.yml.example`.

    === roles/mimir/README.md ===

    Line 99:
      OLD: `Required keys in `inventory/<env>/group_vars/all/vault.yml`:`
      NEW: `Required keys in `inventory/<env>/group_vars/all/secrets.yml`:`

    Lines 103-104:
      OLD:
        | `vault_mimir_s3_access_key` | MinIO access key for all three mimir-* buckets |
        | `vault_mimir_s3_secret_key` | MinIO secret key for all three mimir-* buckets |
      NEW:
        | `mimir_s3_access_key` | MinIO access key for all three mimir-* buckets |
        | `mimir_s3_secret_key` | MinIO secret key for all three mimir-* buckets |

    Lines 106-109 (FULL paragraph substitution — DO NOT truncate; same
    `(vault values change; role templates do not change)` pattern as tempo):
      OLD (verbatim, lines 106-109):
        Per CONTEXT.md D-27, both alias to `vault_minio_root_user` /
        `vault_minio_root_password` in `vault.yml.example`. Future hardening
        milestone splits MinIO into per-backend users (vault values change;
        role templates do not). Cheap forward-compat per D-27.
      NEW (verbatim, replaces all 4 lines):
        Per CONTEXT.md D-27 (superseded by D-90 naming), both alias to
        `minio_root_user` / `minio_root_password` in `secrets.yml.example`.
        Future hardening milestone splits MinIO into per-backend users (secret
        values change; role templates do not). Cheap forward-compat per D-27.

    Line 173:
      OLD: `- **S3 credentials from vault.** Rendered config at mode `0600`.`
      NEW: `- **S3 credentials from operator-supplied secrets.** Rendered config at mode `0600`.`

    Lines 208-209:
      OLD: - **Vault-discipline (OPS-02):** every `{{ vault_* }}` reference has
             a matching key in `vault.yml.example`.
      NEW: - **Secrets-discipline (OPS-02):** every sensitive `{{ <role>_<purpose> }}`
             reference has a matching key in `secrets.yml.example`.

    Leave the `--ask-vault-pass` examples in the role READMEs untouched — those
    refer to the ansible-vault CLI flag which is still the recommended protection
    tool; only the variable NAMING and the "Vault-discipline" gate-label have
    changed.
  </action>
  <verify>
    <automated>! grep -rE 'vault_(minio|loki|tempo|mimir)' roles/minio/README.md roles/loki/README.md roles/tempo/README.md roles/mimir/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -rE 'vault_(minio|loki|tempo|mimir)' roles/minio/README.md roles/loki/README.md roles/tempo/README.md roles/mimir/README.md` exits 0.
    - `grep -cF '`vault.yml`' roles/loki/README.md` returns 0 (Blocker #1 fix: tail-prose `only `vault.yml` changes` is gone).
    - `[ "$(grep -cF 'vault.yml' roles/loki/README.md)" -eq 0 ] && [ "$(grep -cF 'vault.yml' roles/tempo/README.md)" -eq 0 ] && [ "$(grep -cF 'vault.yml' roles/mimir/README.md)" -eq 0 ]` exits 0 (Blocker #2 fix: no stale `vault.yml` filename references in any of the 3 affected role READMEs).
    - `[ "$(grep -cF 'vault values' roles/loki/README.md)" -eq 0 ] && [ "$(grep -cF 'vault values' roles/tempo/README.md)" -eq 0 ] && [ "$(grep -cF 'vault values' roles/mimir/README.md)" -eq 0 ]` exits 0 (Blocker #2 fix: stale `vault values change` parenthetical gone from tempo + mimir).
    - `! grep -E 'vault_(minio|loki|tempo|mimir)' roles/loki/README.md roles/tempo/README.md roles/mimir/README.md` exits 0 (broad sweep — no historical-annotation context exists in these READMEs, so any match is a regression).
    - `grep -cF 'minio_root_user' roles/minio/README.md` >= 1.
    - `grep -cF 'minio_root_password' roles/minio/README.md` >= 1.
    - `grep -cF 'loki_s3_access_key' roles/loki/README.md` >= 1.
    - `grep -cF 'loki_s3_secret_key' roles/loki/README.md` >= 1.
    - `grep -cF 'tempo_s3_access_key' roles/tempo/README.md` >= 1.
    - `grep -cF 'mimir_s3_access_key' roles/mimir/README.md` >= 1.
    - `[ "$(grep -lF 'Secrets-discipline' roles/minio/README.md roles/loki/README.md roles/tempo/README.md roles/mimir/README.md | wc -l)" -eq 4 ]` (one per file).
    - `! grep -F 'Vault-discipline' roles/minio/README.md roles/loki/README.md roles/tempo/README.md roles/mimir/README.md` exits 0.
    - `[ "$(grep -F 'secrets.yml' roles/minio/README.md roles/loki/README.md roles/tempo/README.md roles/mimir/README.md | wc -l)" -ge 4 ]`.
    - `[ "$(grep -F '(superseded by D-90 naming)' roles/loki/README.md roles/tempo/README.md roles/mimir/README.md | wc -l)" -eq 3 ]` (one annotation per non-minio README).
  </acceptance_criteria>
  <done>
    All 4 role READMEs document the unprefixed key names in their "Required keys" tables,
    reference `secrets.yml.example` (not `vault.yml.example`), use "Secrets-discipline"
    (not "Vault-discipline") in their gate-list, and annotate D-27 references with
    "(superseded by D-90 naming)" where applicable. The trailing-prose paragraphs in
    loki/tempo/mimir READMEs are fully substituted (no `vault.yml` or `vault values`
    tokens survive the rename).
  </done>
</task>

<task type="auto">
  <name>Task 4: Doc cascade — PROJECT.md D-90 Key Decisions row, REQUIREMENTS.md OPS-02, roles/README.md Gate 3</name>
  <files>
    .planning/PROJECT.md,
    .planning/REQUIREMENTS.md,
    roles/README.md
  </files>
  <read_first>
    - .planning/PROJECT.md
    - .planning/REQUIREMENTS.md
    - roles/README.md
    - .planning/phases/05-ui-plane/05-CONTEXT.md  (D-90 — doc cascade scope)
    - .planning/ROADMAP.md  (Phase 04.1 — success criterion 3)
  </read_first>
  <action>
    Flip the three canonical doc sources from `vault_<role>_<purpose>` convention to
    "role-namespaced sensitive variables (no vault_ prefix)" wording. Quote-then-replace
    each location exactly.

    === .planning/REQUIREMENTS.md ===

    Line 74 (OPS-02 bullet):
      OLD (verbatim):
      - [x] **OPS-02**: All secrets (Grafana admin password, MinIO access key + secret, Jenkins token, shared webhook secret) are loaded via Ansible vault using the naming convention `vault_<role>_<purpose>`. `.vault_pass` is in `.gitignore`. `vault.yml.example` lives in `inventory/example-homelab/` as a template.

      NEW (verbatim):
      - [x] **OPS-02**: All sensitive variables (Grafana admin password, MinIO access key + secret, etc.) follow the role-namespaced naming convention `<role>_<purpose>` (no `vault_` prefix — see Phase 4.1 D-90). Each role's README "Secrets" section enumerates the keys it expects. The operator chooses the protection mechanism (ansible-vault, sops, env-var injection, external secret manager, or chmod 600 on a homelab); Telemetron documents keys, not mechanism. `.vault_pass` is in `.gitignore` if ansible-vault is used. `secrets.yml.example` lives in `inventory/example-homelab/` as a template (renamed from `vault.yml.example` by Phase 4.1).

    Line 77 (OPS-05 — Info #12 inline parenthetical):
      OLD (verbatim):
      - [x] **OPS-05**: Every role passes a per-role port-acceptance grep gate: zero matches for the regex `inspq|qc\.ca|montreal|québec|vault_inspq_` and zero non-ASCII characters in committed files. This gate is documented in `roles/README.md` as part of the port checklist.

      NEW (verbatim):
      - [x] **OPS-05**: Every role passes a per-role port-acceptance grep gate: zero matches for the regex `inspq|qc\.ca|montreal|québec|vault_inspq_` (note: `vault_inspq_` here is the INSPQ-leftover historical key-name grep gate — distinct from, and unaffected by, the OPS-02 `vault_` prefix convention that D-90 retired) and zero non-ASCII characters in committed files. This gate is documented in `roles/README.md` as part of the port checklist.

    === .planning/PROJECT.md ===

    Note: PROJECT.md currently has ZERO `vault_` references in its body (confirmed
    via `grep -c 'vault_' .planning/PROJECT.md` returning 0). There is no inline
    `vault_<role>_<purpose>` text to update — the convention was tracked elsewhere
    (REQUIREMENTS.md OPS-02, roles/README.md Gate 3, CONTEXT.md). So PROJECT.md
    gets a single addition: a new D-90 row appended to the Key Decisions table
    plus a footer-line update.

    Step A — Append a NEW row to the Key Decisions table (the table runs from
    line 79 through line 99). Insert this row IMMEDIATELY AFTER the line that
    starts with `| Naming normalizations locked before role porting` (currently
    line 98) and BEFORE the `| English-only across all artifacts` line. The new
    row text (single line, verbatim, including the leading `|` and the trailing
    `|`):

      | Drop the `vault_` prefix from all sensitive variables (D-90) | Prefix added no value beyond role-namespace + suffix; steered operators toward Ansible vault when sops/env/external managers are equally valid; implied tooling enforcement Ansible doesn't provide. Per [[feedback-no-decorative-convention-prefixes]] | ✓ Good (shipped Phase 4.1 — 8 keys renamed across 4 roles + `vault.yml.example` -> `secrets.yml.example` + doc cascade) |

    Step B — Grep for any other lingering `vault_<role>` or `vault_<role>_<purpose>`
    matches in PROJECT.md (defensive sweep — current count is 0, but verify after
    editing). For each match found (if any), annotate the line inline by appending
    `(superseded by D-90)` within 3 lines of the match (preserve original text;
    add annotation only). Acceptance criterion (below) enforces zero unannotated
    matches.

    Step C — Update the "Last updated" footer line at the bottom of PROJECT.md to
    include a reference to Phase 4.1:
      OLD: `*Last updated: 2026-05-19 after Phase 4 plan 04-02 (gap closure) ...*`
      NEW: append `; Phase 4.1 (2026-05-19) dropped the `vault_` prefix from 8 sensitive
            variables across 4 roles + renamed `vault.yml.example` -> `secrets.yml.example`
            + doc cascade (REQUIREMENTS.md OPS-02 / roles/README.md Gate 3 / Phase 1+2
            CONTEXT annotations) — see D-90` before the closing `*`.

    === roles/README.md ===

    Lines 54-55 (Gate 3):
      OLD (verbatim):
      **3. Vault-discipline gate (OPS-02):** every `{{ vault_* }}` reference in the role has a matching key declared in `inventory/example-homelab/group_vars/all/vault.yml.example` with a `CHANGE_ME` placeholder and a comment naming the consuming role. The vault naming convention is `vault_<role>_<purpose>`.

      NEW (verbatim):
      **3. Secrets-discipline gate (OPS-02):** every sensitive `{{ <role>_<purpose> }}` reference in the role has a matching key declared in `inventory/example-homelab/group_vars/all/secrets.yml.example` with a `CHANGE_ME` placeholder and a comment naming the consuming role. The naming convention is role-namespaced — `<role>_<purpose>` (no `vault_` prefix); per D-90 the prefix added no value and implied tooling enforcement Ansible doesn't provide. The protection mechanism is the operator's choice (ansible-vault, sops, env-var injection, external secret manager, or chmod 600 on a homelab); Telemetron documents keys, not mechanism.

    Line 32 (Port process step 3): leave untouched -- "INSPQ-isms (vault paths, internal
    domains, NFS share assumptions, Quebec-gov cert chains)" refers to upstream INSPQ
    vault PATHS (e.g. on-disk vault locations), NOT to the `vault_*` variable prefix.

    Line 46 (Gate 1 grep gate): leave UNTOUCHED -- the INSPQ leftover grep
    `vault_inspq` is checking for INSPQ-specific historical vault key names; it is
    NOT the OPS-02 vault-prefix convention. Different concern.
  </action>
  <verify>
    <automated>grep -F 'Secrets-discipline gate (OPS-02)' roles/README.md && grep -F 'secrets.yml.example' roles/README.md && grep -F 'role-namespaced' .planning/REQUIREMENTS.md && grep -F 'D-90' .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'Secrets-discipline gate (OPS-02)' roles/README.md` returns 1 match.
    - `! grep -F 'Vault-discipline gate (OPS-02)' roles/README.md` exits 0.
    - `grep -F 'secrets.yml.example' roles/README.md` returns at least 1 match.
    - `! grep -F 'vault_<role>_<purpose>' roles/README.md` exits 0.
    - `grep -F 'role-namespaced' .planning/REQUIREMENTS.md` returns at least 1 match.
    - `! grep -F 'vault_<role>_<purpose>' .planning/REQUIREMENTS.md` exits 0 (Warning #6 fix: OPS-02 no longer carries the old convention language; broad sweep across REQUIREMENTS.md).
    - `grep -F 'secrets.yml.example' .planning/REQUIREMENTS.md` returns at least 1 match (OPS-02 bullet).
    - `grep -F 'OPS-02 `vault_` prefix convention' .planning/REQUIREMENTS.md` returns 1 match (Info #12 fix: OPS-05 line annotates the distinction between `vault_inspq_` grep gate and OPS-02 prefix convention).
    - `grep -F 'D-90' .planning/PROJECT.md` returns at least 2 matches (1 new Key Decisions row + 1 footer reference).
    - `grep -cF 'D-90' .planning/PROJECT.md` >= 2.
    - `grep -F 'Drop the `vault_` prefix from all sensitive variables (D-90)' .planning/PROJECT.md` returns 1 match (the new Key Decisions row landed).
    - Defensive sweep on PROJECT.md: any `vault_<role>` or `vault_<role>_<purpose>` substring match MUST have `(superseded by D-90)` within ±3 lines. Verify: `awk '/vault_<role>/{found=NR} found && NR <= found+3 && /\(superseded by D-90\)/{found=0} END{exit found?1:0}' .planning/PROJECT.md` exits 0. (Current expected count of base matches is 0, so the check passes trivially; this remains a regression-guard.)
    - `grep -F 'vault_inspq' roles/README.md` returns 1 match (line 46 grep gate — UNCHANGED; gate is checking for INSPQ-specific historical vault key names, NOT the OPS-02 prefix).
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md OPS-02 bullet uses role-namespaced language; REQUIREMENTS.md OPS-05
    inline parenthetical clarifies the `vault_inspq_` grep gate is distinct from the
    OPS-02 prefix convention dropped in D-90; PROJECT.md Key Decisions table has a new
    D-90 row + Last-updated footer references Phase 4.1; roles/README.md Gate 3 flipped
    to "Secrets-discipline gate" with role-namespaced naming language. The INSPQ-leftover
    grep gate (Gate 1, `vault_inspq`) is unaffected.
  </done>
</task>

<task type="auto">
  <name>Task 5: Annotate Phase 1+2 CONTEXT.md historical references (DO NOT rewrite)</name>
  <files>
    .planning/phases/01-foundation-storage/01-CONTEXT.md,
    .planning/phases/02-telemetry-backends/02-CONTEXT.md
  </files>
  <read_first>
    - .planning/phases/01-foundation-storage/01-CONTEXT.md
    - .planning/phases/02-telemetry-backends/02-CONTEXT.md
    - .planning/phases/05-ui-plane/05-CONTEXT.md  (D-90 — annotation convention "(superseded by D-90)", not rewrite)
  </read_first>
  <action>
    Historical CONTEXT.md is institutional memory; per D-90 + ROADMAP Phase 04.1 doc
    cascade rule, these references are ANNOTATED INLINE, NOT rewritten. The decisions
    they captured are still TRUE for the moment they were made — only the convention
    has evolved since.

    === .planning/phases/01-foundation-storage/01-CONTEXT.md ===

    Line 32 (D-09):
      OLD (verbatim):
      - **D-09:** Bootstrap container uses MinIO root credentials from vault: `vault_minio_root_user` + `vault_minio_root_password` injected as env vars on the ephemeral container. Per-backend access-key topology is deferred (root creds are reused by Loki/Tempo/Mimir for now; can be split into per-backend users in a future hardening phase if blast-radius becomes a concern).

      NEW (verbatim):
      - **D-09:** Bootstrap container uses MinIO root credentials from vault: `vault_minio_root_user` + `vault_minio_root_password` (later renamed to `minio_root_user` / `minio_root_password` in Phase 4.1 -- superseded by D-90) injected as env vars on the ephemeral container. Per-backend access-key topology is deferred (root creds are reused by Loki/Tempo/Mimir for now; can be split into per-backend users in a future hardening phase if blast-radius becomes a concern).

    Line 46:
      OLD: `  - `vault.yml.example` — every `vault_*` key (per OPS-02 naming) with `CHANGE_ME` placeholder + inline comment of which role consumes it`
      NEW: `  - `vault.yml.example` — every `vault_*` key (per OPS-02 naming) with `CHANGE_ME` placeholder + inline comment of which role consumes it  *(later renamed to `secrets.yml.example` with `vault_*` -> `<role>_*` keys in Phase 4.1 -- superseded by D-90)*`

    Line 97:
      OLD: `- git commit `ba836d2` — `alert_manager` → `alertmanager`, `postgresql-docker` → `postgres` (and other renames). Phase 1 sets the convention for vault keys (`vault_<role>_<purpose>`), volumes (`telemetron_<role>_data`), config dirs (`/opt/telemetron/<role>/`), tags (one per role).`
      NEW: `- git commit `ba836d2` — `alert_manager` → `alertmanager`, `postgresql-docker` → `postgres` (and other renames). Phase 1 sets the convention for vault keys (`vault_<role>_<purpose>` -- later changed to role-namespaced `<role>_<purpose>` in Phase 4.1 / D-90), volumes (`telemetron_<role>_data`), config dirs (`/opt/telemetron/<role>/`), tags (one per role).`

    Line 109:
      OLD: `- **Vault references** — `{{ vault_<role>_<purpose> }}` per OPS-02. `minio` role establishes `vault_minio_root_user`, `vault_minio_root_password`.`
      NEW: `- **Vault references** — `{{ vault_<role>_<purpose> }}` per OPS-02. `minio` role establishes `vault_minio_root_user`, `vault_minio_root_password`. *(Convention later changed to role-namespaced `<role>_<purpose>` in Phase 4.1 / D-90; this CONTEXT.md is preserved as institutional memory.)*`

    Line 147:
      OLD: `- **Vault file split (per-role `vault_<role>.yml`)** — Reconsider if M1's ~5-7 secrets grow past 15.`
      NEW: `- **Vault file split (per-role `vault_<role>.yml`)** — Reconsider if M1's ~5-7 secrets grow past 15. *(After Phase 4.1 / D-90, naming would be per-role `secrets_<role>.yml` if the split happens.)*`

    Lines 55 and 61: leave UNTOUCHED -- line 55 (D-21 grep gate text) and line 61
    (vault file split bullet) reference the INSPQ-leftover `vault_inspq` grep gate
    (Gate 1), which is preserved and not in scope of D-90.

    === .planning/phases/02-telemetry-backends/02-CONTEXT.md ===

    Lines 35-37 (D-27 bullet body):
      OLD:
        - `roles/loki/templates/loki.yaml.j2` consumes `{{ vault_loki_s3_access_key }}` / `{{ vault_loki_s3_secret_key }}`
        - `roles/tempo/templates/tempo.yaml.j2` consumes `{{ vault_tempo_s3_access_key }}` / `{{ vault_tempo_s3_secret_key }}`
        - `roles/mimir/templates/mimir.yaml.j2` consumes `{{ vault_mimir_s3_access_key }}` / `{{ vault_mimir_s3_secret_key }}`
      NEW (annotated):
        - `roles/loki/templates/loki.yaml.j2` consumes `{{ vault_loki_s3_access_key }}` / `{{ vault_loki_s3_secret_key }}` *(renamed to `{{ loki_s3_access_key }}` / `{{ loki_s3_secret_key }}` in Phase 4.1 / D-90)*
        - `roles/tempo/templates/tempo.yaml.j2` consumes `{{ vault_tempo_s3_access_key }}` / `{{ vault_tempo_s3_secret_key }}` *(renamed to `{{ tempo_s3_access_key }}` / `{{ tempo_s3_secret_key }}` in Phase 4.1 / D-90)*
        - `roles/mimir/templates/mimir.yaml.j2` consumes `{{ vault_mimir_s3_access_key }}` / `{{ vault_mimir_s3_secret_key }}` *(renamed to `{{ mimir_s3_access_key }}` / `{{ mimir_s3_secret_key }}` in Phase 4.1 / D-90)*

    Line 39 (alias-time prose):
      Find the sentence containing "All six keys live in `inventory/example-homelab/group_vars/all/vault.yml.example` with values `"{{ vault_minio_root_user }}"`..." and append at the end of that paragraph:
        ` *(Phase 4.1 / D-90 renamed the file to `secrets.yml.example` and dropped the `vault_` prefix from all 8 keys -- the alias semantics + cheap forward-compat unchanged; only the naming convention evolved.)*`

    Line 159:
      OLD: `- Phase 1 conventions: vault keys `vault_<role>_<purpose>`, volumes `telemetron_<role>_data`, config dirs `/opt/telemetron/<role>/`, tags one-per-role.`
      NEW: `- Phase 1 conventions: vault keys `vault_<role>_<purpose>`, volumes `telemetron_<role>_data`, config dirs `/opt/telemetron/<role>/`, tags one-per-role. *(Vault-key prefix dropped in Phase 4.1 / D-90 — now role-namespaced `<role>_<purpose>`.)*`

    Line 172:
      OLD: `- **`inventory/example-homelab/group_vars/all/vault.yml.example`** — Phase-1 placeholder for `vault_minio_root_user` + `vault_minio_root_password`. Phase 2 extends this file with six aliased keys per D-27 (`vault_loki_s3_access_key`/`_secret_key`, same for tempo and mimir).`
      NEW: `- **`inventory/example-homelab/group_vars/all/vault.yml.example`** — Phase-1 placeholder for `vault_minio_root_user` + `vault_minio_root_password`. Phase 2 extends this file with six aliased keys per D-27 (`vault_loki_s3_access_key`/`_secret_key`, same for tempo and mimir). *(File renamed to `secrets.yml.example` and all 8 keys renamed to unprefixed form in Phase 4.1 / D-90.)*`

    Line 180:
      OLD: `- **Vault reference convention (OPS-02)** — `{{ vault_<role>_<purpose> }}`. Phase 2 adds six new keys per D-27.`
      NEW: `- **Vault reference convention (OPS-02)** — `{{ vault_<role>_<purpose> }}`. Phase 2 adds six new keys per D-27. *(Convention changed to role-namespaced `<role>_<purpose>` in Phase 4.1 / D-90.)*`

    Line 186: UNTOUCHED — references the INSPQ-leftover grep gate.

    Annotation principle: each touched line keeps its original text intact and appends
    an italicized parenthetical pointing forward to Phase 4.1 / D-90 (consistent
    style: `*(...changed in Phase 4.1 / D-90.)*`). Historical CONTEXT.md stays
    accurate to its moment in time; readers see both the original decision and the
    later evolution.
  </action>
  <verify>
    <automated>[ "$(grep -cF 'Phase 4.1 / D-90' .planning/phases/01-foundation-storage/01-CONTEXT.md)" -ge 5 ] && [ "$(grep -cF 'Phase 4.1 / D-90' .planning/phases/02-telemetry-backends/02-CONTEXT.md)" -ge 5 ]</automated>
  </verify>
  <acceptance_criteria>
    - `[ "$(grep -cF 'Phase 4.1 / D-90' .planning/phases/01-foundation-storage/01-CONTEXT.md)" -ge 5 ]` (5 annotated locations).
    - `[ "$(grep -cF 'Phase 4.1 / D-90' .planning/phases/02-telemetry-backends/02-CONTEXT.md)" -ge 5 ]` (5 annotated locations).
    - `grep -cF 'vault_minio_root_user' .planning/phases/01-foundation-storage/01-CONTEXT.md` >= 1 (original D-09 text preserved).
    - `grep -cF 'vault_loki_s3_access_key' .planning/phases/02-telemetry-backends/02-CONTEXT.md` >= 1 (original D-27 text preserved).
    - `git diff --stat .planning/phases/01-foundation-storage/01-CONTEXT.md` shows only additions (annotations), no deletions of historical content. (Bash one-liner check: `git diff .planning/phases/01-foundation-storage/01-CONTEXT.md | grep -E '^-[^-]' | grep -v 'Phase 4.1 / D-90' | wc -l` returns 0 -- only lines being "removed" are the old single-line forms being replaced by single-line forms WITH the annotation appended; no historical bullet text is deleted in isolation.)
  </acceptance_criteria>
  <done>
    Phase 1 CONTEXT.md (5 locations) and Phase 2 CONTEXT.md (5 locations) annotated
    inline with `*(... Phase 4.1 / D-90 ...)*` markers; original historical text
    preserved verbatim alongside each annotation. No historical decision text deleted.
  </done>
</task>

<task type="auto">
  <name>Task 6: Canonical grep + re-run Gates 1-8 on the 4 affected roles</name>
  <files>
    (read-only verification task — no file modifications expected;
    if a gate fails, fix the source file and re-run)
  </files>
  <read_first>
    - roles/README.md  (Gates 1-8 definitions)
    - roles/minio/README.md, roles/loki/README.md, roles/tempo/README.md, roles/mimir/README.md (Gate 6 schema)
  </read_first>
  <action>
    Run the canonical grep across `roles/` and `inventory/` to confirm the rename is
    complete, then walk Gates 1-8 on each of the 4 affected roles. Any gate failure
    means a missed substitution in an earlier task — fix in place and re-run.

    Step A — Canonical acceptance grep (the one ROADMAP success criterion 1 names).
    Use negation form so a clean tree exits 0:

      ! grep -rE 'vault_(minio|loki|tempo|mimir)' roles/ inventory/

    Exit 0 means the rename is complete. If grep finds any line (exit 1 for the
    negation), the rename is incomplete — inspect each match; the match is either a
    missed Jinja reference (re-run Task 1 substitutions on the file), a missed README
    row (Task 3), or a missed inventory/README line (Task 2).

    Step B — Per-role gate re-run for `minio`, `loki`, `tempo`, `mimir`. For each
    role, run:

      ROLE=minio  # then loki, tempo, mimir

      # Gate 1: INSPQ + non-ASCII grep (negation form)
      ! grep -riE 'inspq|qc\.ca|montreal|québec|francais|french|/srv/nfs/inspq|vault_inspq' roles/$ROLE/
      ! grep -rPn '[^\x00-\x7F]' roles/$ROLE/

      # Gate 2: Image pin (negation form)
      ! grep -rE 'image:.*:latest' roles/$ROLE/

      # Gate 3: Secrets-discipline (NEW naming after D-90)
      # Every {{ <role>_<purpose> }} sensitive reference has a matching key in
      # inventory/example-homelab/group_vars/all/secrets.yml.example. For each role:
      #   minio: minio_root_user, minio_root_password
      #   loki:  loki_s3_access_key, loki_s3_secret_key
      #   tempo: tempo_s3_access_key, tempo_s3_secret_key
      #   mimir: mimir_s3_access_key, mimir_s3_secret_key
      # Check each appears in secrets.yml.example:
      for k in minio_root_user minio_root_password loki_s3_access_key loki_s3_secret_key tempo_s3_access_key tempo_s3_secret_key mimir_s3_access_key mimir_s3_secret_key; do
        grep -qE "^${k}:" inventory/example-homelab/group_vars/all/secrets.yml.example || echo "MISSING: $k"
      done
      # expect zero "MISSING" lines

      # Gate 4: Idempotency — defer to Task 7 leviathan smoke (running playbook twice
      # is the canonical idempotency check; covered there).

      # Gate 5: Healthcheck + restart — defer to Task 7 leviathan smoke
      # (docker inspect requires a live container).

      # Gate 6: README schema (variables / modes / tags / volumes / healthcheck / deprecation)
      # Each affected role's README was touched in Task 3. Confirm the section
      # headings + the "Required keys" table still uses the new key names:
      for r in minio loki tempo mimir; do
        # README exists
        test -f roles/$r/README.md
        # New key names appear
        grep -qE "^\| \`${r}_(root_user|root_password|s3_access_key|s3_secret_key)\` \|" roles/$r/README.md || echo "GATE6 SCHEMA: $r README missing renamed key row"
        # Secrets-discipline language present
        grep -qF 'Secrets-discipline' roles/$r/README.md || echo "GATE6 SCHEMA: $r README missing Secrets-discipline label"
      done

      # Gate 7: Telemetron label stamp (no change from rename, but verify still in place)
      for r in minio loki tempo mimir; do
        grep -qF 'org.telemetron.service' roles/$r/tasks/main.yml || echo "GATE7: $r missing org.telemetron.service label"
        grep -qF 'org.telemetron.job' roles/$r/tasks/main.yml || echo "GATE7: $r missing org.telemetron.job label"
      done

      # Gate 8: Parent-directory bind-mount (no change from rename, but verify still in place)
      for r in minio loki tempo mimir; do
        # Look for any single-file bind that contains a file extension followed by :
        # Pattern: `<path>/<file>.<ext>:<container_path>` is anti-pattern; we want `<dir>:<container_dir>`.
        # The 04-02 gap-closure plan converted these; verify no regression by checking
        # for *.{yaml,yml,conf,json,toml,cfg,env}:/etc/<role>/... bind patterns:
        ! grep -rE '"\s*\{\{\s*[a-z_]+_config_dir\s*\}\}/[a-zA-Z0-9._-]+\.(yaml|yml|conf|cfg|env|json|toml):/etc' roles/$r/ || echo "GATE8: $r has a single-file rendered-config bind-mount (anti-pattern); convert to parent-dir mount"
      done

      # Echo of canonical: zero lines is success
      echo "FINAL canonical grep:"
      ! grep -rE 'vault_(minio|loki|tempo|mimir)' roles/ inventory/ && echo "  (zero matches -- pass)"

    Step C — Static playbook syntax check:

      ansible-playbook -i inventory/example-homelab playbooks/deploy_docker.yml --syntax-check

    Exit code 0 means all role templates and tasks parse cleanly with the rename.
    This is a static check; runtime convergence is Task 7.

    If any gate emits an error message, fix the source file in place (re-apply Task 1
    / 2 / 3 substitution to the missed location) and re-run this task's checks until
    all gates emit zero error lines.
  </action>
  <verify>
    <automated>! grep -rE 'vault_(minio|loki|tempo|mimir)' roles/ inventory/ && ansible-playbook -i inventory/example-homelab playbooks/deploy_docker.yml --syntax-check && for r in minio loki tempo mimir; do grep -qF 'Secrets-discipline' roles/$r/README.md; done && for k in minio_root_user minio_root_password loki_s3_access_key loki_s3_secret_key tempo_s3_access_key tempo_s3_secret_key mimir_s3_access_key mimir_s3_secret_key; do grep -qE "^${k}:" inventory/example-homelab/group_vars/all/secrets.yml.example; done && echo "All canonical + gate checks pass"</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -rE 'vault_(minio|loki|tempo|mimir)' roles/ inventory/` exits 0 (this is THE canonical acceptance grep named in ROADMAP success criterion 1).
    - `ansible-playbook -i inventory/example-homelab playbooks/deploy_docker.yml --syntax-check` exits 0.
    - Gate 1: `! grep -riE 'inspq|qc\.ca|montreal|québec|francais|french|/srv/nfs/inspq|vault_inspq' roles/minio/ roles/loki/ roles/tempo/ roles/mimir/` exits 0.
    - Gate 1: `! grep -rPn '[^\x00-\x7F]' roles/minio/ roles/loki/ roles/tempo/ roles/mimir/` exits 0.
    - Gate 2: `! grep -rE 'image:.*:latest' roles/minio/ roles/loki/ roles/tempo/ roles/mimir/` exits 0.
    - Gate 3: each of the 8 new keys appears in `inventory/example-homelab/group_vars/all/secrets.yml.example`: `for k in minio_root_user minio_root_password loki_s3_access_key loki_s3_secret_key tempo_s3_access_key tempo_s3_secret_key mimir_s3_access_key mimir_s3_secret_key; do grep -qE "^${k}:" inventory/example-homelab/group_vars/all/secrets.yml.example; done` exits 0 for all 8.
    - Gate 6: each of the 4 role READMEs contains "Secrets-discipline" — `[ "$(grep -lF 'Secrets-discipline' roles/minio/README.md roles/loki/README.md roles/tempo/README.md roles/mimir/README.md | wc -l)" -eq 4 ]`.
    - Gate 7: `grep -F 'org.telemetron.service' roles/minio/tasks/main.yml roles/loki/tasks/main.yml roles/tempo/tasks/main.yml roles/mimir/tasks/main.yml` returns at least 4 matches (one per role).
    - Gate 8: `! grep -rE '"\s*\{\{\s*[a-z_]+_config_dir\s*\}\}/[a-zA-Z0-9._-]+\.(yaml|yml|conf|cfg|env|json|toml):/etc' roles/minio/ roles/loki/ roles/tempo/ roles/mimir/` exits 0 (no single-file bind regression).
    - Gates 4 + 5 (idempotency + healthcheck): defer to Task 7 (require live container).
  </acceptance_criteria>
  <done>
    Canonical grep returns zero. Gates 1, 2, 3, 6, 7, 8 pass on all 4 affected roles
    via static checks. Gates 4 + 5 are deferred to Task 7 (require leviathan).
    Playbook static syntax-check exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 7: Leviathan smoke test — converge + synthetic log + synthetic metric</name>
  <files>
    (no source-tree modifications; this task runs the playbook against
    leviathan and posts synthetic payloads to verify the rename did not
    break credential plumbing end-to-end. The leviathan inventory file
    rename is operator-local on the leviathan host -- nothing in this
    working copy changes.)
  </files>
  <read_first>
    - inventory/leviathan/host_vars/leviathan/vault.yml  (Rock's personal vault on leviathan, gitignored; this task migrates it via ssh leviathan; see action step A)
    - playbooks/deploy_docker.yml
    - roles/loki/README.md  (Loki push endpoint reference: :3100/loki/api/v1/push)
    - roles/mimir/README.md  (Mimir remote_write endpoint: :9009/api/v1/push)
    - .planning/phases/04-alert-plane/04-UAT.md  (precedent for leviathan smoke tests — gives the operational pattern + UAT Rule 1 poll-loop pattern)
    - .planning/phases/05-ui-plane/05-CONTEXT.md  (D-90 — lines 126-146: locked source of truth for the leviathan file rename `vault.yml` -> `secrets.yml`)
  </read_first>
  <action>
    Validate end-to-end that the rename did not break credential plumbing. Leviathan
    is Rock's homelab Docker host (passwordless SSH; see user memory
    `project_leviathan_uat_host.md`). `inventory/leviathan/` is gitignored — Rock's
    personal inventory on the leviathan host, not part of the shipped example. The
    operator-side migration EXACTLY MIRRORS D-90's locked instruction
    (`.planning/phases/05-ui-plane/05-CONTEXT.md` line 140):

      > "The corresponding `vault.yml` (gitignored, operator-local) becomes
      > `secrets.yml` — leviathan-side migration: `mv vault.yml secrets.yml &&
      > sed -i 's/vault_//g' secrets.yml && ansible-vault encrypt secrets.yml`"

    Claude runs all of this directly via ansible/ssh; do NOT punt to "human_needed".

    ────────────────────────────────────────────────────────────────────────────
    The entire smoke is ONE shell script driven by `set -e` at the top so any
    failure aborts immediately. Steps are chained by newlines (not `;` — `;`
    swallows non-zero exits and produces false-positive convergence reports).
    ────────────────────────────────────────────────────────────────────────────

    Step A — Migrate leviathan's vault file in place AND rename it to `secrets.yml`
    (per D-90 verbatim). The leviathan working copy of telemetron lives on the
    leviathan host; all operations are via `ssh leviathan`. Define `REPO` as the
    leviathan-side working copy path (discover with
    `ssh leviathan 'cd ~/git/telemetron 2>/dev/null && pwd || cd /opt/telemetron 2>/dev/null && pwd'`
    or the operator-known path; persist into the script):

      REPO="$(ssh leviathan 'cd ~/git/telemetron 2>/dev/null && pwd || cd /opt/telemetron 2>/dev/null && pwd')"
      VAULT_PASS=~/.vault_pass_leviathan  # Rock's local copy of the leviathan vault password

      # A.1 — Decrypt the existing vault file on leviathan
      ssh leviathan "ansible-vault decrypt $REPO/inventory/leviathan/host_vars/leviathan/vault.yml --vault-password-file $VAULT_PASS"

      # A.2 — Rename the file (D-90 verbatim: mv vault.yml -> secrets.yml)
      ssh leviathan "mv $REPO/inventory/leviathan/host_vars/leviathan/vault.yml $REPO/inventory/leviathan/host_vars/leviathan/secrets.yml"

      # A.3 — Drop the `vault_` prefix from all 8 keys (anchored to start-of-line;
      # matches exactly the 8 keys, no false positives on aliased RHS values)
      ssh leviathan "sed -i -E 's/^vault_(minio_root_user|minio_root_password|loki_s3_access_key|loki_s3_secret_key|tempo_s3_access_key|tempo_s3_secret_key|mimir_s3_access_key|mimir_s3_secret_key):/\\1:/' $REPO/inventory/leviathan/host_vars/leviathan/secrets.yml"

      # A.4 — Fix the alias RHS values: any `{{ vault_minio_root_user }}` /
      # `{{ vault_minio_root_password }}` references in the file (the loki/tempo/mimir
      # aliases) become `{{ minio_root_user }}` / `{{ minio_root_password }}`
      ssh leviathan "sed -i -E 's/\\{\\{\\s*vault_(minio_root_user|minio_root_password)\\s*\\}\\}/{{ \\1 }}/g' $REPO/inventory/leviathan/host_vars/leviathan/secrets.yml"

      # A.5 — Sanity checks (acceptance criteria for Step A)
      ssh leviathan "test -f $REPO/inventory/leviathan/host_vars/leviathan/secrets.yml"
      ssh leviathan "test ! -f $REPO/inventory/leviathan/host_vars/leviathan/vault.yml"
      [ "$(ssh leviathan "grep -cE '^(minio_root_user|minio_root_password|loki_s3_access_key|loki_s3_secret_key|tempo_s3_access_key|tempo_s3_secret_key|mimir_s3_access_key|mimir_s3_secret_key):' $REPO/inventory/leviathan/host_vars/leviathan/secrets.yml")" = "8" ]
      [ "$(ssh leviathan "grep -cE '^vault_' $REPO/inventory/leviathan/host_vars/leviathan/secrets.yml")" = "0" ]

      # A.6 — Re-encrypt with the same vault password
      ssh leviathan "ansible-vault encrypt $REPO/inventory/leviathan/host_vars/leviathan/secrets.yml --vault-password-file $VAULT_PASS"

    Step B — Run the playbook against leviathan (run 1):

      ssh leviathan "cd $REPO && ansible-playbook -i inventory/leviathan playbooks/deploy_docker.yml --vault-password-file $VAULT_PASS" 2>&1 | tee /tmp/04.1-run1.log
      grep -qE 'failed=0\b' /tmp/04.1-run1.log
      grep -qE 'unreachable=0\b' /tmp/04.1-run1.log

      # Pass criteria: PLAY RECAP shows `failed=0` and `unreachable=0` on leviathan.

    Step C — Idempotency (Gate 4): immediately re-run the same playbook and confirm
    changed=0:

      ssh leviathan "cd $REPO && ansible-playbook -i inventory/leviathan playbooks/deploy_docker.yml --vault-password-file $VAULT_PASS" 2>&1 | tee /tmp/04.1-run2.log
      grep -qE 'changed=0\b' /tmp/04.1-run2.log

      # Pass criteria: PLAY RECAP shows `changed=0` on leviathan.

    Step D — Healthcheck + restart-policy (Gate 5) on leviathan, for each of the 4
    affected containers:

      for c in telemetron-minio telemetron-loki telemetron-tempo telemetron-mimir; do
        ssh leviathan "docker inspect --format '{{.State.Health.Status}}' $c" | grep -q healthy
        ssh leviathan "docker inspect --format '{{.HostConfig.RestartPolicy.Name}}' $c" | grep -q unless-stopped
      done

      # Pass criteria: each container reports `healthy` and `unless-stopped`.

    Step E — Synthetic log push to Loki via raw curl (validates that Loki started
    with the renamed S3 credentials and the loki-chunks bucket is reachable). Per
    UAT Rule 1, Loki ingestion is async — use an `until ... do sleep N; done`
    poll loop, NOT a leading sleep:

      TIMESTAMP_NS=$(date +%s%N)
      ssh leviathan "curl -fsS -X POST http://localhost:3100/loki/api/v1/push \
        -H 'Content-Type: application/json' \
        -d '{\"streams\":[{\"stream\":{\"job\":\"phase-04.1-smoke\",\"host\":\"leviathan\"},\"values\":[[\"${TIMESTAMP_NS}\",\"phase-04.1 rename validation log line\"]]}]}'"

      # Loki returns 204 on accepted push -- curl -fsS will exit non-zero on
      # any non-2xx response, so set -e catches it.

      # Until-poll the query path (Loki ingestion is async)
      QUERY_START=$(( $(date +%s) - 300 ))
      until ssh leviathan "curl -fsS -G 'http://localhost:3100/loki/api/v1/query_range' \
        --data-urlencode 'query={job=\"phase-04.1-smoke\"}' \
        --data-urlencode 'start=${QUERY_START}000000000' \
        --data-urlencode 'end='\$(date +%s)'000000000' \
        --data-urlencode 'limit=10'" | grep -q '"job":"phase-04.1-smoke"'; do
        sleep 5
      done

      # The until loop blocks until the log line is queryable (or hits Bash's
      # default per-command timeout via set -e propagation if leviathan is
      # unreachable). Bound poll attempts to 5 iterations if you want a hard
      # cap; the operator can interrupt if it loops longer than ~30s.

    Step F — Synthetic metric validation via Mimir. Mimir's `:9009/api/v1/push`
    accepts Prometheus remote_write protobuf. The cheapest credential-path proof
    is to query Mimir for the `up` series that Prometheus already remote-wrote
    (proves Mimir-side S3 credentials work end-to-end — Mimir flushes blocks to
    the `mimir-blocks` bucket via the renamed `mimir_s3_*` credentials):

      until ssh leviathan "curl -fsS -G 'http://localhost:9009/prometheus/api/v1/query' \
        --data-urlencode 'query=up'" | grep -q '"status":"success"'; do
        sleep 5
      done

      # Bonus credential-plumbing proof: list objects in the mimir-blocks bucket
      # via the renamed mc env URL (re-run the mimir verify play; it uses
      # MC_HOST_local built from the renamed mimir_s3_* creds):
      ssh leviathan "cd $REPO && ansible-playbook -i inventory/leviathan playbooks/deploy_docker.yml --tags mimir --vault-password-file $VAULT_PASS" 2>&1 | tee /tmp/04.1-mimir-verify.log
      grep -qE 'failed=0\b' /tmp/04.1-mimir-verify.log
      grep -qE 'changed=0\b' /tmp/04.1-mimir-verify.log

    Step G — Capture and persist evidence. Record the run outputs and any caveats
    to `.planning/phases/04.1-drop-vault-prefix/04.1-01-SUMMARY.md` (the SUMMARY is
    written by the execute-plan workflow at the end). Mark each smoke step's
    pass/fail with the exact ssh/curl command + observed output.

    If any step fails (set -e propagates the non-zero exit): the rename has broken
    credential plumbing for that component. Diagnose by inspecting the role's
    container log (`ssh leviathan "docker logs telemetron-<role> --tail 100"`) and
    trace the unresolved variable back to the missed substitution. Re-run the
    failing step's source-side fix and re-converge before declaring the plan done.
  </action>
  <verify>
    <automated>
set -e
REPO="$(ssh leviathan 'cd ~/git/telemetron 2>/dev/null && pwd || cd /opt/telemetron 2>/dev/null && pwd')"
VAULT_PASS=~/.vault_pass_leviathan
ssh leviathan "cd $REPO && ansible-playbook -i inventory/leviathan playbooks/deploy_docker.yml --vault-password-file $VAULT_PASS" 2>&1 | tee /tmp/04.1-run1.log
grep -qE 'failed=0\b' /tmp/04.1-run1.log
grep -qE 'unreachable=0\b' /tmp/04.1-run1.log
ssh leviathan "cd $REPO && ansible-playbook -i inventory/leviathan playbooks/deploy_docker.yml --vault-password-file $VAULT_PASS" 2>&1 | tee /tmp/04.1-run2.log
grep -qE 'changed=0\b' /tmp/04.1-run2.log
for c in telemetron-minio telemetron-loki telemetron-tempo telemetron-mimir; do
  ssh leviathan "docker inspect --format '{{.State.Health.Status}}' $c" | grep -q healthy
  ssh leviathan "docker inspect --format '{{.HostConfig.RestartPolicy.Name}}' $c" | grep -q unless-stopped
done
TIMESTAMP_NS=$(date +%s%N)
ssh leviathan "curl -fsS -X POST http://localhost:3100/loki/api/v1/push -H 'Content-Type: application/json' -d '{\"streams\":[{\"stream\":{\"job\":\"phase-04.1-smoke\",\"host\":\"leviathan\"},\"values\":[[\"${TIMESTAMP_NS}\",\"phase-04.1 rename validation log line\"]]}]}'"
QUERY_START=$(( $(date +%s) - 300 ))
until ssh leviathan "curl -fsS -G 'http://localhost:3100/loki/api/v1/query_range' --data-urlencode 'query={job=\"phase-04.1-smoke\"}' --data-urlencode 'start=${QUERY_START}000000000' --data-urlencode 'end='\$(date +%s)'000000000' --data-urlencode 'limit=10'" | grep -q '"job":"phase-04.1-smoke"'; do sleep 5; done
until ssh leviathan "curl -fsS -G 'http://localhost:9009/prometheus/api/v1/query' --data-urlencode 'query=up'" | grep -q '"status":"success"'; do sleep 5; done
    </automated>
  </verify>
  <acceptance_criteria>
    - Step A: leviathan-side `secrets.yml` exists, `vault.yml` does not:
        `ssh leviathan "test -f $REPO/inventory/leviathan/host_vars/leviathan/secrets.yml"` exits 0.
        `ssh leviathan "test ! -f $REPO/inventory/leviathan/host_vars/leviathan/vault.yml"` exits 0.
    - Step A: leviathan-side `secrets.yml` has all 8 keys renamed (pre-encryption check captured during migration script):
        `[ "$(ssh leviathan "grep -cE '^(minio_root_user|minio_root_password|loki_s3_access_key|loki_s3_secret_key|tempo_s3_access_key|tempo_s3_secret_key|mimir_s3_access_key|mimir_s3_secret_key):' $REPO/inventory/leviathan/host_vars/leviathan/secrets.yml")" = "8" ]` (pre-encrypt check), AND
        post-encrypt: `ansible-vault view inventory/leviathan/host_vars/leviathan/secrets.yml --vault-password-file ~/.vault_pass_leviathan | grep -cE '^(minio_root_user|minio_root_password|loki_s3_access_key|loki_s3_secret_key|tempo_s3_access_key|tempo_s3_secret_key|mimir_s3_access_key|mimir_s3_secret_key):'` returns 8.
    - Step A: leviathan-side `secrets.yml` has zero vault_-prefixed keys:
        `[ "$(ssh leviathan "grep -cE '^vault_' $REPO/inventory/leviathan/host_vars/leviathan/secrets.yml")" = "0" ]` (pre-encrypt), AND
        post-encrypt: `ansible-vault view ... | grep -cE '^vault_'` returns 0.
    - Step B: Playbook run 1 PLAY RECAP shows `failed=0` and `unreachable=0` for leviathan (`grep -qE 'failed=0\b'` and `grep -qE 'unreachable=0\b'` against /tmp/04.1-run1.log both exit 0 under `set -e`).
    - Step C: Playbook run 2 PLAY RECAP shows `changed=0` for leviathan (Gate 4 idempotency).
    - Step D: Each of `telemetron-minio`, `telemetron-loki`, `telemetron-tempo`, `telemetron-mimir` reports `healthy` and `unless-stopped` via `docker inspect` (Gate 5).
    - Step E: Synthetic log push to Loki returns HTTP 2xx (curl -fsS exits 0); subsequent `until` query loop for `{job="phase-04.1-smoke"}` exits the loop (queryable).
    - Step F: Mimir query for `up` series returns `"status":"success"` (proves the renamed `mimir_s3_*` credentials work end-to-end via the Prometheus remote_write -> Mimir -> mimir-blocks bucket flush + query path).
    - Step F: `ansible-playbook --tags mimir` re-run completes with `failed=0` and `changed=0` (mimir verify task uses the renamed `mimir_s3_*` credentials via MC_HOST_local URL).
  </acceptance_criteria>
  <done>
    Leviathan-side `vault.yml` renamed to `secrets.yml` per D-90 verbatim; all 8 keys
    inside are unprefixed; playbook converges (run 1 failed=0 + unreachable=0) and is
    idempotent (run 2 changed=0); all 4 affected containers report healthy +
    unless-stopped; synthetic log push to Loki succeeds and is queryable via the
    until-poll loop; Mimir is queryable for the `up` series (which proves the full
    remote_write -> S3 round trip works with the renamed credentials); mimir verify
    play re-run passes with the renamed mc env vars. ROADMAP success criterion 5
    satisfied. The verify `<automated>` block uses `set -e` so any non-zero exit
    propagates immediately — no `;`-chained false-pass risk.
  </done>
</task>

</tasks>

<verification>
After all 7 tasks complete, run the canonical acceptance grep + Gate-4 + Gate-5
final checks:

```bash
# 1. THE acceptance grep (ROADMAP success criterion 1)
! grep -rE 'vault_(minio|loki|tempo|mimir)' roles/ inventory/

# 2. secrets.yml.example exists, vault.yml.example does not (success criterion 2)
test -f inventory/example-homelab/group_vars/all/secrets.yml.example
test ! -f inventory/example-homelab/group_vars/all/vault.yml.example
[ "$(grep -cE '^(minio_root_user|minio_root_password|loki_s3_access_key|loki_s3_secret_key|tempo_s3_access_key|tempo_s3_secret_key|mimir_s3_access_key|mimir_s3_secret_key):' inventory/example-homelab/group_vars/all/secrets.yml.example)" -eq 8 ]
git status --porcelain | grep -E '^R\s+inventory/example-homelab/group_vars/all/(vault|secrets)\.yml\.example' || git log --diff-filter=R --name-status -1 | grep vault.yml.example

# 3. Doc cascade landed (success criterion 3)
grep -F 'role-namespaced' .planning/REQUIREMENTS.md  # OPS-02 row
grep -F 'Secrets-discipline gate (OPS-02)' roles/README.md  # Gate 3
grep -F 'D-90' .planning/PROJECT.md  # Key Decisions + footer

# 4. Gates 1-8 pass on all 4 affected roles (success criterion 4)
#    (Task 6 ran these statically; Task 7 ran Gate 4 + 5 on leviathan)

# 5. Leviathan smoke (success criterion 5) -- evidence in /tmp/04.1-run1.log,
#    /tmp/04.1-run2.log, /tmp/04.1-mimir-verify.log
```

All five ROADMAP success criteria covered.
</verification>

<success_criteria>
Plan 04.1-01 is COMPLETE when:

1. **Acceptance grep is clean** — `! grep -rE 'vault_(minio|loki|tempo|mimir)' roles/ inventory/` exits 0 (ROADMAP SC1).
2. **File rename via git mv** — `inventory/example-homelab/group_vars/all/secrets.yml.example` exists with 8 unprefixed keys; `vault.yml.example` no longer exists in the tree; `git log --diff-filter=R --name-status` shows the rename (preserving history) (ROADMAP SC2).
3. **Doc cascade landed** — PROJECT.md (new D-90 Key Decisions row + footer), REQUIREMENTS.md (OPS-02 bullet + OPS-05 clarifying parenthetical), roles/README.md (Gate 3 = "Secrets-discipline gate") all reference role-namespaced naming + D-90 (ROADMAP SC3).
4. **Historical CONTEXT.md annotated, not rewritten** — Phase 1 (5 locations) and Phase 2 (5 locations) preserve original text and append `*(... Phase 4.1 / D-90 ...)*` markers (ROADMAP SC3 / D-90 directive).
5. **Gates 1-8 re-green on all 4 affected roles** — `minio`, `loki`, `tempo`, `mimir` each pass Gates 1 (grep clean), 2 (image pin), 3 (Secrets-discipline using new naming), 4 (idempotency on leviathan), 5 (healthcheck + restart on leviathan), 6 (README schema with new key names), 7 (label stamp unchanged), 8 (parent-dir bind unchanged) (ROADMAP SC4).
6. **Leviathan smoke test passes** — leviathan-side `vault.yml` renamed to `secrets.yml` per D-90 verbatim; playbook converges (failed=0 + unreachable=0); idempotent re-run (changed=0); 4 affected containers report healthy + unless-stopped; synthetic log push to Loki succeeds and queryable via until-poll; Mimir queryable for `up` series; mimir verify play re-run passes with renamed mc env vars (ROADMAP SC5).
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-drop-vault-prefix/04.1-01-SUMMARY.md`
documenting:
- 8 renames applied across 8 files in roles/ (Task 1)
- File rename via git mv with 8-key content rewrite (Task 2)
- 4 role READMEs updated, including full-paragraph substitutions in loki/tempo/mimir tails (Task 3)
- 3 canonical doc sources updated, including new D-90 Key Decisions row in PROJECT.md (Task 4)
- 10 historical CONTEXT.md annotations applied (Task 5)
- Canonical grep + Gates 1-8 (3, 6, 7, 8 static; 1, 2 static; 4, 5 on leviathan) all pass (Task 6 + 7)
- Leviathan-side file rename (vault.yml -> secrets.yml per D-90 verbatim) confirmed
- Leviathan run 1 / run 2 / mimir-verify logs attached (Task 7)
- Evidence: paste the canonical grep output (empty), the `git status --porcelain` rename line, the `secrets.yml.example` key count (8), and the leviathan PLAY RECAP from run 1 and run 2
- Any deviations from this plan (e.g. extra files needing rename that were missed in this plan's `read_first` scope -- update STATE.md decisions if so)
</output>
