---
phase: 01-foundation-storage
plan: 03
type: execute
wave: 2
# Both plans must complete before this plan starts (AND gate, not OR).
# This plan reads files produced by BOTH Plan 01 (roles/README.md additions —
# port-acceptance gates section) AND Plan 02 (inventory skeleton + playbook
# pre_task scaffold). gsd orchestrator treats `depends_on` as AND by default.
depends_on:
  - 01
  - 02
files_modified:
  - roles/minio/defaults/main.yml
  - roles/minio/meta/main.yml
  - roles/minio/handlers/main.yml
  - roles/minio/tasks/main.yml
  - roles/minio/tasks/bootstrap.yml
  - roles/minio/templates/minio.env.j2
  - roles/minio/README.md
  - playbooks/deploy_docker.yml
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - OPS-01
  - OPS-02
  - OPS-03
  - OPS-04
  - OPS-05
  - OPS-06
user_setup:
  - service: docker
    why: "MinIO and the mc bootstrap container run as Docker containers; the target host needs a working Docker daemon reachable by the Ansible user."
    env_vars: []
    dashboard_config:
      - task: "Ensure Docker is installed and the Ansible user can run `docker` without sudo (member of `docker` group OR sudoers NOPASSWD)"
        location: "Target host shell"
      - task: "Pre-populate inventory/example-homelab/group_vars/all/vault.yml with real vault_minio_root_user + vault_minio_root_password values (copy from .example, fill in, encrypt with ansible-vault encrypt)"
        location: "Ansible control host shell"
must_haves:
  truths:
    - "Operator runs `ansible-playbook -i inventory/example-homelab playbooks/deploy_docker.yml --tags minio` and the MinIO container starts on the telemetron network"
    - "All five required MinIO buckets (loki-chunks, tempo-traces, mimir-blocks, mimir-ruler, mimir-alerts) exist immediately after the role exits; verifiable via `mc ls --json local/ | jq -r '.key'`"
    - "The bucket-bootstrap step is the LAST task in the role and BLOCKS the playbook on non-zero exit — downstream backends never see a no-bucket MinIO"
    - "MinIO server pin is minio/minio:RELEASE.2025-04-22T22-12-26Z; mc bootstrap pin is minio/mc:RELEASE.2025-04-22T16-23-26Z; no :latest anywhere in roles/minio/"
    - "Container has HEALTHCHECK reporting `healthy` and `restart: unless-stopped`"
    - "Neither :9000 nor :9001 is published to the host by default (minio_publish_host: false)"
    - "Running the playbook a second time on the converged host reports `changed=0`"
    - "Grep gates pass: zero matches for INSPQ leftovers and non-ASCII characters in roles/minio/"
  artifacts:
    - path: "roles/minio/defaults/main.yml"
      provides: "All MinIO role tunable variables with pinned image tags and homelab-safe defaults"
      contains: "minio/minio:RELEASE.2025-04-22T22-12-26Z"
    - path: "roles/minio/tasks/main.yml"
      provides: "Render env file, ensure named volume, start server container with healthcheck + restart policy, include bootstrap.yml as final task"
      contains: "community.docker.docker_container"
    - path: "roles/minio/tasks/bootstrap.yml"
      provides: "Pre-poll MinIO's Docker HEALTHCHECK via docker_container_info, then one-shot mc container creates all 5 buckets and blocks playbook on non-zero exit; verify via `mc ls --json` diff"
      contains: "mc mb --ignore-existing"
    - path: "roles/minio/handlers/main.yml"
      provides: "Single config-change restart handler running `docker restart minio` via ansible.builtin.command — never state: restarted"
      contains: "docker restart"
    - path: "roles/minio/templates/minio.env.j2"
      provides: "Env file rendered with vault root credentials, volumes path, and homelab defaults"
      contains: "MINIO_ROOT_USER"
    - path: "roles/minio/README.md"
      provides: "Per-role README schema (variables, modes, defaults, tags, volumes, healthcheck, deprecation notes) — canonical template every Phase 2-6 role README will mirror"
      min_lines: 80
    - path: "roles/minio/meta/main.yml"
      provides: "Galaxy metadata + collection dependency on community.docker"
      contains: "community.docker"
    - path: "playbooks/deploy_docker.yml"
      provides: "Now includes the minio role tagged [minio]"
      contains: "role: minio"
  key_links:
    - from: "roles/minio/tasks/main.yml (final task)"
      to: "roles/minio/tasks/bootstrap.yml (mc bucket creation)"
      via: "ansible.builtin.include_tasks at end of tasks/main.yml — bootstrap is part of the role, not a separate playbook"
      pattern: "include_tasks:.*bootstrap"
    - from: "roles/minio/tasks/bootstrap.yml (pre-poll)"
      to: "MinIO container Docker HEALTHCHECK status"
      via: "community.docker.docker_container_info polling Health.Status until healthy (D-10a — replaces the original ansible.builtin.uri approach because D-13's no-host-publish makes the control host unable to reach :9000)"
      pattern: "docker_container_info"
    - from: "roles/minio/tasks/bootstrap.yml (mc container)"
      to: "5 buckets created via `mc mb --ignore-existing local/<bucket>`"
      via: "community.docker.docker_container with detach: false, auto_remove: true, image pinned to minio/mc:RELEASE.2025-04-22T16-23-26Z; non-zero exit fails the playbook"
      pattern: "mc mb --ignore-existing"
    - from: "roles/minio/defaults/main.yml MINIO env block"
      to: "{{ vault_minio_root_user }} / {{ vault_minio_root_password }} in inventory vault"
      via: "templates/minio.env.j2 renders these from vault; injected into container env"
      pattern: "vault_minio_root_(user|password)"
    - from: "playbooks/deploy_docker.yml roles: list"
      to: "roles/minio/"
      via: "role: minio with tags: [minio] — single tag per role per D-19"
      pattern: "role: minio"
---

<objective>
Port the MinIO role from the upstream INSPQ stack into `roles/minio/`, including the bucket-bootstrap subtask that is the highest-impact M1 pitfall (Pitfall 1 — Loki/Tempo/Mimir crash-loop without buckets). Wire the role into `playbooks/deploy_docker.yml` so Phase 2-6 backends inherit a verified MinIO with all five buckets present.

This is the canonical role port for Telemetron. The patterns established here (defaults layout, env-file template, named volume, healthcheck + restart policy, handler-restart-never-state-restarted, bootstrap-as-final-blocking-task, per-role README schema) become the template every Phase 2-6 role mirrors.

Purpose:
1. Deliver FOUND-02 (working MinIO with all 5 buckets pre-created, bootstrap blocking) and FOUND-01 (telemetron network in use — created by Plan 02's pre_task, consumed by this role).
2. Establish all cross-cutting OPS gates (OPS-01..06) on real role code, not just docs.
3. Make the bucket-bootstrap one-shot mc container pattern reusable as the model for any future "ensure external state exists before exit" need.

Output: 8 files in `roles/minio/` (defaults, meta, handlers, tasks/main.yml + tasks/bootstrap.yml, templates/minio.env.j2, README.md) + 1 modification to `playbooks/deploy_docker.yml` to wire the role into the orchestrator.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/01-foundation-storage/01-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/research/STACK.md
@inventory/example-homelab/group_vars/all/network.yml
@inventory/example-homelab/group_vars/all/storage.yml
@inventory/example-homelab/group_vars/all/vault.yml.example
@inventory/example-homelab/group_vars/all/minio.yml
@playbooks/deploy_docker.yml
@roles/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create roles/minio/ skeleton — defaults, meta, handlers, templates, README</name>
  <read_first>
    - .planning/phases/01-foundation-storage/01-CONTEXT.md (D-06 self-contained role pattern; D-10/D-10a pre-poll via docker_container_info on HEALTHCHECK; D-11 mc image pin; D-15 vault keys; D-16 volume naming; D-18 config layout; D-19 handler-not-restart; D-20 sorted dict iteration)
    - .planning/research/STACK.md §"Datastores" (MinIO row — image tag and rationale) AND §"§ 1. MinIO crisis" (archive context — must appear in role README)
    - .planning/research/PITFALLS.md §"Pitfall 1: MinIO bucket / Loki-Tempo-Mimir start-order race" (the whole point of the bootstrap subtask in Task 2)
    - .planning/research/PITFALLS.md §"Pitfall 8: Ansible role idempotency cascades" (handler-not-restart, sorted dict iteration)
    - .planning/research/PITFALLS.md §"Security Mistakes" (default-no-publish, no minioadmin/minioadmin)
    - inventory/example-homelab/group_vars/all/storage.yml (telemetron_minio_buckets list — defaults must reference this)
    - inventory/example-homelab/group_vars/all/network.yml (telemetron_network, telemetron_publish_default, telemetron_tz)
    - inventory/example-homelab/group_vars/all/minio.yml (minio_publish_host, minio_container_name — role defaults must respect these)
    - inventory/example-homelab/group_vars/all/vault.yml.example (vault key names the role consumes)
    - roles/README.md (Port process numbered list + Per-role port-acceptance gates section from Plan 01 — establishes the README schema this role's README will define)
  </read_first>
  <files>roles/minio/defaults/main.yml, roles/minio/meta/main.yml, roles/minio/handlers/main.yml, roles/minio/templates/minio.env.j2, roles/minio/README.md</files>
  <action>
    Create the directory structure:
    ```bash
    mkdir -p roles/minio/{defaults,meta,handlers,tasks,templates,vars}
    ```

    Then write each file. **All content is ASCII-only.**

    ---

    **File 1: `roles/minio/defaults/main.yml`**

    Content:
    ```yaml
    ---
    # roles/minio/defaults/main.yml
    # MinIO role tunables. Override per-environment in
    # inventory/<env>/group_vars/all/minio.yml.
    #
    # NOTE on the MinIO image pin: the community version of MinIO was archived
    # by the upstream project in early 2026 (repo archived; community Docker
    # images no longer published). This role pins the last published community
    # release. The role README explains the situation in full; a future
    # milestone will evaluate Garage / SeaweedFS as a replacement.

    # --- Image pins (OPS-01: no :latest, ever) ---
    # MinIO server image — last published community release (2025-04-22).
    # See: https://github.com/minio/minio (archived 2026)
    minio_image: minio/minio
    minio_image_tag: "RELEASE.2025-04-22T22-12-26Z"

    # mc (MinIO Client) image — paired with the server pin. Used only by the
    # one-shot bootstrap container that creates buckets.
    minio_mc_image: minio/mc
    minio_mc_image_tag: "RELEASE.2025-04-22T16-23-26Z"

    # --- Container identity ---
    # Container name on the telemetron network. Other roles reach MinIO at
    # http://{{ minio_container_name }}:9000 — coordinate any change here
    # with every Phase 2 backend role's S3 endpoint config.
    minio_container_name: "{{ minio_container_name | default('minio') }}"

    # --- Host port publishing (D-13, D-14, Security Mistakes from PITFALLS.md) ---
    # Default: zero host publish. Inter-component traffic stays on the
    # telemetron Docker bridge network. Console access via SSH local-forward.
    # Override to `true` (publishes to 0.0.0.0) or `127.0.0.1` (loopback only)
    # in inventory/<env>/group_vars/all/minio.yml — see operator README.
    minio_publish_host: "{{ minio_publish_host | default(false) }}"
    minio_api_port: 9000
    minio_console_port: 9001

    # --- Storage layout (D-16, D-17, D-18) ---
    # Named Docker volume covering MinIO's full data root (/data inside the
    # container). One volume = full data root; Pitfall 12 (Loki marker loss)
    # shape applies to every stateful role.
    minio_data_volume: "{{ telemetron_volume_prefix | default('telemetron') }}_minio_data"
    minio_data_path: /data

    # Host-side config directory (D-18: flat layout, no nested config/ subdir).
    # The rendered env file lives here and is bind-mounted into the container.
    minio_config_dir: "{{ telemetron_config_root | default('/opt/telemetron') }}/minio"

    # --- Bucket list (consumed by tasks/bootstrap.yml) ---
    # Pulled from inventory storage.yml when present; defaults provided here
    # so the role works standalone for testing.
    minio_buckets: "{{ telemetron_minio_buckets | default([
      'loki-chunks',
      'tempo-traces',
      'mimir-blocks',
      'mimir-ruler',
      'mimir-alerts',
    ]) }}"

    # --- Bootstrap pre-poll (D-10 / D-10a amendment) ---
    # The mc bootstrap container only runs after MinIO's Docker HEALTHCHECK
    # reports `healthy` (status polled via community.docker.docker_container_info).
    # These knobs control the pre-poll timing — defaults are 30 retries
    # at 2-second intervals (60s total budget for MinIO to come up).
    minio_health_retries: 30
    minio_health_delay: 2

    # --- Healthcheck (OPS-06) ---
    # Docker HEALTHCHECK — the MinIO image ships its own; we declare it
    # explicitly so `docker inspect --format '{{.State.Health.Status}}'`
    # reflects MinIO readiness, not just "container running." The bootstrap
    # pre-poll (tasks/bootstrap.yml) reads Status.Health.Status via
    # docker_container_info and waits for `healthy` before firing mc.
    minio_healthcheck_test: "CMD-SHELL curl -sf http://127.0.0.1:{{ minio_api_port }}/minio/health/ready || exit 1"
    minio_healthcheck_interval: 30s
    minio_healthcheck_timeout: 5s
    minio_healthcheck_retries: 3
    minio_healthcheck_start_period: 30s

    # --- Restart policy (OPS-06) ---
    # `unless-stopped`: survives host reboots; operator deliberate stops
    # aren't fought by the Docker daemon.
    minio_restart_policy: unless-stopped

    # --- Resource limits (homelab-safe; tune per environment) ---
    # MinIO RAM use scales with active connections; 512m fits a homelab.
    minio_memory_limit: 512m

    # --- Network ---
    # Created by playbooks/deploy_docker.yml pre_tasks; this role does NOT
    # create the network — it only attaches to it.
    minio_network: "{{ telemetron_network | default('telemetron') }}"

    # --- Timezone (D-15 / OPS-06) ---
    minio_tz: "{{ telemetron_tz | default('Etc/UTC') }}"

    # --- Browser redirect (Claude's Discretion per CONTEXT.md) ---
    # Empty string disables the console redirect; override only if console
    # is exposed and you want a non-default redirect target.
    minio_browser_redirect_url: ""
    ```

    ---

    **File 2: `roles/minio/meta/main.yml`**

    Content:
    ```yaml
    ---
    galaxy_info:
      role_name: minio
      author: Rock Martel-Langlois
      description: >
        Deploys MinIO S3-compatible object storage on Docker, with bucket
        bootstrap as a blocking final task. Backing store for Telemetron's
        Loki, Tempo, and Mimir roles.
      license: MIT
      min_ansible_version: "2.15"
      platforms:
        - name: Ubuntu
          versions: [jammy, noble]
        - name: Debian
          versions: [bookworm]
      galaxy_tags:
        - storage
        - minio
        - s3
        - observability
        - telemetron

    dependencies: []

    collections:
      - community.docker
      - ansible.builtin
    ```

    ---

    **File 3: `roles/minio/handlers/main.yml`**

    Per W6 in checker feedback: a single handler doing the real restart. Eliminates the previous two-handler no-op + real pattern (which was confusing and set a bad precedent for Phase 2-6).

    Content:
    ```yaml
    ---
    # roles/minio/handlers/main.yml
    # Per D-19 / Pitfall 8: container restarts on config change use
    # `docker restart <name>` (preserves container state), NEVER
    # `state: restarted` (which force-recreates the container and is
    # non-idempotent).
    #
    # MinIO env-var changes require a container restart (no SIGHUP reload
    # path), so the env-file template notifies this handler.

    - name: Docker restart minio
      ansible.builtin.command:
        cmd: "docker restart {{ minio_container_name }}"
      changed_when: true
      listen: restart minio
    ```

    ---

    **File 4: `roles/minio/templates/minio.env.j2`**

    Content:
    ```jinja
    # {{ ansible_managed }}
    # MinIO server environment file — rendered by roles/minio.
    # Bind-mounted into the container; consumed by MinIO at startup.
    # DO NOT EDIT BY HAND — changes here are overwritten by ansible-playbook.

    MINIO_ROOT_USER={{ vault_minio_root_user }}
    MINIO_ROOT_PASSWORD={{ vault_minio_root_password }}
    MINIO_VOLUMES={{ minio_data_path }}
    MINIO_BROWSER_REDIRECT_URL={{ minio_browser_redirect_url }}
    TZ={{ minio_tz }}
    ```

    ---

    **File 5: `roles/minio/README.md`**

    This README is the canonical template for every Phase 2-6 role README. The schema (Variables, Modes, Defaults, Tags, Volumes, Healthcheck, Deprecation notes, Operator notes) is established here per OPS-03.

    Content (minimum 80 lines, ASCII-only):
    ```markdown
    # roles/minio

    Deploys [MinIO](https://github.com/minio/minio) — S3-compatible object
    storage — on a single Docker host. MinIO is the backing store for
    Telemetron's Loki (log chunks), Tempo (trace blocks), and Mimir
    (metric blocks + ruler + alertmanager state).

    This role is the **first role** ported in Telemetron M1 and the
    canonical template every subsequent role mirrors.

    ## What this role does

    1. Renders `/opt/telemetron/minio/minio.env` from `templates/minio.env.j2`
       with MinIO root credentials pulled from Ansible vault.
    2. Ensures the named Docker volume `telemetron_minio_data` exists.
    3. Starts the `minio` container on the `telemetron` Docker bridge
       network, with a Docker `HEALTHCHECK` and `restart: unless-stopped`.
       Container ports are NOT published to the host by default
       (`minio_publish_host: false`).
    4. **As a blocking final task**, polls MinIO's Docker HEALTHCHECK
       via `community.docker.docker_container_info` until Status.Health.Status
       reports `healthy`, then runs a one-shot `minio/mc` container that
       creates the five required buckets (`loki-chunks`, `tempo-traces`,
       `mimir-blocks`, `mimir-ruler`, `mimir-alerts`). Non-zero exit from
       the bootstrap container fails the playbook. **Downstream backends
       (Loki, Tempo, Mimir in Phase 2) depend on this — they will not
       see a no-bucket MinIO.**

    ## Important: MinIO is archived upstream

    The community MinIO project was archived in early 2026 — the GitHub
    repository is read-only and the community Docker images are no longer
    being published. This role pins the last published community release
    (`minio/minio:RELEASE.2025-04-22T22-12-26Z` and the paired
    `minio/mc:RELEASE.2025-04-22T16-23-26Z`).

    A future Telemetron milestone will evaluate
    [Garage](https://garagehq.deuxfleurs.fr/) and SeaweedFS as
    replacements. **Do not deploy this role for any workload where security
    patching of the storage layer matters.**

    ## Variables

    See `defaults/main.yml` for the full list. Most operators will only
    touch `minio_publish_host` (default `false` — see Security below).

    | Variable | Default | Purpose |
    |----------|---------|---------|
    | `minio_image` | `minio/minio` | MinIO server image (do not change) |
    | `minio_image_tag` | `RELEASE.2025-04-22T22-12-26Z` | Pinned tag (no `:latest`) |
    | `minio_mc_image` | `minio/mc` | mc CLI image for the bootstrap one-shot |
    | `minio_mc_image_tag` | `RELEASE.2025-04-22T16-23-26Z` | Pinned tag |
    | `minio_container_name` | `minio` | DNS name on the `telemetron` network |
    | `minio_publish_host` | `false` | Publish :9000 and :9001 to the host. `false`/`true`/`127.0.0.1` |
    | `minio_api_port` | `9000` | S3 API port |
    | `minio_console_port` | `9001` | Web console port |
    | `minio_data_volume` | `telemetron_minio_data` | Named Docker volume |
    | `minio_data_path` | `/data` | In-container data path |
    | `minio_config_dir` | `/opt/telemetron/minio` | Host config bind-mount source |
    | `minio_buckets` | (5 buckets — see defaults) | Buckets created by bootstrap |
    | `minio_health_retries` | `30` | Pre-bootstrap Docker HEALTHCHECK polls |
    | `minio_health_delay` | `2` | Seconds between polls |
    | `minio_healthcheck_*` | (HEALTHCHECK config) | Docker HEALTHCHECK timing |
    | `minio_restart_policy` | `unless-stopped` | Container restart policy |
    | `minio_memory_limit` | `512m` | Container memory limit |
    | `minio_network` | `telemetron` | Docker network (created by playbook pre_tasks) |
    | `minio_tz` | `Etc/UTC` | Container timezone (Pitfall 6) |
    | `minio_browser_redirect_url` | `""` | Empty disables console redirect |

    ## Vault keys

    Required keys in `inventory/<env>/group_vars/all/vault.yml`:

    | Key | Purpose |
    |-----|---------|
    | `vault_minio_root_user` | MinIO root username (>= 3 chars) |
    | `vault_minio_root_password` | MinIO root password (>= 8 chars; 32+ recommended) |

    Placeholders ship in `inventory/example-homelab/group_vars/all/vault.yml.example`.

    ## Tags

    - `minio` — runs the whole role
    - `always` — pre_tasks (network creation) — set in `playbooks/deploy_docker.yml`

    ## Modes

    Single mode only in M1: monolithic single-node. Distributed MinIO is a
    later-milestone concern and is not part of the role's variable surface yet.

    ## Volumes

    | Volume | Mount | Purpose |
    |--------|-------|---------|
    | `telemetron_minio_data` (named) | `/data` | MinIO data root — covers all object storage. Persistent across container recreates. |
    | `/opt/telemetron/minio/` (bind) | (env file mount path) | Rendered config; bind-mounted read-only. |

    ## Healthcheck

    Docker HEALTHCHECK runs every 30s (`curl -sf http://127.0.0.1:9000/minio/health/ready`).
    Verify with:
    ```bash
    docker inspect telemetron-minio --format '{{.State.Health.Status}}'   # expects: healthy
    ```

    The bucket-bootstrap subtask polls this same HEALTHCHECK status via
    `community.docker.docker_container_info` (reading
    `result.container.State.Health.Status`) before firing the mc container,
    so first-deploy timing races are eliminated without needing host port
    publishing.

    ## Security model

    - **Default: no host port publish.** Operator access via
      `ssh -L 9001:localhost:9001 <host>` and a web browser, or via
      `docker exec` on the host.
    - **Root credentials from vault.** No `minioadmin/minioadmin` default
      anywhere; the role refuses to render the env file if vault keys
      are missing (Ansible errors out on the undefined vault variable).
    - **Inter-component traffic on the `telemetron` Docker bridge network only.**
      Loki, Tempo, and Mimir (Phase 2) will reach MinIO at
      `http://minio:9000` over the bridge — never via the host.
    - **Per-backend access keys are deferred.** Phase 1 reuses MinIO root
      credentials for Loki/Tempo/Mimir; per-backend users with
      bucket-scoped IAM policies are a future hardening phase.
    - **Bootstrap credential exposure (D-09 + I12).** The bucket-bootstrap
      one-shot mc container receives root creds via the
      `MC_HOST_<alias>=http://user:pw@host:port` env-var format that mc
      requires. This is technically visible in `docker inspect` for the
      lifetime of the container. `auto_remove: true` on the bootstrap
      container destroys it on exit (typically <5s), bounding the
      docker-inspect exposure window to the bootstrap run only. See
      `tasks/bootstrap.yml` inline comment.

    ## Idempotency

    Per OPS-04: running the playbook twice in a row against an unchanged
    inventory reports `changed=0` in the PLAY RECAP. Verify with:
    ```bash
    ansible-playbook -i inventory/example-homelab \
                     playbooks/deploy_docker.yml --tags minio \
                     --ask-vault-pass
    # ...first run: changed=N
    ansible-playbook -i inventory/example-homelab \
                     playbooks/deploy_docker.yml --tags minio \
                     --ask-vault-pass
    # ...second run: changed=0
    ```

    Container restarts on env-file change use a handler that runs
    `docker restart minio` — never `state: restarted` (which force-recreates
    the container and is non-idempotent per Pitfall 8).

    ## Port-acceptance gates

    This role passes all five gates documented in `roles/README.md`:

    - **Grep gates (Pitfall 9):**
      `grep -riE 'inspq|qc\.ca|montreal|quebec|francais|french|/srv/nfs/inspq|vault_inspq' roles/minio/` returns 0 matches.
      `grep -rPn '[^\x00-\x7F]' roles/minio/` returns 0 matches.
    - **Image-pin gate:** zero `:latest` references in `roles/minio/`.
    - **Vault-discipline gate:** every `{{ vault_* }}` reference has a
      matching key in `vault.yml.example` with the consuming-role comment.
    - **Idempotency gate:** twice-in-a-row playbook run reports `changed=0`.
    - **Healthcheck + restart-policy gate:** `docker inspect` returns
      `healthy` and `unless-stopped`.

    ## Deprecation notes

    The MinIO community version is archived upstream (see "Important" section
    above). This role is a candidate for replacement with a Garage role in a
    future milestone.

    ## Bring your own bucket names

    Override `telemetron_minio_buckets` in
    `inventory/<env>/group_vars/all/storage.yml` to add or rename buckets.
    Renaming a bucket here MUST be paired with renaming in the consuming
    backend role's S3 config (Phase 2 loki / tempo / mimir).
    ```

    ---

    **All files must be ASCII-only.** No accented characters, no smart quotes, no em dashes (use `--` instead). The non-ASCII grep gate from D-21 will be run against `roles/minio/` and must report zero matches.

    **Note:** Use plain ASCII `quebec` (not `québec`) in the grep-gate comment in the README — the grep pattern `quebec` is what's checked, and writing the accented version inside a comment would itself fail the non-ASCII gate.
  </action>
  <verify>
    <automated>test -d roles/minio/defaults && test -d roles/minio/meta && test -d roles/minio/handlers && test -d roles/minio/templates && echo OK_dirs ; test -f roles/minio/defaults/main.yml && test -f roles/minio/meta/main.yml && test -f roles/minio/handlers/main.yml && test -f roles/minio/templates/minio.env.j2 && test -f roles/minio/README.md && echo OK_files ; grep -c 'minio/minio' roles/minio/defaults/main.yml ; grep -c 'RELEASE.2025-04-22T22-12-26Z' roles/minio/defaults/main.yml ; grep -c 'RELEASE.2025-04-22T16-23-26Z' roles/minio/defaults/main.yml ; grep -cE ':latest' roles/minio/ -r ; grep -c 'MINIO_ROOT_USER' roles/minio/templates/minio.env.j2 ; grep -c 'MINIO_ROOT_PASSWORD' roles/minio/templates/minio.env.j2 ; grep -c 'vault_minio_root_user' roles/minio/templates/minio.env.j2 ; grep -c 'vault_minio_root_password' roles/minio/templates/minio.env.j2 ; grep -c 'community.docker' roles/minio/meta/main.yml ; grep -c 'docker restart' roles/minio/handlers/main.yml ; grep -cE '^- name:' roles/minio/handlers/main.yml ; grep -ciE 'state: restarted' roles/minio/handlers/main.yml ; wc -l roles/minio/README.md ; grep -riEc 'inspq|qc\.ca|montreal|québec|francais|french|/srv/nfs/inspq|vault_inspq' roles/minio/ ; grep -rPnc '[^\x00-\x7F]' roles/minio/ | grep -v ':0$' | wc -l ; python3 -c "import yaml; yaml.safe_load(open('roles/minio/defaults/main.yml')); yaml.safe_load(open('roles/minio/meta/main.yml')); yaml.safe_load(open('roles/minio/handlers/main.yml'))" && echo OK_yaml_parses</automated>
  </verify>
  <acceptance_criteria>
    - All 5 files exist: defaults/main.yml, meta/main.yml, handlers/main.yml, templates/minio.env.j2, README.md
    - `grep -c 'RELEASE.2025-04-22T22-12-26Z' roles/minio/defaults/main.yml` returns `1` (MinIO server pin)
    - `grep -c 'RELEASE.2025-04-22T16-23-26Z' roles/minio/defaults/main.yml` returns `1` (mc bootstrap pin per D-11)
    - **Image-pin gate (OPS-01):** `grep -rcE ':latest' roles/minio/` returns 0 matches (no `:latest` anywhere)
    - `grep -c 'MINIO_ROOT_USER={{ vault_minio_root_user }}' roles/minio/templates/minio.env.j2` returns `1`
    - `grep -c 'MINIO_ROOT_PASSWORD={{ vault_minio_root_password }}' roles/minio/templates/minio.env.j2` returns `1`
    - `grep -c 'MINIO_VOLUMES={{ minio_data_path }}' roles/minio/templates/minio.env.j2` returns `1`
    - `grep -c 'community.docker' roles/minio/meta/main.yml` returns at least `1`
    - `grep -c 'docker restart' roles/minio/handlers/main.yml` returns at least `1`
    - **Single-handler check (W6):** `grep -cE '^- name:' roles/minio/handlers/main.yml` returns `1` (exactly one handler — no no-op sentinel)
    - **Pitfall 8 gate:** `grep -ciE 'state: restarted' roles/minio/handlers/main.yml` returns 0 matches (handler never recreates the container)
    - README is at least 80 lines: `wc -l roles/minio/README.md` returns >= 80
    - README contains the MinIO archive warning (`grep -c 'archived' roles/minio/README.md` returns at least `1`)
    - README documents all 5 port-acceptance gates (image-pin, vault, idempotency, healthcheck+restart, grep) — `grep -c 'Port-acceptance gates' roles/minio/README.md` returns at least `1`
    - README documents D-09/I12 credential exposure model: `grep -c 'MC_HOST' roles/minio/README.md` returns at least `1` AND `grep -c 'auto_remove' roles/minio/README.md` returns at least `1`
    - **Grep gate (Pitfall 9):** `grep -riEc 'inspq|qc\.ca|montreal|québec|francais|french|/srv/nfs/inspq|vault_inspq' roles/minio/` returns 0 matches (no INSPQ leftovers)
    - **Non-ASCII gate:** `grep -rPnc '[^\x00-\x7F]' roles/minio/` reports no files with non-zero matches (zero non-ASCII characters anywhere in roles/minio/)
    - All YAML files parse: defaults/main.yml, meta/main.yml, handlers/main.yml all load with PyYAML
  </acceptance_criteria>
  <done>Role skeleton complete: defaults declare both image pins (server + mc), all tunables, and reference shared inventory vars; meta declares community.docker dependency; handlers contain ONE handler that runs `docker restart` only (never `state: restarted` — no-op sentinel pattern removed per W6); env template renders vault credentials; README documents the role per the OPS-03 schema (variables, vault keys, tags, modes, volumes, healthcheck, security including D-09 credential-exposure note, idempotency, port-acceptance gates, deprecation notes) and includes the MinIO archive warning. All grep gates pass.</done>
</task>

<task type="auto">
  <name>Task 2: Write roles/minio/tasks/main.yml + tasks/bootstrap.yml (container deploy + bucket bootstrap via docker_container_info HEALTHCHECK poll)</name>
  <read_first>
    - roles/minio/defaults/main.yml (Task 1 just created — your tasks reference every variable defined there)
    - roles/minio/templates/minio.env.j2 (Task 1 just created — `template:` task renders this)
    - roles/minio/handlers/main.yml (Task 1 just created — your tasks notify `restart minio` when the env file changes)
    - .planning/phases/01-foundation-storage/01-CONTEXT.md (D-07, D-08, D-09, D-10 + D-10a amendment, D-13, D-19 — the exact bootstrap pattern, network behavior, handler discipline)
    - .planning/research/PITFALLS.md §"Pitfall 1: MinIO bucket / Loki-Tempo-Mimir start-order race" (entire section — your bootstrap.yml directly addresses this)
    - .planning/research/PITFALLS.md §"Pitfall 8: Ansible role idempotency cascades" (use `state: started`, `recreate: false`, notify handlers; NOT `state: restarted`)
    - .planning/research/PITFALLS.md §"Security Mistakes" (specifically the MC_HOST credential URL exposure note — informs I12 inline comment)
    - inventory/example-homelab/group_vars/all/storage.yml (telemetron_minio_buckets is the source of bucket names your bootstrap iterates over)
  </read_first>
  <files>roles/minio/tasks/main.yml, roles/minio/tasks/bootstrap.yml</files>
  <action>
    Two files. **All ASCII-only.**

    ---

    **File 1: `roles/minio/tasks/main.yml`**

    Content:
    ```yaml
    ---
    # roles/minio/tasks/main.yml
    # MinIO server deploy + bucket bootstrap.
    # Per D-08: bootstrap is the LAST task in this role; downstream backends
    # depend on serial role ordering in playbooks/deploy_docker.yml.

    - name: Ensure MinIO config directory exists on host
      ansible.builtin.file:
        path: "{{ minio_config_dir }}"
        state: directory
        mode: "0755"
      tags:
        - minio
        - minio-config

    - name: Render MinIO env file
      ansible.builtin.template:
        src: minio.env.j2
        dest: "{{ minio_config_dir }}/minio.env"
        mode: "0600"
      notify: Docker restart minio
      tags:
        - minio
        - minio-config

    - name: Ensure MinIO data volume exists
      community.docker.docker_volume:
        name: "{{ minio_data_volume }}"
        state: present
      tags:
        - minio

    - name: Pull MinIO server image
      community.docker.docker_image:
        name: "{{ minio_image }}:{{ minio_image_tag }}"
        source: pull
        force_source: false
      tags:
        - minio

    - name: Run MinIO server container
      community.docker.docker_container:
        name: "{{ minio_container_name }}"
        image: "{{ minio_image }}:{{ minio_image_tag }}"
        state: started
        recreate: false
        restart_policy: "{{ minio_restart_policy }}"
        memory: "{{ minio_memory_limit }}"
        env_file: "{{ minio_config_dir }}/minio.env"
        command:
          - server
          - "{{ minio_data_path }}"
          - --console-address
          - ":{{ minio_console_port }}"
        networks:
          - name: "{{ minio_network }}"
            aliases:
              - "{{ minio_container_name }}"
        # Per D-12, D-13: zero host publish by default. Only publish if
        # the operator explicitly sets minio_publish_host to true or 127.0.0.1.
        published_ports: >-
          {{
            (
              [ '{{ minio_api_port }}:{{ minio_api_port }}',
                '{{ minio_console_port }}:{{ minio_console_port }}' ]
              if minio_publish_host is sameas true else
              [ '127.0.0.1:{{ minio_api_port }}:{{ minio_api_port }}',
                '127.0.0.1:{{ minio_console_port }}:{{ minio_console_port }}' ]
              if minio_publish_host == '127.0.0.1' else
              []
            )
          }}
        mounts:
          - source: "{{ minio_data_volume }}"
            target: "{{ minio_data_path }}"
            type: volume
        healthcheck:
          test: ["{{ minio_healthcheck_test.split(' ')[0] }}", "{{ minio_healthcheck_test.split(' ', 1)[1] }}"]
          interval: "{{ minio_healthcheck_interval }}"
          timeout: "{{ minio_healthcheck_timeout }}"
          retries: "{{ minio_healthcheck_retries }}"
          start_period: "{{ minio_healthcheck_start_period }}"
      tags:
        - minio
        - minio-container

    # Bootstrap MUST be the final task — D-08. Downstream Phase 2 backends
    # (loki, tempo, mimir) depend on serial role ordering in deploy_docker.yml
    # so they don't start until this include returns.
    - name: Bootstrap MinIO buckets (blocking)
      ansible.builtin.include_tasks: bootstrap.yml
      tags:
        - minio
        - minio-bootstrap
    ```

    ---

    **File 2: `roles/minio/tasks/bootstrap.yml`**

    This is the core of Pitfall 1's mitigation: pre-poll the Docker HEALTHCHECK status, then run a one-shot mc container that creates every bucket. Non-zero exit fails the playbook.

    **Implementation note (D-10a amendment, 2026-05-17):** The pre-poll uses
    `community.docker.docker_container_info` reading the container's own
    `State.Health.Status` field. The original D-10 design called for
    `ansible.builtin.uri` against `/minio/health/ready`, but D-13's no-host-publish
    rule makes :9000 unreachable from the control host. Polling the Docker
    HEALTHCHECK status achieves the same intent (block until MinIO is actually
    ready) via a cleaner mechanism — and it reuses the HEALTHCHECK that OPS-06
    already mandates exist on the container.

    Content:
    ```yaml
    ---
    # roles/minio/tasks/bootstrap.yml
    # Pre-poll MinIO's Docker HEALTHCHECK status, then run a one-shot mc
    # container to create all required buckets. Blocks the playbook on
    # non-zero exit.
    #
    # Direct mitigation for PITFALLS.md Pitfall 1 (MinIO bucket / LTM race).

    # Step 1: poll the container's Docker HEALTHCHECK status until it
    # reports `healthy`. Per D-10a (amendment to D-10): we read the
    # status via community.docker.docker_container_info rather than firing
    # ansible.builtin.uri against :9000, because D-13's no-host-publish
    # model means the control host cannot reach the MinIO API directly.
    # The HEALTHCHECK itself (defined in tasks/main.yml) is what proves
    # MinIO is answering /minio/health/ready inside the container.
    - name: Wait for MinIO container HEALTHCHECK to report healthy
      community.docker.docker_container_info:
        name: "{{ minio_container_name }}"
      register: minio_health_check
      until: >-
        minio_health_check.container is defined
        and minio_health_check.container.State is defined
        and minio_health_check.container.State.Health is defined
        and minio_health_check.container.State.Health.Status == 'healthy'
      retries: "{{ minio_health_retries }}"
      delay: "{{ minio_health_delay }}"
      changed_when: false
      tags:
        - minio
        - minio-bootstrap

    # Step 2: one-shot mc container that creates every bucket.
    # Per D-07: detach=false + auto_remove=true => Ansible blocks on exit
    # and the container is GC'd after. Non-zero exit aborts the playbook.
    # Per D-09: root credentials reused (per-backend keys deferred to a
    # future hardening phase).
    #
    # Bucket list is iterated via `{% for k in minio_buckets | sort %}` in
    # the mc command line — per D-20 we sort for deterministic output.
    #
    # SECURITY (I12): MC_HOST_<alias>=http://user:pw@host:port is the
    # required mc credential format. The URL embeds vault credentials and
    # IS visible via `docker inspect` for the lifetime of this container.
    # `auto_remove: true` destroys the container on exit (typically <5s),
    # bounding the docker-inspect exposure window to the bootstrap run
    # only (per D-09). See roles/minio/README.md "Security model" section.
    #
    # IDEMPOTENCY (W7): we set `changed_when: false` on this task. The
    # `mc mb --ignore-existing` flag means mc exits 0 in BOTH create and
    # already-exists cases — there is no way to distinguish "we created
    # buckets" from "they were already there" from the mc exit code. The
    # verify task below is the source of truth for "buckets exist"; this
    # task is "ensure they exist" and is not a state-mutation worth
    # reporting as changed. This is what makes the second playbook run
    # report changed=0 (OPS-04).
    - name: Bootstrap required buckets via one-shot mc container
      community.docker.docker_container:
        name: "{{ minio_container_name }}-bootstrap"
        image: "{{ minio_mc_image }}:{{ minio_mc_image_tag }}"
        state: started
        detach: false
        auto_remove: true
        recreate: true
        cleanup: true
        networks:
          - name: "{{ minio_network }}"
        env:
          MC_HOST_local: "http://{{ vault_minio_root_user }}:{{ vault_minio_root_password }}@{{ minio_container_name }}:{{ minio_api_port }}"
        entrypoint: ["/bin/sh", "-c"]
        command:
          - >-
            mc mb --ignore-existing
            {% for b in minio_buckets | sort %}local/{{ b }} {% endfor %}
      register: minio_bootstrap_result
      failed_when: minio_bootstrap_result.status is defined and minio_bootstrap_result.status != 0
      changed_when: false
      tags:
        - minio
        - minio-bootstrap

    # Step 3: verify (cheap read-back via `mc ls --json`).
    # If `mc ls local/` doesn't show all expected buckets, fail loud.
    # Per W8: we parse `mc ls --json` (jq -r '.key') rather than text
    # output — JSON is mc's documented stable interface; column parsing
    # of text output is fragile.
    - name: Verify all required buckets exist
      community.docker.docker_container:
        name: "{{ minio_container_name }}-verify"
        image: "{{ minio_mc_image }}:{{ minio_mc_image_tag }}"
        state: started
        detach: false
        auto_remove: true
        recreate: true
        cleanup: true
        networks:
          - name: "{{ minio_network }}"
        env:
          MC_HOST_local: "http://{{ vault_minio_root_user }}:{{ vault_minio_root_password }}@{{ minio_container_name }}:{{ minio_api_port }}"
        entrypoint: ["/bin/sh", "-c"]
        command:
          - >-
            mc ls --json local/ |
            sed -n 's/.*"key":"\([^"]*\)\/".*/\1/p' |
            sort -u >/tmp/got;
            printf '%s\n' {% for b in minio_buckets | sort %}{{ b }} {% endfor %} |
            sort -u >/tmp/want;
            diff /tmp/want /tmp/got
      register: minio_bucket_verify
      failed_when: minio_bucket_verify.status is defined and minio_bucket_verify.status != 0
      changed_when: false
      tags:
        - minio
        - minio-bootstrap
    ```

    **Notes:**

    - The Step 1 pre-poll reads `result.container.State.Health.Status` — this requires the MinIO container to have a Docker HEALTHCHECK defined (which it does, in tasks/main.yml). If the HEALTHCHECK is missing, `State.Health` will be `null` and the `until:` condition will keep retrying until the retries budget is exhausted — surfacing the misconfiguration as a clear failure.
    - The `mc mb --ignore-existing` flag means mc returns 0 for already-existing buckets, making the bootstrap task naturally idempotent at the mc layer. Combined with `changed_when: false` (per W7), the second playbook run reports `changed=0` overall.
    - The verify task parses `mc ls --json` (per W8) — `--json` is the documented stable mc interface; text output with awk/sed column-position parsing is fragile. We use `sed` to extract the `"key":"<bucket>/"` field from each line (busybox-compatible — works inside the minio/mc image, which is alpine-based and has neither jq nor full GNU coreutils).
    - The verify task uses `diff` against a sorted expected list — if any bucket is missing OR extra, `diff` exits 1 and the playbook fails. This catches both bootstrap failures AND accidental bucket drift.
    - **Why three steps not one:** the pre-poll guarantees we don't fire mc against a half-booted MinIO (especially relevant on first deploy when MinIO's `start_period` of 30s applies). The bootstrap is the actual create. The verify is a cheap diff that catches "bootstrap silently failed" — a known MinIO failure mode where mc exits 0 but a bucket wasn't actually created due to race on permission init.
  </action>
  <verify>
    <automated>test -f roles/minio/tasks/main.yml && test -f roles/minio/tasks/bootstrap.yml && echo OK_files ; grep -c 'include_tasks: bootstrap.yml' roles/minio/tasks/main.yml ; grep -c 'community.docker.docker_container' roles/minio/tasks/main.yml ; grep -c 'community.docker.docker_volume' roles/minio/tasks/main.yml ; grep -c 'community.docker.docker_image' roles/minio/tasks/main.yml ; grep -ciE 'state: restarted' roles/minio/tasks/main.yml roles/minio/tasks/bootstrap.yml ; grep -c 'community.docker.docker_container_info' roles/minio/tasks/bootstrap.yml ; grep -c 'State.Health.Status' roles/minio/tasks/bootstrap.yml ; grep -c 'mc mb --ignore-existing' roles/minio/tasks/bootstrap.yml ; grep -c 'auto_remove: true' roles/minio/tasks/bootstrap.yml ; grep -c 'detach: false' roles/minio/tasks/bootstrap.yml ; grep -c 'failed_when:' roles/minio/tasks/bootstrap.yml ; grep -c 'changed_when: false' roles/minio/tasks/bootstrap.yml ; grep -c 'minio_buckets | sort' roles/minio/tasks/bootstrap.yml ; grep -c 'mc ls --json' roles/minio/tasks/bootstrap.yml ; grep -c 'Vault credentials are URL-embedded\|SECURITY (I12)\|MC_HOST_<alias>' roles/minio/tasks/bootstrap.yml ; grep -c 'tags:' roles/minio/tasks/main.yml ; grep -riEc 'inspq|qc\.ca|montreal|québec|francais|french|/srv/nfs/inspq|vault_inspq' roles/minio/tasks/ ; grep -rPnc '[^\x00-\x7F]' roles/minio/tasks/ | grep -v ':0$' | wc -l ; python3 -c "import yaml; yaml.safe_load(open('roles/minio/tasks/main.yml')); yaml.safe_load(open('roles/minio/tasks/bootstrap.yml'))" && echo OK_yaml_parses ; (command -v ansible-lint >/dev/null && ansible-lint roles/minio/ 2>&1 | tail -10) || echo "ansible-lint not installed — skipping"</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist
    - `grep -c 'include_tasks: bootstrap.yml' roles/minio/tasks/main.yml` returns `1` (bootstrap is included as final task)
    - `grep -c 'community.docker.docker_container' roles/minio/tasks/main.yml` returns at least `1` (server container task)
    - `grep -c 'community.docker.docker_volume' roles/minio/tasks/main.yml` returns `1` (volume ensure task)
    - `grep -c 'community.docker.docker_image' roles/minio/tasks/main.yml` returns `1` (image pull task)
    - **Pitfall 8 gate:** `grep -ciE 'state: restarted' roles/minio/tasks/` returns 0 (no `state: restarted` in any task file)
    - **B3 fix — D-10a amendment:** `grep -c 'community.docker.docker_container_info' roles/minio/tasks/bootstrap.yml` returns `1` (pre-poll uses docker_container_info, NOT ansible.builtin.uri or docker_container_exec)
    - **B3 fix — HEALTHCHECK polling:** `grep -c 'State.Health.Status' roles/minio/tasks/bootstrap.yml` returns at least `1` (until: condition reads the container's HEALTHCHECK status)
    - `grep -c 'mc mb --ignore-existing' roles/minio/tasks/bootstrap.yml` returns `1` (idempotent bucket create)
    - `grep -c 'auto_remove: true' roles/minio/tasks/bootstrap.yml` returns at least `2` (one-shot containers GC themselves — bootstrap + verify)
    - `grep -c 'detach: false' roles/minio/tasks/bootstrap.yml` returns at least `2` (Ansible blocks on exit — bootstrap + verify)
    - `grep -c 'failed_when:' roles/minio/tasks/bootstrap.yml` returns at least `2` (bootstrap + verify both check non-zero exit)
    - **W7 fix — idempotent changed_when:** `grep -c 'changed_when: false' roles/minio/tasks/bootstrap.yml` returns at least `3` (pre-poll, bootstrap, verify all set changed_when: false so the second playbook run reports changed=0)
    - `grep -c 'minio_buckets | sort' roles/minio/tasks/bootstrap.yml` returns at least `2` (D-20 sorted iteration in both bootstrap and verify)
    - **W8 fix — JSON verify:** `grep -c 'mc ls --json' roles/minio/tasks/bootstrap.yml` returns `1` (verify uses mc's documented stable JSON interface, not text-column parsing)
    - **I12 fix — credential exposure note:** `grep -cE 'Vault credentials are URL-embedded|SECURITY \(I12\)|MC_HOST_<alias>' roles/minio/tasks/bootstrap.yml` returns at least `1` (inline comment documents why MC_HOST URL credential embedding is acceptable given auto_remove)
    - Tags present: every task in main.yml has `tags:` with `minio` at minimum
    - **Grep gate (Pitfall 9):** `grep -riEc 'inspq|qc\.ca|montreal|québec|francais|french|/srv/nfs/inspq|vault_inspq' roles/minio/tasks/` returns 0
    - **Non-ASCII gate:** `grep -rPnc '[^\x00-\x7F]' roles/minio/tasks/` reports no files with non-zero matches
    - Both YAML files parse with PyYAML
    - If `ansible-lint` is installed: runs against `roles/minio/` with no `production` profile blocker errors (warnings OK — the role is a port; we tune the lint profile per Claude's Discretion in CONTEXT.md)
  </acceptance_criteria>
  <done>tasks/main.yml renders the env file (notifying the restart handler), ensures the volume, pulls the image, runs the server container with healthcheck + restart policy + no host publish (default) + correct command line, and includes bootstrap.yml as the final task. bootstrap.yml polls MinIO's Docker HEALTHCHECK status via `community.docker.docker_container_info` (D-10a amendment — replaces the unreachable :9000 uri poll), runs the one-shot mc container that creates all five buckets (sorted iteration per D-20, `--ignore-existing` for idempotency, `changed_when: false` per W7 so the second run reports changed=0), and verifies the bucket list via `mc ls --json` diff per W8 (catches silent bootstrap failures via stable JSON interface). Bootstrap container has an I12 inline comment explaining the MC_HOST credential URL exposure and why auto_remove makes it acceptable. All grep + non-ASCII gates pass. No `state: restarted` anywhere.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the minio role into playbooks/deploy_docker.yml and verify end-to-end</name>
  <read_first>
    - playbooks/deploy_docker.yml (created by Plan 02 Task 2 — currently has `roles: []`)
    - roles/minio/tasks/main.yml (Task 2 just created — confirm the role's expected entry point and tag)
    - .planning/phases/01-foundation-storage/01-CONTEXT.md (D-05 dependency tree; D-08 serial role ordering as the gate)
  </read_first>
  <files>playbooks/deploy_docker.yml</files>
  <action>
    Replace the empty `roles: []` line in `playbooks/deploy_docker.yml` with the wired `minio` role. Keep all other content (header comment, hosts, gather_facts, become, collections, pre_tasks) untouched.

    The current line in playbooks/deploy_docker.yml reads:
    ```yaml
      roles: []
      # Phase 1 plan 03 wires in the minio role here:
      #   - role: minio
      #     tags: [minio]
      #
      # Subsequent phase plans extend this list in dependency order:
      ...
    ```

    Replace it with:
    ```yaml
      roles:
        - role: minio
          tags:
            - minio
      # Subsequent phase plans extend this list in dependency order
      # (D-05 dependency tree):
      #   Phase 2: loki, tempo, mimir (after minio — depend on its buckets)
      #   Phase 3: prometheus, opentelemetry, fluentbit, node_exporter
      #   Phase 4: alertmanager, hook_router
      #   Phase 5: grafana, karma, promlens
      #   Phase 6: nfsd (opt-in via enable_nfsd flag)
    ```

    Notes:
    - Single-tag-per-role per Claude's Discretion in CONTEXT.md. Sub-tags like `minio-config` and `minio-bootstrap` exist on individual tasks within the role for finer-grained re-runs (see Task 2).
    - Serial role ordering IS the gate (D-08). When Phase 2 adds `loki`, `tempo`, `mimir` to this list AFTER `minio`, they only start when `minio` (including its bootstrap subtask) has fully exited — no async, no `meta: flush_handlers` gymnastics needed.
    - The existing `pre_tasks:` block (community.docker.docker_network ensuring `telemetron`) is untouched — the minio role's container task assumes that network already exists.

    After the edit, run an end-to-end validation:

    1. `ansible-playbook --syntax-check -i inventory/example-homelab playbooks/deploy_docker.yml` — must pass (no syntax errors).

    2. If a Docker daemon is available on the target host (Ansible control host in the example inventory's `ansible_connection: local` case):
       - First-time vault setup (one-time, manual): `cp inventory/example-homelab/group_vars/all/vault.yml.example inventory/example-homelab/group_vars/all/vault.yml ; sed -i 's/CHANGE_ME/admin/g; s/admin$/admin12345/' inventory/example-homelab/group_vars/all/vault.yml ; ansible-vault encrypt --vault-password-file /tmp/.vp inventory/example-homelab/group_vars/all/vault.yml` (using a throwaway password in /tmp/.vp). **The executor should only perform this if Docker is confirmed available; otherwise document the test sequence in the SUMMARY for human-run validation.**
       - First playbook run: `ansible-playbook -i inventory/example-homelab playbooks/deploy_docker.yml --tags minio --vault-password-file /tmp/.vp` — must succeed, container starts, buckets created.
       - Verify: `docker inspect minio --format '{{.State.Health.Status}}'` returns `healthy` (may need to wait up to `start_period`=30s).
       - Verify: `docker run --rm --network telemetron -e MC_HOST_local="http://admin:admin12345@minio:9000" minio/mc:RELEASE.2025-04-22T16-23-26Z mc ls --json local/ | jq -r '.key' | sort` lists all 5 buckets.
       - Second playbook run (idempotency gate): same command — must report `changed=0` in the PLAY RECAP.
       - Cleanup: `docker rm -f minio ; docker volume rm telemetron_minio_data ; docker network rm telemetron ; rm -f inventory/example-homelab/group_vars/all/vault.yml /tmp/.vp`.

    3. If Docker is NOT available on the executor's host, the SUMMARY must clearly document the human-run validation sequence above so the operator can run it before declaring Phase 1 done.
  </action>
  <verify>
    <automated>grep -c '^      roles:$' playbooks/deploy_docker.yml ; grep -c '- role: minio' playbooks/deploy_docker.yml ; grep -c '- minio' playbooks/deploy_docker.yml ; grep -c '^      roles: \[\]' playbooks/deploy_docker.yml ; grep -c 'community.docker.docker_network' playbooks/deploy_docker.yml ; grep -rPnc '[^\x00-\x7F]' playbooks/deploy_docker.yml ; python3 -c "import yaml; yaml.safe_load(open('playbooks/deploy_docker.yml'))" && echo OK_yaml_parses ; ansible-playbook --syntax-check -i inventory/example-homelab playbooks/deploy_docker.yml 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '- role: minio' playbooks/deploy_docker.yml` returns `1` (role wired in)
    - `grep -c '^      roles: \[\]' playbooks/deploy_docker.yml` returns `0` (empty roles list replaced)
    - `grep -c 'community.docker.docker_network' playbooks/deploy_docker.yml` returns `1` (pre_task untouched from Plan 02)
    - `grep -c '- minio' playbooks/deploy_docker.yml` returns at least `1` (tag entry present)
    - Non-ASCII gate: `grep -rPnc '[^\x00-\x7F]' playbooks/deploy_docker.yml` returns `0`
    - INSPQ gate: `grep -iE 'inspq|qc\.ca|montreal|québec|francais|french|vault_inspq' playbooks/deploy_docker.yml` returns 0 matches
    - YAML parses: `python3 -c "import yaml; yaml.safe_load(open('playbooks/deploy_docker.yml'))"` exits 0
    - Ansible syntax check passes: `ansible-playbook --syntax-check -i inventory/example-homelab playbooks/deploy_docker.yml` exits 0
    - **If Docker is available on the executor:** the end-to-end validation sequence in the action's step 2 completes successfully (first run brings up container + creates all 5 buckets + reports healthy; second run reports `changed=0`). If Docker is not available, the SUMMARY must explicitly document the human-run sequence with copy-pasteable commands so the operator can run it.
  </acceptance_criteria>
  <done>The `minio` role is wired into `playbooks/deploy_docker.yml` with tag `[minio]`. Syntax check passes against the example inventory. The pre_task creating the `telemetron` network from Plan 02 is untouched. End-to-end validation either succeeded (Docker available) or is documented in the SUMMARY for human run (Docker unavailable).</done>
</task>

</tasks>

<verification>
After all three tasks complete, run the full Phase 1 acceptance gate:

```bash
# 1. Role files all exist with expected structure
test -f roles/minio/defaults/main.yml
test -f roles/minio/meta/main.yml
test -f roles/minio/handlers/main.yml
test -f roles/minio/tasks/main.yml
test -f roles/minio/tasks/bootstrap.yml
test -f roles/minio/templates/minio.env.j2
test -f roles/minio/README.md

# 2. Image-pin gate (OPS-01): no :latest in roles/minio/
grep -rcE ':latest' roles/minio/   # = 0

# 3. Grep gate (OPS-05 / Pitfall 9): no INSPQ leftovers
grep -riE 'inspq|qc\.ca|montreal|québec|francais|french|/srv/nfs/inspq|vault_inspq' roles/minio/   # = 0 matches

# 4. Non-ASCII gate (OPS-05): no non-ASCII characters
grep -rPn '[^\x00-\x7F]' roles/minio/   # = 0 matches

# 5. Idempotency gate (OPS-04 / Pitfall 8): no state: restarted anywhere in the role
grep -rciE 'state: restarted' roles/minio/   # = 0

# 6. Single-handler check (W6 fix)
grep -cE '^- name:' roles/minio/handlers/main.yml   # = 1

# 7. D-10a amendment applied (B3 fix): bootstrap uses docker_container_info on HEALTHCHECK status
grep -c 'community.docker.docker_container_info' roles/minio/tasks/bootstrap.yml   # = 1
grep -c 'State.Health.Status' roles/minio/tasks/bootstrap.yml                     # >= 1

# 8. JSON verify (W8 fix)
grep -c 'mc ls --json' roles/minio/tasks/bootstrap.yml   # = 1

# 9. Idempotency on bootstrap (W7 fix) — changed_when: false on the mc tasks
grep -c 'changed_when: false' roles/minio/tasks/bootstrap.yml   # >= 3

# 10. Vault-discipline gate (OPS-02): every vault_ ref in the role exists in vault.yml.example
for v in $(grep -rhoE 'vault_[a-z_]+' roles/minio/ | sort -u); do
  grep -q "$v" inventory/example-homelab/group_vars/all/vault.yml.example \
    || echo "MISSING: $v not declared in vault.yml.example"
done
# Expects no MISSING output.

# 11. Both image pins present
grep -c 'RELEASE.2025-04-22T22-12-26Z' roles/minio/defaults/main.yml   # = 1
grep -c 'RELEASE.2025-04-22T16-23-26Z' roles/minio/defaults/main.yml   # = 1

# 12. Bootstrap is the final task in main.yml
tail -10 roles/minio/tasks/main.yml | grep -c 'include_tasks: bootstrap.yml'   # = 1

# 13. Playbook syntax check
ansible-playbook --syntax-check -i inventory/example-homelab playbooks/deploy_docker.yml

# 14. (If Docker available) end-to-end test:
#     ansible-playbook -i inventory/example-homelab playbooks/deploy_docker.yml --tags minio --ask-vault-pass
#     docker inspect minio --format '{{.State.Health.Status}}'   # = healthy
#     docker run --rm --network telemetron \
#       -e MC_HOST_local="http://$USER:$PWD@minio:9000" \
#       minio/mc:RELEASE.2025-04-22T16-23-26Z mc ls --json local/ | jq -r '.key' | sort   # lists 5 buckets
#     # Re-run: PLAY RECAP must show changed=0
```
</verification>

<success_criteria>
- `roles/minio/` is complete: `defaults/main.yml` (both image pins, all tunables referencing inventory shared vars), `meta/main.yml` (galaxy info + community.docker dep), `handlers/main.yml` (single handler — `docker restart` via ansible.builtin.command, no state: restarted, no no-op sentinel per W6), `tasks/main.yml` (render env, ensure volume, pull image, run server with healthcheck + restart + no host publish + correct command line, include bootstrap as final task), `tasks/bootstrap.yml` (pre-poll Docker HEALTHCHECK status via `community.docker.docker_container_info` per D-10a amendment, one-shot mc container creates 5 buckets with `changed_when: false` for idempotency per W7, verify via `mc ls --json` diff per W8, I12 inline comment documenting MC_HOST credential exposure model), `templates/minio.env.j2` (renders vault credentials + volumes + TZ), `README.md` (canonical per-role schema with MinIO archive warning AND the D-09 credential-exposure security note).
- `playbooks/deploy_docker.yml` wires the `minio` role with tag `[minio]`; pre_task for `telemetron` network from Plan 02 untouched.
- All five port-acceptance gates pass on `roles/minio/`: image-pin, vault-discipline, grep (INSPQ), non-ASCII, no-state-restarted (idempotency proxy).
- Ansible syntax check passes.
- If Docker available: end-to-end test succeeds (container runs, 5 buckets created, second run is changed=0). If not: SUMMARY documents the human-run validation sequence.
- The canonical patterns (defaults layout, env file template, named volume, healthcheck + restart, single-handler restart per W6, bootstrap-as-final-task via Docker HEALTHCHECK poll per D-10a, bootstrap idempotency via changed_when:false per W7, JSON-based verification per W8, README schema) are established for Phase 2-6 roles to mirror.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-storage/01-foundation-storage-03-SUMMARY.md` documenting:
- Files created: 7 in `roles/minio/` (defaults, meta, handlers, tasks/main.yml + tasks/bootstrap.yml, templates/minio.env.j2, README.md)
- Files modified: 1 (`playbooks/deploy_docker.yml` — roles list filled in)
- Image pins confirmed: `minio/minio:RELEASE.2025-04-22T22-12-26Z` (server) + `minio/mc:RELEASE.2025-04-22T16-23-26Z` (bootstrap)
- The 5 buckets bootstrapped: `loki-chunks`, `tempo-traces`, `mimir-blocks`, `mimir-ruler`, `mimir-alerts`
- The bucket-bootstrap pattern (post-revision):
  - Pre-poll via `community.docker.docker_container_info` on `State.Health.Status == 'healthy'` (D-10a amendment — replaces unreachable `:9000` uri poll)
  - One-shot mc container with `mc mb --ignore-existing` (D-07, sorted iteration per D-20, `changed_when: false` per W7)
  - Verify via `mc ls --json` parsed by sed and diffed against expected list (W8)
- Cross-cutting patterns established for Phase 2-6 to mirror: defaults file layout, env file template + bind mount, named volume covering full data root (D-17), `community.docker.docker_container` with `state: started + recreate: false`, **single handler-restart via `docker restart` per W6** (no two-handler sentinel pattern), per-role README schema (OPS-03), MC_HOST credential exposure documented in role README + bootstrap.yml inline comment (I12)
- All five port-acceptance gates passing on roles/minio/ (image-pin, vault, grep, non-ASCII, no-state-restarted)
- End-to-end test outcome: ran successfully OR documented for human run (specify which, with exact commands if the latter)
- Any deviations from the plan (with rationale)
</output>
