---
phase: 4
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - roles/alertmanager/defaults/main.yml
  - roles/alertmanager/tasks/main.yml
  - roles/alertmanager/tasks/verify.yml
  - roles/alertmanager/handlers/main.yml
  - roles/alertmanager/templates/alertmanager.yml.j2
  - roles/alertmanager/meta/main.yml
  - roles/alertmanager/README.md
  - roles/prometheus/templates/prometheus.yml.j2
  - roles/prometheus/defaults/main.yml
  - roles/prometheus/templates/rules-baseline.yml.j2
  - playbooks/deploy_docker.yml
  - inventory/example-homelab/group_vars/all/alertmanager.yml
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - CLAUDE.md
  - hooks/README.md
  - roles/README.md
  - .planning/research/SUMMARY.md
  - .planning/research/FEATURES.md
  - .planning/research/ARCHITECTURE.md
requirements: [ALERT-01, ALERT-02, ALERT-03, ALERT-04, ALERT-05, ALERT-06]
requirements_addressed: [ALERT-01, ALERT-02, ALERT-03, ALERT-04, ALERT-05, ALERT-06]
autonomous: true
must_haves:
  truths:
    - "Operator runs `ansible-playbook -i inventory/example-homelab playbooks/deploy_docker.yml --tags alertmanager` and `docker inspect telemetron-alertmanager --format '{{.State.Health.Status}}'` returns `healthy`."
    - "`curl -fsS http://alertmanager:9093/api/v2/receivers` returns `[{\"name\":\"null\"}]` (single null receiver default, D-60)."
    - "`curl -fsS http://alertmanager:9093/api/v2/status | jq -r '.config.original'` contains the substrings `group_by:` with `alertname/cluster/service`, `group_interval: 5m`, `repeat_interval: 4h`, `group_wait: 30s` (D-61)."
    - "`curl -fsS http://prometheus:9090/api/v1/alertmanagers | jq -e '.data.activeAlertmanagers[0].url == \"http://alertmanager:9093/api/v2/alerts\"'` exits 0 (Prom->AM wiring live, D-64)."
    - "`docker exec telemetron-alertmanager /bin/amtool --alertmanager.url=http://localhost:9093 alert add alertname=TestAlert severity=warning instance=verify-host` exits 0, and the subsequent `amtool alert query alertname=TestAlert` lists the alert in active state (D-69)."
    - "Persistent named volume `telemetron_alertmanager_data` is mounted at `/alertmanager` inside the container (`docker inspect telemetron-alertmanager --format '{{range .Mounts}}{{.Name}}:{{.Destination}} {{end}}'` includes `telemetron_alertmanager_data:/alertmanager`) (D-62)."
    - "Rendered `/opt/telemetron/alertmanager/alertmanager.yml` contains `inhibit_rules:` with `source_matchers:` and `target_matchers:` (NOT `source_match:`/`target_match:` -- corrected syntax per Research Q2) and `equal: [instance]` (D-63)."
    - "REQUIREMENTS.md `### Alert plane (ALERT)` section retains only ALERT-01 active; ALERT-02..06 are moved to `## v2 Requirements` as ALERT-V2-01..05 with the descriptions specified in D-58."
    - "PROJECT.md `### Active` no longer contains the line `Write the hook router Flask app source` as an in-scope item; hooks/README.md explicitly states deferral to ALERT-V2-01..05."
    - "`grep -riE 'inspq|qc\\.ca|montreal|québec|francais|french|/srv/nfs/inspq|vault_inspq|soir|nuit|mep_|noreply@inspq' roles/alertmanager/ inventory/example-homelab/group_vars/all/alertmanager.yml` returns 0 lines (Gate 1, D-67/D-68)."
    - "`grep -rPn '[^\\x00-\\x7F]' roles/alertmanager/ inventory/example-homelab/group_vars/all/alertmanager.yml` returns 0 lines (non-ASCII gate, Gate 1)."
    - "Second back-to-back `ansible-playbook --tags alertmanager` run reports `changed=0` (Gate 4 idempotency)."
    - "`community.docker.docker_container` task in `roles/alertmanager/tasks/main.yml` carries `labels: { org.telemetron.service: telemetron, org.telemetron.job: alertmanager }` (Gate 7)."
  artifacts:
    - path: "roles/alertmanager/defaults/main.yml"
      provides: "Role tunables -- image pin v0.32.1, container args, healthcheck timing, network/volume/config-path defaults"
      contains: "alertmanager_image_tag: \"v0.32.1\""
    - path: "roles/alertmanager/tasks/main.yml"
      provides: "Pull image -> ensure config dir -> render template -> ensure data volume -> run container with explicit HEALTHCHECK + Gate-7 labels -> include verify.yml"
      contains: "community.docker.docker_container"
    - path: "roles/alertmanager/tasks/verify.yml"
      provides: "D-69 in-network verify: D-10a HEALTHCHECK poll, /-/ready + /-/healthy curl probes, /api/v2/receivers null-receiver assertion, /api/v2/status YAML-grep assertions, Prometheus alertmanagers API assertion, amtool synthetic alert add/query + silence add/query"
      contains: "docker_container_exec"
    - path: "roles/alertmanager/templates/alertmanager.yml.j2"
      provides: "Rendered AM config: route with null receiver + D-61 intervals, inhibit_rules with NEW source_matchers syntax (D-63 corrected), receivers list with null + extras"
      contains: "source_matchers:"
    - path: "roles/alertmanager/handlers/main.yml"
      provides: "Single handler `Docker restart alertmanager` listening on `restart alertmanager` (D-19/W6)"
      contains: "docker restart"
    - path: "roles/alertmanager/meta/main.yml"
      provides: "Galaxy metadata mirroring node_exporter shape (author, license MIT, min_ansible_version, platforms, galaxy_tags)"
    - path: "roles/alertmanager/README.md"
      provides: "OPS-03 schema: Variables -> Modes -> Tags -> Volumes -> Healthcheck -> Security -> Deviations from upstream INSPQ; explicit `null`-receiver-is-deliberate paragraph"
      contains: "Default receiver is `null`"
    - path: "roles/prometheus/templates/prometheus.yml.j2"
      provides: "Adds `alerting: alertmanagers:` static_configs block between `rule_files:` and `remote_write:` (D-64)"
      contains: "alerting:"
    - path: "roles/prometheus/defaults/main.yml"
      provides: "New default `prometheus_alertmanager_target: alertmanager:9093` (D-64)"
      contains: "prometheus_alertmanager_target"
    - path: "playbooks/deploy_docker.yml"
      provides: "Appends `- role: alertmanager` with tag `alertmanager` after fluentbit"
      contains: "role: alertmanager"
    - path: "inventory/example-homelab/group_vars/all/alertmanager.yml"
      provides: "D-61 routing knobs + D-60 extension lists + D-30 publish flag -- operator-tunable surface"
      contains: "alertmanager_group_interval: 5m"
    - path: ".planning/REQUIREMENTS.md"
      provides: "ALERT-02..06 moved to v2 Requirements as ALERT-V2-01..05; traceability rows updated; Alert plane note added"
      contains: "ALERT-V2-01"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 4 goal rewritten to alertmanager-only; success criteria rewritten; Plans count finalized to 1"
      contains: "alertmanager"
    - path: "hooks/README.md"
      provides: "Rewritten to mark hook router deferred to ALERT-V2-01..05"
      contains: "deferred"
  key_links:
    - from: "playbooks/deploy_docker.yml"
      to: "roles/alertmanager/tasks/main.yml"
      via: "role include in roles: list (after fluentbit)"
      pattern: "role: alertmanager"
    - from: "roles/prometheus/templates/prometheus.yml.j2"
      to: "roles/alertmanager (alertmanager:9093)"
      via: "alerting.alertmanagers[0].static_configs[0].targets"
      pattern: "alerting:\\s*\\n\\s*alertmanagers:"
    - from: "roles/alertmanager/templates/alertmanager.yml.j2"
      to: "the rendered /opt/telemetron/alertmanager/alertmanager.yml"
      via: "ansible.builtin.template task in tasks/main.yml"
      pattern: "src: alertmanager.yml.j2"
    - from: "roles/alertmanager/tasks/main.yml (docker_container)"
      to: "telemetron_alertmanager_data:/alertmanager"
      via: "named-volume mount + --storage.path=/alertmanager CLI flag"
      pattern: "telemetron_alertmanager_data"
    - from: "roles/alertmanager/tasks/verify.yml"
      to: "telemetron-alertmanager via docker_container_exec for amtool + curlimages/curl one-shots on telemetron network"
      via: "Final task in role; reads /api/v2/receivers, /api/v2/status, runs amtool alert/silence"
      pattern: "amtool"
---

<objective>
Deliver Phase 4 -- Alert Plane -- as a single plan delivering ONE new role
(`roles/alertmanager/`) + a cross-role Prometheus wiring extension + a
nine-file doc-rework cascade that records the hook-router deferral. The
phase's only code-complete requirement is ALERT-01 (Alertmanager v0.32.1
on :9093, group_by [alertname, cluster, service], group_interval 5m,
repeat_interval 4h, null receiver, one inhibit rule, persistent volume).
ALERT-02..06 are addressed by relocating them to `## v2 Requirements`
in `.planning/REQUIREMENTS.md` as ALERT-V2-01..05 and rewriting every
upstream reference (PROJECT.md, ROADMAP.md, CLAUDE.md, hooks/README.md,
.planning/research/*) per D-58.

Purpose: Phase 4 in CONTEXT.md was reshaped from 3-plans-with-hook-router
to 1-plan-alertmanager-only (D-56/D-57). The hook router (Flask app +
role + sample bundles + outbound auth) is deferred to a future
milestone. M1 ships "single-host LGTM observability plane with alerts
visible in Karma" (Phase 5); the "turnkey runbook automation via Jenkins
buildWithParameters" pitch returns in a later milestone.

Output:
- `roles/alertmanager/` (7 files) -- single-instance monolithic AM,
  pinned `quay.io/prometheus/alertmanager:v0.32.1`, explicit HEALTHCHECK
  (image has none, Research Q1), `--cluster.listen-address=""` to
  disable gossip, persistent `telemetron_alertmanager_data` volume on
  `/alertmanager`, route with single `null` receiver + D-61 intervals,
  one D-63 inhibit rule using corrected `source_matchers:` syntax
  (Research Q2 -- `source_match:` is deprecated).
- `roles/prometheus/` updates -- `prometheus.yml.j2` gains `alerting:
  alertmanagers:` block; `defaults/main.yml` gains
  `prometheus_alertmanager_target: alertmanager:9093` (D-64).
- `playbooks/deploy_docker.yml` -- `alertmanager` role appended after
  `fluentbit`.
- `inventory/example-homelab/group_vars/all/alertmanager.yml` -- full
  operator-tunable surface (D-61 + D-60 + D-30 knobs).
- Doc-cascade across 9 files moving ALERT-02..06 disposition (deferred
  to v2) and rewriting the Phase-4 goal/scope language.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/04-alert-plane/04-CONTEXT.md
@.planning/phases/04-alert-plane/04-RESEARCH.md
@.planning/phases/01-foundation-storage/01-CONTEXT.md
@.planning/phases/02-telemetry-backends/02-CONTEXT.md
@.planning/phases/03-ingest-plane/03-CONTEXT.md
@roles/README.md
@roles/node_exporter/defaults/main.yml
@roles/node_exporter/tasks/main.yml
@roles/node_exporter/tasks/verify.yml
@roles/node_exporter/handlers/main.yml
@roles/node_exporter/meta/main.yml
@roles/minio/README.md
@roles/minio/tasks/bootstrap.yml
@roles/prometheus/templates/prometheus.yml.j2
@roles/prometheus/templates/rules-baseline.yml.j2
@roles/prometheus/defaults/main.yml
@roles/prometheus/handlers/main.yml
@playbooks/deploy_docker.yml
@inventory/example-homelab/group_vars/all/network.yml
@inventory/example-homelab/group_vars/all/storage.yml
@hooks/README.md

<interfaces>
<!-- Extracted from codebase. Executor uses these contracts directly -- no exploration needed. -->

From inventory/example-homelab/group_vars/all/network.yml:
```yaml
telemetron_network: telemetron
telemetron_publish_default: false
telemetron_tz: Etc/UTC
```

From inventory/example-homelab/group_vars/all/storage.yml:
```yaml
telemetron_volume_prefix: telemetron
telemetron_config_root: /opt/telemetron
```

From roles/prometheus/defaults/main.yml (existing knobs; this plan ADDS prometheus_alertmanager_target):
```yaml
prometheus_container_name: telemetron-prometheus
prometheus_http_port: 9090
prometheus_remote_write_url: "http://mimir:9009/api/v1/push"
# After plan: prometheus_alertmanager_target: "alertmanager:9093"
```

From roles/prometheus/handlers/main.yml -- the handler name to notify on prometheus.yml.j2 changes:
```yaml
- name: Docker restart prometheus
  listen: restart prometheus
```
(Existing template task in roles/prometheus/tasks/main.yml that renders prometheus.yml.j2 already has `notify: restart prometheus` -- adding the alerting block triggers an automatic restart on plan execution.)

From roles/prometheus/templates/rules-baseline.yml.j2 -- severity labels ALREADY PRESENT on all 4 rules (Research Q11 confirmed):
- HostDown: severity: critical
- FilesystemAlmostFull: severity: warning
- ContainerRestartLoop: severity: warning
- OTelCollectorDroppingSignals: severity: warning
(D-65 per RESEARCH correction is VERIFY-ONLY, not an edit.)

From roles/node_exporter/handlers/main.yml -- handler shape template (mirror this for alertmanager):
```yaml
- name: Docker restart node_exporter
  ansible.builtin.command:
    cmd: "docker restart {{ node_exporter_container_name }}"
  changed_when: true
  listen: restart node_exporter
```

Alertmanager image facts (Research Q1/Q4/Q6/Q8 -- all live-verified):
- HEALTHCHECK: none in image (Q1 -- explicit required)
- VOLUME: /alertmanager (Q6)
- Binary paths: /bin/alertmanager, /bin/amtool (Q3)
- HTTP API: /-/ready, /-/healthy, /api/v2/status, /api/v2/receivers, /api/v2/alerts, /api/v2/silences (Q7/Q8)
- /api/v2/status returns YAML-as-string in .config.original (Q8) -- grep instead of jq for route fields
- --cluster.listen-address="" disables gossip cluster (Q4)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Doc-rework cascade (D-58) -- defer hook router to v2</name>
  <files>
    .planning/PROJECT.md,
    .planning/REQUIREMENTS.md,
    .planning/ROADMAP.md,
    CLAUDE.md,
    hooks/README.md,
    roles/README.md,
    .planning/research/SUMMARY.md,
    .planning/research/FEATURES.md,
    .planning/research/ARCHITECTURE.md
  </files>
  <read_first>
    .planning/PROJECT.md (current "Active" section + "Key Decisions" table; the "Hook router security model" row needs status change),
    .planning/REQUIREMENTS.md (current `### Alert plane (ALERT)` block lines 47-53 covering ALERT-01..06; current `## v2 Requirements` block starting at line 93),
    .planning/ROADMAP.md (Phase 4 section lines 74-84 -- goal paragraph + success criteria + "Plans: TBD"),
    CLAUDE.md (Tech-stack recommendation sheet hook_router row + "Custom / Glue" Hook Router entry + Port-allocation table Hook Router 5000 line + Dependency-Graph hook-router mentions),
    hooks/README.md (current state -- describes planned router/ + jobs/ layout, mentions Jenkins buildWithParameters),
    roles/README.md (rows for alertmanager and hook_router in the "Planned roles" table; "Added vs. upstream" paragraph; Gate 7 paragraph at the end),
    .planning/research/SUMMARY.md (lines 14, 60, 104, 154-162 mentioning hook router as "strongest differentiator"),
    .planning/research/FEATURES.md (lines 50, 53, 99-104, 143, 162 mentioning hook router),
    .planning/research/ARCHITECTURE.md (lines 53-61 ASCII diagram with hook_router Flask :5001),
    .planning/phases/04-alert-plane/04-CONTEXT.md (D-56, D-57, D-58 source of truth for what to change),
    .planning/phases/04-alert-plane/04-RESEARCH.md (Q12, Q13, Q14, Q15 -- current file state verifications)
  </read_first>
  <action>
Touch nine files in this order (single atomic task -- no in-flight inconsistency between "spec says hook router" and "code skipped it"):

**(1) `hooks/README.md`** -- REPLACE entire file with the following content:
```markdown
# Hooks

The hook router was originally planned as part of Telemetron M1
(`ALERT-02..06`) -- a Flask service bridging Alertmanager webhooks to
Jenkins `buildWithParameters` calls, with a per-rule allowlist,
per-`(alertname, job)` rate limit, and a vault-supplied outbound token.

**Status: deferred to a future milestone.**

Mid-M1 discussion concluded that Jenkins-as-target was no longer the
right pitch for 2026 and that re-framing the router as
backend-agnostic (generic webhook -> CI / automation) is a larger
reshape than M1 should absorb. The deferred design -- including the
two-tier `hook_router_backends` / `hook_router_rules` schema, the
shared-secret inbound auth header, the in-memory rate limit, and the
observability surface -- is captured in
`.planning/phases/04-alert-plane/04-DISCUSSION-LOG.md` for the
milestone that picks it up.

See `.planning/REQUIREMENTS.md` `## v2 Requirements` -- `ALERT-V2-01`
through `ALERT-V2-05` -- for the planned shape. The `hooks/` directory
survives as institutional memory; this README is its only content
until v2.

In M1, Alertmanager (`roles/alertmanager/` -- Phase 4) ships with a
single default `null` receiver. Alerts are visible in the Alertmanager
UI and (via Phase 5) in Karma but are not dispatched to anything
automatically. Operators wire their own receivers via
`alertmanager_extra_receivers` in inventory.

M1 does NOT build this image.
```

**(2) `roles/README.md`** -- three edits:
- In the "Planned roles (port status)" table:
  - Change row `| \`alertmanager\` | alert routing | -checked- |` to mark ported (replace empty checkbox with the checked one used elsewhere in the table -- mirror the style used for `loki` / `mimir` / `minio` rows that are already ticked).
  - Change row `| \`hook_router\` | Alertmanager -> CI bridge (new -- was inline upstream) | -unchecked- |` to: description becomes `Alertmanager -> generic CI/automation webhook bridge (deferred to a future milestone -- see REQUIREMENTS.md ALERT-V2-01..05)`; status cell remains unchecked or uses an em-dash to indicate deferral.
- In the "Added vs. upstream" paragraph: change the `hook_router (Alertmanager -> CI bridge; was inline upstream)` clause to `hook_router (Alertmanager -> generic CI/automation webhook bridge; was inline upstream; planned for a future milestone, see REQUIREMENTS.md ALERT-V2-01..05)`.
- In the Gate 7 paragraph (last paragraph of file): change `Phase 4 (alertmanager, hook_router) and Phase 5 (grafana, karma, promlens) role ports MUST stamp these labels.` to `Phase 4 (alertmanager) and Phase 5 (grafana, karma, promlens) role ports MUST stamp these labels. (The hook_router role is deferred to a future milestone -- see REQUIREMENTS.md ALERT-V2-01..05.)`

**(3) `.planning/REQUIREMENTS.md`** -- five edits:
- In `### Alert plane (ALERT)` section, KEEP `- [ ] **ALERT-01**: ...` exactly as-is.
- REMOVE the 5 bullets `- [ ] **ALERT-02**: ...` through `- [ ] **ALERT-06**: ...` (lines 49-53 of current file).
- IMMEDIATELY AFTER the ALERT-01 line in `### Alert plane (ALERT)`, INSERT the note:
  ```
  > **ALERT-02..06 moved to v2 Requirements as ALERT-V2-01..05** during the Phase 4 scope reshape (CONTEXT.md D-56/D-57/D-58). The hook router (Flask app + `roles/hook_router/` + sample bundles + outbound auth) is deferred to a future milestone. M1 ships Alertmanager with a `null` default receiver; alerts are visible in Karma (Phase 5) but not dispatched automatically.
  ```
- In `## v2 Requirements (deferred -- not in M1)` section, INSERT five new bullets directly after the existing `TEMPO-V2-01` line (preserve alphabetical-ish grouping):
  ```
  - **ALERT-V2-01**: Hook router Flask app under `hooks/router/` -- backend-agnostic webhook bridge (was M1 ALERT-02).
  - **ALERT-V2-02**: Hook router enforces an explicit per-rule allowlist; unrecognized alerts return 4xx and are logged (was M1 ALERT-03).
  - **ALERT-V2-03**: Per-`(alertname, backend)` rate limit, default 6 per hour, configurable; overflow returns 429 + counter (was M1 ALERT-04; "job" renamed to "backend" to match the backend-agnostic v2 design).
  - **ALERT-V2-04**: `hooks/jobs/` sample bundles -- GitHub Actions repository_dispatch, Slack incoming webhook, generic curl, optionally Jenkinsfile for historical context (was M1 ALERT-05).
  - **ALERT-V2-05**: `roles/hook_router/` Ansible role that builds the Flask image locally + wires Alertmanager `webhook_configs` receiver + injects shared-secret + outbound token from vault (was M1 ALERT-06).
  ```
- In the `## Traceability` table at the bottom, REMOVE the five rows `| ALERT-02 | Phase 4 -- Alert Plane |` through `| ALERT-06 | Phase 4 -- Alert Plane |`. KEEP `| ALERT-01 | Phase 4 -- Alert Plane |`.

**(4) `.planning/ROADMAP.md`** -- two edits:
- In the "Phases" list around line 18, REPLACE the Phase 4 one-line summary with: `Phase 4: Alert Plane - Alertmanager (\`quay.io/prometheus/alertmanager:v0.32.1\`) on :9093 with \`group_by: [alertname, cluster, service]\`, \`group_interval: 5m\`, \`repeat_interval: 4h\`, a single \`null\` default receiver (Karma in Phase 5 is the operator UX), one default inhibit rule (\`severity=critical -> severity=warning, equal: [instance]\`), persistent \`telemetron_alertmanager_data\` volume; Prometheus extended with an \`alerting: alertmanagers:\` block targeting it. Hook router work is deferred to a future milestone -- see REQUIREMENTS.md ALERT-V2-01..05.`
- In `### Phase 4: Alert Plane` section (around line 74), REPLACE the entire **Goal** paragraph + **Success Criteria** bullets + **Plans** line with:
  ```
  **Goal**: Operator can run the playbook and have Alertmanager running on `:9093` (`quay.io/prometheus/alertmanager:v0.32.1`) configured with `group_by: [alertname, cluster, service]`, `group_interval: 5m`, `repeat_interval: 4h`, `group_wait: 30s`, a single default `null` receiver, and one default inhibit rule (`source severity=critical -> target severity=warning, equal: [instance]`). Persistent state survives container restart via the `telemetron_alertmanager_data` named volume mounted at `/alertmanager`. Prometheus' `prometheus.yml` is extended with an `alerting: alertmanagers:` block pointing at `alertmanager:9093` so the four baseline rules from Phase 3 (`HostDown`, `FilesystemAlmostFull`, `ContainerRestartLoop`, `OTelCollectorDroppingSignals`) actually reach Alertmanager when they fire. The hook router (Flask app + role + sample bundles) is deferred to a future milestone -- see REQUIREMENTS.md ALERT-V2-01..05.
  **Depends on**: Phase 3
  **Requirements**: ALERT-01 (ALERT-02..06 deferred to v2 as ALERT-V2-01..05)
  **Success Criteria** (what must be TRUE):
    1. Operator runs `ansible-playbook -i inventory/example-homelab playbooks/deploy_docker.yml --tags alertmanager` and `curl -fsS http://alertmanager:9093/-/ready` returns HTTP 200; `docker inspect telemetron-alertmanager --format '{{.State.Health.Status}}'` returns `healthy`.
    2. `curl -fsS http://alertmanager:9093/api/v2/receivers | jq -e '.[0].name == "null"'` exits 0 (single null receiver default).
    3. `curl -fsS http://alertmanager:9093/api/v2/status | jq -r '.config.original'` contains `group_by:` listing `alertname/cluster/service`, `group_interval: 5m`, `repeat_interval: 4h`, `group_wait: 30s` (D-61).
    4. `curl -fsS http://prometheus:9090/api/v1/alertmanagers | jq -e '.data.activeAlertmanagers[0].url == "http://alertmanager:9093/api/v2/alerts"'` exits 0 (Prom->AM wiring live).
    5. `docker exec telemetron-alertmanager /bin/amtool --alertmanager.url=http://localhost:9093 alert add alertname=TestAlert severity=warning instance=verify-host` exits 0; the subsequent `amtool alert query alertname=TestAlert` lists the alert as active; `amtool silence add` followed by `amtool silence query` confirms silence persistence.
  **Plans**: 1
  Plans:
  - [ ] 04-01-PLAN.md -- Alertmanager role port (ALERT-01) + Prometheus alerting wiring (D-64) + doc-rework cascade (D-58, defers ALERT-02..06 to v2 as ALERT-V2-01..05); Wave 1
  ```

**(5) `CLAUDE.md`** -- three edits to the Tech-stack content:
- In the TL;DR table row for `hook_router`, change the "Verdict for M1" cell from `Build in \`hooks/router/\`` to `Deferred to v2 milestone (see .planning/REQUIREMENTS.md ALERT-V2-01..05)`. Optionally also flip Confidence from `HIGH` to `--`.
- In `## Recommended Stack -- Detailed` section under `### Custom / Glue`, change the `Hook router` row Verdict text: REPLACE everything starting with `Source ships in \`hooks/router/\` per \`PROJECT.md\`...` through the end of the cell with: `**Deferred to a future milestone** -- see \`.planning/REQUIREMENTS.md\` v2 Requirements ALERT-V2-01..05. The original Flask + Gunicorn + per-rule-allowlist + per-tuple-rate-limit design is preserved in \`.planning/phases/04-alert-plane/04-DISCUSSION-LOG.md\` for the milestone that picks it up. M1 does NOT build this image.`
- In `### Port-allocation snapshot (homelab single-host)` table, change row `| Hook Router | 5000 | 5000 |` to `| Hook Router (v2 -- deferred) | 5000 | -- |`.

**(6) `.planning/PROJECT.md`** -- three edits:
- In `## Core Value` paragraph: change `Alertmanager + Karma + hook-router for alerts` to `Alertmanager + Karma for alerts` (drops the `+ hook-router` clause).
- In `### Active` section: REMOVE the bullet `- [ ] Write the hook router Flask app source under \`hooks/router/\` (was inline upstream) and ship sample Jenkinsfile runbooks under \`hooks/jobs/\``. Also update the bullet `- [ ] Roles in scope: \`alertmanager\`, \`fluentbit\`, \`grafana\`, \`hook_router\`, ...` to: `- [ ] Roles in scope: \`alertmanager\`, \`fluentbit\`, \`grafana\`, \`karma\`, \`loki\`, \`mimir\`, \`minio\`, \`nfsd\`, \`node_exporter\`, \`opentelemetry\`, \`prometheus\`, \`promlens\`, \`tempo\` (13 deployed + \`hook_router\` deferred to v2 -- see REQUIREMENTS.md ALERT-V2-01..05)`.
- In `## Key Decisions` table, change the Outcome column on the row whose Decision column begins with `Hook router security model` from `-- Pending (M1)` to `Deferred to v2 (mid-M1 reshape, 2026-05-18 -- see Phase 4 CONTEXT.md D-56)`. Preserve the Rationale column verbatim as institutional memory.

**(7) `.planning/research/SUMMARY.md`** -- In the Phase 4 subsection (and any line referencing the hook router as "single highest-leverage M1 differentiator" or "strongest competitive differentiator"), REWRITE to scope the phase as alertmanager-only: keep the alertmanager paragraph (groups/repeat intervals, null receiver, inhibit rule, persistent volume); replace any sentence whose subject is the hook router with `The hook router (Flask + allowlist + rate limit + vault-supplied token) is deferred to a future milestone -- see REQUIREMENTS.md ALERT-V2-01..05.`. Specifically rewrite the "strongest differentiator" sentence around line 14 to `M1's alert-plane scope is the Alertmanager port plus the Prometheus alerting wiring; the hook router pitch returns in a later milestone.`

**(8) `.planning/research/FEATURES.md`** -- On every line that frames the hook router as in-scope (lines 50, 53, 99-104, 143, 162), wrap or replace with deferral language: `The hook router is deferred to a future milestone (ALERT-V2-01..05). Rationale preserved here for that milestone's spec:` and keep the technical content beneath as institutional memory. Do NOT delete the technical detail -- re-tag it as v2 reference material.

**(9) `.planning/research/ARCHITECTURE.md`** -- In the ASCII signal-flow diagram (around lines 53-61), the `hook_router Flask :5001` box should be re-labelled as `[hook_router :5001 -- deferred to v2]`. Add a footnote line below the diagram: `Note: hook_router is deferred to a future milestone (REQUIREMENTS.md ALERT-V2-01..05). In M1 the dotted line from Alertmanager terminates at the null receiver and is surfaced in Karma (Phase 5).`

After all nine files are edited, run the grep gates listed in the acceptance criteria and confirm zero matches for hook-router-as-active.
  </action>
  <verify>
    <automated>
bash -c '
set -e
# REQUIREMENTS.md: ALERT-01 still active, ALERT-02..06 moved
grep -E "^- \[ \] \*\*ALERT-01\*\*" .planning/REQUIREMENTS.md
! grep -E "^- \[ \] \*\*ALERT-0[2-6]\*\*" .planning/REQUIREMENTS.md
grep -E "ALERT-V2-01" .planning/REQUIREMENTS.md
grep -E "ALERT-V2-05" .planning/REQUIREMENTS.md
# ROADMAP.md: Phase 4 plan count = 1
grep -E "04-01-PLAN.md" .planning/ROADMAP.md
# PROJECT.md: hook router no longer in Active
! grep -E "Write the hook router Flask app source under" .planning/PROJECT.md
# hooks/README.md: deferral statement present
grep -i "deferred" hooks/README.md
grep -E "ALERT-V2-0[1-5]" hooks/README.md
# roles/README.md: alertmanager marked ported (will be true after task 2 deploys); for this task only check hook_router row reworded
grep -E "hook_router.*deferred|deferred.*hook_router" roles/README.md
# CLAUDE.md: hook router deferred in tech stack
grep -i "deferred" CLAUDE.md | grep -i "hook"
# Research files mention deferral
grep -i "deferred" .planning/research/SUMMARY.md
grep -i "deferred" .planning/research/FEATURES.md
grep -i "deferred" .planning/research/ARCHITECTURE.md
echo "Task 1 doc-cascade verify: OK"
'
    </automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains `- [ ] **ALERT-01**:` exactly once in the `### Alert plane (ALERT)` section.
    - `.planning/REQUIREMENTS.md` does NOT contain any line matching `^- \[ \] \*\*ALERT-0[2-6]\*\*` (those are MOVED, not duplicated).
    - `.planning/REQUIREMENTS.md` contains the bullets `**ALERT-V2-01**`, `**ALERT-V2-02**`, `**ALERT-V2-03**`, `**ALERT-V2-04**`, `**ALERT-V2-05**` within the `## v2 Requirements (deferred -- not in M1)` section.
    - `.planning/REQUIREMENTS.md` Traceability table contains `| ALERT-01 | Phase 4 -- Alert Plane |` but does NOT contain rows for ALERT-02 through ALERT-06.
    - `.planning/ROADMAP.md` Phase 4 section contains `- [ ] 04-01-PLAN.md` and `**Plans**: 1` (or `**Plans**: 1 plan`).
    - `.planning/ROADMAP.md` Phase 4 Goal paragraph contains `null` (as a receiver reference) AND `alertmanager:v0.32.1` (or `:v0.32.1`) AND does NOT contain `hook router` (case-insensitive) in the in-scope portion.
    - `.planning/PROJECT.md` Active section does NOT contain `Write the hook router Flask app source`.
    - `.planning/PROJECT.md` Core Value paragraph does NOT contain `hook-router for alerts` (it should read `Alertmanager + Karma for alerts`).
    - `.planning/PROJECT.md` Key Decisions row for `Hook router security model` has Outcome column starting with `Deferred to v2`.
    - `CLAUDE.md` TL;DR row for hook_router has the word `Deferred` (case-insensitive) in the Verdict cell.
    - `CLAUDE.md` Port-allocation table row for Hook Router has the word `deferred` (case-insensitive) and host-port value `--` (em-dash equivalent).
    - `hooks/README.md` contains the strings `deferred`, `ALERT-V2-01`, `ALERT-V2-05`, and the explicit sentence `M1 does NOT build this image.`
    - `roles/README.md` table row for `hook_router` contains `deferred` (case-insensitive) and references `ALERT-V2`.
    - `roles/README.md` Gate 7 paragraph references `Phase 4 (alertmanager)` (without `hook_router` in the same parenthetical).
    - `.planning/research/SUMMARY.md` contains the word `deferred` near a hook_router reference (grep `-A2 -B2 "hook"` shows `deferred` within 2 lines).
    - `.planning/research/FEATURES.md` contains the word `deferred` near a hook_router reference.
    - `.planning/research/ARCHITECTURE.md` contains the word `deferred` and references `ALERT-V2-` in the diagram caption or footnote.
  </acceptance_criteria>
  <done>
    All nine doc files updated atomically. ALERT-02..06 disposition (deferred to v2 as ALERT-V2-01..05) is delivered. No upstream document still claims the hook router is in M1 scope. All grep assertions above return 0 exit codes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Scaffold roles/alertmanager/ (full role port -- defaults, tasks, verify, handler, template, meta, README)</name>
  <files>
    roles/alertmanager/defaults/main.yml,
    roles/alertmanager/tasks/main.yml,
    roles/alertmanager/tasks/verify.yml,
    roles/alertmanager/handlers/main.yml,
    roles/alertmanager/templates/alertmanager.yml.j2,
    roles/alertmanager/meta/main.yml,
    roles/alertmanager/README.md
  </files>
  <read_first>
    roles/node_exporter/defaults/main.yml (closest structural analog -- knob layout, image-pin/healthcheck/timezone/restart/curl-image sections),
    roles/node_exporter/tasks/main.yml (docker_image pull -> docker_container run -> include verify.yml shape; Gate 7 labels stanza; healthcheck conditional-omit pattern),
    roles/node_exporter/tasks/verify.yml (D-10a HEALTHCHECK poll Step 1a/1b + in-network curl one-shot Step 2 shape),
    roles/node_exporter/handlers/main.yml (single-handler `Docker restart <role>` shape -- listen pattern, changed_when: true),
    roles/node_exporter/meta/main.yml (galaxy_info shape -- author, license, min_ansible_version, platforms, galaxy_tags),
    roles/minio/README.md (OPS-03 canonical README schema -- variables table -> vault keys -> tags -> modes -> volumes -> healthcheck -> security -> idempotency -> port-acceptance gates -> deprecation notes),
    roles/minio/tasks/bootstrap.yml (D-10a docker_container_info poll until State.Health.Status == 'healthy' pattern; W7 changed_when:false on one-shot containers),
    roles/prometheus/handlers/main.yml (single `listen:` based handler invocation pattern),
    inventory/example-homelab/group_vars/all/network.yml (telemetron_network/telemetron_publish_default/telemetron_tz defaults),
    inventory/example-homelab/group_vars/all/storage.yml (telemetron_volume_prefix/telemetron_config_root defaults),
    .planning/phases/04-alert-plane/04-CONTEXT.md (D-59 to D-69 -- authoritative for all design choices),
    .planning/phases/04-alert-plane/04-RESEARCH.md (Q1=no HEALTHCHECK; Q2=source_matchers not source_match; Q3=amtool via docker_container_exec; Q4=--cluster.listen-address=""; Q6=/alertmanager volume; Q8=/api/v2/status YAML-in-string; CRITICAL CORRECTIONS to CONTEXT.md)
  </read_first>
  <action>
Create seven files. All paths absolute below from project root.

**(A) `roles/alertmanager/defaults/main.yml`** -- full content:
```yaml
# (YAML doc start marker)
# roles/alertmanager/defaults/main.yml
# Alertmanager v0.32.1 role tunables. Override per-environment in
# inventory/<env>/group_vars/all/alertmanager.yml.
#
# Single-instance monolithic Alertmanager on :9093. Single default
# 'null' receiver (D-60). One inhibit rule using NEW source_matchers
# syntax (D-63 + Research Q2 -- source_match is deprecated in v0.32.1).
# Persistent telemetron_alertmanager_data volume mounted at /alertmanager
# (D-62; preserves silences + nflog across restarts).

# --- Image pin (OPS-01; ALERT-01; D-59) ---
# See: https://github.com/prometheus/alertmanager/releases/tag/v0.32.1
# Quay (not Docker Hub) -- co-published with the Prometheus family;
# matches Phase-3 node_exporter registry choice.
alertmanager_image: quay.io/prometheus/alertmanager
alertmanager_image_tag: "v0.32.1"

# --- Container identity ---
alertmanager_container_name: telemetron-alertmanager

# --- Host port publishing (D-30) ---
# Default false. Operator UI access via SSH local-forward:
#   ssh -L 9093:localhost:9093 <host>
alertmanager_publish_host: "{{ alertmanager_publish_host | default(false) }}"

# --- Port matrix ---
alertmanager_http_port: 9093

# --- Storage layout (D-16, D-17, D-18) ---
alertmanager_data_volume: "{{ telemetron_volume_prefix | default('telemetron') }}_alertmanager_data"
alertmanager_data_path: /alertmanager           # image VOLUME (Research Q6)
alertmanager_config_dir: "{{ telemetron_config_root | default('/opt/telemetron') }}/alertmanager"
alertmanager_config_container_path: /etc/alertmanager/alertmanager.yml

# --- Routing intervals (ALERT-01; D-61) ---
# Default values are the ALERT-01-mandated values. Operator override
# per environment via group_vars/all/alertmanager.yml.
alertmanager_group_by: [alertname, cluster, service]
alertmanager_group_wait: 30s
alertmanager_group_interval: 5m
alertmanager_repeat_interval: 4h

# --- Logging ---
alertmanager_log_level: info

# --- Operator extension knobs (D-60; sorted-keys per D-20) ---
alertmanager_extra_receivers: []
alertmanager_extra_routes: []
alertmanager_extra_inhibit_rules: []
alertmanager_extra_time_intervals: []
alertmanager_extra_mute_time_intervals: []

# --- Healthcheck (Research Q1: image has NO built-in HEALTHCHECK; explicit required) ---
# busybox base means wget is available (no distroless concern).
alertmanager_healthcheck_enabled: true
alertmanager_healthcheck_test: ["CMD-SHELL", "wget --spider -q http://localhost:9093/-/healthy || exit 1"]
alertmanager_healthcheck_interval: 15s
alertmanager_healthcheck_timeout: 5s
alertmanager_healthcheck_retries: 5
alertmanager_healthcheck_start_period: 30s

# --- Verify pre-poll (D-10a) ---
# 60s total budget = 30 retries x 2s. Alertmanager cold-start is fast
# (config parse + start HTTP server).
alertmanager_health_retries: 30
alertmanager_health_delay: 2

# --- Restart policy (OPS-06) ---
alertmanager_restart_policy: unless-stopped

# --- Resource limits ---
alertmanager_memory_limit: 256m

# --- Network ---
alertmanager_network: "{{ telemetron_network | default('telemetron') }}"

# --- Timezone (OPS-06; Pitfall 6) ---
alertmanager_tz: "{{ telemetron_tz | default('Etc/UTC') }}"

# --- Curl image pin for verify (OPS-01) ---
alertmanager_curl_image: curlimages/curl
alertmanager_curl_image_tag: "8.10.1"
```

**(B) `roles/alertmanager/tasks/main.yml`** -- full content:
```yaml
# (YAML doc start marker)
# roles/alertmanager/tasks/main.yml
# Alertmanager v0.32.1 deploy + in-network verify.
# Mirrors the canonical roles/node_exporter/tasks/main.yml shape modulo
# alertmanager specifics: rendered config bind-mount, persistent named
# data volume on /alertmanager, --cluster.listen-address="" disables
# gossip (Research Q4; D-59 single-instance), explicit HEALTHCHECK
# (Research Q1: image has none).

- name: Ensure alertmanager config directory exists (D-18 flat layout)
  ansible.builtin.file:
    path: "{{ alertmanager_config_dir }}"
    state: directory
    mode: "0755"
  tags:
    - alertmanager
    - alertmanager-config

- name: Render alertmanager.yml from template (D-60/D-61/D-63)
  ansible.builtin.template:
    src: alertmanager.yml.j2
    dest: "{{ alertmanager_config_dir }}/alertmanager.yml"
    mode: "0644"
  notify: restart alertmanager
  tags:
    - alertmanager
    - alertmanager-config

- name: Ensure telemetron_alertmanager_data named volume exists (D-16/D-62)
  community.docker.docker_volume:
    name: "{{ alertmanager_data_volume }}"
    state: present
  tags:
    - alertmanager

- name: Pull alertmanager image
  community.docker.docker_image:
    name: "{{ alertmanager_image }}:{{ alertmanager_image_tag }}"
    source: pull
    force_source: false
  tags:
    - alertmanager

- name: Run alertmanager container (single-instance monolithic, explicit HEALTHCHECK)
  community.docker.docker_container:
    name: "{{ alertmanager_container_name }}"
    image: "{{ alertmanager_image }}:{{ alertmanager_image_tag }}"
    state: started
    recreate: false
    restart_policy: "{{ alertmanager_restart_policy }}"
    memory: "{{ alertmanager_memory_limit }}"
    networks:
      - name: "{{ alertmanager_network }}"
        aliases:
          - "{{ alertmanager_container_name }}"
          - alertmanager
    # Gate 7: telemetron label stamp (INGEST-07 / Plan 03-05).
    labels:
      org.telemetron.service: telemetron
      org.telemetron.job: alertmanager
    # D-30 / D-12: no host publish by default. Operators reach the AM UI
    # from the workstation via SSH local-forward
    # (ssh -L 9093:localhost:9093 <host>); Prometheus reaches AM over
    # the telemetron Docker bridge at alertmanager:9093.
    published_ports: >-
      {{
        (
          [ alertmanager_http_port | string + ':' + alertmanager_http_port | string ]
          if alertmanager_publish_host is sameas true else
          [ '127.0.0.1:' + alertmanager_http_port | string + ':' + alertmanager_http_port | string ]
          if alertmanager_publish_host == '127.0.0.1' else
          []
        )
      }}
    command:
      - "--config.file={{ alertmanager_config_container_path }}"
      - "--storage.path={{ alertmanager_data_path }}"
      - "--web.listen-address=:{{ alertmanager_http_port }}"
      # Research Q4 -- empty string disables gossip cluster. Without
      # this AM tries to bind :9094 and emits noisy peer-connect errors
      # even in single-instance mode.
      - "--cluster.listen-address="
      - "--log.level={{ alertmanager_log_level }}"
    mounts:
      - source: "{{ alertmanager_data_volume }}"
        target: "{{ alertmanager_data_path }}"
        type: volume
      - source: "{{ alertmanager_config_dir }}/alertmanager.yml"
        target: "{{ alertmanager_config_container_path }}"
        type: bind
        read_only: true
    # Research Q1 -- image ships no HEALTHCHECK. Explicit probe via
    # wget (busybox base, wget available -- no distroless concern).
    healthcheck: "{{ alertmanager_container_healthcheck if (alertmanager_healthcheck_enabled | bool) else omit }}"
    env:
      TZ: "{{ alertmanager_tz }}"
  vars:
    alertmanager_container_healthcheck:
      test: "{{ alertmanager_healthcheck_test }}"
      interval: "{{ alertmanager_healthcheck_interval }}"
      timeout: "{{ alertmanager_healthcheck_timeout }}"
      retries: "{{ alertmanager_healthcheck_retries }}"
      start_period: "{{ alertmanager_healthcheck_start_period }}"
  tags:
    - alertmanager
    - alertmanager-container

# Verify MUST be the final task (D-32). Mirrors roles/{node_exporter,prometheus,fluentbit}/tasks/main.yml pattern.
- name: Verify alertmanager end-to-end (readiness + Prom->AM round-trip + amtool synthetic alert + silence)
  ansible.builtin.include_tasks: verify.yml
  tags:
    - alertmanager
    - alertmanager-verify
```

**(C) `roles/alertmanager/tasks/verify.yml`** -- full content (note: uses `docker_container_exec` for amtool per Research Q3 + uses `/api/v2/receivers` for null-receiver check and YAML-grep on `/api/v2/status` config.original for route knobs, per Research Q8 + Risk 1):
```yaml
# (YAML doc start marker)
# roles/alertmanager/tasks/verify.yml
# Pre-poll alertmanager readiness, then exercise the public API surface
# in-network: /-/ready, /-/healthy, /api/v2/receivers (null receiver
# structural check), /api/v2/status (YAML config grep for route knobs --
# Research Q8: config.original is YAML-as-string, NOT nested JSON), the
# Prometheus alertmanagers API (round-trip), and amtool synthetic alert
# add/query + silence add/query (Research Q3: amtool is bundled at
# /bin/amtool in the AM image -- use docker_container_exec, not a
# separate one-shot image).
#
# Mirrors roles/node_exporter/tasks/verify.yml conditional readiness
# shape (D-10a + W7 + D-32 + D-54). Authoritative readiness gate is the
# in-network curl probe (Step 2) plus the Prom->AM and amtool steps.

# Step 1: poll HEALTHCHECK status (D-10a; per W7 changed_when:false).
- name: Wait for alertmanager container HEALTHCHECK to report healthy
  community.docker.docker_container_info:
    name: "{{ alertmanager_container_name }}"
  register: am_health_check
  until: >-
    am_health_check.container is defined
    and am_health_check.container.State is defined
    and am_health_check.container.State.Health is defined
    and am_health_check.container.State.Health.Status == 'healthy'
  retries: "{{ alertmanager_health_retries }}"
  delay: "{{ alertmanager_health_delay }}"
  changed_when: false
  when: alertmanager_healthcheck_enabled | bool
  tags:
    - alertmanager
    - alertmanager-verify

# Step 2: /-/ready + /-/healthy probes (W7 auto_remove + changed_when:false).
- name: Curl-probe /-/ready and /-/healthy in-network
  community.docker.docker_container:
    name: "{{ alertmanager_container_name }}-verify-ready"
    image: "{{ alertmanager_curl_image }}:{{ alertmanager_curl_image_tag }}"
    state: started
    detach: false
    auto_remove: true
    recreate: true
    cleanup: true
    networks:
      - name: "{{ alertmanager_network }}"
    entrypoint: ["/bin/sh", "-c"]
    command:
      - >-
        curl -fsS http://{{ alertmanager_container_name }}:{{ alertmanager_http_port }}/-/ready
        && curl -fsS http://{{ alertmanager_container_name }}:{{ alertmanager_http_port }}/-/healthy
        && echo "ready_and_healthy_ok"
  changed_when: false
  tags:
    - alertmanager
    - alertmanager-verify

# Step 3: /api/v2/receivers assertion (proper JSON shape; Research Q8).
# Asserts a receiver named 'null' exists (D-60 default).
- name: Curl-probe /api/v2/receivers and assert null receiver exists
  community.docker.docker_container:
    name: "{{ alertmanager_container_name }}-verify-receivers"
    image: "{{ alertmanager_curl_image }}:{{ alertmanager_curl_image_tag }}"
    state: started
    detach: false
    auto_remove: true
    recreate: true
    cleanup: true
    networks:
      - name: "{{ alertmanager_network }}"
    entrypoint: ["/bin/sh", "-c"]
    command:
      - >-
        BODY=$(curl -fsS http://{{ alertmanager_container_name }}:{{ alertmanager_http_port }}/api/v2/receivers) ;
        echo "$BODY" | grep -q '"name":"null"' || { echo "null receiver not found in $BODY" ; exit 1 ; } ;
        echo "receivers_null_ok"
  changed_when: false
  tags:
    - alertmanager
    - alertmanager-verify

# Step 4: /api/v2/status YAML-grep assertions (Research Q8; Risk 1).
# config.original is a YAML-formatted string -- grep it for the D-61
# route knobs rather than jq-ing nested fields that do not exist.
- name: Curl-probe /api/v2/status and grep route knobs in config.original
  community.docker.docker_container:
    name: "{{ alertmanager_container_name }}-verify-status"
    image: "{{ alertmanager_curl_image }}:{{ alertmanager_curl_image_tag }}"
    state: started
    detach: false
    auto_remove: true
    recreate: true
    cleanup: true
    networks:
      - name: "{{ alertmanager_network }}"
    entrypoint: ["/bin/sh", "-c"]
    command:
      - >-
        BODY=$(curl -fsS http://{{ alertmanager_container_name }}:{{ alertmanager_http_port }}/api/v2/status) ;
        echo "$BODY" | grep -q '"status":"disabled"' || { echo "cluster status not disabled" ; exit 1 ; } ;
        echo "$BODY" | grep -q 'group_interval: 5m' || { echo "group_interval 5m not found" ; exit 1 ; } ;
        echo "$BODY" | grep -q 'repeat_interval: 4h' || { echo "repeat_interval 4h not found" ; exit 1 ; } ;
        echo "$BODY" | grep -q 'group_wait: 30s' || { echo "group_wait 30s not found" ; exit 1 ; } ;
        echo "$BODY" | grep -q 'alertname' && echo "$BODY" | grep -q 'cluster' && echo "$BODY" | grep -q 'service' || { echo "group_by labels missing" ; exit 1 ; } ;
        echo "status_route_knobs_ok"
  changed_when: false
  tags:
    - alertmanager
    - alertmanager-verify

# Step 5: Prometheus -> Alertmanager round-trip (D-64 wiring live).
# Asserts prometheus' activeAlertmanagers list includes alertmanager:9093.
- name: Curl-probe Prometheus /api/v1/alertmanagers and assert AM target present
  community.docker.docker_container:
    name: "{{ alertmanager_container_name }}-verify-promam"
    image: "{{ alertmanager_curl_image }}:{{ alertmanager_curl_image_tag }}"
    state: started
    detach: false
    auto_remove: true
    recreate: true
    cleanup: true
    networks:
      - name: "{{ alertmanager_network }}"
    entrypoint: ["/bin/sh", "-c"]
    command:
      - >-
        i=0 ;
        while [ $i -lt 30 ] ; do
          BODY=$(curl -fsS http://prometheus:9090/api/v1/alertmanagers 2>/dev/null || true) ;
          if echo "$BODY" | grep -q 'http://alertmanager:9093/api/v2/alerts' ; then echo "prom_am_wiring_ok" ; exit 0 ; fi ;
          i=$((i+1)) ; sleep 2 ;
        done ;
        echo "Prometheus did not register alertmanager:9093 within 60s. Last response: $BODY" ;
        exit 1
  changed_when: false
  tags:
    - alertmanager
    - alertmanager-verify

# Step 6: amtool synthetic alert add (Research Q3 -- docker_container_exec
# on the running AM container; amtool is at /bin/amtool inside the image).
- name: amtool synthetic alert add (D-69)
  community.docker.docker_container_exec:
    container: "{{ alertmanager_container_name }}"
    command: >-
      /bin/amtool --alertmanager.url=http://localhost:9093
      alert add alertname=TestAlert severity=warning instance=verify-host
  changed_when: false
  tags:
    - alertmanager
    - alertmanager-verify

# Step 7: amtool alert query -- assert TestAlert listed.
- name: amtool alert query and assert TestAlert active
  community.docker.docker_container_exec:
    container: "{{ alertmanager_container_name }}"
    command: >-
      /bin/amtool --alertmanager.url=http://localhost:9093 alert query alertname=TestAlert
  register: am_alert_query
  failed_when: "'TestAlert' not in am_alert_query.stdout"
  changed_when: false
  tags:
    - alertmanager
    - alertmanager-verify

# Step 8: amtool silence add + query -- assert silence persistence.
- name: amtool silence add for TestAlert
  community.docker.docker_container_exec:
    container: "{{ alertmanager_container_name }}"
    command: >-
      /bin/amtool --alertmanager.url=http://localhost:9093
      silence add --comment="telemetron verify silence" alertname=TestAlert
  register: am_silence_add
  failed_when: am_silence_add.rc is defined and am_silence_add.rc != 0
  changed_when: false
  tags:
    - alertmanager
    - alertmanager-verify

- name: amtool silence query and assert one active silence on TestAlert
  community.docker.docker_container_exec:
    container: "{{ alertmanager_container_name }}"
    command: >-
      /bin/amtool --alertmanager.url=http://localhost:9093 silence query alertname=TestAlert
  register: am_silence_query
  failed_when: "'TestAlert' not in am_silence_query.stdout"
  changed_when: false
  tags:
    - alertmanager
    - alertmanager-verify
```

**(D) `roles/alertmanager/handlers/main.yml`** -- full content:
```yaml
# (YAML doc start marker)
# roles/alertmanager/handlers/main.yml
# Per D-19 / Pitfall 8: container restarts on config change use
# `docker restart <name>`. The module-level state parameter is never
# used for restarts -- force-recreate is non-idempotent.
#
# tasks/main.yml renders templates/alertmanager.yml.j2 with
# `notify: restart alertmanager`. Re-rendering on knob change triggers
# this handler which docker-restarts the container so the new config
# is loaded. Alertmanager picks up the new file on restart.

- name: Docker restart alertmanager
  ansible.builtin.command:
    cmd: "docker restart {{ alertmanager_container_name }}"
  changed_when: true
  listen: restart alertmanager
```

**(E) `roles/alertmanager/templates/alertmanager.yml.j2`** -- full content (uses NEW `source_matchers:` syntax per Research Q2; CONTEXT.md D-63 showed deprecated `source_match:`):
```yaml
# {{ '{{' }} ansible_managed {{ '}}' }}
# Alertmanager v0.32.1 -- Telemetron M1 default config.
# D-60: null receiver default -- alerts visible in UI and Karma (Phase 5)
#       but dispatched to nothing automatically. Add a real receiver before
#       relying on this stack to wake you up.
# D-61: ALERT-01 routing intervals explicit (not defaulted).
# D-63: One inhibit rule bounds critical->warning replay flood.
#       NEW source_matchers / target_matchers syntax (PromQL-style matcher
#       list) -- v0.32.1 accepts both old `source_match` and new
#       `source_matchers` forms, but `source_match` is DEPRECATED and emits
#       a warning. We use the current syntax.

route:
  receiver: 'null'
  group_by:
{% for label in alertmanager_group_by %}
    - {{ label }}
{% endfor %}
  group_wait: {{ alertmanager_group_wait }}
  group_interval: {{ alertmanager_group_interval }}
  repeat_interval: {{ alertmanager_repeat_interval }}
  routes:
{% for r in alertmanager_extra_routes | default([]) | sort(attribute='receiver') %}
    - receiver: {{ r.receiver }}
{% if r.matchers is defined %}
      matchers:
{% for m in r.matchers %}
        - '{{ m }}'
{% endfor %}
{% endif %}
{% if r.continue is defined %}
      continue: {{ r.continue | to_json }}
{% endif %}
{% endfor %}

inhibit_rules:
  - source_matchers:
      - severity = "critical"
    target_matchers:
      - severity = "warning"
    equal: [instance]
{% for rule in alertmanager_extra_inhibit_rules | default([]) | sort(attribute='name') %}
  - source_matchers: {{ rule.source_matchers | to_json }}
    target_matchers: {{ rule.target_matchers | to_json }}
{% if rule.equal is defined %}
    equal: {{ rule.equal | to_json }}
{% endif %}
{% endfor %}

receivers:
  - name: 'null'
{% for r in alertmanager_extra_receivers | default([]) | sort(attribute='name') %}
  - name: {{ r.name }}
{% if r.webhook_configs is defined %}
    webhook_configs:
{% for w in r.webhook_configs %}
      - url: {{ w.url }}
{% if w.send_resolved is defined %}
        send_resolved: {{ w.send_resolved | to_json }}
{% endif %}
{% endfor %}
{% endif %}
{% endfor %}

{% if alertmanager_extra_time_intervals | default([]) | length > 0 %}
time_intervals:
{% for ti in alertmanager_extra_time_intervals | sort(attribute='name') %}
  - name: {{ ti.name }}
    time_intervals: {{ ti.time_intervals | to_json }}
{% endfor %}
{% endif %}

{% if alertmanager_extra_mute_time_intervals | default([]) | length > 0 %}
mute_time_intervals:
{% for mti in alertmanager_extra_mute_time_intervals | sort(attribute='name') %}
  - name: {{ mti.name }}
    time_intervals: {{ mti.time_intervals | to_json }}
{% endfor %}
{% endif %}
```

**(F) `roles/alertmanager/meta/main.yml`** -- full content:
```yaml
# (YAML doc start marker)
galaxy_info:
  role_name: alertmanager
  author: Rock Martel-Langlois
  description: >
    Deploys Prometheus Alertmanager v0.32.1 on the telemetron Docker bridge
    network in single-instance monolithic mode. Ships ALERT-01 routing
    intervals (group_by [alertname, cluster, service], group_interval 5m,
    repeat_interval 4h), a single default null receiver (Karma in Phase 5
    is the operator UX), one default inhibit rule (severity=critical ->
    severity=warning, equal [instance]), and a persistent
    telemetron_alertmanager_data named volume preserving silences and
    nflog across container restarts.
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Ubuntu
      versions: [jammy, noble]
    - name: Debian
      versions: [bookworm]
  galaxy_tags:
    - alerting
    - alertmanager
    - observability
    - telemetron

dependencies: []

collections:
  - community.docker
  - ansible.builtin
```

**(G) `roles/alertmanager/README.md`** -- full content following OPS-03 schema (mirror roles/minio/README.md shape):
```markdown
# roles/alertmanager

Deploys [Prometheus Alertmanager](https://github.com/prometheus/alertmanager) v0.32.1 -- single-instance monolithic mode -- on the `telemetron` Docker bridge network. Receives evaluated alerts from Prometheus (`alerting:` block wired by `roles/prometheus/` in this phase) and dispatches them according to its `route` / `receivers` / `inhibit_rules` config.

This role ships ALERT-01 from `.planning/REQUIREMENTS.md`. ALERT-02..06 (the hook router Flask app + sample bundles + outbound auth) were deferred to a future milestone during the Phase 4 scope reshape; see `.planning/REQUIREMENTS.md` `## v2 Requirements` `ALERT-V2-01..05` for the planned shape.

## What this role does

1. Creates `/opt/telemetron/alertmanager/` on the host (D-18 flat config layout).
2. Renders `/opt/telemetron/alertmanager/alertmanager.yml` from `templates/alertmanager.yml.j2`. The default config has:
   - A single `route` with `receiver: 'null'`, `group_by: [alertname, cluster, service]`, `group_wait: 30s`, `group_interval: 5m`, `repeat_interval: 4h` (ALERT-01 + D-61).
   - One `inhibit_rules` entry: `source_matchers: [severity = "critical"]` -> `target_matchers: [severity = "warning"]`, `equal: [instance]` (D-63; new `source_matchers` syntax -- the old `source_match` is deprecated in v0.32.1).
   - A single `receivers` entry named `null` (the deliberate D-60 default -- see Security model below).
3. Ensures the `telemetron_alertmanager_data` named Docker volume exists (D-16).
4. Starts the `telemetron-alertmanager` container with:
   - Image pin `quay.io/prometheus/alertmanager:v0.32.1` (OPS-01 / D-59).
   - Explicit `HEALTHCHECK` (`wget --spider -q http://localhost:9093/-/healthy`) -- the image has no built-in healthcheck (confirmed via `docker inspect`).
   - `restart_policy: unless-stopped` (OPS-06).
   - `--cluster.listen-address=""` to disable gossip cluster (single-instance M1; HA mode is a future milestone).
   - `org.telemetron.service=telemetron` / `org.telemetron.job=alertmanager` labels (Gate 7).
   - Persistent named volume `telemetron_alertmanager_data` mounted at `/alertmanager` for silences + nflog.
5. Runs the verify suite (`tasks/verify.yml`) -- D-10a HEALTHCHECK poll, `/-/ready` + `/-/healthy` curl probes, `/api/v2/receivers` null-receiver assertion, `/api/v2/status` config-grep for route knobs, Prometheus alertmanagers API round-trip, and `amtool` synthetic alert add / query + silence add / query.

## Default receiver is `null` -- read this

Default receiver is `null`. Alerts are visible in the Alertmanager UI (port 9093) and in Karma (Phase 5) but are **not dispatched to anything automatically**. Add a real receiver before you rely on this stack to wake you up.

The reason this exists: M1 ships the observability plane plus alerting visibility, but the original M1 plan for routing alerts to Jenkins via a Flask hook router was deferred to a future milestone. Operators wire their own receivers via `alertmanager_extra_receivers` (and corresponding routes via `alertmanager_extra_routes`) in their inventory -- Slack, PagerDuty, email, or a custom webhook target.

## Variables

See `defaults/main.yml` for the full list. Most operators only touch the routing intervals (if their alert volume is different) and `alertmanager_extra_receivers` to wire a real notification target.

| Variable | Default | Purpose |
|----------|---------|---------|
| `alertmanager_image` | `quay.io/prometheus/alertmanager` | Image (do not change) |
| `alertmanager_image_tag` | `v0.32.1` | Pinned tag (OPS-01) |
| `alertmanager_container_name` | `telemetron-alertmanager` | DNS name on the `telemetron` network |
| `alertmanager_publish_host` | `false` | Publish :9093 to the host. `false`/`true`/`127.0.0.1` |
| `alertmanager_http_port` | `9093` | HTTP API + UI port |
| `alertmanager_data_volume` | `telemetron_alertmanager_data` | Named Docker volume for silences + nflog |
| `alertmanager_data_path` | `/alertmanager` | In-container data path (matches image VOLUME) |
| `alertmanager_config_dir` | `/opt/telemetron/alertmanager` | Host config bind-mount source |
| `alertmanager_group_by` | `[alertname, cluster, service]` | ALERT-01 / D-61 |
| `alertmanager_group_wait` | `30s` | AM default; explicit for documentation discipline |
| `alertmanager_group_interval` | `5m` | ALERT-01 / D-61 |
| `alertmanager_repeat_interval` | `4h` | ALERT-01 / D-61 |
| `alertmanager_log_level` | `info` | `--log.level` (Claude's discretion default) |
| `alertmanager_extra_receivers` | `[]` | Operator-defined receivers (sorted-keys per D-20) |
| `alertmanager_extra_routes` | `[]` | Operator-defined sub-routes |
| `alertmanager_extra_inhibit_rules` | `[]` | Operator-defined inhibit rules |
| `alertmanager_extra_time_intervals` | `[]` | Operator-defined time-of-day windows |
| `alertmanager_extra_mute_time_intervals` | `[]` | Operator-defined mute windows |
| `alertmanager_healthcheck_*` | (see defaults) | Explicit healthcheck timing (image ships no default) |
| `alertmanager_restart_policy` | `unless-stopped` | Container restart policy |
| `alertmanager_memory_limit` | `256m` | Container memory limit |
| `alertmanager_network` | `telemetron` | Docker network |
| `alertmanager_tz` | `Etc/UTC` | Container timezone (Pitfall 6) |

## Vault keys

None added in Phase 4 (D-66). Alertmanager has no outbound auth in M1 (null receiver = no destination = no credentials) and no inbound auth either (single-host bridge network -- D-26 / D-55 / D-66 trust boundary).

## Tags

- `alertmanager` -- runs the whole role
- `alertmanager-config` -- render config + restart on change
- `alertmanager-container` -- container lifecycle
- `alertmanager-verify` -- verify suite only

## Modes

Single mode: single-instance monolithic (`--cluster.listen-address=""`). HA cluster mode is deferred to a future milestone alongside HAProxy and distributed-mode backends.

## Volumes

| Volume | Mount | Purpose |
|--------|-------|---------|
| `telemetron_alertmanager_data` (named) | `/alertmanager` | Silences + notification log (nflog) + active-alert state. Persistent -- losing this volume loses silences and dedup memory across restart (Pitfall 7 replay-storm risk). |
| `/opt/telemetron/alertmanager/alertmanager.yml` (bind, ro) | `/etc/alertmanager/alertmanager.yml` | Rendered config |

## Healthcheck

The `quay.io/prometheus/alertmanager:v0.32.1` image ships **no default HEALTHCHECK** (confirmed via `docker inspect`). This role provides an explicit one:

```yaml
healthcheck:
  test: ["CMD-SHELL", "wget --spider -q http://localhost:9093/-/healthy || exit 1"]
  interval: 15s
  timeout: 5s
  retries: 5
  start_period: 30s
```

`busybox` base means `wget` is available (no distroless concern, unlike Tempo / Mimir / OTel). Verify with:

```bash
docker inspect telemetron-alertmanager --format '{{.State.Health.Status}}'   # expects: healthy
```

## Security model

- **Default: no host port publish** (`alertmanager_publish_host: false`). Operator UI access via `ssh -L 9093:localhost:9093 <host>`. Prometheus reaches AM at `http://alertmanager:9093` over the bridge.
- **No outbound credentials in M1.** The null receiver has no destination. Operators who add a real receiver via `alertmanager_extra_receivers` are responsible for vault-storing any outbound tokens.
- **No inbound auth.** Single-host trust boundary (D-26 / D-55 / D-66). When operators wire a real notification destination, inbound auth from upstream callers (e.g. a webhook receiver behind it) is the operator's responsibility.

## Deviations from upstream INSPQ

The upstream `~/git/inspq/ansible/alert_manager/` role embeds INSPQ's on-call topology: time-window vocabulary in French (evening / night / weekday / weekend / release-window), multi-team routing, SMTP receivers, the local timezone, kubernetes/Helm task paths, and an `alert_manager_*` var prefix. This role's port (D-68 audit):

- **Removed:** all time-window vars, all team-routing vars, all SMTP knobs (`alert_manager_smtp_*`, `alert_manager_email_config_send_resolve`), all k8s/Helm/OpenShift task paths, `alert_manager_kubernetes_*`, `alert_manager_helm_*`, `alert_manager_namespace`, `alert_manager_storage_class`, `alert_manager_persistence_enabled`, `alert_manager_pod_security_context`, `alert_manager_openshift_*`, `alert_manager_include_alertnames`, `alert_manager_external_url` (templated FQDN form), `alert_manager_state`, `alert_manager_container_recreate`, and the misspelled `alert_amanger_weekend_jours`.
- **Translated:** SMTP from-address removed; timezone defaults to `Etc/UTC` (consumes `telemetron_tz` from `group_vars/all/network.yml`); inline comments stripped.
- **Renamed:** `alert_manager_*` prefix -> `alertmanager_*` (locked naming normalization `ba836d2`).
- **Re-defaulted:** `alert_manager_docker_image_version: latest` -> `v0.32.1` (D-59 / OPS-01); `alert_manager_docker_restart_policy: always` -> `unless-stopped` (OPS-06).
- **Re-shaped:** `alert_manager_route: {}` + `alert_manager_receivers: []` -> explicit D-60/D-61/D-63 structure with operator extension lists.
- **Added:** `--cluster.listen-address=""` (single-instance gossip-disable, Research Q4); explicit HEALTHCHECK via wget (image has none, Research Q1); Gate 7 telemetron labels; `org.telemetron.*` label stamping for Fluent Bit Lua-enrichment (INGEST-07).

## Idempotency

Per OPS-04: running the playbook twice in a row against an unchanged inventory reports `changed=0` in the PLAY RECAP. Verify with:

```bash
ansible-playbook -i inventory/example-homelab playbooks/deploy_docker.yml --tags alertmanager
# ...first run: changed=N
ansible-playbook -i inventory/example-homelab playbooks/deploy_docker.yml --tags alertmanager
# ...second run: changed=0
```

Config-change restarts run via the `restart alertmanager` handler (`docker restart {{ alertmanager_container_name }}`) -- never `state: restarted`.

## Port-acceptance gates

This role passes all seven gates documented in `roles/README.md`:

- **Gate 1 (grep gates):** `grep -riE 'inspq|qc.ca|montreal|quebec|francais|french|/srv/nfs/inspq|vault_inspq|soir|nuit|mep_|noreply@inspq' roles/alertmanager/` returns 0 matches. `grep -rPn '[^\x00-\x7F]' roles/alertmanager/` returns 0 matches (non-ASCII).
- **Gate 2 (image pin):** `quay.io/prometheus/alertmanager:v0.32.1`; no `:latest` anywhere.
- **Gate 3 (vault discipline):** zero `{{ vault_* }}` references (D-66).
- **Gate 4 (idempotency):** see above.
- **Gate 5 (healthcheck + restart policy):** explicit HEALTHCHECK + `unless-stopped`.
- **Gate 6 (README schema):** this file.
- **Gate 7 (telemetron label stamp):** `org.telemetron.service: telemetron` + `org.telemetron.job: alertmanager`.

## Deprecation notes

None. Alertmanager v0.32.1 is current stable as of 2026-04-29.

## Bring your own receivers

The default `null` receiver is intentional -- M1 ships alerting visibility, not auto-dispatch. To wire a real notification target (Slack, PagerDuty, email, custom webhook), set `alertmanager_extra_receivers` and `alertmanager_extra_routes` in `inventory/<env>/group_vars/all/alertmanager.yml`:

```yaml
alertmanager_extra_receivers:
  - name: slack-ops
    webhook_configs:
      - url: "{{ vault_slack_webhook_url }}"
        send_resolved: true

alertmanager_extra_routes:
  - receiver: slack-ops
    matchers:
      - 'severity="critical"'
    continue: false
```

(Operators who add a vault-supplied URL must also extend `inventory/example-homelab/group_vars/all/vault.yml.example` with the placeholder.)
```
  </action>
  <verify>
    <automated>
bash -c '
set -e
# All seven files exist
test -f roles/alertmanager/defaults/main.yml
test -f roles/alertmanager/tasks/main.yml
test -f roles/alertmanager/tasks/verify.yml
test -f roles/alertmanager/handlers/main.yml
test -f roles/alertmanager/templates/alertmanager.yml.j2
test -f roles/alertmanager/meta/main.yml
test -f roles/alertmanager/README.md
# Image pin correct
grep -E "alertmanager_image_tag: \"v0.32.1\"" roles/alertmanager/defaults/main.yml
grep -E "quay.io/prometheus/alertmanager" roles/alertmanager/defaults/main.yml
# Routing knobs match ALERT-01 / D-61
grep -E "alertmanager_group_interval: 5m" roles/alertmanager/defaults/main.yml
grep -E "alertmanager_repeat_interval: 4h" roles/alertmanager/defaults/main.yml
grep -E "alertmanager_group_wait: 30s" roles/alertmanager/defaults/main.yml
grep -E "alertname, cluster, service" roles/alertmanager/defaults/main.yml
# Healthcheck shipped explicitly (Research Q1)
grep -E "wget --spider -q http://localhost:9093/-/healthy" roles/alertmanager/defaults/main.yml
grep -E "healthcheck:" roles/alertmanager/tasks/main.yml
# --cluster.listen-address="" present (Research Q4)
grep -E "cluster.listen-address=" roles/alertmanager/tasks/main.yml
# Gate 7 labels
grep -E "org.telemetron.service: telemetron" roles/alertmanager/tasks/main.yml
grep -E "org.telemetron.job: alertmanager" roles/alertmanager/tasks/main.yml
# Persistent volume
grep -E "telemetron_alertmanager_data" roles/alertmanager/defaults/main.yml
grep -E "telemetron_alertmanager_data" roles/alertmanager/tasks/main.yml
# Template uses CORRECTED source_matchers, NOT deprecated source_match
grep -E "source_matchers:" roles/alertmanager/templates/alertmanager.yml.j2
! grep -E "^  source_match:" roles/alertmanager/templates/alertmanager.yml.j2
grep -E "target_matchers:" roles/alertmanager/templates/alertmanager.yml.j2
# Null receiver
grep -E "name: .null." roles/alertmanager/templates/alertmanager.yml.j2
# Verify amtool via docker_container_exec (Research Q3)
grep -E "community.docker.docker_container_exec" roles/alertmanager/tasks/verify.yml
grep -E "/bin/amtool" roles/alertmanager/tasks/verify.yml
grep -E "/api/v2/receivers" roles/alertmanager/tasks/verify.yml
grep -E "/api/v1/alertmanagers" roles/alertmanager/tasks/verify.yml
# Handler shape
grep -E "listen: restart alertmanager" roles/alertmanager/handlers/main.yml
grep -E "docker restart" roles/alertmanager/handlers/main.yml
# Gate 1 grep gates
test "$(grep -rE '\''inspq|qc\\.ca|montreal|quebec|francais|french|/srv/nfs/inspq|vault_inspq|soir|nuit|mep_|noreply@inspq'\'' roles/alertmanager/ | wc -l)" = "0"
test "$(grep -rPn '\''[^\\x00-\\x7F]'\'' roles/alertmanager/ | wc -l)" = "0"
# Gate 2 image pin
! grep -rE "image:.*:latest" roles/alertmanager/
# README has OPS-03 sections
grep -E "^## Variables" roles/alertmanager/README.md
grep -E "^## Modes" roles/alertmanager/README.md
grep -E "^## Tags" roles/alertmanager/README.md
grep -E "^## Volumes" roles/alertmanager/README.md
grep -E "^## Healthcheck" roles/alertmanager/README.md
grep -E "^## Deviations from upstream INSPQ" roles/alertmanager/README.md
grep -E "Default receiver is .null." roles/alertmanager/README.md
echo "Task 2 role-scaffold verify: OK"
'
    </automated>
  </verify>
  <acceptance_criteria>
    - `roles/alertmanager/defaults/main.yml` exists and contains the EXACT strings `alertmanager_image_tag: "v0.32.1"`, `quay.io/prometheus/alertmanager`, `alertmanager_group_interval: 5m`, `alertmanager_repeat_interval: 4h`, `alertmanager_group_wait: 30s`, `alertname, cluster, service`, `telemetron_alertmanager_data`, `wget --spider -q http://localhost:9093/-/healthy`.
    - `roles/alertmanager/tasks/main.yml` contains the EXACT strings `community.docker.docker_container`, `org.telemetron.service: telemetron`, `org.telemetron.job: alertmanager`, `"--cluster.listen-address="` (or `cluster.listen-address=` followed by no value), `healthcheck:`, `notify: restart alertmanager` (in the template task), `community.docker.docker_volume`, `community.docker.docker_image`, `include_tasks: verify.yml`.
    - `roles/alertmanager/tasks/verify.yml` contains the EXACT strings `community.docker.docker_container_exec`, `/bin/amtool`, `alertname=TestAlert`, `silence add`, `silence query`, `/api/v2/receivers`, `/api/v2/status`, `/api/v1/alertmanagers`, `http://alertmanager:9093/api/v2/alerts` (as a literal in the grep assertion).
    - `roles/alertmanager/handlers/main.yml` contains `listen: restart alertmanager` AND `docker restart {{ alertmanager_container_name }}` AND `changed_when: true`.
    - `roles/alertmanager/templates/alertmanager.yml.j2` contains `source_matchers:` AND `target_matchers:` AND `equal: [instance]` AND does NOT contain the deprecated `source_match:` line at the top of an inhibit_rules entry (grep for `^  source_match:` returns 0).
    - `roles/alertmanager/templates/alertmanager.yml.j2` contains a receiver named `null` (i.e. `name: 'null'` literal) and a route directive `receiver: 'null'`.
    - `roles/alertmanager/meta/main.yml` contains `license: MIT`, `role_name: alertmanager`, and `min_ansible_version: "2.15"`.
    - `roles/alertmanager/README.md` contains OPS-03 sections (`## Variables`, `## Modes`, `## Tags`, `## Volumes`, `## Healthcheck`, `## Security model`, `## Deviations from upstream INSPQ`, `## Idempotency`, `## Port-acceptance gates`, `## Deprecation notes`), the explicit `null`-receiver-is-deliberate paragraph (string `Default receiver is .null.` matches), and the v0.32.1 image tag.
    - Gate 1 -- `grep -riE 'inspq|qc.ca|montreal|quebec|francais|french|/srv/nfs/inspq|vault_inspq|soir|nuit|mep_|noreply@inspq' roles/alertmanager/` returns 0 lines.
    - Gate 1 -- `grep -rPn '[^\x00-\x7F]' roles/alertmanager/` returns 0 lines (non-ASCII).
    - Gate 2 -- `grep -rE 'image:.*:latest' roles/alertmanager/` returns 0 lines (no floating tags).
  </acceptance_criteria>
  <done>
    The roles/alertmanager/ directory is a complete, lint-clean port of the upstream INSPQ alert_manager role, mirroring the Phase-1..3 canonical role shape. The seven files compose a deployable role that brings up Alertmanager v0.32.1 with ALERT-01 routing knobs, single null receiver, one D-63 inhibit rule (corrected source_matchers syntax), persistent /alertmanager volume, Gate-7 labels, and a verify suite using docker_container_exec for amtool.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend roles/prometheus/ -- alerting block + alertmanager target default (D-64) + verify D-65 severity labels already present</name>
  <files>
    roles/prometheus/templates/prometheus.yml.j2,
    roles/prometheus/defaults/main.yml,
    roles/prometheus/templates/rules-baseline.yml.j2
  </files>
  <read_first>
    roles/prometheus/templates/prometheus.yml.j2 (current state -- confirm `rule_files:` block at lines 16-18 followed directly by `remote_write:` block at line 20; clean insertion point per Research Q10),
    roles/prometheus/defaults/main.yml (existing knob layout; add `prometheus_alertmanager_target` after `prometheus_remote_write_url` section, before scrape-target defaults),
    roles/prometheus/templates/rules-baseline.yml.j2 (Research Q11 confirms all four rules already carry `severity:` labels: HostDown=critical, FilesystemAlmostFull=warning, ContainerRestartLoop=warning, OTelCollectorDroppingSignals=warning -- D-65 task is VERIFY-ONLY per orchestrator instructions),
    roles/prometheus/handlers/main.yml (existing handler `restart prometheus` -- adding to prometheus.yml.j2 triggers it automatically via the existing template task's notify directive),
    .planning/phases/04-alert-plane/04-CONTEXT.md (D-64 + D-65 design),
    .planning/phases/04-alert-plane/04-RESEARCH.md (Q5 confirms promtool check config OK; Q10 confirms insertion point; Q11 confirms D-65 is no-op)
  </read_first>
  <action>
Three edits across three files.

**(A) Edit `roles/prometheus/templates/prometheus.yml.j2`** -- INSERT the `alerting:` block between the existing `rule_files:` block (lines 16-18) and the `remote_write:` block (lines 20-28). The insertion goes immediately after line 18 (the last `rule_files:` entry) and before line 20 (`remote_write:`). The inserted block:

```yaml

alerting:
  # D-64: Phase-4 wiring -- Prometheus dispatches evaluated alerts to
  # Alertmanager on the telemetron Docker bridge. Default target
  # `alertmanager:9093` resolves via Docker DNS to the
  # telemetron-alertmanager container deployed by roles/alertmanager/.
  # Override per environment via prometheus_alertmanager_target in
  # inventory/<env>/group_vars/all/prometheus.yml.
  alertmanagers:
    - static_configs:
        - targets: ['{{ prometheus_alertmanager_target }}']
```

Final file shape: `global:` (lines 9-15) -> `rule_files:` (lines 16-18) -> **`alerting:` (new block, ~12 lines)** -> `remote_write:` (now starting around line 32) -> `scrape_configs:` (unchanged).

**(B) Edit `roles/prometheus/defaults/main.yml`** -- INSERT the new default variable. Add the following block IMMEDIATELY AFTER the existing `# --- remote_write target (INGEST-02; Phase-2 D-26 no X-Scope-OrgID) ---` section (which ends with `prometheus_remote_write_max_shards: 5` at line 45) and BEFORE the `# --- Default scrape targets (D-42 + INGEST-08) ---` section header (line 47):

```yaml

# --- Alertmanager target (D-64 -- Phase 4 wiring) ---
# DNS name on the telemetron Docker bridge. Resolves to the
# telemetron-alertmanager container (port 9093) deployed by Phase 4
# roles/alertmanager/. The static_configs target consumed by the
# `alerting:` block in templates/prometheus.yml.j2.
prometheus_alertmanager_target: "alertmanager:9093"
```

**(C) D-65 VERIFY-ONLY on `roles/prometheus/templates/rules-baseline.yml.j2`** -- Per Research Q11 the four baseline rules already carry the correct `severity:` labels. NO EDITS to the rule blocks. The acceptance check is a grep assertion confirming the labels are present (so if a future edit removes them this task catches the regression). Add an inline comment to the file at the TOP of the existing comment block (after line 9 `# any reference to a processor-side refused metric is wrong).` and before line 10 blank line) -- INSERT one new comment block:

```
# Phase 4 (D-65) verified: severity labels on all four rules are
# correctly present (HostDown=critical, FilesystemAlmostFull=warning,
# ContainerRestartLoop=warning, OTelCollectorDroppingSignals=warning).
# Required for roles/alertmanager/templates/alertmanager.yml.j2 D-63
# inhibit rule to match (source_matchers: severity = "critical").
```

This is a documentation-only change -- the four rule blocks themselves are unchanged.

Note on handlers: `roles/prometheus/tasks/main.yml` already wires the template task for `prometheus.yml.j2` with `notify: restart prometheus`, so on the next playbook run the file diff (added `alerting:` block) triggers a `docker restart telemetron-prometheus` automatically. No handler-wiring changes needed.
  </action>
  <verify>
    <automated>
bash -c '
set -e
# alerting block present in prometheus.yml.j2
grep -E "^alerting:" roles/prometheus/templates/prometheus.yml.j2
grep -E "^\s+alertmanagers:" roles/prometheus/templates/prometheus.yml.j2
grep -E "static_configs:" roles/prometheus/templates/prometheus.yml.j2 | head -1
grep -E "prometheus_alertmanager_target" roles/prometheus/templates/prometheus.yml.j2
# Insertion ordering: alerting must appear AFTER rule_files: and BEFORE remote_write:
awk "/^rule_files:/{rf=NR} /^alerting:/{al=NR} /^remote_write:/{rw=NR} END{ if(rf<al && al<rw){print \"order_ok\"} else {print \"order_bad rf=\" rf \" al=\" al \" rw=\" rw; exit 1} }" roles/prometheus/templates/prometheus.yml.j2
# defaults/main.yml has new knob
grep -E "^prometheus_alertmanager_target: \"alertmanager:9093\"" roles/prometheus/defaults/main.yml
# D-65 verify-only: severity labels still present on all 4 rules
grep -E "alert: HostDown" roles/prometheus/templates/rules-baseline.yml.j2
grep -A4 "alert: HostDown" roles/prometheus/templates/rules-baseline.yml.j2 | grep -E "severity: critical"
grep -A4 "alert: FilesystemAlmostFull" roles/prometheus/templates/rules-baseline.yml.j2 | grep -E "severity: warning"
grep -A4 "alert: ContainerRestartLoop" roles/prometheus/templates/rules-baseline.yml.j2 | grep -E "severity: warning"
grep -A4 "alert: OTelCollectorDroppingSignals" roles/prometheus/templates/rules-baseline.yml.j2 | grep -E "severity: warning"
# D-65 inline comment present
grep -E "Phase 4 \(D-65\) verified" roles/prometheus/templates/rules-baseline.yml.j2
# INSPQ grep gate still clean on prometheus role
test "$(grep -rE '\''inspq|qc\\.ca|montreal|quebec|francais|french|/srv/nfs/inspq|vault_inspq'\'' roles/prometheus/ | wc -l)" = "0"
test "$(grep -rPn '\''[^\\x00-\\x7F]'\'' roles/prometheus/ | wc -l)" = "0"
echo "Task 3 prometheus-extension verify: OK"
'
    </automated>
  </verify>
  <acceptance_criteria>
    - `roles/prometheus/templates/prometheus.yml.j2` contains a top-level `alerting:` block (line starts with `alerting:`).
    - The `alerting:` block contains the sub-keys `alertmanagers:`, `static_configs:`, and `targets:` referencing `{{ prometheus_alertmanager_target }}`.
    - Block ordering is correct: `rule_files:` line appears BEFORE `alerting:` line which appears BEFORE `remote_write:` line.
    - `roles/prometheus/defaults/main.yml` contains the exact line `prometheus_alertmanager_target: "alertmanager:9093"`.
    - `roles/prometheus/templates/rules-baseline.yml.j2` still contains `severity: critical` within 4 lines after `alert: HostDown`, and `severity: warning` within 4 lines after each of `alert: FilesystemAlmostFull`, `alert: ContainerRestartLoop`, `alert: OTelCollectorDroppingSignals`.
    - `roles/prometheus/templates/rules-baseline.yml.j2` contains the new inline comment line containing `Phase 4 (D-65) verified`.
    - `grep -rE 'inspq|qc.ca|montreal|quebec|francais|french|/srv/nfs/inspq|vault_inspq' roles/prometheus/` returns 0 lines (Gate 1 remains clean after edit).
    - `grep -rPn '[^\x00-\x7F]' roles/prometheus/` returns 0 lines (non-ASCII gate remains clean after edit).
  </acceptance_criteria>
  <done>
    `roles/prometheus/templates/prometheus.yml.j2` carries the D-64 `alerting:` block targeting `alertmanager:9093` by default; the existing prometheus restart handler will fire automatically on the next playbook run. The D-65 severity-label retrofit is a confirmed no-op per Research Q11; an inline comment documents the verification so future edits cannot silently regress.
  </done>
</task>

<task type="auto">
  <name>Task 4: Ship inventory/example-homelab/group_vars/all/alertmanager.yml (D-15 operator-tunable surface)</name>
  <files>inventory/example-homelab/group_vars/all/alertmanager.yml</files>
  <read_first>
    inventory/example-homelab/group_vars/all/network.yml (telemetron_network/telemetron_tz are inherited, NOT duplicated),
    inventory/example-homelab/group_vars/all/storage.yml (telemetron_volume_prefix/telemetron_config_root are inherited, NOT duplicated),
    roles/alertmanager/defaults/main.yml (full list of operator-tunable knobs; this inventory file surfaces the subset operators typically tune per environment),
    .planning/phases/04-alert-plane/04-CONTEXT.md (D-15 + D-60 + D-61 + D-66 -- vault file is NOT extended in this phase)
  </read_first>
  <action>
CREATE new file `inventory/example-homelab/group_vars/all/alertmanager.yml` with the following exact content (full operator-tunable surface):

```yaml
# (YAML doc start marker)
# Telemetron -- alertmanager-specific inventory overrides (Phase 4, D-15)
# Consumed by roles/alertmanager/ at playbook time.
#
# All values are defaults shipped by the role -- this file surfaces the
# operator-tunable subset for visibility. Operators tweak per-environment
# without editing role defaults.
#
# Vault keys NOT extended in Phase 4 (D-66): alertmanager has no outbound
# auth in M1 (null receiver = no destination) and no inbound auth (single-
# host telemetron bridge trust boundary). Operators wiring real receivers
# via alertmanager_extra_receivers must add their own vault keys.

# --- Host publish (D-30) ---
# false  -- no host port publish (default; SSH local-forward for UI access)
# true   -- publish :9093 on 0.0.0.0
# 127.0.0.1 -- publish on loopback only
alertmanager_publish_host: false

# --- Routing intervals (ALERT-01 / D-61) ---
# Defaults match the ALERT-01-mandated values. Override per environment.
alertmanager_group_by:
  - alertname
  - cluster
  - service
alertmanager_group_wait: 30s
alertmanager_group_interval: 5m
alertmanager_repeat_interval: 4h

# --- Log level ---
alertmanager_log_level: info

# --- Operator extension lists (D-60; sorted-keys-iterated per D-20) ---
# Default to empty. Operators add real receivers + routes here once they
# wire a notification target (Slack, PagerDuty, custom webhook, etc.).
# See roles/alertmanager/README.md "Bring your own receivers" for an
# example.
alertmanager_extra_receivers: []
alertmanager_extra_routes: []
alertmanager_extra_inhibit_rules: []
alertmanager_extra_time_intervals: []
alertmanager_extra_mute_time_intervals: []
```
  </action>
  <verify>
    <automated>
bash -c '
set -e
test -f inventory/example-homelab/group_vars/all/alertmanager.yml
grep -E "^alertmanager_group_interval: 5m" inventory/example-homelab/group_vars/all/alertmanager.yml
grep -E "^alertmanager_repeat_interval: 4h" inventory/example-homelab/group_vars/all/alertmanager.yml
grep -E "^alertmanager_group_wait: 30s" inventory/example-homelab/group_vars/all/alertmanager.yml
grep -E "^alertmanager_publish_host: false" inventory/example-homelab/group_vars/all/alertmanager.yml
grep -E "^alertmanager_extra_receivers: \\[\\]" inventory/example-homelab/group_vars/all/alertmanager.yml
grep -E "^alertmanager_extra_routes: \\[\\]" inventory/example-homelab/group_vars/all/alertmanager.yml
grep -E "^alertmanager_extra_inhibit_rules: \\[\\]" inventory/example-homelab/group_vars/all/alertmanager.yml
# Vault file unchanged in Phase 4 (D-66) -- file must contain NO new vault_alertmanager_* keys
! grep -E "vault_alertmanager_" inventory/example-homelab/group_vars/all/vault.yml.example
# Gate 1
test "$(grep -rE '\''inspq|qc\\.ca|montreal|quebec|francais|french|/srv/nfs/inspq|vault_inspq'\'' inventory/example-homelab/group_vars/all/alertmanager.yml | wc -l)" = "0"
test "$(grep -rPn '\''[^\\x00-\\x7F]'\'' inventory/example-homelab/group_vars/all/alertmanager.yml | wc -l)" = "0"
echo "Task 4 inventory verify: OK"
'
    </automated>
  </verify>
  <acceptance_criteria>
    - `inventory/example-homelab/group_vars/all/alertmanager.yml` exists.
    - File contains the exact lines `alertmanager_group_interval: 5m`, `alertmanager_repeat_interval: 4h`, `alertmanager_group_wait: 30s`, `alertmanager_publish_host: false`, `alertmanager_log_level: info`.
    - File contains the exact lines `alertmanager_extra_receivers: []`, `alertmanager_extra_routes: []`, `alertmanager_extra_inhibit_rules: []`, `alertmanager_extra_time_intervals: []`, `alertmanager_extra_mute_time_intervals: []`.
    - File `alertmanager_group_by:` is a YAML list containing `alertname`, `cluster`, `service` (in any order -- three entries total).
    - `inventory/example-homelab/group_vars/all/vault.yml.example` does NOT contain any `vault_alertmanager_*` keys (D-66: vault file is NOT extended).
    - Gate 1 -- `grep -rE 'inspq|qc.ca|montreal|quebec|francais|french|/srv/nfs/inspq|vault_inspq' inventory/example-homelab/group_vars/all/alertmanager.yml` returns 0 lines.
    - Gate 1 -- `grep -rPn '[^\x00-\x7F]' inventory/example-homelab/group_vars/all/alertmanager.yml` returns 0 lines.
  </acceptance_criteria>
  <done>
    Operator can edit `inventory/example-homelab/group_vars/all/alertmanager.yml` to tune routing intervals, host-publish, log level, and extension lists without ever touching `roles/alertmanager/defaults/main.yml`. The vault file is unchanged per D-66.
  </done>
</task>

<task type="auto">
  <name>Task 5: Wire alertmanager role into playbooks/deploy_docker.yml after fluentbit</name>
  <files>playbooks/deploy_docker.yml</files>
  <read_first>
    playbooks/deploy_docker.yml (current state -- last role entry is `- role: fluentbit` with `tags: [fluentbit]`; comments at the end reference Phase 4 alertmanager + hook_router),
    .planning/phases/04-alert-plane/04-CONTEXT.md (D-22 single-tag-per-role; D-23 plan wires own role),
    .planning/phases/04-alert-plane/04-RESEARCH.md (Q17 confirms file shape + exact append syntax)
  </read_first>
  <action>
Two edits to `playbooks/deploy_docker.yml`.

**(A)** Append the alertmanager role entry to the `roles:` list. After the existing `- role: fluentbit` / `tags: [fluentbit]` block (which currently ends at line ~59), INSERT immediately before the trailing `# Subsequent phase plans extend this list in dependency order` comment block (line ~60):

```yaml
    - role: alertmanager
      tags:
        - alertmanager
```

Resulting `roles:` list ends with: `... fluentbit -> alertmanager`.

**(B)** Update the trailing comment to reflect Phase 4 partial completion. The existing comment line `#   Phase 4: alertmanager, hook_router` (line ~63 in current file) -> REPLACE with:

```
  #   Phase 4: alertmanager (hook_router deferred to a future milestone -- see REQUIREMENTS.md ALERT-V2-01..05)
```

Keep the comment lines for Phase 5 / Phase 6 unchanged.

The playbook does NOT need any `pre_tasks` updates -- the `telemetron` network is already created by the existing pre_tasks; alertmanager attaches to it via `networks: [{name: telemetron}]` in its role tasks. No `vars:` overrides are needed on the playbook entry (all role tunables live in `roles/alertmanager/defaults/main.yml` + `inventory/example-homelab/group_vars/all/alertmanager.yml`).
  </action>
  <verify>
    <automated>
bash -c '
set -e
# alertmanager role entry present
grep -E "^\\s+- role: alertmanager" playbooks/deploy_docker.yml
# alertmanager appears AFTER fluentbit in the roles: list
awk "/- role: fluentbit/{fb=NR} /- role: alertmanager/{am=NR} END{ if(fb<am){print \"order_ok\"} else {print \"order_bad\"; exit 1} }" playbooks/deploy_docker.yml
# Comment line updated -- hook_router marked deferred
grep -E "hook_router deferred" playbooks/deploy_docker.yml
# Phase 5 / Phase 6 comment lines preserved
grep -E "Phase 5: grafana, karma, promlens" playbooks/deploy_docker.yml
grep -E "Phase 6: nfsd" playbooks/deploy_docker.yml
# pre_tasks block unchanged (still has telemetron network creation)
grep -E "community.docker.docker_network" playbooks/deploy_docker.yml
# Static syntax check on the playbook
ansible-playbook playbooks/deploy_docker.yml --syntax-check || { echo "SYNTAX FAIL"; exit 1; }
echo "Task 5 playbook-wire verify: OK"
'
    </automated>
  </verify>
  <acceptance_criteria>
    - `playbooks/deploy_docker.yml` contains the exact line `    - role: alertmanager` (with leading whitespace matching the existing roles entries).
    - The `- role: alertmanager` entry appears AFTER the `- role: fluentbit` entry in the YAML roles list.
    - `playbooks/deploy_docker.yml` contains the substring `hook_router deferred` (case-sensitive, in the trailing comment block).
    - The comment lines mentioning `Phase 5: grafana, karma, promlens` and `Phase 6: nfsd` are preserved.
    - `ansible-playbook playbooks/deploy_docker.yml --syntax-check` exits 0.
    - `community.docker.docker_network` reference in pre_tasks is unchanged (still present).
  </acceptance_criteria>
  <done>
    The playbook now orchestrates `minio -> loki -> tempo -> mimir -> node_exporter -> opentelemetry -> prometheus -> fluentbit -> alertmanager` in dependency order. `ansible-playbook --tags alertmanager` runs the role in isolation; full playbook brings up the whole stack including AM.
  </done>
</task>

<task type="auto">
  <name>Task 6: Port-acceptance gates 1-7 (D-67) full-sweep check across alertmanager artifacts</name>
  <files>
    roles/alertmanager/,
    inventory/example-homelab/group_vars/all/alertmanager.yml,
    playbooks/deploy_docker.yml
  </files>
  <read_first>
    roles/README.md "Per-role port-acceptance gates" section (the 7-gate canonical list -- Gates 1-7),
    .planning/phases/04-alert-plane/04-CONTEXT.md (D-67 enumerates the seven gates; D-68 itemizes the INSPQ deviation list the grep gates must catch),
    inventory/example-homelab/group_vars/all/vault.yml.example (to confirm Gate 3: file is UNCHANGED in Phase 4 -- D-66),
    roles/alertmanager/tasks/main.yml (to confirm Gate 5 healthcheck + restart_policy + Gate 7 labels are present),
    roles/alertmanager/README.md (to confirm Gate 6 OPS-03 schema sections present)
  </read_first>
  <action>
Run all seven gates as automated checks. NO file edits in this task -- it is a verification step. If ANY gate fails, the executor must back up to the offending task (Task 1-5) and fix; do NOT attempt to patch the violation here.

**Gate 1 -- INSPQ grep gate + non-ASCII gate** (D-21, expanded by D-68):
```bash
grep -rE 'inspq|qc\.ca|montreal|quebec|francais|french|/srv/nfs/inspq|vault_inspq|soir|nuit|mep_|noreply@inspq|America/Montreal|alert_manager_' roles/alertmanager/ inventory/example-homelab/group_vars/all/alertmanager.yml | wc -l
# expect: 0
grep -rPn '[^\x00-\x7F]' roles/alertmanager/ inventory/example-homelab/group_vars/all/alertmanager.yml | wc -l
# expect: 0
```

**Gate 2 -- Image pin** (OPS-01):
```bash
grep -rE 'image:.*:latest' roles/alertmanager/ | wc -l  # expect: 0
grep -E 'alertmanager_image_tag: "v0\.32\.1"' roles/alertmanager/defaults/main.yml  # expect: match
grep -E 'alertmanager_curl_image_tag: "8\.10\.1"' roles/alertmanager/defaults/main.yml  # expect: match
```

**Gate 3 -- Vault discipline** (OPS-02 + D-66):
```bash
# Phase 4 adds NO vault keys per D-66. vault.yml.example must NOT contain any new vault_alertmanager_*
grep -E 'vault_alertmanager_' inventory/example-homelab/group_vars/all/vault.yml.example | wc -l  # expect: 0
# roles/alertmanager/ must contain NO {{ vault_* }} references
grep -rE '\{\{\s*vault_' roles/alertmanager/ | wc -l  # expect: 0
```

**Gate 4 -- Idempotency** (OPS-04 + Pitfall 8):
```bash
ansible-playbook playbooks/deploy_docker.yml --syntax-check
# Run twice if a live host is available -- second run must show changed=0.
# In this gate task, syntax-check is the static-only check; the live idempotency
# check happens at execute time in Task 7.
```

**Gate 5 -- Healthcheck + restart policy** (OPS-06):
```bash
grep -E 'restart_policy: "\{\{ alertmanager_restart_policy \}\}"' roles/alertmanager/tasks/main.yml  # match
grep -E 'alertmanager_restart_policy: unless-stopped' roles/alertmanager/defaults/main.yml  # match
grep -E 'healthcheck:' roles/alertmanager/tasks/main.yml  # match
grep -E '/-/healthy' roles/alertmanager/defaults/main.yml  # match
```

**Gate 6 -- README schema** (OPS-03):
```bash
grep -E '^## Variables' roles/alertmanager/README.md
grep -E '^## Modes' roles/alertmanager/README.md
grep -E '^## Tags' roles/alertmanager/README.md
grep -E '^## Volumes' roles/alertmanager/README.md
grep -E '^## Healthcheck' roles/alertmanager/README.md
grep -E '^## Deviations from upstream INSPQ' roles/alertmanager/README.md
grep -E '^## Idempotency' roles/alertmanager/README.md
grep -E '^## Port-acceptance gates' roles/alertmanager/README.md
grep -E '^## Deprecation notes' roles/alertmanager/README.md
```

**Gate 7 -- Telemetron label stamp** (Plan 03-05 / INGEST-07):
```bash
grep -E 'org\.telemetron\.service: telemetron' roles/alertmanager/tasks/main.yml  # match
grep -E 'org\.telemetron\.job: alertmanager' roles/alertmanager/tasks/main.yml  # match
```

If ALL gates pass, this task is done. If any gate fails, the executor MUST NOT proceed to Task 7 -- go back and fix the originating file.
  </action>
  <verify>
    <automated>
bash -c '
set -e
# Gate 1
test "$(grep -rE '\''inspq|qc\\.ca|montreal|quebec|francais|french|/srv/nfs/inspq|vault_inspq|soir|nuit|mep_|noreply@inspq|America/Montreal|alert_manager_'\'' roles/alertmanager/ inventory/example-homelab/group_vars/all/alertmanager.yml | wc -l)" = "0"
test "$(grep -rPn '\''[^\\x00-\\x7F]'\'' roles/alertmanager/ inventory/example-homelab/group_vars/all/alertmanager.yml | wc -l)" = "0"
# Gate 2
test "$(grep -rE '\''image:.*:latest'\'' roles/alertmanager/ | wc -l)" = "0"
grep -qE "alertmanager_image_tag: \"v0\\.32\\.1\"" roles/alertmanager/defaults/main.yml
# Gate 3
test "$(grep -E '\''vault_alertmanager_'\'' inventory/example-homelab/group_vars/all/vault.yml.example | wc -l)" = "0"
test "$(grep -rE '\''\\{\\{\\s*vault_'\'' roles/alertmanager/ | wc -l)" = "0"
# Gate 4
ansible-playbook playbooks/deploy_docker.yml --syntax-check
# Gate 5
grep -qE "alertmanager_restart_policy: unless-stopped" roles/alertmanager/defaults/main.yml
grep -qE "healthcheck:" roles/alertmanager/tasks/main.yml
grep -qE "/-/healthy" roles/alertmanager/defaults/main.yml
# Gate 6
for section in "## Variables" "## Modes" "## Tags" "## Volumes" "## Healthcheck" "## Deviations from upstream INSPQ" "## Idempotency" "## Port-acceptance gates" "## Deprecation notes" ; do
  grep -qE "^${section}" roles/alertmanager/README.md || { echo "Gate 6 missing ${section}"; exit 1; }
done
# Gate 7
grep -qE "org\\.telemetron\\.service: telemetron" roles/alertmanager/tasks/main.yml
grep -qE "org\\.telemetron\\.job: alertmanager" roles/alertmanager/tasks/main.yml
echo "Task 6 all 7 gates: OK"
'
    </automated>
  </verify>
  <acceptance_criteria>
    - Gate 1 -- `grep -rE 'inspq|qc.ca|montreal|quebec|francais|french|/srv/nfs/inspq|vault_inspq|soir|nuit|mep_|noreply@inspq|America/Montreal|alert_manager_' roles/alertmanager/ inventory/example-homelab/group_vars/all/alertmanager.yml` returns 0 lines.
    - Gate 1 -- `grep -rPn '[^\x00-\x7F]' roles/alertmanager/ inventory/example-homelab/group_vars/all/alertmanager.yml` returns 0 lines.
    - Gate 2 -- `grep -rE 'image:.*:latest' roles/alertmanager/` returns 0 lines AND `roles/alertmanager/defaults/main.yml` contains `alertmanager_image_tag: "v0.32.1"`.
    - Gate 3 -- `grep -E 'vault_alertmanager_' inventory/example-homelab/group_vars/all/vault.yml.example` returns 0 lines AND `grep -rE '\{\{\s*vault_' roles/alertmanager/` returns 0 lines.
    - Gate 4 -- `ansible-playbook playbooks/deploy_docker.yml --syntax-check` exits 0. (Live two-run idempotency check happens in Task 7 against the target host.)
    - Gate 5 -- `roles/alertmanager/defaults/main.yml` contains `alertmanager_restart_policy: unless-stopped` AND `roles/alertmanager/tasks/main.yml` contains `healthcheck:` AND `roles/alertmanager/defaults/main.yml` contains `/-/healthy`.
    - Gate 6 -- `roles/alertmanager/README.md` contains all nine section headings (`## Variables`, `## Modes`, `## Tags`, `## Volumes`, `## Healthcheck`, `## Deviations from upstream INSPQ`, `## Idempotency`, `## Port-acceptance gates`, `## Deprecation notes`).
    - Gate 7 -- `roles/alertmanager/tasks/main.yml` contains both `org.telemetron.service: telemetron` and `org.telemetron.job: alertmanager`.
  </acceptance_criteria>
  <done>
    All seven port-acceptance gates pass on the static artifacts. The role is ready for the live D-69 verify run (which executes in `tasks/verify.yml` during deployment). Any gate failure here is a regression in tasks 1-5 and must be fixed before proceeding.
  </done>
</task>

<task type="auto">
  <name>Task 7: Live deployment + D-69 in-network verify suite</name>
  <files>
    (no file edits -- this task deploys and exercises the artifacts created by Tasks 1-6)
  </files>
  <read_first>
    roles/alertmanager/tasks/verify.yml (Steps 1-8 -- H/C poll, /-/ready/healthy probes, /api/v2/receivers, /api/v2/status YAML grep, Prom->AM round-trip, amtool alert add/query, amtool silence add/query),
    .planning/phases/04-alert-plane/04-CONTEXT.md (D-69 enumerates the verify steps),
    .planning/phases/04-alert-plane/04-RESEARCH.md (Q3 amtool via docker_container_exec; Q8 /api/v2/status YAML-grep; Risk 1 corrects CONTEXT.md jq assertions)
  </read_first>
  <action>
Deploy the playbook against the target Docker host and run the D-69 verify suite. This is the M1 acceptance gate for Phase 4 -- the role must come up healthy AND Prometheus->AM wiring must be live AND amtool round-trips must succeed.

**(A) First-run deploy** (full playbook OR --tags alertmanager,prometheus depending on host state):
```bash
ansible-playbook -i inventory/example-homelab playbooks/deploy_docker.yml --tags alertmanager,prometheus --ask-vault-pass
```
Expected: `PLAY RECAP` shows `changed=N` (N>=1) for the target host. The verify.yml steps run as the final tasks of the alertmanager role and must all succeed (Steps 1-8 above).

**(B) Idempotency gate** (Gate 4 -- live confirmation):
```bash
ansible-playbook -i inventory/example-homelab playbooks/deploy_docker.yml --tags alertmanager --ask-vault-pass
```
Expected: `PLAY RECAP` shows `changed=0` for the target host. The verify suite (verify.yml) runs again but every `changed_when: false` task in it reports `ok` -- second run confirms idempotency.

**(C) Manual cross-check assertions** (run from operator workstation against the target host):
```bash
# Container health
ssh <host> "docker inspect telemetron-alertmanager --format '{{.State.Health.Status}}'"
# Expect: healthy

# Restart policy
ssh <host> "docker inspect telemetron-alertmanager --format '{{.HostConfig.RestartPolicy.Name}}'"
# Expect: unless-stopped

# Volume mount
ssh <host> "docker inspect telemetron-alertmanager --format '{{range .Mounts}}{{.Name}}:{{.Destination}} {{end}}'"
# Expect output to include: telemetron_alertmanager_data:/alertmanager

# Gate 7 labels
ssh <host> "docker inspect telemetron-alertmanager --format '{{json .Config.Labels}}'"
# Expect: {"org.telemetron.service":"telemetron","org.telemetron.job":"alertmanager"}

# Prom->AM live (from a one-shot container in the telemetron network):
ssh <host> "docker run --rm --network telemetron curlimages/curl:8.10.1 -fsS http://prometheus:9090/api/v1/alertmanagers"
# Expect JSON with data.activeAlertmanagers[0].url == "http://alertmanager:9093/api/v2/alerts"

# AM persistence test -- create silence, restart container, confirm silence persists:
ssh <host> "docker exec telemetron-alertmanager /bin/amtool --alertmanager.url=http://localhost:9093 silence add --comment='persistence test' alertname=PersistenceTest"
ssh <host> "docker restart telemetron-alertmanager"
sleep 30
ssh <host> "docker exec telemetron-alertmanager /bin/amtool --alertmanager.url=http://localhost:9093 silence query alertname=PersistenceTest"
# Expect: silence is still listed (proves the named volume preserves nflog + silences across restart -- D-62)
```

**(D) Failure investigation paths** -- if any step fails:
- Container not healthy -> check `docker logs telemetron-alertmanager` for config parse errors; check rendered `/opt/telemetron/alertmanager/alertmanager.yml` matches expectations.
- Prom->AM list empty -> check `docker logs telemetron-prometheus` for failed alertmanager-discovery errors; confirm `alerting:` block in rendered `/opt/telemetron/prometheus/prometheus.yml`; confirm the prometheus restart handler fired on plan execution.
- `amtool` exits non-zero -> check `--alertmanager.url` reachability; confirm container is healthy first.
- Idempotency failure (run 2 reports `changed > 0`) -> identify the offending task in `ansible-playbook --diff` output; the root cause is almost always non-sorted iteration over a dict or a `state: restarted` slip-through.
  </action>
  <verify>
    <automated>
bash -c '
# This task runs against a live target host. The automated check here is the
# in-network verify suite EMBEDDED in roles/alertmanager/tasks/verify.yml --
# which executes during ansible-playbook -- AND a syntax-check that is safe
# to run from any environment.
set -e
ansible-playbook -i inventory/example-homelab playbooks/deploy_docker.yml --tags alertmanager --syntax-check
echo "Task 7 deploy-and-verify: syntax-check OK (live execution happens at execute-phase time)"
'
    </automated>
  </verify>
  <acceptance_criteria>
    - `ansible-playbook -i inventory/example-homelab playbooks/deploy_docker.yml --tags alertmanager` first run completes with `failed=0` and `unreachable=0` on the target host.
    - Second back-to-back run with the same command reports `changed=0` in PLAY RECAP (Gate 4 idempotency, live).
    - `docker inspect telemetron-alertmanager --format '{{.State.Health.Status}}'` returns `healthy` on the target host.
    - `docker inspect telemetron-alertmanager --format '{{.HostConfig.RestartPolicy.Name}}'` returns `unless-stopped`.
    - `docker inspect telemetron-alertmanager --format '{{range .Mounts}}{{.Name}}:{{.Destination}} {{end}}'` output contains the substring `telemetron_alertmanager_data:/alertmanager`.
    - `docker inspect telemetron-alertmanager --format '{{json .Config.Labels}}'` returns a JSON object containing the keys `org.telemetron.service` with value `telemetron` AND `org.telemetron.job` with value `alertmanager`.
    - Inside the role's verify.yml Steps 1-8 all complete with `ok` (the embedded checks ARE the acceptance criteria for the in-network probes -- /-/ready, /-/healthy, /api/v2/receivers null-receiver assertion, /api/v2/status YAML-grep for group_interval/repeat_interval/group_wait/group_by labels, Prometheus /api/v1/alertmanagers round-trip, amtool alert add/query, amtool silence add/query).
    - Persistence cross-check: silence added via `amtool silence add`, container restarted, then `amtool silence query` still returns the silence (proves D-62 named-volume persistence).
  </acceptance_criteria>
  <done>
    Alertmanager is deployed, healthy, dispatching alerts from Prometheus to the null receiver, the persistent named volume is preserving silences and nflog across restart, and the D-69 verify suite passes on both first and second playbook runs (idempotency).
  </done>
</task>

</tasks>

<verification>

## Phase 4 overall acceptance (post-execution)

After Tasks 1-7 complete, the following high-level checks confirm Phase 4 is done:

### Code-complete (ALERT-01)

```bash
# Container up + healthy + restarting policy + persistent volume
ssh <host> "docker inspect telemetron-alertmanager --format '{{.State.Health.Status}}'"          # healthy
ssh <host> "docker inspect telemetron-alertmanager --format '{{.HostConfig.RestartPolicy.Name}}'" # unless-stopped
ssh <host> "docker inspect telemetron-alertmanager --format '{{range .Mounts}}{{.Name}}:{{.Destination}} {{end}}'" | grep -E "telemetron_alertmanager_data:/alertmanager"

# Routing knobs explicit per D-61
ssh <host> "docker run --rm --network telemetron curlimages/curl:8.10.1 -fsS http://alertmanager:9093/api/v2/status" | python3 -c "import sys,json; d=json.load(sys.stdin); print(d['config']['original'])" | grep -E "group_interval: 5m"

# Single null receiver
ssh <host> "docker run --rm --network telemetron curlimages/curl:8.10.1 -fsS http://alertmanager:9093/api/v2/receivers" | python3 -c "import sys,json; assert json.load(sys.stdin)[0]['name']=='null'"

# Prom->AM wiring live (D-64)
ssh <host> "docker run --rm --network telemetron curlimages/curl:8.10.1 -fsS http://prometheus:9090/api/v1/alertmanagers" | python3 -c "import sys,json; d=json.load(sys.stdin); assert any(am['url']=='http://alertmanager:9093/api/v2/alerts' for am in d['data']['activeAlertmanagers'])"

# amtool round-trips (synthetic alert + silence)
ssh <host> "docker exec telemetron-alertmanager /bin/amtool --alertmanager.url=http://localhost:9093 alert add alertname=PhaseSmokeTest severity=warning"
ssh <host> "docker exec telemetron-alertmanager /bin/amtool --alertmanager.url=http://localhost:9093 alert query alertname=PhaseSmokeTest" | grep -q "PhaseSmokeTest"

# Inhibit rule active (one entry, corrected syntax)
ssh <host> "docker run --rm --network telemetron curlimages/curl:8.10.1 -fsS http://alertmanager:9093/api/v2/status" | python3 -c "import sys,json; d=json.load(sys.stdin); assert 'source_matchers' in d['config']['original'] and 'source_match:' not in d['config']['original']"
```

### Doc-complete (ALERT-02..06 disposition)

```bash
# ALERT-02..06 NOT in M1 §Alert plane
! grep -E "^- \[ \] \*\*ALERT-0[2-6]\*\*" .planning/REQUIREMENTS.md

# ALERT-V2-01..05 IN §v2 Requirements
for n in 01 02 03 04 05 ; do grep -qE "\*\*ALERT-V2-${n}\*\*" .planning/REQUIREMENTS.md ; done

# ROADMAP / PROJECT no longer claim hook router in M1
! grep -E "Write the hook router Flask app source under" .planning/PROJECT.md
grep -qE "hook_router deferred" .planning/ROADMAP.md || grep -qE "deferred to.*milestone" .planning/ROADMAP.md

# hooks/README.md states deferral
grep -qiE "deferred" hooks/README.md
grep -qE "ALERT-V2-0[1-5]" hooks/README.md

# CLAUDE.md hook_router rows mark deferred
grep -qiE "deferred" CLAUDE.md
grep -qE "Hook Router \(v2 -- deferred\)" CLAUDE.md

# roles/README.md alertmanager ticked, hook_router deferred
grep -qE "alertmanager" roles/README.md
grep -qE "hook_router.+deferred" roles/README.md
```

### Gates (all seven, static)

```bash
# Gate 1
test "$(grep -rE 'inspq|qc.ca|montreal|quebec|francais|french|/srv/nfs/inspq|vault_inspq|soir|nuit|mep_|noreply@inspq|America/Montreal|alert_manager_' roles/alertmanager/ inventory/example-homelab/group_vars/all/alertmanager.yml | wc -l)" = "0"
test "$(grep -rPn '[^\x00-\x7F]' roles/alertmanager/ inventory/example-homelab/group_vars/all/alertmanager.yml | wc -l)" = "0"

# Gate 2-7 -- see Task 6 verify block
```

### Idempotency (live)

```bash
# Run twice; second run must show changed=0
ansible-playbook -i inventory/example-homelab playbooks/deploy_docker.yml --tags alertmanager
ansible-playbook -i inventory/example-homelab playbooks/deploy_docker.yml --tags alertmanager
# Expected PLAY RECAP: changed=0 unreachable=0 failed=0
```

</verification>

<success_criteria>

Phase 4 is complete when ALL of the following are true:

1. **`roles/alertmanager/` exists as a fully ported role** -- 7 files (defaults, tasks/main, tasks/verify, handlers, templates, meta, README) mirroring the canonical Phase-1..3 role shape; passes all 7 port-acceptance gates.
2. **Alertmanager runs healthy** -- `docker inspect telemetron-alertmanager` reports `Healthy` state with `unless-stopped` restart policy, persistent `telemetron_alertmanager_data` volume mounted at `/alertmanager`, `org.telemetron.*` Gate-7 labels stamped.
3. **Routing config is ALERT-01 / D-61** -- `/api/v2/status` YAML-grep confirms `group_by` [alertname, cluster, service], `group_wait: 30s`, `group_interval: 5m`, `repeat_interval: 4h`.
4. **Single null receiver default** -- `/api/v2/receivers` returns proper JSON `[{"name":"null"}]`.
5. **One D-63 inhibit rule with CORRECTED `source_matchers:` syntax** (not deprecated `source_match:`) -- rendered `/opt/telemetron/alertmanager/alertmanager.yml` contains `source_matchers:` AND `target_matchers:` AND `equal: [instance]`; `/api/v2/status` config.original confirms parse-clean (no deprecation warnings in container logs).
6. **Prometheus->AM wiring live (D-64)** -- `roles/prometheus/templates/prometheus.yml.j2` carries the `alerting: alertmanagers:` block; `roles/prometheus/defaults/main.yml` has `prometheus_alertmanager_target: "alertmanager:9093"`; existing prometheus restart handler fires automatically on next deploy; `curl http://prometheus:9090/api/v1/alertmanagers` shows `http://alertmanager:9093/api/v2/alerts` as active.
7. **D-65 severity labels confirmed present** -- All four baseline rules (HostDown, FilesystemAlmostFull, ContainerRestartLoop, OTelCollectorDroppingSignals) carry their `severity:` labels (already-present per Research Q11; inline comment now documents this so future regressions are caught).
8. **D-69 verify suite passes** -- Steps 1-8 in `roles/alertmanager/tasks/verify.yml` all complete `ok` on first and second playbook runs.
9. **Inventory file shipped** -- `inventory/example-homelab/group_vars/all/alertmanager.yml` exists with the full operator-tunable knob surface (D-61 intervals + D-60 extension lists + D-30 publish flag + log level).
10. **Playbook wires alertmanager after fluentbit** -- `playbooks/deploy_docker.yml` roles list ends `... -> fluentbit -> alertmanager`; `ansible-playbook --tags alertmanager` runs the role in isolation.
11. **Doc-rework cascade complete (D-58)** -- All nine files updated: ALERT-02..06 moved to ALERT-V2-01..05 in REQUIREMENTS.md; ROADMAP Phase 4 goal rewritten; PROJECT.md Active no longer claims hook_router in scope; CLAUDE.md tech-stack + port-allocation flags hook_router deferred; hooks/README.md states deferral; roles/README.md alertmanager row ticked + hook_router row reworded; research SUMMARY/FEATURES/ARCHITECTURE reword the hook-router framing to "deferred."
12. **Vault file unchanged (D-66)** -- No `vault_alertmanager_*` keys added to `vault.yml.example`.
13. **Idempotency (Gate 4 live)** -- Second back-to-back playbook run with `--tags alertmanager` reports `changed=0`.

</success_criteria>

<output>
After execution completes, create `.planning/phases/04-alert-plane/04-01-SUMMARY.md` recording:

- What was built (alertmanager role, prometheus extension, inventory file, playbook wiring, doc cascade)
- Decisions exercised (D-56 through D-69 applied; Research corrections honored -- source_matchers, explicit HEALTHCHECK, docker_container_exec for amtool, /api/v2/receivers for null-receiver JSON, YAML-grep on /api/v2/status for route knobs)
- Files modified (full list from frontmatter -- 7 role files + 3 prometheus files + 1 playbook + 1 inventory + 9 docs)
- D-65 verification outcome (severity labels were already present per Research Q11; no edit needed; inline comment added for regression protection)
- Test/verify command outputs (PLAY RECAP first and second run, docker inspect outputs, /api/v2/* curl outputs, amtool round-trip outputs)
- Gates that ran (1-7) and their pass/fail status
- Carryover notes for Phase 5 -- Karma in Phase 5 expands to "primary alert UX" (per CONTEXT.md "Specifics" note); Karma points at this Alertmanager via the `karma_alertmanager_uri: http://alertmanager:9093` knob (to be set in Phase 5 inventory)
- ALERT-02..06 -> ALERT-V2-01..05 traceability -- the requirements MOVED, not deleted; the deferred design captured in `.planning/phases/04-alert-plane/04-DISCUSSION-LOG.md` for the v2 milestone that picks the hook router back up
</output>
