---
phase: 5
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - roles/grafana/defaults/main.yml
  - roles/grafana/tasks/main.yml
  - roles/grafana/tasks/verify.yml
  - roles/grafana/handlers/main.yml
  - roles/grafana/templates/grafana.ini.j2
  - roles/grafana/templates/datasources/datasources.yaml.j2
  - roles/grafana/templates/dashboards/dashboards.yaml.j2
  - roles/grafana/files/dashboards/host-health.json
  - roles/grafana/files/dashboards/loki-explore-landing.json
  - roles/grafana/files/dashboards/tempo-explore-landing.json
  - roles/grafana/files/dashboards/otel-collector-self-metrics.json
  - roles/grafana/files/dashboards/loki-self-metrics.json
  - roles/grafana/files/dashboards/tempo-self-metrics.json
  - roles/grafana/files/dashboards/mimir-self-metrics.json
  - roles/grafana/files/_rewrite_uids.py
  - roles/grafana/meta/main.yml
  - roles/grafana/README.md
  - inventory/example-homelab/group_vars/all/grafana.yml
  - inventory/example-homelab/group_vars/all/secrets.yml.example
  - inventory/example-homelab/group_vars/all/network.yml
  - playbooks/deploy_docker.yml
  - roles/README.md
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
requirements: [UI-01, UI-02, UI-03, UI-04]
requirements_addressed: [UI-01, UI-02, UI-03, UI-04]
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Operator runs `ansible-playbook -i inventory/example-homelab playbooks/deploy_docker.yml --tags grafana` and `docker inspect telemetron-grafana --format '{{.State.Health.Status}}'` returns `healthy`."
    - "Operator opens `http://<host>:3000`, logs in with `admin` + `grafana_admin_password` from inventory, and the four provisioned datasources appear with UIDs exactly `prometheus`, `loki`, `tempo`, `mimir` (UI-02)."
    - "`curl -fsS -u admin:<pw> http://<host>:3000/api/datasources/uid/prometheus/health` returns HTTP 200 with body containing `\"status\":\"OK\"`. Same for `loki`, `tempo`, `mimir` UIDs (Gate 9 / D-73)."
    - "All seven role-shipped dashboards (host-health, loki-explore-landing, tempo-explore-landing, otel-collector-self-metrics, loki-self-metrics, tempo-self-metrics, mimir-self-metrics) appear in the Grafana UI under folder `Telemetron`; opening any of them shows real query results with no `Datasource not found` errors (UI-03 / D-75)."
    - "Tempo datasource's rendered `jsonData.tracesToLogsV2.query` field equals exactly `{${__tags}} | trace_id=\"${__span.traceId}\"` (D-79). Loki datasource's rendered `jsonData.derivedFields[0]` has `name: trace_id`, `matcherType: label`, `matcherRegex: trace_id`, `datasourceUid: tempo` (UI-04 / D-79)."
    - "`curl -fsS http://grafana:3000/api/datasources/proxy/uid/prometheus/api/v1/query?query=up` returns JSON with non-empty `data.result` array (Gate 9 canonical query for prometheus). Same query against `mimir` UID also returns non-empty result (Gate 9 canonical query for mimir)."
    - "`curl -fsS 'http://grafana:3000/api/datasources/proxy/uid/loki/loki/api/v1/query?query=%7Bjob%3D~%22.%2B%22%7D'` returns JSON with `\"status\":\"success\"` (Gate 9 canonical query for loki)."
    - "`curl -fsS 'http://grafana:3000/api/datasources/proxy/uid/tempo/api/search?limit=1'` returns HTTP 200 with body containing `\"traces\":` or `\"results\":` (Gate 9 canonical query for tempo, accepting empty array as stable state per D-73)."
    - "Persistent named volume `telemetron_grafana_data` mounted at `/var/lib/grafana` inside the container (UI-01)."
    - "Rendered `/opt/telemetron/grafana/provisioning/datasources/datasources.yaml` contains all four datasources with `editable: false` and exact UIDs `prometheus`, `loki`, `tempo`, `mimir`."
    - "Rendered `/opt/telemetron/grafana/provisioning/dashboards/dashboards.yaml` declares two providers: `telemetron` folder (path: `/etc/grafana/provisioning/dashboards/telemetron`, `disableDeletion: true`, `allowUiUpdates: false`) and `operator` folder (path: `/etc/grafana/provisioning/dashboards/operator`, `disableDeletion: false`, `allowUiUpdates: true`) (D-76)."
    - "Container env injects `GF_SECURITY_ADMIN_USER`, `GF_SECURITY_ADMIN_PASSWORD`, `GF_SECURITY_ADMIN_EMAIL`, `GF_AUTH_ANONYMOUS_ENABLED` (D-86, D-87, D-88). README documents `grafana_admin_password` is first-boot-only (RESEARCH §2.1 Risk 2; rotation via `docker exec telemetron-grafana grafana-cli admin reset-admin-password <new>`)."
    - "`grep -riE 'inspq|qc\\.ca|montreal|québec|francais|french|/srv/nfs/inspq|vault_inspq|america/(toronto|montreal)|noreply@inspq|ldap.toml' roles/grafana/ inventory/example-homelab/group_vars/all/grafana.yml` returns 0 lines (Gate 1, D-25 audit per RESEARCH §6.1)."
    - "`grep -rPn '[^\\x00-\\x7F]' roles/grafana/ inventory/example-homelab/group_vars/all/grafana.yml` returns 0 lines (non-ASCII gate)."
    - "Second back-to-back `ansible-playbook --tags grafana` run reports `changed=0` (Gate 4 idempotency)."
    - "`community.docker.docker_container` task carries `labels: { org.telemetron.service: telemetron, org.telemetron.job: grafana }` (Gate 7)."
    - "Config bind-mount is parent-dir form: `/opt/telemetron/grafana → /etc/grafana` (Gate 8). NO single-file bind mounts."
    - "Host port :3000 published (D-82 inverts D-30 for UI plane); bind address `0.0.0.0` default (D-83); D-84 port matrix confirmed unchanged."
    - "`playbooks/deploy_docker.yml` lists `- role: grafana` (tag: grafana) immediately AFTER `- role: alertmanager`."
    - "`roles/README.md` ticks `grafana` row in port-status table; `roles/README.md` `## Per-role port-acceptance gates` section grows a new Gate 9 entry describing the datasources-resolve-real-data check (D-73)."
    - "`inventory/example-homelab/group_vars/all/secrets.yml.example` contains an active (uncommented) line `grafana_admin_password: CHANGE_ME` (Phase 5 first new secret; uses D-90 unprefixed convention)."
  artifacts:
    - path: "roles/grafana/defaults/main.yml"
      provides: "Role tunables: image pin `grafana/grafana-oss:13.0.1`, container args, healthcheck timing (explicit -- image has none per RESEARCH §2.1), publish/bind/port knobs (D-82/D-83/D-84), admin user/email (D-88), anonymous knob (D-87 default off), dashboard_extra_dir (D-76), curl-image pin"
      contains: "grafana_image_tag: \"13.0.1\""
    - path: "roles/grafana/tasks/main.yml"
      provides: "Ensure config tree -> render datasources.yaml + dashboards.yaml + grafana.ini -> copy 7 dashboard JSONs -> ensure named volume -> pull image -> run container with explicit HEALTHCHECK + Gate-7 labels + parent-dir bind mounts + GF_ env injection -> flush_handlers -> include verify.yml"
      contains: "community.docker.docker_container"
    - path: "roles/grafana/tasks/verify.yml"
      provides: "D-10a HEALTHCHECK poll -> /api/health curl -> Gate 9 D-73 four-UID health check (looped) -> Gate 9 D-73 four canonical queries (prometheus up, loki labels, tempo search, mimir up) -> tracesToLogsV2 + derivedFields shape assertions via /api/datasources/uid/tempo and /api/datasources/uid/loki"
      contains: "datasources/uid/prometheus/health"
    - path: "roles/grafana/templates/datasources/datasources.yaml.j2"
      provides: "Single-file datasource provisioning manifest with all four UID-pinned datasources (D-77 hardcoded UIDs). Tempo datasource carries D-79 tracesToLogsV2 customQuery YAML verbatim. Loki datasource carries D-79 derivedFields YAML verbatim."
      contains: "uid: prometheus"
    - path: "roles/grafana/templates/dashboards/dashboards.yaml.j2"
      provides: "D-76 two-provider manifest: telemetron folder (read-only, disableDeletion:true) + operator folder (writable, allowUiUpdates:true)"
      contains: "name: telemetron"
    - path: "roles/grafana/files/dashboards/host-health.json"
      provides: "Grafana.com 1860 rev 45 (node_exporter full), UIDs rewritten to `prometheus` per D-77"
      contains: "node_exporter"
    - path: "roles/grafana/files/dashboards/loki-explore-landing.json"
      provides: "Hand-rolled Loki Explore landing page (RESEARCH §3 minimal-stub shape) referencing `loki` UID"
      contains: "telemetron-loki-explore"
    - path: "roles/grafana/files/dashboards/tempo-explore-landing.json"
      provides: "Hand-rolled Tempo Explore landing page referencing `tempo` UID"
      contains: "telemetron-tempo-explore"
    - path: "roles/grafana/files/dashboards/otel-collector-self-metrics.json"
      provides: "Grafana.com 15983 rev 29 (OTel Collector self-metrics), UIDs rewritten to `prometheus` per D-77"
      contains: "otelcol_"
    - path: "roles/grafana/files/dashboards/loki-self-metrics.json"
      provides: "Upstream grafana/loki v3.7.2 `dashboard-loki-operational.json`, UIDs rewritten ($datasource->prometheus / $loki_datasource->loki) via Python rewrite at fork time (RESEARCH §3 Type B + OQ-1)"
      contains: "loki_request_duration_seconds"
    - path: "roles/grafana/files/dashboards/tempo-self-metrics.json"
      provides: "Upstream grafana/tempo v2.10.5 `tempo-operational.json`, UIDs rewritten ($ds->prometheus / $logsds->loki) at fork time"
      contains: "tempo_request_duration_seconds"
    - path: "roles/grafana/files/dashboards/mimir-self-metrics.json"
      provides: "Upstream grafana/mimir mimir-3.0.6 `mimir-overview.json`, UID rewritten ($datasource->prometheus per RESEARCH OQ-4: Mimir's self-metrics are scraped by Prometheus, not queried via Mimir as datastore)"
      contains: "cortex_"
    - path: "roles/grafana/files/_rewrite_uids.py"
      provides: "Idempotent committed-once Python helper used at fork time to substitute upstream datasource template variables for hardcoded UIDs. Documents the per-dashboard substitution map (RESEARCH §3 OQ-1)."
      contains: "import json"
    - path: "roles/grafana/templates/grafana.ini.j2"
      provides: "Structural Grafana config (paths, [server], [database] sqlite3, [users] default_theme, [auth.anonymous] driven by grafana_anonymous_enabled knob). Security/admin settings deliberately driven by GF_ env vars per RESEARCH §2.1 recommendation."
      contains: "[paths]"
    - path: "roles/grafana/handlers/main.yml"
      provides: "Single handler `Docker restart grafana` listening on `restart grafana` (D-19 / W6). Triggered only on grafana.ini changes -- datasource and dashboard manifests hot-reload without restart."
      contains: "docker restart"
    - path: "roles/grafana/meta/main.yml"
      provides: "Galaxy metadata mirroring alertmanager/node_exporter shape (author Rock Martel-Langlois, license MIT, min_ansible_version 2.15, platforms Ubuntu jammy/noble + Debian bookworm)"
    - path: "roles/grafana/README.md"
      provides: "OPS-03 schema: Variables -> Vault keys -> Tags -> Modes -> Volumes -> Healthcheck -> Security/Access model -> Reverse proxy (D-85) -> Trace-to-logs correlation (D-78 honest framing) -> Label mapping (D-81 service_name truth) -> Password rotation note (RESEARCH §2.1 Risk 2) -> Deviations from upstream INSPQ"
      contains: "tracesToLogsV2"
    - path: "inventory/example-homelab/group_vars/all/grafana.yml"
      provides: "Operator-tunable surface: publish_host/bind_address/host_port/anonymous_enabled/admin_user/admin_email/dashboard_extra_dir/extra_datasources"
      contains: "grafana_publish_host: true"
    - path: "inventory/example-homelab/group_vars/all/secrets.yml.example"
      provides: "Adds active line `grafana_admin_password: CHANGE_ME` under a new Phase 5 Grafana section header"
      contains: "grafana_admin_password: CHANGE_ME"
    - path: "inventory/example-homelab/group_vars/all/network.yml"
      provides: "Port-allocation comment block extended with Grafana :3000 entry under a UI plane subsection"
      contains: "grafana"
    - path: "playbooks/deploy_docker.yml"
      provides: "Appends `- role: grafana` with tag `grafana` after the `alertmanager` role entry"
      contains: "role: grafana"
    - path: "roles/README.md"
      provides: "Ticks grafana row; ADDS Gate 9 D-73 datasources-resolve-real-data acceptance gate paragraph under `## Per-role port-acceptance gates`"
      contains: "Gate 9"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 5 Plans list ticks 05-01-PLAN.md; Plans count finalized to 3"
      contains: "05-01-PLAN.md"
  key_links:
    - from: "playbooks/deploy_docker.yml"
      to: "roles/grafana/tasks/main.yml"
      via: "role include in roles: list (immediately after alertmanager)"
      pattern: "role: grafana"
    - from: "roles/grafana/templates/datasources/datasources.yaml.j2"
      to: "roles/prometheus (prometheus:9090) / roles/loki (loki:3100) / roles/tempo (tempo:3200) / roles/mimir (mimir:9009/prometheus)"
      via: "datasources[].url field per Docker bridge DNS"
      pattern: "url: http://(prometheus|loki|tempo|mimir):"
    - from: "roles/grafana/templates/datasources/datasources.yaml.j2 (Tempo entry)"
      to: "roles/loki (loki UID datasource)"
      via: "jsonData.tracesToLogsV2.datasourceUid = loki + customQuery `{${__tags}} | trace_id=\"${__span.traceId}\"` (D-79)"
      pattern: "tracesToLogsV2:"
    - from: "roles/grafana/templates/datasources/datasources.yaml.j2 (Loki entry)"
      to: "roles/tempo (tempo UID datasource)"
      via: "jsonData.derivedFields[0].datasourceUid = tempo + matcherType label + matcherRegex trace_id (D-79)"
      pattern: "derivedFields:"
    - from: "roles/grafana/tasks/main.yml (docker_container)"
      to: "telemetron_grafana_data:/var/lib/grafana + /opt/telemetron/grafana:/etc/grafana (parent-dir bind mount; Gate 8)"
      via: "named-volume + parent-directory bind mount"
      pattern: "telemetron_grafana_data"
    - from: "roles/grafana/tasks/verify.yml"
      to: "telemetron-grafana via curlimages/curl one-shots on telemetron network using admin:{{ grafana_admin_password }} bearer"
      via: "Gate 9 D-73 four-UID /health + four canonical-query proofs"
      pattern: "datasources/uid/.*health"
    - from: "playbooks/deploy_docker.yml + alertmanager role"
      to: "grafana role (alertmanager is a dependency-true predecessor per D-71)"
      via: "roles: list order"
      pattern: "alertmanager.*grafana"
---

<objective>
Deliver Phase 5 plan 1 -- the Grafana role port. This is the heavy plan
of Phase 5 and the de-facto end-to-end smoke test for everything Phases
1-4 shipped (per ROADMAP.md Phase 5 description). If Grafana boots and
its four datasources resolve real data via Gate 9, the entire
pre-Phase-5 stack is validated.

Purpose: Operator runs `ansible-playbook --tags grafana`, opens
`http://<host>:3000`, logs in with `admin` + `grafana_admin_password`
from inventory, and sees the four datasources (Prometheus, Loki, Tempo,
Mimir) at hardcoded UIDs `prometheus`, `loki`, `tempo`, `mimir` (UI-02),
plus seven curated dashboards rendering real data (UI-03), plus
trace-to-logs correlation wired via tracesToLogsV2 + derivedFields
(UI-04), all backed by a `telemetron_grafana_data` named volume holding
the embedded SQLite store (UI-01).

Output:
- `roles/grafana/` (16 files): defaults, tasks/main.yml + tasks/verify.yml,
  handlers, meta, README, two template files (datasources YAML + dashboards
  YAML provisioning manifests), one ini template (grafana.ini), seven
  pre-rewritten dashboard JSONs, one Python rewrite helper.
- `inventory/example-homelab/group_vars/all/grafana.yml` -- full
  operator-tunable surface (publish/bind/port/anonymous/admin/extras).
- `inventory/example-homelab/group_vars/all/secrets.yml.example` --
  adds active `grafana_admin_password: CHANGE_ME` line.
- `inventory/example-homelab/group_vars/all/network.yml` -- port-allocation
  comment block extended with Grafana :3000.
- `playbooks/deploy_docker.yml` -- appends `- role: grafana` after alertmanager.
- `roles/README.md` -- ticks grafana row; ADDS Gate 9 paragraph (D-73).
- `.planning/ROADMAP.md` -- Phase 5 Plans list grows three entries; 05-01 marked complete.
- `.planning/PROJECT.md` -- (if any "Active" line counts remaining roles, decrement).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/05-ui-plane/05-CONTEXT.md
@.planning/phases/05-ui-plane/05-RESEARCH.md
@.planning/phases/04-alert-plane/04-CONTEXT.md
@.planning/phases/03-ingest-plane/03-CONTEXT.md
@.planning/phases/02-telemetry-backends/02-CONTEXT.md
@.planning/phases/01-foundation-storage/01-CONTEXT.md
@roles/README.md
@roles/alertmanager/defaults/main.yml
@roles/alertmanager/tasks/main.yml
@roles/alertmanager/tasks/verify.yml
@roles/alertmanager/handlers/main.yml
@roles/alertmanager/templates/alertmanager.yml.j2
@roles/alertmanager/meta/main.yml
@roles/alertmanager/README.md
@roles/prometheus/defaults/main.yml
@roles/loki/defaults/main.yml
@roles/tempo/defaults/main.yml
@roles/mimir/defaults/main.yml
@roles/opentelemetry/defaults/main.yml
@inventory/example-homelab/group_vars/all/network.yml
@inventory/example-homelab/group_vars/all/storage.yml
@inventory/example-homelab/group_vars/all/secrets.yml.example
@playbooks/deploy_docker.yml

<interfaces>
<!-- Extracted from codebase. Executor uses these contracts directly -- no exploration needed. -->

Shared knobs the role consumes from group_vars/all/:
```yaml
# network.yml
telemetron_network: telemetron
telemetron_publish_default: false   # OVERRIDDEN per D-82 (UI plane exempt)
telemetron_tz: Etc/UTC

# storage.yml
telemetron_volume_prefix: telemetron
telemetron_config_root: /opt/telemetron
```

Backend service DNS names on the telemetron bridge (each role's container_name + role-aliases from each role's docker_container task):
- prometheus :9090         (HTTP query API; from roles/prometheus)
- loki       :3100         (HTTP API incl. /loki/api/v1/* and /otlp ingest; from roles/loki, auth_enabled:false)
- tempo      :3200         (HTTP API incl. /api/search, /api/traces; from roles/tempo; OTLP receivers on internal-only :14317/:14318 NOT used by Grafana)
- mimir      :9009/prometheus  (Prometheus-compatible API subpath; from roles/mimir, multitenancy_enabled:false -- no X-Scope-OrgID header needed per D-26)
- alertmanager :9093       (HTTP API; from roles/alertmanager; not consumed by Grafana role itself but referenced by Karma)

Image facts (RESEARCH §2.1 -- HIGH confidence, all from upstream Dockerfile):
- Image: grafana/grafana-oss:13.0.1
- Container UID: 472 (GF_UID build arg, group 0). DO NOT set `user:` in docker_container -- inherit image default.
- Image VOLUME: /var/lib/grafana
- HEALTHCHECK in image: NONE -- explicit declaration required (RESEARCH Risk 3).
- Entrypoint: /run.sh; CMD: grafana server
- HTTP endpoint for healthcheck: /api/health (returns 200 + JSON {"commit":"...","database":"ok","version":"13.0.1"} when ready)
- `GF_SECURITY_ADMIN_PASSWORD` is FIRST-BOOT-ONLY (RESEARCH Risk 2). Env env-var rotation does NOT update the stored password; operator must `docker exec telemetron-grafana grafana-cli admin reset-admin-password <new>` to rotate.

Provisioning paths (Grafana 13 defaults):
- /etc/grafana/grafana.ini                                (main config; reads on startup)
- /etc/grafana/provisioning/datasources/datasources.yaml  (datasource YAML; reads on startup)
- /etc/grafana/provisioning/dashboards/dashboards.yaml    (dashboard provider manifest; reads on startup)
- /etc/grafana/provisioning/dashboards/telemetron/*.json  (role-shipped dashboard JSONs)
- /etc/grafana/provisioning/dashboards/operator/*.json    (operator drop-in dir, conditionally mounted)

Per D-76, both folders are declared as providers in dashboards.yaml. Datasources reload on file change; dashboards hot-reload every `updateIntervalSeconds`.

Per D-77, dashboard JSONs are committed in their FINAL form (UID strings already substituted; NO Jinja templating of JSON).

Per D-79, the Tempo datasource jsonData carries this EXACT block (literal, NOT operator-overridable in M1):

```yaml
tracesToLogsV2:
  datasourceUid: loki
  spanStartTimeShift: '-1h'
  spanEndTimeShift: '1h'
  tags:
    - key: 'service.name'
      value: 'service_name'
  filterByTraceID: false
  filterBySpanID: false
  customQuery: true
  query: '{${__tags}} | trace_id="${__span.traceId}"'
```

Per D-79, the Loki datasource jsonData carries this EXACT block:

```yaml
derivedFields:
  - name: trace_id
    matcherType: label
    matcherRegex: trace_id
    url: '${__value.raw}'
    datasourceUid: tempo
    urlDisplayLabel: 'View in Tempo'
```

Conditional-healthcheck pattern (proven on alertmanager v0.32.1, tempo, mimir, otel):
```yaml
healthcheck: "{{ grafana_container_healthcheck if (grafana_healthcheck_enabled | bool) else omit }}"
# with vars:
grafana_container_healthcheck:
  test: "{{ grafana_healthcheck_test }}"
  interval: "{{ grafana_healthcheck_interval }}"
  timeout: "{{ grafana_healthcheck_timeout }}"
  retries: "{{ grafana_healthcheck_retries }}"
  start_period: "{{ grafana_healthcheck_start_period }}"
```

Curl image pin already in use across Phase 1-4: `curlimages/curl:8.10.1`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold roles/grafana/ -- defaults, tasks/main.yml, handlers, meta, grafana.ini.j2, README</name>
  <files>
    roles/grafana/defaults/main.yml,
    roles/grafana/tasks/main.yml,
    roles/grafana/handlers/main.yml,
    roles/grafana/templates/grafana.ini.j2,
    roles/grafana/meta/main.yml,
    roles/grafana/README.md
  </files>
  <read_first>
    roles/alertmanager/defaults/main.yml (closest structural analog: knob layout, image-pin/healthcheck/timezone/restart/curl-image sections, conditional-healthcheck pattern, publish_host knob shape with false/true/127.0.0.1 trinary),
    roles/alertmanager/tasks/main.yml (docker_image pull -> docker_container run -> flush_handlers -> include verify.yml shape; Gate 7 labels stanza; conditional-healthcheck omit pattern; parent-dir bind mount per Gate 8),
    roles/alertmanager/handlers/main.yml (single-handler `Docker restart <role>` shape -- listen pattern, changed_when: true),
    roles/alertmanager/meta/main.yml (galaxy_info shape -- author Rock Martel-Langlois, license MIT, min_ansible_version 2.15, platforms, galaxy_tags),
    roles/alertmanager/README.md (OPS-03 canonical README schema),
    inventory/example-homelab/group_vars/all/network.yml (telemetron_network/telemetron_publish_default/telemetron_tz defaults),
    inventory/example-homelab/group_vars/all/storage.yml (telemetron_volume_prefix/telemetron_config_root defaults),
    .planning/phases/05-ui-plane/05-CONTEXT.md (D-70..D-90 -- authoritative for design choices),
    .planning/phases/05-ui-plane/05-RESEARCH.md §2.1 (Grafana container identity, HEALTHCHECK absence, env vars, SQLite default, anonymous viewer config, grafana.ini template skeleton; ALL section content),
    .planning/phases/05-ui-plane/05-RESEARCH.md §6.1 (INSPQ audit -- exhaustive list of FR strings to translate and INSPQ-isms to DROP),
    roles/prometheus/defaults/main.yml (prometheus_http_port: 9090 -- referenced for datasource URL templating in Task 2),
    roles/loki/defaults/main.yml (loki_http_port -- referenced for datasource URL templating in Task 2),
    roles/tempo/defaults/main.yml (tempo_http_port: 3200 -- referenced for datasource URL templating in Task 2),
    roles/mimir/defaults/main.yml (mimir_http_port: 9009 -- referenced for datasource URL templating in Task 2)
  </read_first>
  <action>
Create six files under `roles/grafana/`. All paths absolute from project root.

**(A) `roles/grafana/defaults/main.yml`** -- full content:

```yaml

# roles/grafana/defaults/main.yml
# Grafana OSS 13.0.1 role tunables. Override per-environment in
# inventory/<env>/group_vars/all/grafana.yml.
#
# UI plane role -- D-82 exempts from telemetron_publish_default:false
# (UI must be reachable from the browser without inventory edits).
# Container runs as GF_UID=472 (image default); DO NOT override `user:`.
# Embedded SQLite at /var/lib/grafana/grafana.db on telemetron_grafana_data
# volume (D-04 / UI-01). HEALTHCHECK is explicit -- image ships none
# (RESEARCH §2.1).

# --- Image pin (OPS-01; UI-01) ---
# OSS variant explicitly -- avoids Enterprise feature gates.
grafana_image: grafana/grafana-oss
grafana_image_tag: "13.0.1"

# --- Container identity ---
grafana_container_name: telemetron-grafana

# --- Host port publishing (D-82 inverts D-30 for UI plane) ---
# UI must be browser-reachable. Operator flips to false + runs reverse
# proxy on the telemetron bridge for production. true/false/127.0.0.1
# trinary same as alertmanager_publish_host.
grafana_publish_host: true
grafana_bind_address: 0.0.0.0   # D-83

# --- Port matrix (D-84) ---
grafana_http_port: 3000

# --- Storage layout (D-16, D-17, D-18) ---
grafana_data_volume: "{{ telemetron_volume_prefix | default('telemetron') }}_grafana_data"
grafana_data_path: /var/lib/grafana                     # image VOLUME, container UID 472 owns
grafana_config_dir: "{{ telemetron_config_root | default('/opt/telemetron') }}/grafana"
grafana_config_container_path: /etc/grafana
grafana_provisioning_container_path: /etc/grafana/provisioning

# --- Admin user / access model (D-86, D-87, D-88, D-89) ---
grafana_admin_user: admin
grafana_admin_email: "admin@telemetron.local"
# grafana_admin_password -- defined by operator inventory (D-86); no default here.
# RESEARCH Risk 2: env var is FIRST-BOOT-ONLY. Document in README.

grafana_anonymous_enabled: false                        # D-87 default off
grafana_default_theme: dark

# --- Datasource backend hostnames (telemetron bridge DNS) ---
# Sourced from the corresponding role's container_name. Overridable here
# only if a downstream operator runs split-stack and exposes a different DNS.
grafana_prometheus_url: "http://prometheus:9090"
grafana_loki_url: "http://loki:3100"
grafana_tempo_url: "http://tempo:3200"
grafana_mimir_url: "http://mimir:9009/prometheus"       # Mimir Prometheus-compatible subpath; D-26 multitenancy off

# --- Dashboard provisioning (D-76) ---
# Role-shipped JSONs live under roles/grafana/files/dashboards/ and are
# copied to {{ grafana_config_dir }}/provisioning/dashboards/telemetron/.
# Operator drop-in dir is a host path conditionally mounted into the
# container at /etc/grafana/provisioning/dashboards/operator (D-76).
grafana_dashboard_extra_dir: ""                         # empty = no operator drop-in mount

# --- Operator extension knobs (sorted-keys per D-20) ---
grafana_extra_datasources: []                           # operator-supplied datasources to merge
grafana_extra_env: {}                                   # extra container env vars (operator escape hatch)

# --- Healthcheck (RESEARCH §2.1 Risk 3: image has NO built-in HEALTHCHECK; explicit required) ---
# busybox base means wget is available (no distroless concern).
grafana_healthcheck_enabled: true
grafana_healthcheck_test: ["CMD-SHELL", "wget --spider -q http://localhost:3000/api/health || exit 1"]
grafana_healthcheck_interval: 15s
grafana_healthcheck_timeout: 5s
grafana_healthcheck_retries: 5
grafana_healthcheck_start_period: 60s                   # generous -- first-boot SQLite init

# --- Verify pre-poll (D-10a) ---
# 90s total budget = 45 retries x 2s. First-boot Grafana initializes
# SQLite + runs provisioning before HTTP server opens.
grafana_health_retries: 45
grafana_health_delay: 2

# --- Restart policy (OPS-06) ---
grafana_restart_policy: unless-stopped

# --- Resource limits ---
grafana_memory_limit: 512m

# --- Network ---
grafana_network: "{{ telemetron_network | default('telemetron') }}"

# --- Timezone (OPS-06; Pitfall 6) ---
grafana_tz: "{{ telemetron_tz | default('Etc/UTC') }}"

# --- Curl image pin for verify (OPS-01) ---
grafana_curl_image: curlimages/curl
grafana_curl_image_tag: "8.10.1"
```

**(B) `roles/grafana/tasks/main.yml`** -- full content:

```yaml

# roles/grafana/tasks/main.yml
# Grafana OSS 13.0.1 deploy + in-network verify.
# Mirrors roles/alertmanager/tasks/main.yml shape modulo Grafana
# specifics: three nested config subdirs under /opt/telemetron/grafana
# (provisioning/datasources, provisioning/dashboards, provisioning/dashboards/telemetron),
# rendered grafana.ini + two YAML manifests + seven JSON dashboards,
# parent-dir bind mount of /opt/telemetron/grafana -> /etc/grafana
# (Gate 8), env-injected GF_SECURITY_ADMIN_* (RESEARCH §2.1 first-boot-only),
# explicit HEALTHCHECK (image has none -- RESEARCH Risk 3).

- name: Ensure grafana config dirs exist
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    mode: "0755"
  loop:
    - "{{ grafana_config_dir }}"
    - "{{ grafana_config_dir }}/provisioning"
    - "{{ grafana_config_dir }}/provisioning/datasources"
    - "{{ grafana_config_dir }}/provisioning/dashboards"
    - "{{ grafana_config_dir }}/provisioning/dashboards/telemetron"
  tags:
    - grafana
    - grafana-config

- name: Render grafana.ini
  ansible.builtin.template:
    src: grafana.ini.j2
    dest: "{{ grafana_config_dir }}/grafana.ini"
    mode: "0644"
  notify: restart grafana
  tags:
    - grafana
    - grafana-config

- name: Render datasources.yaml (UI-02 + UI-04; D-77/D-79/D-80)
  ansible.builtin.template:
    src: datasources/datasources.yaml.j2
    dest: "{{ grafana_config_dir }}/provisioning/datasources/datasources.yaml"
    mode: "0644"
  # No notify -- Grafana hot-reloads provisioning files (RESEARCH §2.1).
  tags:
    - grafana
    - grafana-config

- name: Render dashboards.yaml provisioning manifest (D-76)
  ansible.builtin.template:
    src: dashboards/dashboards.yaml.j2
    dest: "{{ grafana_config_dir }}/provisioning/dashboards/dashboards.yaml"
    mode: "0644"
  # No notify -- Grafana hot-reloads provisioning files.
  tags:
    - grafana
    - grafana-config

- name: Copy role-shipped dashboard JSONs (D-75 -- exactly 7)
  ansible.builtin.copy:
    src: "dashboards/{{ item }}"
    dest: "{{ grafana_config_dir }}/provisioning/dashboards/telemetron/{{ item }}"
    mode: "0644"
  loop:
    - host-health.json
    - loki-explore-landing.json
    - tempo-explore-landing.json
    - otel-collector-self-metrics.json
    - loki-self-metrics.json
    - tempo-self-metrics.json
    - mimir-self-metrics.json
  tags:
    - grafana
    - grafana-config

- name: Ensure telemetron_grafana_data named volume exists (D-16; UI-01)
  community.docker.docker_volume:
    name: "{{ grafana_data_volume }}"
    state: present
  tags:
    - grafana

- name: Pull grafana image
  community.docker.docker_image:
    name: "{{ grafana_image }}:{{ grafana_image_tag }}"
    source: pull
    force_source: false
  tags:
    - grafana

- name: Run grafana container (explicit HEALTHCHECK, env-injected admin, parent-dir bind mount)
  community.docker.docker_container:
    name: "{{ grafana_container_name }}"
    image: "{{ grafana_image }}:{{ grafana_image_tag }}"
    state: started
    recreate: false
    restart_policy: "{{ grafana_restart_policy }}"
    memory: "{{ grafana_memory_limit }}"
    networks:
      - name: "{{ grafana_network }}"
        aliases:
          - "{{ grafana_container_name }}"
          - grafana
    # Gate 7: telemetron label stamp (INGEST-07 / Plan 03-05).
    labels:
      org.telemetron.service: telemetron
      org.telemetron.job: grafana
    # D-82 + D-83 + D-84: UI plane is browser-reachable by default.
    # Bind-address knob lets security-conscious operators clamp to loopback.
    published_ports: >-
      {{
        (
          [ grafana_bind_address + ':' + grafana_http_port | string + ':' + grafana_http_port | string ]
          if grafana_publish_host is sameas true else
          [ '127.0.0.1:' + grafana_http_port | string + ':' + grafana_http_port | string ]
          if grafana_publish_host == '127.0.0.1' else
          []
        )
      }}
    mounts:
      - source: "{{ grafana_data_volume }}"
        target: "{{ grafana_data_path }}"
        type: volume
      # Gate 8 parent-directory bind: container path /etc/grafana receives
      # grafana.ini AND provisioning/ tree. Single mount, single inode
      # surface -- no moby/moby#6011 stale-inode class.
      - source: "{{ grafana_config_dir }}"
        target: "{{ grafana_config_container_path }}"
        type: bind
        read_only: true
    # Conditional operator drop-in: when grafana_dashboard_extra_dir is
    # set, bind-mount its parent dir at /etc/grafana/provisioning/dashboards/operator.
    # Implemented as a Jinja-conditional second mount; absent when knob empty.
    # NOTE: keep this list of mounts under the same `mounts:` key by using
    # a single Jinja-expanded literal in the role action if needed.
    # Healthcheck conditional-omit (mirrors alertmanager pattern; RESEARCH Risk 3).
    healthcheck: "{{ grafana_container_healthcheck if (grafana_healthcheck_enabled | bool) else omit }}"
    env:
      # RESEARCH §2.1 Risk 2: GF_SECURITY_ADMIN_PASSWORD is FIRST-BOOT-ONLY.
      # Documented in README "Secrets" subsection.
      GF_SECURITY_ADMIN_USER: "{{ grafana_admin_user }}"
      GF_SECURITY_ADMIN_PASSWORD: "{{ grafana_admin_password }}"
      GF_SECURITY_ADMIN_EMAIL: "{{ grafana_admin_email }}"
      GF_AUTH_ANONYMOUS_ENABLED: "{{ grafana_anonymous_enabled | string | lower }}"
      GF_AUTH_ANONYMOUS_ORG_ROLE: "Viewer"
      GF_AUTH_ANONYMOUS_ORG_NAME: "Main Org."
      GF_USERS_DEFAULT_THEME: "{{ grafana_default_theme }}"
      TZ: "{{ grafana_tz }}"
  vars:
    grafana_container_healthcheck:
      test: "{{ grafana_healthcheck_test }}"
      interval: "{{ grafana_healthcheck_interval }}"
      timeout: "{{ grafana_healthcheck_timeout }}"
      retries: "{{ grafana_healthcheck_retries }}"
      start_period: "{{ grafana_healthcheck_start_period }}"
  tags:
    - grafana
    - grafana-container

# Flush handlers before verify (Bug 1 TIER 2 + Bug 2 belt pattern from Phase 4).
- name: Flush handlers before verify (so verify sees post-restart state)
  ansible.builtin.meta: flush_handlers
  tags:
    - grafana
    - grafana-verify

# Verify MUST be the final task (D-32). Mirrors roles/alertmanager/tasks/main.yml.
- name: Verify grafana end-to-end (HEALTHCHECK + datasource UIDs + Gate 9 D-73 datasources-resolve-real-data)
  ansible.builtin.include_tasks: verify.yml
  tags:
    - grafana
    - grafana-verify
```

NOTE on the operator drop-in mount: the `mounts:` list above shows the two always-present mounts. If `grafana_dashboard_extra_dir != ""`, an additional mount of `{{ grafana_dashboard_extra_dir }}` -> `/etc/grafana/provisioning/dashboards/operator` (ro) must be appended. Two valid implementation approaches; executor chooses:
  (i) Jinja-expand the entire `mounts:` value as a list comprehension with conditional append, OR
  (ii) Split into two separate `community.docker.docker_container` invocations gated by `when:`, with the second one used only when extra_dir is set (NOT recommended -- two containers can't share the same name).
Recommended: approach (i) -- build the mounts list in `vars:` and reference it from `mounts:`. The executor MUST keep `state: started` + `recreate: false` so re-runs are idempotent.

**(C) `roles/grafana/handlers/main.yml`** -- full content:

```yaml

# roles/grafana/handlers/main.yml
# Per D-19 / Pitfall 8: container restarts on grafana.ini changes use
# `docker restart <name>`. Datasource / dashboard provisioning files
# hot-reload without restart (RESEARCH §2.1), so they don't notify here.
#
# tasks/main.yml renders templates/grafana.ini.j2 with
# `notify: restart grafana`. Re-rendering on knob change triggers this
# handler which docker-restarts the container so the new ini is loaded.

- name: Docker restart grafana
  ansible.builtin.command:
    cmd: "docker restart {{ grafana_container_name }}"
  changed_when: true
  listen: restart grafana
```

**(D) `roles/grafana/templates/grafana.ini.j2`** -- full content. Use the RESEARCH §2.1 minimal template verbatim:

```ini
# {{ ansible_managed }}
# Grafana OSS 13.0.1 -- Telemetron M1 default config.
# Security/admin settings driven by GF_ env vars (RESEARCH §2.1 -- env > ini
# precedence avoids the ini-vs-env-var precedence-confusion surface).
# This file covers structural config: paths, server bind, sqlite store,
# default UI theme, anonymous-viewer toggle (D-87).

[paths]
data = /var/lib/grafana
logs = /var/log/grafana
plugins = /var/lib/grafana/plugins
provisioning = /etc/grafana/provisioning

[server]
http_port = {{ grafana_http_port }}
http_addr = 0.0.0.0

[database]
# UI-01: embedded SQLite on telemetron_grafana_data volume.
type = sqlite3
path = grafana.db

[users]
default_theme = {{ grafana_default_theme }}

[auth.anonymous]
# D-87: default off. Operator flips grafana_anonymous_enabled: true for
# wall-display / TV-mode kiosks. GF_AUTH_ANONYMOUS_* env vars also set
# (env > ini precedence) -- this ini section ensures the value survives
# the env-var first-boot-only edge case for anonymous toggling.
enabled = {{ grafana_anonymous_enabled | string | lower }}
org_role = Viewer
org_name = Main Org.
```

**(E) `roles/grafana/meta/main.yml`** -- full content (mirror alertmanager shape):

```yaml

galaxy_info:
  role_name: grafana
  author: Rock Martel-Langlois
  description: >
    Deploys Grafana OSS 13.0.1 on the telemetron Docker bridge network
    with embedded SQLite backing store on a persistent named volume,
    four datasources provisioned at hardcoded UIDs (prometheus, loki,
    tempo, mimir), seven curated starter dashboards (host health, Loki
    Explore landing, Tempo Explore landing, OTel Collector self-metrics,
    Loki/Tempo/Mimir self-metrics), and trace-to-logs correlation wired
    via tracesToLogsV2 + a structured-metadata trace_id derived field.
    Ships UI-01, UI-02, UI-03, UI-04.
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Ubuntu
      versions: [jammy, noble]
    - name: Debian
      versions: [bookworm]
  galaxy_tags:
    - dashboards
    - grafana
    - observability
    - telemetron
    - ui

dependencies: []

collections:
  - community.docker
  - ansible.builtin
```

**(F) `roles/grafana/README.md`** -- full content following the OPS-03 schema mirrored from roles/alertmanager/README.md. Sections in order:

1. **Title + one-paragraph description.** State image pin, port, role-shipped artifacts (datasources + 7 dashboards), and UI-01/02/03/04 traceability. Cross-link Karma (Phase 5 alert UX) + PromLens (deprecation candidate) per CONTEXT.md.

2. **What this role does** (numbered list mirroring alertmanager README): creates config tree, renders grafana.ini + datasources.yaml + dashboards.yaml, copies 7 dashboard JSONs, ensures named volume, runs container with parent-dir bind + explicit HEALTHCHECK + Gate-7 labels + GF_ env injection, runs verify suite incl. Gate 9.

3. **Variables** -- full table mirroring alertmanager README format. Document every knob in defaults/main.yml.

4. **Secrets** subsection with HONEST first-boot-only doc (RESEARCH §2.1 Risk 2). Verbatim text:

   > `grafana_admin_password` is consumed via the `GF_SECURITY_ADMIN_PASSWORD` env var. **Grafana sets the admin password when it initializes the database on first startup.** Subsequent restarts with a different env-var value do NOT update the stored password (this is documented Grafana behavior, not a Telemetron defect). To rotate the admin password after the initial deploy:
   >
   > ```bash
   > docker exec telemetron-grafana grafana-cli admin reset-admin-password '<new-password>'
   > ```
   >
   > Then update the inventory `grafana_admin_password` to match (so future fresh deploys + verify Gate 9 use the new password).

5. **Tags** -- list `grafana`, `grafana-config`, `grafana-container`, `grafana-verify`.

6. **Modes** -- single mode (embedded SQLite single-instance). Postgres / HA Grafana deferred to a future milestone (PROJECT.md).

7. **Volumes** -- table:

   | Volume | Mount | Purpose |
   |--------|-------|---------|
   | `telemetron_grafana_data` (named) | `/var/lib/grafana` | Embedded SQLite (dashboards SQLite, sessions, plugins cache). Persistent. |
   | `/opt/telemetron/grafana` (bind, ro) | `/etc/grafana` | Rendered config tree: `grafana.ini`, `provisioning/datasources/datasources.yaml`, `provisioning/dashboards/dashboards.yaml`, `provisioning/dashboards/telemetron/*.json`. Parent-dir bind per Gate 8. |
   | `{{ grafana_dashboard_extra_dir }}` (optional bind, ro) | `/etc/grafana/provisioning/dashboards/operator` | Operator drop-in dir (D-76). Only mounted when knob is set. |

8. **Healthcheck** -- explicit (image has none per RESEARCH §2.1); show the YAML block from defaults; `docker inspect` verify command.

9. **Access model** subsection covering:
   - Login required by default (`grafana_anonymous_enabled: false`); operator flips to `true` for wall-display kiosk mode (D-87).
   - Admin user `admin` + email `admin@telemetron.local` (D-88); SSO/LDAP deferred (post-M1).
   - Stock `Main Org.` (D-89); multi-org provisioning deferred.

10. **Network -- Host publishing** subsection: D-82 inverts D-30 for UI plane. Operator can flip `grafana_publish_host: false` and bind via reverse proxy (see Reverse proxy section).

11. **Network -- Bind address** subsection: D-83 default `0.0.0.0`; loopback `127.0.0.1` option for ssh-tunnel-only access; security trade-off documented.

12. **Reverse proxy** subsection (D-85): document the Caddy/Traefik/nginx pattern -- flip `grafana_publish_host: false`, run reverse proxy on the telemetron bridge (or host network), proxy `https://grafana.example.com -> http://grafana:3000`. Note `<!-- TODO Phase 6: docs/quickstart.md cross-link -->`.

13. **Datasources** subsection: enumerate the four hardcoded UIDs (D-77). Document the Mimir = `prometheus` datasource type pointed at `:9009/prometheus` (D-26 multitenancy off -- no X-Scope-OrgID header).

14. **Dashboards** subsection: list the 7 role-shipped dashboards (D-75) with folder name `Telemetron`. Document the operator drop-in dir at `/etc/grafana/provisioning/dashboards/operator` (D-76).

15. **Trace-to-logs correlation** subsection (D-78 honest framing): "Trace-to-logs correlation works for apps instrumented with an OTel SDK that push OTLP-native logs to the OTel Collector (`:4318`). Loki 3.7.2 stores `trace_id` as structured metadata on those records. FB-tailed Docker stdout/stderr logs do NOT carry `trace_id` unless the app embeds it in the log body. Clicking 'View in Tempo' on a Loki log line with `trace_id` structured metadata navigates to the corresponding trace in Tempo Explore (D-79 derivedFields). Clicking 'Logs for this span' on a Tempo trace navigates to Loki Explore with `{service_name='<svc>'} | trace_id='<id>'` pre-applied (D-79 + D-80 customQuery)." Reference 999.4 backlog as future option (D-81).

16. **Label mapping** subsection (D-81 documenting reality): "Live Loki labels are `{host, job, service_name}`. The Phase-3 spec'd `service` label is surfaced as `service_name` because the OTel Collector's `otlphttp/loki` exporter promotes the `service.name` resource attribute to `service_name` (Loki 3.x OTLP-native ingestion default). Telemetron M1 accepts this and standardises on `service_name`; D-80 forwards `service.name -> service_name` as the only `tracesToLogsV2` tag. A future post-M1 milestone may retrofit canonical `service` naming via an OTel exporter relabel rule -- see ROADMAP.md §999.4."

17. **Security** subsection: single-host bridge trust boundary (D-26 / D-66 / D-90); no inbound auth on backend datasources (Loki/Tempo/Mimir/Prometheus all auth-disabled on the telemetron bridge); admin password env-injected; READMe links to the password-rotation doc above.

18. **Idempotency** subsection: second back-to-back run = `changed=0` (OPS-04); container restarts via handler-on-config-change, never `state: restarted`.

19. **Per-role port-acceptance gates** subsection: confirm Gates 1-9 apply. Gate 9 (D-73) is documented in `roles/README.md`.

20. **Deviations from upstream INSPQ** subsection -- D-25 audit summary citing RESEARCH §6.1. Headline: dropped `community.grafana.grafana_datasource`/`grafana_folder` API-call provisioning in favor of file provisioning (idempotent, no race with Grafana startup, survives `docker volume rm`); dropped SMTP, LDAP, `files/ldap.toml`, `grafana_plugins`, Kubernetes/Helm tasks, `application_web_docker` dep, INSPQ SMTP defaults, `America/Toronto` TZ, FR-language task names. Image pin moved from `grafana/grafana:latest` to `grafana/grafana-oss:13.0.1`.
  </action>
  <verify>
    <automated>
bash -c '
set -e
test -f roles/grafana/defaults/main.yml
test -f roles/grafana/tasks/main.yml
test -f roles/grafana/handlers/main.yml
test -f roles/grafana/templates/grafana.ini.j2
test -f roles/grafana/meta/main.yml
test -f roles/grafana/README.md
grep -q "grafana_image_tag: \"13.0.1\"" roles/grafana/defaults/main.yml
grep -q "grafana_publish_host: true" roles/grafana/defaults/main.yml
grep -q "grafana_data_volume:" roles/grafana/defaults/main.yml
grep -q "grafana_admin_user: admin" roles/grafana/defaults/main.yml
grep -q "admin@telemetron.local" roles/grafana/defaults/main.yml
grep -q "grafana_anonymous_enabled: false" roles/grafana/defaults/main.yml
grep -q "wget --spider -q http://localhost:3000/api/health" roles/grafana/defaults/main.yml
grep -q "grafana_health_retries: 45" roles/grafana/defaults/main.yml
grep -q "org.telemetron.service: telemetron" roles/grafana/tasks/main.yml
grep -q "org.telemetron.job: grafana" roles/grafana/tasks/main.yml
grep -q "GF_SECURITY_ADMIN_PASSWORD" roles/grafana/tasks/main.yml
grep -q "GF_AUTH_ANONYMOUS_ENABLED" roles/grafana/tasks/main.yml
grep -q "telemetron_grafana_data" roles/grafana/tasks/main.yml
grep -q "/etc/grafana" roles/grafana/tasks/main.yml
grep -q "include_tasks: verify.yml" roles/grafana/tasks/main.yml
grep -q "flush_handlers" roles/grafana/tasks/main.yml
grep -q "listen: restart grafana" roles/grafana/handlers/main.yml
grep -q "\[paths\]" roles/grafana/templates/grafana.ini.j2
grep -q "type = sqlite3" roles/grafana/templates/grafana.ini.j2
grep -q "MIT" roles/grafana/meta/main.yml
grep -q "grafana-cli admin reset-admin-password" roles/grafana/README.md
grep -q "tracesToLogsV2" roles/grafana/README.md
grep -q "service_name" roles/grafana/README.md
grep -q "Deviations from upstream INSPQ" roles/grafana/README.md
# Grep gates -- INSPQ-noise and non-ASCII
! grep -riE "inspq|qc\\.ca|montreal|québec|francais|french|/srv/nfs/inspq|vault_inspq|america/(toronto|montreal)|noreply@inspq|ldap.toml" roles/grafana/
! grep -rPn "[^\\x00-\\x7F]" roles/grafana/
echo "Task 1 scaffolding OK"
'
    </automated>
  </verify>
  <acceptance_criteria>
    - `roles/grafana/defaults/main.yml` exists and contains `grafana_image_tag: "13.0.1"`, `grafana_publish_host: true`, `grafana_data_volume:` (templated with telemetron_volume_prefix), `grafana_admin_user: admin`, `grafana_admin_email: "admin@telemetron.local"`, `grafana_anonymous_enabled: false`, the explicit healthcheck block, and `grafana_health_retries: 45`, `grafana_health_delay: 2`.
    - `roles/grafana/tasks/main.yml` exists and contains: (a) `community.docker.docker_volume` ensuring `{{ grafana_data_volume }}`, (b) `community.docker.docker_image` pulling `{{ grafana_image }}:{{ grafana_image_tag }}`, (c) `community.docker.docker_container` with `labels: { org.telemetron.service: telemetron, org.telemetron.job: grafana }`, (d) env keys `GF_SECURITY_ADMIN_USER`, `GF_SECURITY_ADMIN_PASSWORD`, `GF_SECURITY_ADMIN_EMAIL`, `GF_AUTH_ANONYMOUS_ENABLED`, `GF_AUTH_ANONYMOUS_ORG_ROLE: "Viewer"`, `GF_AUTH_ANONYMOUS_ORG_NAME: "Main Org."`, `TZ`, (e) parent-dir bind mount `{{ grafana_config_dir }} -> /etc/grafana`, (f) named-volume mount `{{ grafana_data_volume }} -> /var/lib/grafana`, (g) conditional healthcheck via `omit`, (h) `flush_handlers` task BEFORE `include_tasks: verify.yml`, (i) `include_tasks: verify.yml` is the FINAL task.
    - **Conditional operator drop-in mount (D-76):** the `community.docker.docker_container` task assembles its `mounts:` value from a vars-level list that conditionally appends a third entry when `grafana_dashboard_extra_dir != ""`. Verifiable: when `grafana_dashboard_extra_dir` is empty, `docker inspect telemetron-grafana --format '{{json .Mounts}}'` shows exactly 2 mounts (the named volume + the parent-dir config bind); when `grafana_dashboard_extra_dir` is set to a non-empty path, the SAME task definition produces a third bind mount of that path at `/etc/grafana/provisioning/dashboards/operator` (ro), without requiring a separate `docker_container` invocation. Recommended implementation: `set_fact: grafana_mounts: "{{ base_mounts + ([operator_mount] if grafana_dashboard_extra_dir else []) }}"` then `mounts: "{{ grafana_mounts }}"` -- approach (i) per the NOTE in the Task 1 action. Approach (ii) (two separate `docker_container` invocations) is explicitly disallowed (can't share container_name).
    - `roles/grafana/handlers/main.yml` exists with single handler `Docker restart grafana` listening on `restart grafana`.
    - `roles/grafana/templates/grafana.ini.j2` exists with sections `[paths]`, `[server]`, `[database]` (`type = sqlite3`), `[users]`, `[auth.anonymous]`.
    - `roles/grafana/meta/main.yml` exists with `license: MIT`, `min_ansible_version: "2.15"`, `author: Rock Martel-Langlois`.
    - `roles/grafana/README.md` exists and contains the strings: `grafana-cli admin reset-admin-password` (Secrets/rotation), `tracesToLogsV2` (Trace-to-logs subsection), `service_name` (Label mapping), `Deviations from upstream INSPQ`, `Gate 9`, `null`-receiver-free (i.e. does not mention hook router as active scope).
    - Grep gate: `grep -riE 'inspq|qc\.ca|montreal|québec|francais|french|/srv/nfs/inspq|vault_inspq|america/(toronto|montreal)|noreply@inspq|ldap.toml' roles/grafana/` returns 0 matches (Gate 1).
    - Non-ASCII gate: `grep -rPn '[^\x00-\x7F]' roles/grafana/` returns 0 matches.
  </acceptance_criteria>
  <done>
    `roles/grafana/` exists with six committed files. The role can be parsed by ansible-playbook --syntax-check (no missing dependencies; tasks/verify.yml is referenced but Task 3 creates it). Image pin, healthcheck, Gate 7 label, parent-dir bind mount, GF_ env injection, and SQLite database type are all in place. INSPQ-noise and non-ASCII grep gates pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build the four-datasource provisioning template + dashboard provisioning manifest + seven UID-rewritten dashboard JSONs</name>
  <files>
    roles/grafana/templates/datasources/datasources.yaml.j2,
    roles/grafana/templates/dashboards/dashboards.yaml.j2,
    roles/grafana/files/dashboards/host-health.json,
    roles/grafana/files/dashboards/loki-explore-landing.json,
    roles/grafana/files/dashboards/tempo-explore-landing.json,
    roles/grafana/files/dashboards/otel-collector-self-metrics.json,
    roles/grafana/files/dashboards/loki-self-metrics.json,
    roles/grafana/files/dashboards/tempo-self-metrics.json,
    roles/grafana/files/dashboards/mimir-self-metrics.json,
    roles/grafana/files/_rewrite_uids.py
  </files>
  <read_first>
    .planning/phases/05-ui-plane/05-CONTEXT.md (D-74 hybrid source strategy, D-75 the exact 7 dashboards, D-76 two-provider manifest YAML block verbatim, D-77 hardcoded UIDs, D-78 OTLP-only correlation scope, D-79 EXACT tracesToLogsV2 + derivedFields YAML blocks, D-80 service.name->service_name tag mapping, D-81 999.4 backlog acceptance),
    .planning/phases/05-ui-plane/05-RESEARCH.md §2.1 "Datasource Provisioning Schema (UI-02)" (full YAML for four datasources -- copy verbatim into the template; this is the authoritative source),
    .planning/phases/05-ui-plane/05-RESEARCH.md §2.1 "Dashboard Provisioning Schema (UI-03)" (full YAML for the two-provider manifest -- copy verbatim),
    .planning/phases/05-ui-plane/05-RESEARCH.md §3 "Dashboard Sourcing Inventory" (table of seven dashboards + per-dashboard UID-substitution procedure -- AUTHORITATIVE source for download URLs, tag pins, and substitution maps),
    .planning/phases/05-ui-plane/05-RESEARCH.md §3 "Hand-Rolled Landing Page Shape" (minimal Grafana 13 landing-page JSON template -- copy verbatim for loki-explore-landing.json and tempo-explore-landing.json),
    .planning/phases/05-ui-plane/05-RESEARCH.md §4 "Trace-to-Logs Deep Dive" (confirms D-79 YAML is correct as written; matcherType: label works for Loki 3.x structured metadata),
    .planning/phases/05-ui-plane/05-RESEARCH.md §7 Risk 1 (Dashboard UID template-variable substitution -- per-dashboard nuance),
    .planning/phases/05-ui-plane/05-RESEARCH.md §9 OQ-1 (Loki operational dashboard substitution -- Python rewrite recommended over sed),
    .planning/phases/05-ui-plane/05-RESEARCH.md §9 OQ-4 (Mimir overview dashboard uses `prometheus` UID, NOT `mimir` -- because Mimir's self-metrics are scraped by Prometheus),
    roles/grafana/defaults/main.yml (from Task 1: grafana_prometheus_url, grafana_loki_url, grafana_tempo_url, grafana_mimir_url -- the URL knobs the templates reference)
  </read_first>
  <action>

**(A) `roles/grafana/templates/datasources/datasources.yaml.j2`** -- full content (copy RESEARCH §2.1 schema verbatim; ALL FOUR datasources must include `editable: false` per RESEARCH; Tempo carries D-79 tracesToLogsV2 verbatim; Loki carries D-79 derivedFields verbatim; Mimir uses `type: prometheus` per RESEARCH §2.1 + §9 OQ-4):

```yaml
# {{ ansible_managed }}
# Grafana datasource provisioning -- UI-02 + UI-04.
# Hardcoded UIDs per D-77: prometheus, loki, tempo, mimir.
# These UIDs are referenced by the seven role-shipped dashboard JSONs;
# do not change without coordinating with files/dashboards/*.json.

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus
    url: {{ grafana_prometheus_url }}
    access: proxy
    isDefault: false
    editable: false
    jsonData:
      prometheusType: Prometheus
      prometheusVersion: "3.11.3"

  - name: Loki
    type: loki
    uid: loki
    url: {{ grafana_loki_url }}
    access: proxy
    editable: false
    # D-79: derivedFields for trace_id -> Tempo navigation.
    # matcherType: label works for Loki 3.x structured metadata fields
    # (RESEARCH §4 -- Grafana docs explicit: "any type of label - indexed,
    # parsed or structured metadata").
    jsonData:
      derivedFields:
        - name: trace_id
          matcherType: label
          matcherRegex: trace_id
          url: '${__value.raw}'
          datasourceUid: tempo
          urlDisplayLabel: 'View in Tempo'

  - name: Tempo
    type: tempo
    uid: tempo
    url: {{ grafana_tempo_url }}
    access: proxy
    editable: false
    # D-79 + D-80: tracesToLogsV2 customQuery. service.name -> service_name
    # is the only forwarded tag (D-80; matches OTel-native Loki label shape
    # per D-81 / 999.4 backlog acceptance).
    jsonData:
      tracesToLogsV2:
        datasourceUid: loki
        spanStartTimeShift: '-1h'
        spanEndTimeShift: '1h'
        tags:
          - key: 'service.name'
            value: 'service_name'
        filterByTraceID: false
        filterBySpanID: false
        customQuery: true
        query: '{${__tags}} | trace_id="${__span.traceId}"'
      serviceMap:
        datasourceUid: prometheus
      nodeGraph:
        enabled: true

  - name: Mimir
    type: prometheus
    uid: mimir
    url: {{ grafana_mimir_url }}
    access: proxy
    isDefault: false
    editable: false
    # D-26 multitenancy_enabled:false in Mimir -- no X-Scope-OrgID header.
    # Mimir exposes a Prometheus-compatible API at /prometheus subpath;
    # type: prometheus + url: http://mimir:9009/prometheus is correct
    # (NOT type: mimir; Grafana has no first-party mimir datasource type).
    jsonData:
      prometheusType: Mimir

{% for ds in grafana_extra_datasources | default([]) | sort(attribute='name') %}
  - {{ ds | to_nice_yaml(indent=2) | indent(4) }}
{% endfor %}
```

**(B) `roles/grafana/templates/dashboards/dashboards.yaml.j2`** -- full content (copy RESEARCH §2.1 dashboard provisioning schema verbatim):

```yaml
# {{ ansible_managed }}
# Grafana dashboard provisioning -- D-76 two-provider manifest.
# telemetron folder: role-shipped, read-only (disableDeletion:true / allowUiUpdates:false).
# operator folder: operator drop-in (writable; only effective when grafana_dashboard_extra_dir is set).

apiVersion: 1

providers:
  - name: telemetron
    type: file
    folder: Telemetron
    disableDeletion: true
    updateIntervalSeconds: 30
    allowUiUpdates: false
    options:
      path: /etc/grafana/provisioning/dashboards/telemetron

  - name: operator
    type: file
    folder: Operator
    disableDeletion: false
    updateIntervalSeconds: 60
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/operator
```

**(C) `roles/grafana/files/_rewrite_uids.py`** -- full content. Idempotent Python helper that documents AND performs the upstream-mixin UID substitution at fork time. Committed alongside the JSONs so the substitution is reproducible/auditable, but NOT executed at runtime (D-74 "no runtime download"). The script's `main()` reads upstream files from a local checkout dir (operator-supplied via env vars or argv) and writes the substituted form to `roles/grafana/files/dashboards/`. Executor runs this script ONCE during plan execution to generate the five upstream-derived JSONs; commits both the script and its generated outputs.

```python
#!/usr/bin/env python3
"""
roles/grafana/files/_rewrite_uids.py

Fork-time helper for substituting upstream dashboard datasource template
variables for Telemetron's hardcoded UIDs (D-77 + RESEARCH §3 per-dashboard
substitution map).

This script is committed alongside the JSONs it generates so the
substitution is reproducible. It is NOT executed at container runtime
(D-74 "no runtime download" -- all seven JSONs land on disk pre-rewritten).

When upstream mixin shapes change in a future Telemetron version, refresh
the JSONs by re-downloading sources and re-running this script -- a
deliberate maintenance task, not a recurring role task.

Usage:
  python3 roles/grafana/files/_rewrite_uids.py \\
      --src-dir /tmp/upstream-dashboards \\
      --dest-dir roles/grafana/files/dashboards

The --src-dir is expected to contain the FIVE upstream JSONs
(unmodified, as fetched from their source URLs):
  - dashboard-1860-rev45.json            (Grafana.com 1860 rev 45 -- node-exporter-full)
  - dashboard-15983-rev29.json           (Grafana.com 15983 rev 29 -- OTel Collector)
  - dashboard-loki-operational.json      (grafana/loki v3.7.2)
  - tempo-operational.json               (grafana/tempo v2.10.5)
  - mimir-overview.json                  (grafana/mimir mimir-3.0.6)

Source URLs (RESEARCH §3 table):
  https://grafana.com/api/dashboards/1860/revisions/45/download
  https://grafana.com/api/dashboards/15983/revisions/29/download
  https://raw.githubusercontent.com/grafana/loki/v3.7.2/production/loki-mixin/dashboards/dashboard-loki-operational.json
  https://raw.githubusercontent.com/grafana/tempo/v2.10.5/operations/tempo-mixin/dashboards/tempo-operational.json
  https://raw.githubusercontent.com/grafana/mimir/mimir-3.0.6/operations/mimir-mixin-compiled/dashboards/mimir-overview.json
"""

import argparse
import json
import os
import re
import sys


# Per RESEARCH §3 table + OQ-4:
# All five upstream dashboards have datasource template variables that
# Telemetron substitutes for hardcoded UID references at fork time.
#
# OQ-4 NOTE: Mimir overview's $datasource is the PROMETHEUS scraper that
# collects Mimir's :9009/metrics self-metrics (NOT the Mimir datasource as
# long-term store). UID -> prometheus.
#
# Loki operational dashboard uses RAW STRINGS "$datasource" / "$loki_datasource"
# (no template vars defined in JSON) -- requires string-level replace,
# substituting in JSON objects per Type B procedure in RESEARCH §3.
SUBSTITUTIONS = {
    "dashboard-1860-rev45.json": {
        "output": "host-health.json",
        "type": "template_var",
        "vars": {"DS_PROMETHEUS": "prometheus"},
        "uid_refs": {"${DS_PROMETHEUS}": {"type": "prometheus", "uid": "prometheus"}},
    },
    "dashboard-15983-rev29.json": {
        "output": "otel-collector-self-metrics.json",
        "type": "template_var",
        "vars": {"datasource": "prometheus"},
        "uid_refs": {"${datasource}": {"type": "prometheus", "uid": "prometheus"}},
    },
    "dashboard-loki-operational.json": {
        "output": "loki-self-metrics.json",
        "type": "raw_string",
        "string_subs": {
            '"$datasource"': '{"type":"prometheus","uid":"prometheus"}',
            '"$loki_datasource"': '{"type":"loki","uid":"loki"}',
        },
    },
    "tempo-operational.json": {
        "output": "tempo-self-metrics.json",
        "type": "template_var",
        "vars": {"ds": "prometheus", "logsds": "loki"},
        "uid_refs": {
            "${ds}": {"type": "prometheus", "uid": "prometheus"},
            "${logsds}": {"type": "loki", "uid": "loki"},
        },
    },
    "mimir-overview.json": {
        "output": "mimir-self-metrics.json",
        "type": "template_var",
        # OQ-4: $datasource is the Prometheus scraper of Mimir self-metrics
        # (NOT the Mimir long-term store).
        "vars": {"datasource": "prometheus"},
        "uid_refs": {"${datasource}": {"type": "prometheus", "uid": "prometheus"}},
    },
}


def rewrite_template_var(src_path, dest_path, vars_map, uid_refs):
    """Type A: proper Grafana template variables.

    1. Load JSON.
    2. Remove the template variable entries from .templating.list whose .name
       matches a key in vars_map.
    3. Walk .panels recursively, replacing any datasource ref whose .uid
       matches the template-var form with the hardcoded {type, uid} dict.
    """
    with open(src_path) as f:
        d = json.load(f)

    # 1. Filter templating.list to remove the substituted datasource vars
    if "templating" in d and "list" in d["templating"]:
        d["templating"]["list"] = [
            v for v in d["templating"]["list"]
            if v.get("type") != "datasource" or v.get("name") not in vars_map
        ]

    # 2. Walk panels (and nested rows.panels) substituting datasource.uid
    def walk(obj):
        if isinstance(obj, dict):
            if "datasource" in obj and isinstance(obj["datasource"], dict):
                uid = obj["datasource"].get("uid")
                if uid in uid_refs:
                    obj["datasource"] = dict(uid_refs[uid])
            # Older format: datasource is a string
            if "datasource" in obj and isinstance(obj["datasource"], str):
                if obj["datasource"] in uid_refs:
                    obj["datasource"] = dict(uid_refs[obj["datasource"]])
            for v in obj.values():
                walk(v)
        elif isinstance(obj, list):
            for v in obj:
                walk(v)

    walk(d)

    with open(dest_path, "w") as f:
        json.dump(d, f, indent=2, sort_keys=True)
        f.write("\n")


def rewrite_raw_string(src_path, dest_path, string_subs):
    """Type B: raw-string substitution for jsonnet-generated dashboards
    that left literal `"$datasource"` strings without template vars."""
    with open(src_path) as f:
        content = f.read()
    for src, replacement in string_subs.items():
        content = content.replace(src, replacement)
    # Re-parse + pretty-print to confirm validity
    d = json.loads(content)
    with open(dest_path, "w") as f:
        json.dump(d, f, indent=2, sort_keys=True)
        f.write("\n")


def main():
    parser = argparse.ArgumentParser(description=__doc__, formatter_class=argparse.RawDescriptionHelpFormatter)
    parser.add_argument("--src-dir", required=True, help="Directory containing the FIVE upstream JSONs.")
    parser.add_argument("--dest-dir", default="roles/grafana/files/dashboards", help="Output directory for rewritten JSONs.")
    args = parser.parse_args()

    os.makedirs(args.dest_dir, exist_ok=True)

    for src_name, cfg in SUBSTITUTIONS.items():
        src_path = os.path.join(args.src_dir, src_name)
        dest_path = os.path.join(args.dest_dir, cfg["output"])
        if not os.path.exists(src_path):
            print(f"SKIP (source missing): {src_path}", file=sys.stderr)
            continue
        if cfg["type"] == "template_var":
            rewrite_template_var(src_path, dest_path, cfg["vars"], cfg["uid_refs"])
        elif cfg["type"] == "raw_string":
            rewrite_raw_string(src_path, dest_path, cfg["string_subs"])
        print(f"WROTE: {dest_path}")


if __name__ == "__main__":
    main()
```

**(D) `roles/grafana/files/dashboards/loki-explore-landing.json`** -- full content. Use RESEARCH §3 hand-rolled stub as starting point but ensure structural validity for Grafana 13:

```json
{
  "annotations": { "list": [] },
  "editable": false,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "panels": [
    {
      "type": "text",
      "title": "Loki Explore -- Quick Links",
      "gridPos": { "h": 10, "w": 24, "x": 0, "y": 0 },
      "datasource": { "type": "datasource", "uid": "-- Grafana --" },
      "options": {
        "mode": "markdown",
        "content": "## Loki Explore\n\nClick a link to open Loki Explore filtered to a common Telemetron stream.\n\n- **All Telemetron stack logs**: [`{job=\"telemetron\"}`](/explore?orgId=1&left=%7B%22datasource%22%3A%22loki%22%2C%22queries%22%3A%5B%7B%22refId%22%3A%22A%22%2C%22expr%22%3A%22%7Bjob%3D%5C%22telemetron%5C%22%7D%22%7D%5D%2C%22range%22%3A%7B%22from%22%3A%22now-1h%22%2C%22to%22%3A%22now%22%7D%7D)\n- **Per-service shortcuts**: `{service_name=\"telemetron-grafana\"}`, `{service_name=\"telemetron-loki\"}`, `{service_name=\"telemetron-prometheus\"}`, `{service_name=\"telemetron-alertmanager\"}`.\n- **Errors only**: `{job=\"telemetron\"} |= \"error\" | json | __error__ = \"\"`.\n\n### Trace-to-logs\n\nLogs from OTel-SDK-instrumented apps carry `trace_id` as structured metadata. Click the **View in Tempo** link beside any log line containing `trace_id` to jump to the matching trace.\n\nSee `roles/grafana/README.md` -> Trace-to-logs correlation for the honest scope (FB-tailed Docker stdout/stderr does NOT carry `trace_id` unless the app embeds it)."
      }
    }
  ],
  "refresh": "",
  "schemaVersion": 39,
  "tags": ["telemetron", "loki"],
  "templating": { "list": [] },
  "time": { "from": "now-1h", "to": "now" },
  "timepicker": {},
  "timezone": "browser",
  "title": "Loki -- Explore Landing",
  "uid": "telemetron-loki-explore",
  "version": 1
}
```

**(E) `roles/grafana/files/dashboards/tempo-explore-landing.json`** -- mirror (D) with Tempo specifics:

```json
{
  "annotations": { "list": [] },
  "editable": false,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "panels": [
    {
      "type": "text",
      "title": "Tempo Explore -- Quick Links",
      "gridPos": { "h": 10, "w": 24, "x": 0, "y": 0 },
      "datasource": { "type": "datasource", "uid": "-- Grafana --" },
      "options": {
        "mode": "markdown",
        "content": "## Tempo Explore\n\nOpen [Tempo Explore](/explore?orgId=1&left=%7B%22datasource%22%3A%22tempo%22%2C%22queries%22%3A%5B%7B%22refId%22%3A%22A%22%2C%22queryType%22%3A%22traceqlSearch%22%2C%22filters%22%3A%5B%5D%7D%5D%2C%22range%22%3A%7B%22from%22%3A%22now-1h%22%2C%22to%22%3A%22now%22%7D%7D) to search recent traces.\n\n### Common queries\n\n- **All spans from a service**: TraceQL `{ resource.service.name = \"<service>\" }`\n- **Slow requests**: `{ duration > 1s }`\n- **Errors**: `{ status = error }`\n\n### Trace-to-logs\n\nClick a trace span and use the **Logs for this span** link to pivot to Loki Explore with `{service_name=\"<svc>\"} | trace_id=\"<id>\"` pre-applied (D-79 + D-80). The link works only for OTel-SDK-instrumented apps whose logs carry `trace_id` in structured metadata."
      }
    }
  ],
  "refresh": "",
  "schemaVersion": 39,
  "tags": ["telemetron", "tempo"],
  "templating": { "list": [] },
  "time": { "from": "now-1h", "to": "now" },
  "timepicker": {},
  "timezone": "browser",
  "title": "Tempo -- Explore Landing",
  "uid": "telemetron-tempo-explore",
  "version": 1
}
```

**(F) The five upstream-derived dashboard JSONs:** `host-health.json`, `otel-collector-self-metrics.json`, `loki-self-metrics.json`, `tempo-self-metrics.json`, `mimir-self-metrics.json`.

The executor performs these substeps **once** during plan execution (this is a one-time port operation, NOT a recurring role task):

1. Create a scratch dir at `/tmp/telemetron-upstream-dashboards/` (or similar; doesn't matter -- gitignored).

2. Download the five upstream sources via `curl -fsSL`:

   ```bash
   mkdir -p /tmp/telemetron-upstream-dashboards
   cd /tmp/telemetron-upstream-dashboards
   curl -fsSL -o dashboard-1860-rev45.json https://grafana.com/api/dashboards/1860/revisions/45/download
   curl -fsSL -o dashboard-15983-rev29.json https://grafana.com/api/dashboards/15983/revisions/29/download
   curl -fsSL -o dashboard-loki-operational.json https://raw.githubusercontent.com/grafana/loki/v3.7.2/production/loki-mixin/dashboards/dashboard-loki-operational.json
   curl -fsSL -o tempo-operational.json https://raw.githubusercontent.com/grafana/tempo/v2.10.5/operations/tempo-mixin/dashboards/tempo-operational.json
   curl -fsSL -o mimir-overview.json https://raw.githubusercontent.com/grafana/mimir/mimir-3.0.6/operations/mimir-mixin-compiled/dashboards/mimir-overview.json
   ```

3. Run the rewrite script:

   ```bash
   python3 roles/grafana/files/_rewrite_uids.py \
       --src-dir /tmp/telemetron-upstream-dashboards \
       --dest-dir roles/grafana/files/dashboards
   ```

4. Validate each rewritten JSON: `python3 -c "import json; json.load(open('roles/grafana/files/dashboards/<file>.json'))"` exits 0 for all five.

5. Spot-check that no upstream template variable strings survived in the dest files (these would indicate failed substitution):

   ```bash
   ! grep -rEn '\$datasource|\$loki_datasource|\$ds_prometheus|"\$ds"|"\$logsds"' roles/grafana/files/dashboards/
   ```

   (The Loki dashboard's `$datasource` and `$loki_datasource` are the most fragile per RESEARCH OQ-1; verify by `python3 -c "import json; d=json.load(open('roles/grafana/files/dashboards/loki-self-metrics.json')); print([p.get('datasource') for p in d.get('panels',[])])"` and confirming no raw `$datasource` strings appear in the output.)

6. Commit all five rewritten JSONs to `roles/grafana/files/dashboards/`.

**Fallback / contingency:** if any upstream URL returns 404 (URL drift) or the upstream JSON shape has changed enough that the substitution map in `_rewrite_uids.py` doesn't fully clean it (some `${datasource}` strings survive), the executor MUST EITHER (a) update the SUBSTITUTIONS dict in `_rewrite_uids.py` to handle the new shape and re-run the rewrite, OR (b) flag the dashboard in the SUMMARY as a Phase 5 gap to close in a follow-up plan -- do NOT commit a dashboard that still references unresolved template vars (that's the Risk 1 failure mode RESEARCH §7 warns about).
  </action>
  <verify>
    <automated>
bash -c '
set -e
test -f roles/grafana/templates/datasources/datasources.yaml.j2
test -f roles/grafana/templates/dashboards/dashboards.yaml.j2
test -f roles/grafana/files/_rewrite_uids.py
test -f roles/grafana/files/dashboards/host-health.json
test -f roles/grafana/files/dashboards/loki-explore-landing.json
test -f roles/grafana/files/dashboards/tempo-explore-landing.json
test -f roles/grafana/files/dashboards/otel-collector-self-metrics.json
test -f roles/grafana/files/dashboards/loki-self-metrics.json
test -f roles/grafana/files/dashboards/tempo-self-metrics.json
test -f roles/grafana/files/dashboards/mimir-self-metrics.json
# Datasource template MUST contain the four UIDs and tracesToLogsV2 + derivedFields per D-79
grep -q "uid: prometheus" roles/grafana/templates/datasources/datasources.yaml.j2
grep -q "uid: loki" roles/grafana/templates/datasources/datasources.yaml.j2
grep -q "uid: tempo" roles/grafana/templates/datasources/datasources.yaml.j2
grep -q "uid: mimir" roles/grafana/templates/datasources/datasources.yaml.j2
grep -q "tracesToLogsV2:" roles/grafana/templates/datasources/datasources.yaml.j2
grep -q "derivedFields:" roles/grafana/templates/datasources/datasources.yaml.j2
grep -q "matcherType: label" roles/grafana/templates/datasources/datasources.yaml.j2
grep -q "service.name" roles/grafana/templates/datasources/datasources.yaml.j2
grep -q "service_name" roles/grafana/templates/datasources/datasources.yaml.j2
grep -qF "{${__tags}} | trace_id=\"${__span.traceId}\"" roles/grafana/templates/datasources/datasources.yaml.j2
grep -q "editable: false" roles/grafana/templates/datasources/datasources.yaml.j2
# Dashboards manifest D-76 two-provider shape
grep -q "name: telemetron" roles/grafana/templates/dashboards/dashboards.yaml.j2
grep -q "name: operator" roles/grafana/templates/dashboards/dashboards.yaml.j2
grep -q "disableDeletion: true" roles/grafana/templates/dashboards/dashboards.yaml.j2
grep -q "allowUiUpdates: true" roles/grafana/templates/dashboards/dashboards.yaml.j2
grep -q "/etc/grafana/provisioning/dashboards/telemetron" roles/grafana/templates/dashboards/dashboards.yaml.j2
grep -q "/etc/grafana/provisioning/dashboards/operator" roles/grafana/templates/dashboards/dashboards.yaml.j2
# Validate all seven JSONs parse
for f in host-health loki-explore-landing tempo-explore-landing otel-collector-self-metrics loki-self-metrics tempo-self-metrics mimir-self-metrics; do
  python3 -c "import json; json.load(open(\"roles/grafana/files/dashboards/$f.json\"))" || { echo "JSON parse FAIL: $f"; exit 1; }
done
# Hand-rolled landing pages reference the right UIDs
grep -q "telemetron-loki-explore" roles/grafana/files/dashboards/loki-explore-landing.json
grep -q "telemetron-tempo-explore" roles/grafana/files/dashboards/tempo-explore-landing.json
# No unsubstituted template variable strings survive in the rewritten upstream JSONs
! grep -lE "\\$datasource|\\$loki_datasource|\\$ds_prometheus|\"\\$ds\"|\"\\$logsds\"" roles/grafana/files/dashboards/host-health.json roles/grafana/files/dashboards/otel-collector-self-metrics.json roles/grafana/files/dashboards/loki-self-metrics.json roles/grafana/files/dashboards/tempo-self-metrics.json roles/grafana/files/dashboards/mimir-self-metrics.json 2>&1 | grep -q .
# Python rewrite script is executable and syntactically valid
python3 -c "import py_compile; py_compile.compile(\"roles/grafana/files/_rewrite_uids.py\", doraise=True)"
echo "Task 2 datasources+dashboards OK"
'
    </automated>
  </verify>
  <acceptance_criteria>
    - `roles/grafana/templates/datasources/datasources.yaml.j2` contains all four datasource blocks with exact UIDs `prometheus`, `loki`, `tempo`, `mimir`; `editable: false` on all four; Tempo block contains literal `query: '{${__tags}} | trace_id="${__span.traceId}"'`; Tempo block has `tags:` list with `key: 'service.name'` + `value: 'service_name'`; Loki block has `derivedFields:` with `name: trace_id`, `matcherType: label`, `matcherRegex: trace_id`, `datasourceUid: tempo`; Mimir block has `type: prometheus` (NOT `type: mimir`) and URL containing `/prometheus` suffix.
    - `roles/grafana/templates/dashboards/dashboards.yaml.j2` contains both providers `telemetron` (disableDeletion: true, allowUiUpdates: false) and `operator` (disableDeletion: false, allowUiUpdates: true) with the exact paths `/etc/grafana/provisioning/dashboards/telemetron` and `/etc/grafana/provisioning/dashboards/operator`.
    - All seven JSON files exist in `roles/grafana/files/dashboards/` and `python3 -c "import json; json.load(open(<path>))"` exits 0 for each.
    - `roles/grafana/files/dashboards/loki-explore-landing.json` has `uid: telemetron-loki-explore` and references `loki` datasource in panel content.
    - `roles/grafana/files/dashboards/tempo-explore-landing.json` has `uid: telemetron-tempo-explore` and references `tempo` datasource in panel content.
    - None of the five upstream-derived dashboards contains any unresolved template variable strings (`$datasource`, `$loki_datasource`, `$ds_prometheus`, `"$ds"`, `"$logsds"`).
    - `roles/grafana/files/_rewrite_uids.py` is a valid Python 3 file (`python3 -c "import py_compile; py_compile.compile(<path>, doraise=True)"` exits 0) and contains a `SUBSTITUTIONS` dict with entries for all five upstream JSONs.
  </acceptance_criteria>
  <done>
    Datasource provisioning template renders correctly with all four UID-pinned datasources and the D-79 tracesToLogsV2 + derivedFields blocks verbatim. Two-provider dashboard manifest matches D-76. All seven dashboard JSONs are committed, valid, and contain no unresolved template-variable strings. The Python rewrite helper documents the substitution rationale for future maintainers.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write verify.yml with Gate 9 (D-73) datasources-resolve-real-data check + add inventory/playbook/doc-cascade integration</name>
  <files>
    roles/grafana/tasks/verify.yml,
    inventory/example-homelab/group_vars/all/grafana.yml,
    inventory/example-homelab/group_vars/all/secrets.yml.example,
    inventory/example-homelab/group_vars/all/network.yml,
    playbooks/deploy_docker.yml,
    roles/README.md,
    .planning/ROADMAP.md,
    .planning/PROJECT.md
  </files>
  <read_first>
    roles/alertmanager/tasks/verify.yml (canonical Gate-9-like multi-step in-network verify shape -- D-10a HEALTHCHECK poll, curlimages/curl one-shots with auto_remove + changed_when:false, loop iteration through assertion items),
    roles/grafana/defaults/main.yml (from Task 1: grafana_container_name, grafana_http_port, grafana_admin_user, grafana_health_retries/delay, grafana_curl_image/tag, grafana_network),
    .planning/phases/05-ui-plane/05-RESEARCH.md §5 (Gate 9 Verify Implementation -- exact curl commands per UID + auth pattern + in-network container shape),
    .planning/phases/05-ui-plane/05-CONTEXT.md D-73 (Gate 9 canonical query specs verbatim: prometheus `up`, loki `{job=~".+"}`, tempo `/api/search?limit=1`, mimir `prometheus/api/v1/query?query=up`),
    inventory/example-homelab/group_vars/all/network.yml (existing port-allocation comment block -- extend with UI plane section),
    inventory/example-homelab/group_vars/all/secrets.yml.example (existing structure -- the `# Examples for future planning visibility` block at the bottom includes `grafana_admin_password: CHANGE_ME` as a commented-out future hint; Task 3 promotes it to active),
    playbooks/deploy_docker.yml (current roles list ending at alertmanager -- this task appends grafana entry),
    roles/README.md (current "Planned roles (port status)" table and "Per-role port-acceptance gates" section -- this task ticks grafana row AND adds Gate 9 paragraph),
    .planning/ROADMAP.md (Phase 5 section -- this task fills in the Plans list with three 05-NN entries and marks 05-01 complete),
    .planning/PROJECT.md (look for any "Active" section line that enumerates remaining role count -- decrement if present per D-72)
  </read_first>
  <action>

**(A) `roles/grafana/tasks/verify.yml`** -- full content. Implements D-73 Gate 9 + standard D-10a HEALTHCHECK poll + datasource structural assertions. Mirrors roles/alertmanager/tasks/verify.yml shape:

```yaml

# roles/grafana/tasks/verify.yml
# Pre-poll Grafana readiness then exercise the UI-02 + UI-03 + UI-04 surface
# in-network. Implements Gate 9 (D-73): datasources-resolve-real-data.
#
# Step pattern: curlimages/curl one-shot per assertion. Auth via Basic Auth
# with admin + {{ grafana_admin_password }} -- the password stored in
# Grafana's SQLite matches whatever was set on first boot
# (RESEARCH §2.1 Risk 2 -- FIRST-BOOT-ONLY behavior).
#
# Mirrors roles/alertmanager/tasks/verify.yml conditional readiness shape
# (D-10a + W7 + D-32 + D-54 + D-69).

# Step 1: poll HEALTHCHECK status (D-10a; per W7 changed_when:false).
- name: Wait for grafana container HEALTHCHECK to report healthy
  community.docker.docker_container_info:
    name: "{{ grafana_container_name }}"
  register: grafana_health_check
  until: >-
    grafana_health_check.container is defined
    and grafana_health_check.container.State is defined
    and grafana_health_check.container.State.Health is defined
    and grafana_health_check.container.State.Health.Status == 'healthy'
  retries: "{{ grafana_health_retries }}"
  delay: "{{ grafana_health_delay }}"
  changed_when: false
  when: grafana_healthcheck_enabled | bool
  tags:
    - grafana
    - grafana-verify

# Step 2: /api/health structural probe (in-network curl one-shot).
- name: Curl-probe /api/health in-network
  community.docker.docker_container:
    name: "{{ grafana_container_name }}-verify-health"
    image: "{{ grafana_curl_image }}:{{ grafana_curl_image_tag }}"
    state: started
    detach: false
    auto_remove: true
    recreate: true
    cleanup: true
    networks:
      - name: "{{ grafana_network }}"
    entrypoint: ["/bin/sh", "-c"]
    command:
      - >-
        curl -fsS http://{{ grafana_container_name }}:{{ grafana_http_port }}/api/health
        | grep -q '"database":"ok"'
        || { echo "/api/health did not report database:ok" ; exit 1 ; } ;
        echo "api_health_ok"
  changed_when: false
  tags:
    - grafana
    - grafana-verify

# Step 3: Gate 9 D-73 -- datasource /health check for each of the four UIDs.
- name: "Gate 9 -- datasource /health check ({{ item }})"
  community.docker.docker_container:
    name: "{{ grafana_container_name }}-verify-ds-health-{{ item }}"
    image: "{{ grafana_curl_image }}:{{ grafana_curl_image_tag }}"
    state: started
    detach: false
    auto_remove: true
    recreate: true
    cleanup: true
    networks:
      - name: "{{ grafana_network }}"
    entrypoint: ["/bin/sh", "-c"]
    command:
      - >-
        BODY=$(curl -fsS -u {{ grafana_admin_user }}:{{ grafana_admin_password }}
        http://{{ grafana_container_name }}:{{ grafana_http_port }}/api/datasources/uid/{{ item }}/health) ;
        echo "$BODY" | grep -q '"status":"OK"'
        || { echo "datasource {{ item }} health FAIL: $BODY" ; exit 1 ; } ;
        echo "datasource_{{ item }}_health_ok"
  changed_when: false
  loop:
    - prometheus
    - loki
    - tempo
    - mimir
  tags:
    - grafana
    - grafana-verify

# Step 4: Gate 9 D-73 -- canonical query against prometheus UID.
# Asserts non-empty result on `up` query (proxy reaches Prometheus + scrape targets exist).
- name: "Gate 9 -- canonical query (prometheus: up)"
  community.docker.docker_container:
    name: "{{ grafana_container_name }}-verify-query-prom"
    image: "{{ grafana_curl_image }}:{{ grafana_curl_image_tag }}"
    state: started
    detach: false
    auto_remove: true
    recreate: true
    cleanup: true
    networks:
      - name: "{{ grafana_network }}"
    entrypoint: ["/bin/sh", "-c"]
    command:
      - >-
        BODY=$(curl -fsS -u {{ grafana_admin_user }}:{{ grafana_admin_password }}
        'http://{{ grafana_container_name }}:{{ grafana_http_port }}/api/datasources/proxy/uid/prometheus/api/v1/query?query=up') ;
        echo "$BODY" | grep -q '"result":\[' || { echo "prometheus query no result: $BODY" ; exit 1 ; } ;
        echo "$BODY" | grep -q '"metric"' || { echo "prometheus query empty result: $BODY" ; exit 1 ; } ;
        echo "canonical_query_prometheus_ok"
  changed_when: false
  tags:
    - grafana
    - grafana-verify

# Step 5: Gate 9 D-73 -- canonical query against loki UID.
# Asserts JSON parses to status:success (D-73 accepts empty streams as stable state).
- name: "Gate 9 -- canonical query (loki: {job=~\".+\"})"
  community.docker.docker_container:
    name: "{{ grafana_container_name }}-verify-query-loki"
    image: "{{ grafana_curl_image }}:{{ grafana_curl_image_tag }}"
    state: started
    detach: false
    auto_remove: true
    recreate: true
    cleanup: true
    networks:
      - name: "{{ grafana_network }}"
    entrypoint: ["/bin/sh", "-c"]
    command:
      - >-
        BODY=$(curl -fsS -u {{ grafana_admin_user }}:{{ grafana_admin_password }}
        'http://{{ grafana_container_name }}:{{ grafana_http_port }}/api/datasources/proxy/uid/loki/loki/api/v1/query?query=%7Bjob%3D~%22.%2B%22%7D') ;
        echo "$BODY" | grep -q '"status":"success"' || { echo "loki query non-success: $BODY" ; exit 1 ; } ;
        echo "canonical_query_loki_ok"
  changed_when: false
  tags:
    - grafana
    - grafana-verify

# Step 6: Gate 9 D-73 -- canonical query against tempo UID.
# D-73 explicit: "expect at least one trace OR explicit 'no traces yet' stable state."
- name: "Gate 9 -- canonical query (tempo: /api/search?limit=1)"
  community.docker.docker_container:
    name: "{{ grafana_container_name }}-verify-query-tempo"
    image: "{{ grafana_curl_image }}:{{ grafana_curl_image_tag }}"
    state: started
    detach: false
    auto_remove: true
    recreate: true
    cleanup: true
    networks:
      - name: "{{ grafana_network }}"
    entrypoint: ["/bin/sh", "-c"]
    command:
      - >-
        BODY=$(curl -fsS -u {{ grafana_admin_user }}:{{ grafana_admin_password }}
        'http://{{ grafana_container_name }}:{{ grafana_http_port }}/api/datasources/proxy/uid/tempo/api/search?limit=1') ;
        echo "$BODY" | grep -qE '"traces":|"results":' || { echo "tempo query no array key: $BODY" ; exit 1 ; } ;
        echo "canonical_query_tempo_ok"
  changed_when: false
  tags:
    - grafana
    - grafana-verify

# Step 7: Gate 9 D-73 -- canonical query against mimir UID.
# Mimir exposes Prometheus-compatible API at /prometheus subpath.
- name: "Gate 9 -- canonical query (mimir: up via /prometheus/api/v1/query)"
  community.docker.docker_container:
    name: "{{ grafana_container_name }}-verify-query-mimir"
    image: "{{ grafana_curl_image }}:{{ grafana_curl_image_tag }}"
    state: started
    detach: false
    auto_remove: true
    recreate: true
    cleanup: true
    networks:
      - name: "{{ grafana_network }}"
    entrypoint: ["/bin/sh", "-c"]
    command:
      - >-
        BODY=$(curl -fsS -u {{ grafana_admin_user }}:{{ grafana_admin_password }}
        'http://{{ grafana_container_name }}:{{ grafana_http_port }}/api/datasources/proxy/uid/mimir/api/v1/query?query=up') ;
        echo "$BODY" | grep -q '"status":"success"' || { echo "mimir query non-success: $BODY" ; exit 1 ; } ;
        echo "canonical_query_mimir_ok"
  changed_when: false
  tags:
    - grafana
    - grafana-verify

# Step 8: tracesToLogsV2 shape assertion on Tempo datasource (UI-04 wiring proof).
- name: "UI-04 -- assert Tempo datasource tracesToLogsV2 customQuery is wired"
  community.docker.docker_container:
    name: "{{ grafana_container_name }}-verify-t2l"
    image: "{{ grafana_curl_image }}:{{ grafana_curl_image_tag }}"
    state: started
    detach: false
    auto_remove: true
    recreate: true
    cleanup: true
    networks:
      - name: "{{ grafana_network }}"
    entrypoint: ["/bin/sh", "-c"]
    command:
      - >-
        BODY=$(curl -fsS -u {{ grafana_admin_user }}:{{ grafana_admin_password }}
        http://{{ grafana_container_name }}:{{ grafana_http_port }}/api/datasources/uid/tempo) ;
        echo "$BODY" | grep -q 'tracesToLogsV2' || { echo "tempo datasource missing tracesToLogsV2 block" ; exit 1 ; } ;
        echo "$BODY" | grep -q 'trace_id' || { echo "tempo datasource tracesToLogsV2 missing trace_id query" ; exit 1 ; } ;
        echo "tracesToLogsV2_wired_ok"
  changed_when: false
  tags:
    - grafana
    - grafana-verify

# Step 9: derivedFields shape assertion on Loki datasource (UI-04 wiring proof).
- name: "UI-04 -- assert Loki datasource derivedFields trace_id is wired"
  community.docker.docker_container:
    name: "{{ grafana_container_name }}-verify-df"
    image: "{{ grafana_curl_image }}:{{ grafana_curl_image_tag }}"
    state: started
    detach: false
    auto_remove: true
    recreate: true
    cleanup: true
    networks:
      - name: "{{ grafana_network }}"
    entrypoint: ["/bin/sh", "-c"]
    command:
      - >-
        BODY=$(curl -fsS -u {{ grafana_admin_user }}:{{ grafana_admin_password }}
        http://{{ grafana_container_name }}:{{ grafana_http_port }}/api/datasources/uid/loki) ;
        echo "$BODY" | grep -q 'derivedFields' || { echo "loki datasource missing derivedFields" ; exit 1 ; } ;
        echo "$BODY" | grep -q '"datasourceUid":"tempo"' || { echo "loki derivedFields not linked to tempo UID" ; exit 1 ; } ;
        echo "derivedFields_wired_ok"
  changed_when: false
  tags:
    - grafana
    - grafana-verify
```

**(B) `inventory/example-homelab/group_vars/all/grafana.yml`** -- new file, full content. Surfaces the operator-tunable knobs from defaults/main.yml as inventory hints (with the canonical M1 defaults documented inline):

```yaml

# inventory/example-homelab/group_vars/all/grafana.yml
# Operator-tunable knobs for roles/grafana (Phase 5; UI-01..UI-04).
# Defaults below match roles/grafana/defaults/main.yml -- override as needed.

# --- Host publishing (D-82 inverts D-30 for UI plane) ---
# Grafana MUST be browser-reachable for the homelab quickstart.
# Flip to false + run a reverse proxy on the telemetron bridge for
# production hardening (see roles/grafana/README.md Reverse proxy).
# Acceptable values: true / false / "127.0.0.1"
grafana_publish_host: true
grafana_bind_address: "0.0.0.0"   # D-83
grafana_http_port: 3000           # D-84

# --- Access model (D-86, D-87, D-88, D-89) ---
grafana_admin_user: admin
grafana_admin_email: "admin@telemetron.local"
# grafana_admin_password -- defined in secrets.yml (D-86).
# RESEARCH §2.1 Risk 2: GF_SECURITY_ADMIN_PASSWORD is FIRST-BOOT-ONLY.
# To rotate post-deploy: docker exec telemetron-grafana grafana-cli admin reset-admin-password '<new>'

grafana_anonymous_enabled: false   # D-87 default off; true for wall-display TV mode
grafana_default_theme: dark

# --- Datasource backend hostnames (telemetron bridge DNS; overrides per-role split-stack) ---
# Defaults align with the Telemetron stack; only override if you split
# backends across hosts and need different DNS names.
# grafana_prometheus_url: "http://prometheus:9090"
# grafana_loki_url: "http://loki:3100"
# grafana_tempo_url: "http://tempo:3200"
# grafana_mimir_url: "http://mimir:9009/prometheus"

# --- Operator dashboard drop-in dir (D-76) ---
# Set to a host path containing your own *.json dashboards to have them
# auto-loaded into the "Operator" folder in Grafana. Empty (default) =
# no operator drop-in mount.
grafana_dashboard_extra_dir: ""

# --- Extra datasources (sorted-keys per D-20) ---
# List of additional datasources to merge into the provisioned manifest.
# Each entry follows Grafana's datasource YAML schema.
grafana_extra_datasources: []

# --- Extra env (escape hatch) ---
# Additional GF_* env vars to inject. Use sparingly; prefer the typed knobs above.
grafana_extra_env: {}
```

**(C) `inventory/example-homelab/group_vars/all/secrets.yml.example`** -- edit only. Locate the bottom block that says `# Examples for future planning visibility -- do NOT add real values yet:` and PROMOTE the `grafana_admin_password: CHANGE_ME` line out of the commented hint. Insert a new `# --- Phase 5: Grafana ---` section immediately after the `# --- Phase 2: Mimir ---` block (or after the `# --- Phase 2: ... ---` comment immediately before the future-hints block).

Specifically, replace the text:

```
# --- Phase 2+ additional keys will be added here as their roles land ---
# (Examples for future planning visibility -- do NOT add real values yet:)
# grafana_admin_password: CHANGE_ME   # Phase 5 -- Grafana admin login
#
# Note: Phase 4 (alertmanager) adds NO secret keys (D-66). ...
```

With:

```
# --- Phase 5: Grafana (D-86; UI-01) ---
# Consumed by: roles/grafana (GF_SECURITY_ADMIN_PASSWORD env injection on
# the telemetron-grafana container).
# IMPORTANT: this env var is FIRST-BOOT-ONLY (RESEARCH §2.1 Risk 2). On
# the initial deploy it sets the admin password in Grafana's SQLite store;
# subsequent restarts do NOT re-apply changes. To rotate the password
# after first deploy:
#   docker exec telemetron-grafana grafana-cli admin reset-admin-password '<new>'
# Then update this value to match so Gate 9 verify (which curls
# /api/datasources/uid/*/health with Basic Auth) keeps passing.
grafana_admin_password: CHANGE_ME

# --- Phase 2+ additional keys will be added here as their roles land ---
# Note: Phase 4 (alertmanager) adds NO secret keys (D-66). The null
# default receiver has no outbound destination; the single-host bridge
# trust boundary covers inbound. The previously-planned hook_router
# secret keys (jenkins_api_token, shared-secret) moved with the
# hook-router deferral to a future milestone -- see REQUIREMENTS.md
# ALERT-V2-01..05.
```

**(D) `inventory/example-homelab/group_vars/all/network.yml`** -- if a port-allocation comment block exists, extend it under a "UI plane" subsection. If no such block exists yet, ADD a new comment block at the bottom of the file:

```yaml
# --- UI plane port allocation (Phase 5; D-82 / D-84) ---
# Grafana    :3000  (host-published default; grafana_publish_host: true)
# Karma      :8082  (host-published default; karma_publish_host: true; Phase 5 plan 05-02)
# PromLens   :8081  (host-published default; promlens_publish_host: true; Phase 5 plan 05-03)
#
# Reverse-proxy / TLS termination is operator-supplied (D-85). See
# roles/grafana/README.md Reverse proxy section for the Caddy/Traefik/nginx
# pattern. Operators flip <role>_publish_host: false and proxy from the
# host network to the telemetron bridge.
```

Note: plans 05-02 and 05-03 will modify this same block on their executions; this initial block sets the UI plane subsection. If grafana.yml is the only UI plane role at the time of execution, the Karma + PromLens lines can be placeholders (commented `# TBD plan 05-02/03`) OR included as written -- the executor's call; the spirit is documentation of the UI plane port surface.

**(E) `playbooks/deploy_docker.yml`** -- single edit. Locate the `roles:` list (currently ends with `- role: alertmanager / tags: [alertmanager]`) and APPEND immediately after, before the trailing comments:

```yaml
    - role: grafana
      tags:
        - grafana
```

Also update the trailing comment block to reflect Phase 5 progress:

```yaml
  # Subsequent phase plans extend this list in dependency order
  # (D-05 dependency tree). Phase 5 is IN PROGRESS:
  # node_exporter + opentelemetry + prometheus + fluentbit + alertmanager
  # + grafana are all wired. Remaining:
  #   Phase 5: karma (plan 05-02), promlens (plan 05-03)
  #   Phase 6: nfsd (opt-in via enable_nfsd flag)
```

**(F) `roles/README.md`** -- two edits:

1. **Tick the grafana row** in the "Planned roles (port status)" table. Change:

   ```
   | `grafana`                | dashboards, datasources, provisioning             | ☐ |
   ```

   To:

   ```
   | `grafana`                | dashboards, datasources, provisioning             | ☑ |
   ```

2. **Add Gate 9 paragraph** under `## Per-role port-acceptance gates` (after the existing Gate 8 / parent-directory bind-mount paragraph, before any trailing material). Verbatim text:

   ```markdown
   **9. Datasources-resolve-real-data gate (Plan 05-01; D-73; UI-02 / UI-03 / Gate 9):**

   Applies to the `roles/grafana/` role specifically (the only role with cross-component datasource provisioning). The role's verify step MUST:

   1. Issue `curl http://grafana:3000/api/datasources/uid/<uid>/health` for each of the four UIDs (`prometheus`, `loki`, `tempo`, `mimir`) and assert HTTP 200 + body containing `"status":"OK"`.
   2. Issue one canonical query against each datasource:
      - `prometheus`: `/api/v1/query?query=up` -- expect non-empty `result[]`.
      - `loki`: `/loki/api/v1/query?query={job=~".+"}` -- expect `"status":"success"`.
      - `tempo`: `/api/search?limit=1` -- expect 200 with `"traces":[` or `"results":[` (empty array acceptable as stable state).
      - `mimir`: `/api/v1/query?query=up` (proxied through the mimir datasource which prepends `/prometheus`) -- expect `"status":"success"`.
   3. Assert that `/api/datasources/uid/tempo` returns a JSON body containing `tracesToLogsV2` and `trace_id` (UI-04 wiring).
   4. Assert that `/api/datasources/uid/loki` returns a JSON body containing `derivedFields` and `"datasourceUid":"tempo"` (UI-04 wiring).

   Auth: Basic Auth with admin + `grafana_admin_password`. In-network via `curlimages/curl:8.10.1` one-shot containers on the `telemetron` bridge (D-54 / D-69 pattern). This is THE M1 acceptance heuristic for "everything wired correctly" -- if Grafana boots and Gate 9 passes, the whole pre-Phase-5 stack is validated end-to-end.
   ```

**(G) `.planning/ROADMAP.md`** -- two edits in the Phase 5 section:

1. Change `**Plans**: TBD` to `**Plans**: 3 plans`.
2. Add the Plans list under `**Plans**: 3 plans` (mirroring the format used by other phases):

   ```
   Plans:
   - [x] 05-01-PLAN.md -- Grafana role port (UI-01, UI-02, UI-03, UI-04) + Gate 9 D-73 + doc cascade; Wave 1
   - [ ] 05-02-PLAN.md -- Karma role port (UI-05); Wave 2
   - [ ] 05-03-PLAN.md -- PromLens role port (UI-06) + deprecation banner; Wave 2
   ```

   Also in the top-level "Phases" bullet list (around line 19), the entry `- [ ] **Phase 5: UI Plane**...` stays unchecked (Phase still in progress until 05-02 and 05-03 land); leave its text as-is.

3. In the Progress table at the bottom, change `| 5. UI Plane | 0/TBD | Not started | - |` to `| 5. UI Plane | 1/3 | In progress | - |`.

**(H) `.planning/PROJECT.md`** -- scan for any "Active" section line that enumerates a role count (e.g. "X remaining roles" or "Roles in scope: ..."). If such a line exists, decrement / update it per D-72 (5-1=2 remaining UI plane roles -- karma + promlens; or "13 deployed + hook_router deferred" stays unchanged since grafana port is what makes that count true). If no such line exists (PROJECT.md is structured differently), this step is a no-op. The executor should check and update if applicable; this is a light cascade per D-72, not a hard requirement to add new content.
  </action>
  <verify>
    <automated>
bash -c '
set -e
# verify.yml exists and contains all nine steps
test -f roles/grafana/tasks/verify.yml
grep -q "docker_container_info" roles/grafana/tasks/verify.yml
grep -q "Health.Status == .healthy." roles/grafana/tasks/verify.yml
grep -q "datasources/uid/{{ item }}/health" roles/grafana/tasks/verify.yml
# All four UIDs looped in Gate 9 health check
grep -A20 "Gate 9 -- datasource /health check" roles/grafana/tasks/verify.yml | grep -q "prometheus"
grep -A20 "Gate 9 -- datasource /health check" roles/grafana/tasks/verify.yml | grep -q "loki"
grep -A20 "Gate 9 -- datasource /health check" roles/grafana/tasks/verify.yml | grep -q "tempo"
grep -A20 "Gate 9 -- datasource /health check" roles/grafana/tasks/verify.yml | grep -q "mimir"
# Four canonical queries
grep -q "canonical_query_prometheus_ok" roles/grafana/tasks/verify.yml
grep -q "canonical_query_loki_ok" roles/grafana/tasks/verify.yml
grep -q "canonical_query_tempo_ok" roles/grafana/tasks/verify.yml
grep -q "canonical_query_mimir_ok" roles/grafana/tasks/verify.yml
# UI-04 wiring assertions
grep -q "tracesToLogsV2_wired_ok" roles/grafana/tasks/verify.yml
grep -q "derivedFields_wired_ok" roles/grafana/tasks/verify.yml
# Inventory artifacts
test -f inventory/example-homelab/group_vars/all/grafana.yml
grep -q "grafana_publish_host: true" inventory/example-homelab/group_vars/all/grafana.yml
grep -q "grafana_anonymous_enabled: false" inventory/example-homelab/group_vars/all/grafana.yml
grep -q "admin@telemetron.local" inventory/example-homelab/group_vars/all/grafana.yml
# secrets.yml.example: active line present (not just commented hint)
grep -E "^grafana_admin_password: CHANGE_ME" inventory/example-homelab/group_vars/all/secrets.yml.example
grep -q "Phase 5: Grafana" inventory/example-homelab/group_vars/all/secrets.yml.example
grep -q "grafana-cli admin reset-admin-password" inventory/example-homelab/group_vars/all/secrets.yml.example
# Playbook updated
grep -q "role: grafana" playbooks/deploy_docker.yml
# Verify grafana follows alertmanager in the roles list
python3 -c "
import re
with open(\"playbooks/deploy_docker.yml\") as f:
    text = f.read()
am = text.find(\"role: alertmanager\")
gr = text.find(\"role: grafana\")
assert am > 0, \"alertmanager not in playbook\"
assert gr > am, \"grafana must follow alertmanager (got am=%d gr=%d)\" % (am, gr)
print(\"role-order OK\")
"
# roles/README.md: grafana ticked, Gate 9 added
grep -E "\\| .grafana. .*\\|.*☑" roles/README.md
grep -q "Gate 9" roles/README.md
grep -q "Datasources-resolve-real-data" roles/README.md
grep -q "datasources/uid/" roles/README.md
# ROADMAP.md updated
grep -q "05-01-PLAN.md" .planning/ROADMAP.md
grep -q "05-02-PLAN.md" .planning/ROADMAP.md
grep -q "05-03-PLAN.md" .planning/ROADMAP.md
# Final grep gates -- INSPQ-noise + non-ASCII on the now-complete roles/grafana/ tree
! grep -riE "inspq|qc\\.ca|montreal|québec|francais|french|/srv/nfs/inspq|vault_inspq|america/(toronto|montreal)|noreply@inspq|ldap.toml" roles/grafana/ inventory/example-homelab/group_vars/all/grafana.yml
! grep -rPn "[^\\x00-\\x7F]" roles/grafana/ inventory/example-homelab/group_vars/all/grafana.yml
echo "Task 3 verify+integration OK"
'
    </automated>
  </verify>
  <acceptance_criteria>
    - `roles/grafana/tasks/verify.yml` exists and contains: Step 1 D-10a HEALTHCHECK poll via `community.docker.docker_container_info` with `until:` condition asserting `State.Health.Status == 'healthy'`; Step 2 `/api/health` curl probe asserting `"database":"ok"`; Step 3 looped Gate 9 /health check across all four UIDs (`prometheus`, `loki`, `tempo`, `mimir`); Steps 4-7 four canonical-query proofs (one per UID, matching D-73 specs); Step 8 tracesToLogsV2 wiring assertion on Tempo datasource; Step 9 derivedFields wiring assertion on Loki datasource. All verify steps use `curlimages/curl` one-shot containers with `auto_remove: true` and `changed_when: false`.
    - `inventory/example-homelab/group_vars/all/grafana.yml` exists with `grafana_publish_host: true`, `grafana_bind_address: "0.0.0.0"`, `grafana_http_port: 3000`, `grafana_admin_user: admin`, `grafana_admin_email: "admin@telemetron.local"`, `grafana_anonymous_enabled: false`, `grafana_dashboard_extra_dir: ""`, `grafana_extra_datasources: []`.
    - `inventory/example-homelab/group_vars/all/secrets.yml.example` contains an active (uncommented) line `grafana_admin_password: CHANGE_ME` under a `# --- Phase 5: Grafana` section comment block, with the password-rotation grafana-cli command documented.
    - `inventory/example-homelab/group_vars/all/network.yml` contains a "UI plane port allocation" subsection listing Grafana :3000 (Karma :8082 and PromLens :8081 may appear as TBD or actual; either is acceptable).
    - `playbooks/deploy_docker.yml` contains `- role: grafana` with tag `grafana`, positioned AFTER `- role: alertmanager` in the `roles:` list.
    - `roles/README.md` shows the grafana row as ticked (☑ checkbox) in the Planned roles table.
    - `roles/README.md` contains a "Gate 9" section under `## Per-role port-acceptance gates` with title "Datasources-resolve-real-data gate" referencing D-73, all four UIDs, the four canonical queries, and the UI-04 wiring assertions.
    - `.planning/ROADMAP.md` Phase 5 section shows `**Plans**: 3 plans`, contains plan list lines for 05-01-PLAN.md (checked) + 05-02-PLAN.md + 05-03-PLAN.md (both unchecked), and the Progress table row shows `1/3` plans complete with status `In progress`.
    - Cumulative grep gates: `grep -riE 'inspq|qc\.ca|montreal|québec|francais|french|/srv/nfs/inspq|vault_inspq|america/(toronto|montreal)|noreply@inspq|ldap.toml' roles/grafana/ inventory/example-homelab/group_vars/all/grafana.yml` returns 0 matches.
    - Cumulative non-ASCII gate: `grep -rPn '[^\x00-\x7F]' roles/grafana/ inventory/example-homelab/group_vars/all/grafana.yml` returns 0 matches.
  </acceptance_criteria>
  <done>
    Gate 9 D-73 verify suite implemented end-to-end (HEALTHCHECK poll + /api/health + four /health checks + four canonical queries + UI-04 wiring assertions). Inventory surface complete (grafana.yml + secrets.yml.example + network.yml). Playbook wires grafana after alertmanager. roles/README.md ticks grafana and documents Gate 9 as a project-wide gate. ROADMAP.md reflects plan 05-01 complete + 05-02 + 05-03 pending. All cumulative grep gates pass on the full role tree.
  </done>
</task>

</tasks>

<verification>
After all three tasks complete, the following must be true:

**Cross-task acceptance:**
- `ansible-playbook --syntax-check -i inventory/example-homelab playbooks/deploy_docker.yml` exits 0.
- `ansible-lint roles/grafana/` exits 0 (warnings acceptable; errors are not).
- Running the playbook with `--tags grafana` on leviathan creates the `telemetron-grafana` container with HEALTHCHECK healthy and Gate 9's nine verify steps all green.

**Slice of phase success criteria delivered by 05-01:**
- SC-1 (UI-01 + UI-02): Grafana on :3000 with four datasources at hardcoded UIDs `prometheus`/`loki`/`tempo`/`mimir`. `curl http://<host>:3000/api/datasources/uid/loki` returns 200.
- SC-2 (UI-03): Seven bundled dashboards under Telemetron folder rendering real data.
- SC-3 (UI-04): tracesToLogsV2 + Loki derivedFields wired (D-79 / D-80).
- (SC-4 Karma/PromLens delivered by 05-02/05-03)
- (SC-5 PromLens deprecation README + Karma GHCR image delivered by 05-02/05-03)

**Phase 5 gate progression:**
- Gates 1-8 pass on roles/grafana/.
- Gate 9 added to roles/README.md, applied by 05-01 verify.
- Plan 05-01 ticked in ROADMAP.md.
</verification>

<success_criteria>
- `roles/grafana/` exists with 17 committed files (six from Task 1, ten from Task 2, one verify.yml from Task 3).
- Container `telemetron-grafana` boots healthy on the telemetron Docker bridge with admin login working using `grafana_admin_password` from inventory.
- All four datasources resolve via /api/datasources/uid/<uid>/health = `"status":"OK"`.
- All four canonical-query proofs pass (Gate 9 D-73).
- Both UI-04 wiring assertions pass (tracesToLogsV2 on Tempo datasource; derivedFields on Loki datasource).
- Inventory surface complete; secrets.yml.example has active `grafana_admin_password` key.
- playbooks/deploy_docker.yml wires grafana after alertmanager.
- roles/README.md ticks grafana + documents Gate 9 as a project-wide port-acceptance gate.
- All cumulative grep gates (INSPQ + non-ASCII) pass on the new role tree.
- Second back-to-back `--tags grafana` run reports `changed=0` (idempotency Gate 4).
</success_criteria>

<output>
After completion, create `.planning/phases/05-ui-plane/05-01-SUMMARY.md` documenting:
- Files created (full list, 17+ files).
- Key implementation decisions actually made (e.g. how the conditional operator drop-in mount was implemented in tasks/main.yml).
- Live-host UAT results on leviathan: Gate 9 nine-step verify output (healthcheck status + four datasource health checks + four canonical queries + two UI-04 wiring assertions).
- Any RESEARCH §9 open questions resolved during execution (OQ-1 Loki rewrite outcome; OQ-3 Tempo proxy path confirmation; OQ-4 Mimir overview $datasource final UID; OQ-5 actual first-boot timing on leviathan).
- D-25 deviation audit summary against the upstream INSPQ grafana role (what was stripped per RESEARCH §6.1).
- Plan handoff to 05-02 (karma) -- which expects Grafana already running for cross-role smoke-test context.
</output>

## Must-haves

- **Truth**: Grafana running at `http://<host>:3000` with admin login working using `grafana_admin_password` from `inventory/example-homelab/group_vars/all/secrets.yml`.
- **Truth**: All four datasources (Prometheus, Loki, Tempo, Mimir) appear with hardcoded UIDs `prometheus`, `loki`, `tempo`, `mimir`.
- **Truth**: `curl -u admin:<pw> http://<host>:3000/api/datasources/uid/<uid>/health` returns HTTP 200 + `"status":"OK"` for each of the four UIDs.
- **Truth**: Seven role-shipped dashboards visible in the Telemetron folder; each renders real query results on a fresh deploy.
- **Truth**: tracesToLogsV2 on Tempo datasource carries the exact D-79 customQuery `{${__tags}} | trace_id="${__span.traceId}"`.
- **Truth**: derivedFields on Loki datasource carries `name: trace_id`, `matcherType: label`, `matcherRegex: trace_id`, `datasourceUid: tempo`.
- **Truth**: `telemetron_grafana_data` named volume mounted at `/var/lib/grafana` (embedded SQLite store, UI-01).
- **Truth**: Config bind-mount is parent-dir form `/opt/telemetron/grafana → /etc/grafana` (Gate 8 -- no single-file mounts).
- **Truth**: `community.docker.docker_container` labels include `org.telemetron.service: telemetron` and `org.telemetron.job: grafana` (Gate 7).
- **Truth**: Second back-to-back `ansible-playbook --tags grafana` run reports `changed=0` (Gate 4 idempotency).
- **Truth**: `grep -riE 'inspq|qc\.ca|montreal|québec|francais|french|/srv/nfs/inspq|vault_inspq|america/(toronto|montreal)|noreply@inspq|ldap.toml' roles/grafana/` returns zero matches (Gate 1 + D-25 audit).
- **Truth**: Gate 9 (D-73) documented in `roles/README.md` under per-role port-acceptance gates.
- **Truth**: `playbooks/deploy_docker.yml` lists `- role: grafana` immediately after `- role: alertmanager`.
- **Artifact**: `roles/grafana/defaults/main.yml` (image pin `grafana/grafana-oss:13.0.1`, explicit healthcheck, publish/bind/port knobs, admin user/email, anonymous knob, dashboard_extra_dir, curl-image pin).
- **Artifact**: `roles/grafana/tasks/main.yml` (config-dir tree ensured, datasources + dashboards manifests + grafana.ini + 7 JSONs rendered/copied, named volume ensured, pull image, run container with GF_ env + Gate-7 labels + parent-dir bind + conditional healthcheck, flush_handlers, include verify.yml).
- **Artifact**: `roles/grafana/tasks/verify.yml` (Step 1 HEALTHCHECK poll, Step 2 /api/health probe, Step 3 looped /health Gate 9 check across four UIDs, Steps 4-7 four canonical queries, Steps 8-9 UI-04 wiring assertions).
- **Artifact**: `roles/grafana/handlers/main.yml` (single `Docker restart grafana` handler).
- **Artifact**: `roles/grafana/templates/datasources/datasources.yaml.j2` (all four datasources with hardcoded UIDs + D-79 tracesToLogsV2 + derivedFields).
- **Artifact**: `roles/grafana/templates/dashboards/dashboards.yaml.j2` (D-76 two-provider manifest).
- **Artifact**: `roles/grafana/templates/grafana.ini.j2` (paths, server, database sqlite3, users, auth.anonymous).
- **Artifact**: Seven `roles/grafana/files/dashboards/*.json` (host-health, loki-explore-landing, tempo-explore-landing, otel-collector-self-metrics, loki-self-metrics, tempo-self-metrics, mimir-self-metrics) -- all valid JSON, none containing unresolved template variable strings.
- **Artifact**: `roles/grafana/files/_rewrite_uids.py` (committed-once Python helper documenting per-dashboard UID substitution).
- **Artifact**: `roles/grafana/meta/main.yml` (galaxy_info, license MIT, min_ansible_version 2.15).
- **Artifact**: `roles/grafana/README.md` (OPS-03 schema; password-rotation note per RESEARCH Risk 2; D-78 trace-to-logs honest framing; D-81 service_name label-mapping documentation; D-85 reverse-proxy recipe; D-25 INSPQ deviation summary).
- **Artifact**: `inventory/example-homelab/group_vars/all/grafana.yml` (operator-tunable surface).
- **Artifact**: `inventory/example-homelab/group_vars/all/secrets.yml.example` (active `grafana_admin_password: CHANGE_ME` line under Phase 5 section).
- **Artifact**: `playbooks/deploy_docker.yml` (grafana role appended after alertmanager).
- **Artifact**: `roles/README.md` (grafana row ticked; Gate 9 paragraph added).
- **Artifact**: `.planning/ROADMAP.md` (Phase 5 Plans list = 3; 05-01 marked complete; progress table updated).
- **Key link**: `playbooks/deploy_docker.yml -> roles/grafana/tasks/main.yml` via `- role: grafana` (after alertmanager).
- **Key link**: `roles/grafana/templates/datasources/datasources.yaml.j2 -> roles/{prometheus,loki,tempo,mimir}` via `datasources[].url` referencing each role's container DNS name on the telemetron bridge.
- **Key link**: `roles/grafana/templates/datasources/datasources.yaml.j2 (Tempo) -> Loki datasource (uid:loki)` via `jsonData.tracesToLogsV2.datasourceUid` + customQuery `{${__tags}} | trace_id="${__span.traceId}"` (D-79).
- **Key link**: `roles/grafana/templates/datasources/datasources.yaml.j2 (Loki) -> Tempo datasource (uid:tempo)` via `jsonData.derivedFields[0].datasourceUid` + `matcherType: label` + `matcherRegex: trace_id` (D-79).
- **Key link**: `roles/grafana/tasks/verify.yml -> /api/datasources/uid/<uid>/health and /api/datasources/proxy/uid/<uid>/...` via curlimages/curl one-shots on the telemetron bridge using Basic Auth admin:{{ grafana_admin_password }} (Gate 9 D-73).
- **Key link**: `roles/grafana/tasks/main.yml (docker_container.mounts) -> telemetron_grafana_data:/var/lib/grafana + /opt/telemetron/grafana:/etc/grafana` (UI-01 + Gate 8).
