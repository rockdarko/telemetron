# Requirements Archive: v1.3.0 Backup & Restore

**Archived:** 2026-06-05
**Status:** SHIPPED

For current requirements, see `.planning/REQUIREMENTS.md`.

---

# Requirements: Telemetron

**Defined:** 2026-06-02
**Milestone:** v1.3.0 — Backup & Restore
**Core Value:** A homelab operator can clone the repo, point the bundled example inventory at one of their own Docker hosts, run a single playbook, and end up with a working observability plane — Prometheus + Mimir for metrics, Loki for logs, Tempo for traces, Grafana on top, Alertmanager + Karma for alerts, all fed by OpenTelemetry Collector. v1.3.0 extends that promise: when something goes wrong, the operator has a tested path to restore from a backup.

## v1.3.0 Requirements

18 requirements across 5 categories. Each maps to exactly one phase (10/11/12 phases of v1.0/v1.1/v1.2 are archived in `.planning/milestones/`; v1.3.0 phases are 13/14/15).

### Backup (per-role tasks + orchestrator)

- [x] **BACKUP-V13-01**: Operator can run `ansible-playbook playbooks/backup_docker.yml --tags garage --ask-vault-pass` and the Garage role's `tasks/backup.yml` produces a single tarball at `/opt/telemetron/backups/garage/garage-<UTC-timestamp>.tar.zst` (mode 0600) containing both Docker volume contents (`telemetron_garage_meta` + `telemetron_garage_data`) AND the host-mounted `s3-credentials` file (`{{ garage_s3_credentials_file }}`). The Garage container is `docker stop`'d before the tar runs and `docker start`'d after. Verify step confirms the role is healthy post-restart.
- [x] **BACKUP-V13-02**: Operator can run the same orchestrator with `--tags prometheus` and Prometheus's `tasks/backup.yml` produces a single tarball at `/opt/telemetron/backups/prometheus/prometheus-<UTC-timestamp>.tar.zst` (mode 0600) containing the `telemetron_prometheus_data` volume contents (including `wal/`, `chunks_head/`, all compacted blocks). The container is `docker stop`'d before tar and `docker start`'d after; verify confirms health post-restart.
- [x] **BACKUP-V13-03**: Operator can run the orchestrator with `--tags grafana` and Grafana's `tasks/backup.yml` produces a single tarball at `/opt/telemetron/backups/grafana/grafana-<UTC-timestamp>.tar.zst` (mode 0600) containing the `telemetron_grafana_data` volume (`grafana.db` + `plugins/`). The container is `docker stop`'d before tar; verify confirms health post-restart.
- [x] **BACKUP-V13-04**: Operator can run the orchestrator with `--tags alertmanager` and Alertmanager's `tasks/backup.yml` produces a single tarball at `/opt/telemetron/backups/alertmanager/alertmanager-<UTC-timestamp>.tar.zst` (mode 0600) containing the `telemetron_alertmanager_data` volume (which may include `data/silences` + `data/nflog` protobuf files or be empty on fresh deploys — both cases handled cleanly). The container is `docker stop`'d before tar; verify confirms health post-restart.
- [x] **BACKUP-V13-05**: Operator can run `ansible-playbook playbooks/backup_docker.yml --ask-vault-pass` with no `--tags` flag and the orchestrator iterates the 4 stateful roles in forward-deploy order (garage → prometheus → grafana → alertmanager), serial (one role at a time, minimising combined downtime). PLAY-start banner (`tags: always`, D-160 style) summarises the target directory + `backup_continue_on_failure` status without enumerating role names. Stateless-role tag invocations (`--tags loki`, `--tags fluentbit`, etc.) produce an empty 0-task play — no failure.

### Restore (per-role tasks + orchestrator)

- [x] **RESTORE-V13-01**: Operator can run `ansible-playbook playbooks/restore_docker.yml --tags garage --extra-vars backup_restore_confirm=true --ask-vault-pass` and Garage's `tasks/restore.yml` validates the source tarball exists + integrity-checks (`tar tf` returns 0), `docker stop`s the container, wipes existing `/var/lib/docker/volumes/telemetron_garage_{meta,data}/_data/` contents, untars the backup (restoring both volumes AND the `s3-credentials` host file), `docker start`s the container, runs verify. After restore, redeploying Loki/Tempo/Mimir against the restored Garage produces a working stack with no manual key intervention required.
- [x] **RESTORE-V13-02**: Operator can run the orchestrator with `--tags prometheus --extra-vars backup_restore_confirm=true` and Prometheus's `tasks/restore.yml` does the symmetric inverse of BACKUP-V13-02, with one Prometheus-specific safety step: after untar but before `docker start`, the stale `/prometheus/lock` file (PID-based, left over from the backup-time process) is explicitly deleted. Verify confirms Prometheus serves `/metrics` post-restart.
- [x] **RESTORE-V13-03**: Operator can run the orchestrator with `--tags grafana --extra-vars backup_restore_confirm=true` and Grafana's `tasks/restore.yml` does the symmetric inverse of BACKUP-V13-03. Provisioned dashboards/datasources are re-rendered from `{{ grafana_config_dir }}/provisioning/` on container start (Grafana's normal behaviour); UI-only state in the restored `grafana.db` is preserved. Verify confirms the 4 datasource UIDs resolve.
- [x] **RESTORE-V13-04**: Operator can run the orchestrator with `--tags alertmanager --extra-vars backup_restore_confirm=true` and Alertmanager's `tasks/restore.yml` does the symmetric inverse of BACKUP-V13-04. Empty backups (fresh-deploy state with no `data/` dir) restore cleanly to fresh-deploy state. Verify confirms Alertmanager API serves `/api/v2/status`.
- [x] **RESTORE-V13-05**: Operator can run `ansible-playbook playbooks/restore_docker.yml --extra-vars backup_restore_confirm=true --ask-vault-pass` with no `--tags` flag and the orchestrator iterates the 4 stateful roles in the order required by Garage dependencies: **first stop Loki + Tempo + Mimir (Garage writers)**, then garage → prometheus → grafana → alertmanager restore, then restart Loki + Tempo + Mimir against the restored Garage. PLAY-start banner is an escalated D-160 WARN (`tags: always`) explicitly stating that restore will PERMANENTLY REPLACE volume contents.

### Operator experience

- [x] **OPS-V13-01**: `playbooks/restore_docker.yml` refuses to run without `--extra-vars backup_restore_confirm=true`. The confirm-gate fires both in the orchestrator AND in each per-role `tasks/restore.yml` (so a standalone `include_role: tasks_from=restore` from a custom playbook also enforces it). Mirrors the v1.2.0 `telemetron_purge_data=true` opt-in safety contract (D-159 precedent).
- [x] **OPS-V13-02**: `playbooks/backup_docker.yml` defaults to bail-out on the first role's failure (operator sees the problem immediately, partial backups don't accumulate silently). `--extra-vars backup_continue_on_failure=true` opts into "continue past failed roles" for partial-backup operators. Each per-role `tasks/backup.yml` wraps its quiesce-tar-restart sequence in `block:`/`rescue:`/`always:` so the container is always restarted, even on tar failure (no half-state where a role is stopped and not restarted).
- [x] **OPS-V13-03**: `playbooks/backup_docker.yml --tags <role>` and `playbooks/restore_docker.yml --tags <role>` work for any of the 4 stateful roles (garage, prometheus, grafana, alertmanager) using the same `--tags <role>` convention established by `deploy_docker.yml` and `undeploy_docker.yml`. Each per-role task carries tags `[<role>, backup]` (or `[<role>, restore]`) so `--tags backup` and `--tags restore` are also legitimate cross-cutting operator commands.
- [x] **OPS-V13-04**: Each per-role `tasks/backup.yml` and `tasks/restore.yml` includes a pre-task that ensures the `zstd` package is present via `ansible.builtin.package: name: zstd, state: present, become: true`. Idempotent — second-run produces `changed=0` on hosts that already have `zstd`. Works on Debian 12, Ubuntu 22.04 LTS, RHEL 9 / AlmaLinux / Rocky (the three Telemetron-supported distro families).

### End-to-end acceptance

- [x] **UAT-V13-01**: The full backup ↔ restore round-trip is proven on leviathan via `14-HUMAN-UAT.md`'s 7-step scenario: (1) `deploy_docker.yml`, (2) `smoke_test.yml` records `smoke_trace_id` + `smoke_run_id`, (3) `backup_docker.yml`, (4) `undeploy_docker.yml --extra-vars telemetron_purge_data=true`, (5) `deploy_docker.yml` (fresh containers, new D-146 bootstrap path), (6) `restore_docker.yml --extra-vars backup_restore_confirm=true backup_restore_from=<timestamp>`, (7) `smoke_test.yml --extra-vars smoke_trace_id=<recorded> smoke_run_id=<recorded>` asserts the same synthetic OTLP signals are visible in Grafana. Round-trip ends with no manual intervention required.

### Documentation cascade

- [x] **DOCS-V13-01**: `roles/README.md` documents Gate 11 ("every stateful role ships a tested `tasks/backup.yml` and `tasks/restore.yml`, proven on leviathan end-to-end") in the per-role port-acceptance gates section, in the same style and detail level as Gates 1-10. Names the 4 stateful roles (garage, prometheus, grafana, alertmanager) explicitly; states that stateless roles document their no-backup status in their role README only (no empty no-op task files).
- [x] **DOCS-V13-02**: `docs/quickstart.md` contains a `## Backup and restore` section that covers the default backup command line, the operator workflow for restore (including the `--extra-vars backup_restore_confirm=true` gate), the order-of-operations expectation (Loki/Tempo/Mimir stop before Garage restore), the local-disk destination + retention model (Telemetron writes dated tarballs; operator manages retention with rsync/restic/find/etc.), and the manual tarball-extraction fallback for operators who don't want to use `restore_docker.yml`. Root README Quick Start section contains a "When something goes wrong" line that links to `docs/quickstart.md#backup-and-restore`.
- [x] **DOCS-V13-03**: Every stateful role README (garage, prometheus, grafana, alertmanager) contains a new `## Backup` H2 section documenting what's backed up, what's NOT backed up (Garage's `s3-credentials` IS captured even though it's a host file; Grafana's provisioning ISN'T captured because it re-renders from version-controlled config; etc.), and the per-role tag invocation. Every stateless role README (loki, tempo, mimir, fluentbit, karma, node_exporter, opentelemetry, nfsd) contains a one-line note in its existing `## Uninstall` section neighborhood explaining why the role has no `tasks/backup.yml` (e.g., "Loki data lives in Garage S3 buckets — backed up via the garage role"; "Karma is stateless — no operator state to preserve").

## Future Requirements

Tracked but explicitly deferred from v1.3.0. Operators who need these today should wrap Telemetron's local-disk output with their own tooling.

### Backup destinations (off-host)

- [ ] **BACKUP-V14-01**: `backup_remote_target` opt-in rsync/SSH push after each role's local backup
- [ ] **BACKUP-V14-02**: `backup_remote_target_s3` pluggable for restic / rclone integration
- [ ] **BACKUP-V14-03**: Per-role retention via `backup_retention_keep_last_N` knob (default off)

### Backup features

- [ ] **BACKUP-V14-04**: Encryption at rest via opt-in `age` recipient (`backup_encrypt_age_recipient`)
- [ ] **BACKUP-V14-05**: Hot snapshot mechanisms — Garage `garage meta snapshot --all`, Prometheus `/api/v1/admin/tsdb/snapshot`, Grafana SQLite `.backup`, Alertmanager state file copy with appropriate mutex — for zero-downtime backup
- [ ] **BACKUP-V14-06**: Backup verification via parallel-restore (heavyweight integrity check)
- [ ] **BACKUP-V14-07**: Cross-version backup compatibility (v1.3.0 backups restoring under v1.4.0+ schema changes for Grafana/Prometheus)

### Operator surfaces (already deferred from prior milestones)

- [ ] **PREFLIGHT-V14-01..N**: Host-readiness diagnostic playbook (port conflicts, kernel modules, disk space, leftover Telemetron state)
- [ ] **DOCTOR-V14-01..N**: `docker_doctor` health-probe playbook (per-container `Health.Status`, label-stamp Gate 7 compliance, dashboard datasource resolvability)
- [ ] **SECRETS-V14-01..N**: Secrets rotation (Grafana admin password, Prometheus bearer tokens, Garage S3 keys without redeploying the world)
- [ ] **ALERT-V14-01..05** (originally ALERT-V2-01..05): Hook router (Flask + per-rule allowlist + per-tuple rate limit + Jenkins `buildWithParameters` auth)
- [ ] **DIST-V14-01..03** (originally DIST-01..03): Distributed / scale-out mode for Loki/Tempo/Mimir + HAProxy + multi-host inventory
- [ ] **ARCH-V14-01..02** (originally ARCH-01..02): Multi-arch (arm64 — Pi 5 / Apple Silicon) base image testing + CI matrix
- [ ] **DOCS-V14-01..07** (originally DOCS-V2-01..07): Documentation deep-dives — alerts, retention, fluentbit-timestamps, hook-router, instrumentation-otel, migration-from-inspq, metrics

## Out of Scope

Explicit exclusions with reasoning so they're not re-added.

- **Backups for stateless roles** — Loki, Tempo, Mimir don't get their own `tasks/backup.yml` because their data lives in Garage S3 (covered by Garage backup). OTel Collector, Fluent Bit, Karma, node_exporter, nfsd don't carry operator state (only deploy-time rendered config from version-controlled templates). Adding empty no-op `tasks/backup.yml` files for these would add maintenance surface for fiction — operator confusion + future-contributor drift. Per-role README one-liner is sufficient.
- **Multi-host coordination** — v1.3.0 ships single-host single-deploy. Multi-host backup (e.g., 3-node Garage cluster snapshot) is part of the DIST-V14-* deferred milestone alongside distributed mode itself.
- **Cross-Telemetron-version backup compatibility** — A v1.3.0 backup restored under v1.4.0 with schema migrations (e.g., Grafana 14 SQLite changes, Prometheus 3.x TSDB format bump) is explicitly out of v1.3.0. Same-version restore is the v1.3.0 quality bar.
- **Cross-host restore** — Backups taken on host A and restored on host B are not validated. Single-host backup-and-restore is the v1.3.0 scope. (In practice it likely works because the tarballs are filesystem-portable, but it's untested.)
- **Backup verification by parallel-restore** — Tar `--list` (`tar tf`) integrity check ships in v1.3.0; full parallel-restore verification (spin up a second container, restore into it, run smoke_test against it) is deferred — too heavy for the homelab quality bar.
- **Incremental / differential backups** — v1.3.0 ships full-only. Operators who need incremental wrap with rsync / restic / borg externally.
- **Encryption at rest** — Operators who need it wrap with `age` / `gpg` / restic / LUKS externally. Keeps `secrets.yml` surface unchanged.
- **Operator-managed retention via Telemetron knobs** — No `backup_retention_keep_last_N` knob in v1.3.0. Operator runs their own `find /opt/telemetron/backups/ -mtime +N -delete` cron / equivalent.
- **Off-host shipping (rsync, S3, rclone push)** — Operator wraps Telemetron's local-disk output with their own tooling. Telemetron stays opinion-free on the off-host layer (mirrors the reverse-proxy-agnostic philosophy from v1.0).
- **Auto-restore on next deploy** — Operator must explicitly invoke `restore_docker.yml`. No "if I find a backup, restore it automatically" behaviour. Restore is a deliberate operator action, not a startup heuristic.
- **Last-good-backup auto-discovery via integrity check** — `find | sort -r | head -1` (latest by filename/timestamp) is the v1.3.0 default; integrity-verified-latest is deferred to v1.4.0+.
- **PromLens backup** — PromLens was removed in v1.0.1; not applicable.

## Traceability

| REQ-ID | Phase | Status |
|--------|-------|--------|
| BACKUP-V13-01 | 13 | Complete |
| BACKUP-V13-02 | 13 | Complete |
| BACKUP-V13-03 | 13 | Complete |
| BACKUP-V13-04 | 13 | Complete |
| BACKUP-V13-05 | 14 | Complete |
| RESTORE-V13-01 | 13 | Complete |
| RESTORE-V13-02 | 13 | Complete |
| RESTORE-V13-03 | 13 | Complete |
| RESTORE-V13-04 | 13 | Complete |
| RESTORE-V13-05 | 14 | Complete |
| OPS-V13-01 | 14 | Complete |
| OPS-V13-02 | 14 | Complete |
| OPS-V13-03 | 14 | Complete |
| OPS-V13-04 | 13 | Complete |
| UAT-V13-01 | 14 | Complete |
| DOCS-V13-01 | 15 | Complete |
| DOCS-V13-02 | 15 | Complete |
| DOCS-V13-03 | 15 | Complete |
