---
phase: 13-per-role-backup-restore-tasks
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - roles/garage/tasks/backup.yml
  - roles/garage/tasks/restore.yml
autonomous: true
requirements:
  - BACKUP-V13-01
  - RESTORE-V13-01
  - OPS-V13-04
user_setup: []

must_haves:
  truths:
    - "Operator can include_role: name=garage tasks_from=backup and a single `garage-<UTC>.tar.zst` is created at `/opt/telemetron/backups/garage/` mode 0600, dir mode 0700"
    - "The Garage tarball contains three top-level entries: `meta/`, `data/`, `s3-credentials`"
    - "The Garage container is `docker stop`'d via `ansible.builtin.command` (NEVER `community.docker state: stopped`) for cold-quiesce; container is ALWAYS restarted afterward via block/rescue/always"
    - "Restore refuses to run without `backup_restore_confirm=true` (fail-fast inside the role-task itself, not just the orchestrator)"
    - "Restore integrity-checks the tarball via `tar tf` before wiping any volume contents"
    - "After restore, Garage container is running and HEALTHCHECK reports healthy (inline poll, no verify.yml since Garage ships none)"
    - "Both tasks/backup.yml and tasks/restore.yml begin with an `ansible.builtin.package: zstd state: present` pre-task; idempotent (changed=0 on second run if zstd already installed)"
  artifacts:
    - path: "roles/garage/tasks/backup.yml"
      provides: "Cold-quiesce backup with 3-entry tarball (D-176)"
      contains: "ansible.builtin.command: docker stop"
      min_lines: 80
    - path: "roles/garage/tasks/restore.yml"
      provides: "Confirm-gated wipe + untar restore with inline health poll"
      contains: "backup_restore_confirm"
      min_lines: 100
  key_links:
    - from: "roles/garage/tasks/backup.yml"
      to: "{{ garage_meta_volume }}, {{ garage_data_volume }}, {{ garage_s3_credentials_file }}"
      via: "tar --zstd -cpf with --transform for 3 entries"
      pattern: "tar --zstd"
    - from: "roles/garage/tasks/restore.yml"
      to: "/var/lib/docker/volumes/{{ garage_meta_volume }}/_data, /var/lib/docker/volumes/{{ garage_data_volume }}/_data, {{ garage_s3_credentials_file }}"
      via: "find -mindepth 1 -delete then tar --zstd -xpf"
      pattern: "find .* -mindepth 1 -delete"
    - from: "block: ... always:"
      to: "ansible.builtin.command: docker start telemetron-garage"
      via: "always-clause restart guarantee (AN-2)"
      pattern: "docker start {{ garage_container_name }}"
---

<objective>
Ship `roles/garage/tasks/backup.yml` and `roles/garage/tasks/restore.yml` — the most complex of the 4 stateful role backup/restore pairs because Garage carries 3 distinct entries (meta volume + data volume + host bind-mount `s3-credentials`) in a single tarball per locked decision D-176.

Purpose:
- BACKUP-V13-01: produce `/opt/telemetron/backups/garage/garage-<UTC-timestamp>.tar.zst` (mode 0600) containing the 3 entries via cold-quiesce of `telemetron-garage`
- RESTORE-V13-01: symmetric inverse with `backup_restore_confirm=true` gate, `tar tf` integrity check, wipe-and-untar of both volume `_data/` paths AND `s3-credentials` host file, then restart Garage and inline-poll until healthy
- OPS-V13-04: `zstd` ensure-present pre-task in both files (idempotent — `changed=0` on second run)

Garage is unique among the 4 stateful roles because (a) it has NO `tasks/verify.yml` — the inline `community.docker.docker_container_info` healthy-poll from `bootstrap.yml` lines 21-35 is the verify mechanism; (b) the tarball captures a host file that lives outside both Docker volumes (per GP-3 — avoids the post-restore D-146 self-healing branch and keeps Loki/Tempo/Mimir's credentials in sync).

Output: 2 new Ansible task files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-per-role-backup-restore-tasks/13-CONTEXT.md
@.planning/phases/13-per-role-backup-restore-tasks/13-PATTERNS.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/research/STACK.md
@.planning/phases/13-per-role-backup-restore-tasks/13-01-PLAN.md

<interfaces>
<!-- Variable names + analog file line ranges executor needs. Extracted from existing role + 13-PATTERNS.md. -->

From roles/garage/defaults/main.yml (existing, line numbers approximate):
- `garage_container_name: telemetron-garage` (line 21)
- `garage_meta_volume: "{{ telemetron_volume_prefix | default('telemetron') }}_garage_meta"` (line 37)
- `garage_data_volume: "{{ telemetron_volume_prefix | default('telemetron') }}_garage_data"` (line 38)
- `garage_config_dir: "{{ telemetron_config_root | default('/opt/telemetron') }}/garage"` (line 44)
- `garage_health_retries: 30` (line 62)
- `garage_health_delay: 2` (line 63)
- `garage_s3_credentials_file: "{{ garage_config_dir }}/s3-credentials"` (line 101)
- `garage_backup_stop_timeout: "{{ backup_stop_timeout | default(60) }}"` (added by plan 13-01)

From inventory/example-homelab/group_vars/all/backup.yml (created by plan 13-01):
- `backup_dest_root: /opt/telemetron/backups`
- `backup_stop_timeout: 60`
- `backup_restore_confirm: false`
- `backup_restore_from: ""`

Analog files (read for shape, do NOT modify):
- roles/garage/tasks/uninstall.yml (header narrative + 4-task structural shape; D-145/D-146/D-147 reference comments — 13-PATTERNS.md lines 38-66)
- roles/garage/tasks/bootstrap.yml lines 21-35 (inline `community.docker.docker_container_info` healthy-poll — 13-PATTERNS.md lines 159-175)
- roles/garage/tasks/purge.yml lines 66-72 (D-159 WARN-before-destruction shape; `when: telemetron_purge_data | default(false) | bool` gate idiom — 13-PATTERNS.md lines 282-301)
- roles/garage/tasks/main.yml lines 7-14 (`ansible.builtin.file: state: directory` for destination dir create — 13-PATTERNS.md lines 111-137)
- roles/nfsd/tasks/install_debian.yml lines 5-11 (only existing `ansible.builtin.package:` precedent — 13-PATTERNS.md lines 85-92)

Key tar invocation (NEW pattern, 13-PATTERNS.md lines 207-228 verbatim):
```
tar --zstd -cpf {{ backup_dest_root }}/garage/garage-{{ backup_timestamp.stdout }}.tar.zst \
  --transform 's,^var/lib/docker/volumes/{{ garage_meta_volume }}/_data,meta,' \
  --transform 's,^var/lib/docker/volumes/{{ garage_data_volume }}/_data,data,' \
  --transform 's,^{{ garage_s3_credentials_file | regex_replace("^/", "") }}$,s3-credentials,' \
  /var/lib/docker/volumes/{{ garage_meta_volume }}/_data \
  /var/lib/docker/volumes/{{ garage_data_volume }}/_data \
  {{ garage_s3_credentials_file }}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create roles/garage/tasks/backup.yml — cold-quiesce 3-entry tarball</name>
  <files>roles/garage/tasks/backup.yml</files>
  <read_first>
    - roles/garage/tasks/uninstall.yml (full file — analog for docstring-style header narrative; 13-PATTERNS.md lines 38-66 has the verbatim adaptation for the header comment)
    - roles/garage/tasks/bootstrap.yml lines 21-35 (verbatim shape for `community.docker.docker_container_info` healthy-poll; 13-PATTERNS.md lines 159-175 and 240-254)
    - roles/garage/tasks/main.yml lines 7-14 (analog for destination-dir creation via `ansible.builtin.file: state: directory`; 13-PATTERNS.md lines 111-137)
    - roles/nfsd/tasks/install_debian.yml lines 5-11 (only existing `ansible.builtin.package:` precedent in the repo; 13-PATTERNS.md lines 85-105 has the adapted shape for zstd ensure-present)
    - .planning/phases/13-per-role-backup-restore-tasks/13-PATTERNS.md (Garage backup.yml full pattern walk-through, lines 26-256)
    - .planning/phases/13-per-role-backup-restore-tasks/13-CONTEXT.md (D-176 single-tarball 3-entry decision; D-177 stop-timeout; D-178 inline poll because Garage has no verify.yml; D-179 timestamp format; carry-forwards: cold-quiesce + `ansible.builtin.command: docker stop` per XP-1; block/rescue/always per AN-2)
    - .planning/research/PITFALLS.md (GP-1 LMDB cold-copy correctness; GP-2 both volumes in scope; GP-3 s3-credentials MUST be captured; XP-1 NEVER `community.docker state: stopped`; XP-4 mode 0600/0700; XP-5 host `_data/` path access; AN-1 no `state: absent`; AN-2 always-restart)
    - .planning/research/ARCHITECTURE.md § 1 (7-task backup shape) and § 8 (storage layout / permissions)
  </read_first>
  <action>
    Create `roles/garage/tasks/backup.yml` per the verbatim walk-through in `.planning/phases/13-per-role-backup-restore-tasks/13-PATTERNS.md` lines 26-256. The file MUST implement these task lines in this order:

    1. Docstring-style header comment block (50-60 lines) per 13-PATTERNS.md lines 38-66 — name the file, cite D-176 single-tarball decision with the 3-entry breakdown, cite GP-1 LMDB cold-copy requirement, AN-2 always-restart guarantee, and note that Garage has NO `tasks/verify.yml` (inline poll per D-178).

    2. `ansible.builtin.package: name: zstd state: present become: true` — first task in the file, idempotent, tags `[garage, backup]`. Adapt from `roles/nfsd/tasks/install_debian.yml` lines 5-11 minus `update_cache` (per 13-PATTERNS.md lines 96-105). This implements OPS-V13-04.

    3. `ansible.builtin.file: path: "{{ backup_dest_root }}/garage" state: directory mode: "0700" owner: root group: root become: true` — destination dir create, lazy idempotent. Adapt from `roles/garage/tasks/main.yml` lines 7-14 (per 13-PATTERNS.md lines 125-137). Mode 0700 per XP-4 (tarball may contain s3-credentials secret).

    4. `ansible.builtin.command: date -u +%Y%m%dT%H%M%SZ` registered as `backup_timestamp`, `changed_when: false` — D-179 ISO 8601 basic UTC. Per 13-PATTERNS.md lines 144-151.

    5. The block/rescue/always wrapper begins (Pattern A from 13-PATTERNS.md lines 737-761):
       - `block:` opens
         - `ansible.builtin.command: "docker stop -t {{ garage_backup_stop_timeout }} {{ garage_container_name }}"` with `changed_when: true become: true` — XP-1 critical: NEVER `community.docker.docker_container state: stopped`. Cite XP-1 in inline comment.
         - `community.docker.docker_container_info: name: "{{ garage_container_name }}"` registered as `garage_stopped_check` with `until: ... State.Running == false`, `retries: 30`, `delay: 2`, `changed_when: false`. Verbatim shape from `roles/garage/tasks/bootstrap.yml` lines 21-35 with `until:` clause flipped from `Health.Status == 'healthy'` to `State.Running == false` (per 13-PATTERNS.md lines 188-200).
         - `ansible.builtin.command: cmd: |- tar --zstd -cpf ... --transform ... --transform ... --transform ... /var/lib/docker/volumes/{{ garage_meta_volume }}/_data /var/lib/docker/volumes/{{ garage_data_volume }}/_data {{ garage_s3_credentials_file }}` — the 3-entry tarball. Use the exact shell command from 13-PATTERNS.md lines 207-220. `become: true changed_when: true`.
         - `ansible.builtin.file: path: "{{ backup_dest_root }}/garage/garage-{{ backup_timestamp.stdout }}.tar.zst" mode: "0600" owner: root group: root become: true` — set tarball perms per XP-4.
       - `always:` opens (AN-2 — restart even on tar failure)
         - `ansible.builtin.command: "docker start {{ garage_container_name }}"` with `changed_when: true become: true`.
         - `community.docker.docker_container_info: name: "{{ garage_container_name }}"` registered as `garage_post_backup_health` with `until: ... State.Health.Status == 'healthy'`, `retries: "{{ garage_health_retries }}"`, `delay: "{{ garage_health_delay }}"`, `changed_when: false`. Verbatim shape from `roles/garage/tasks/bootstrap.yml` lines 21-35 (per 13-PATTERNS.md lines 240-254). NO `include_tasks: verify.yml` because Garage has none (D-178).
       - `tags: [garage, backup]` on the block parent. Do NOT carry tags inside the include or on every sub-task — the parent tag is sufficient and matches ARCHITECTURE.md § 5.

    Naming convention reminders (Rock's memory): container name `telemetron-garage` uses a hyphen (RFC 1123); Ansible variable names use underscores; tarball filename uses hyphens. No `vault_*` prefix on anything.

    Per Garage v2 key list format (Rock's memory): this file does NOT parse `garage key list` output, so no regex is needed. If a future plan adds a key-list verify step inside backup.yml, copy regex verbatim from `roles/garage/tasks/bootstrap.yml` line 185.

    Use the Write tool, not heredoc. Do NOT inline placeholder task names like `Task 1: ...` — use full descriptive Ansible `name:` strings (English only per CLAUDE.md).
  </action>
  <verify>
    <automated>test -f roles/garage/tasks/backup.yml && python3 -c "import yaml; yaml.safe_load(open('roles/garage/tasks/backup.yml'))" && grep -c 'ansible.builtin.command: docker stop' roles/garage/tasks/backup.yml | grep -q '^[1-9]' && grep -c '^  block:$' roles/garage/tasks/backup.yml | grep -q '^[1-9]' && grep -c '^  always:$' roles/garage/tasks/backup.yml | grep -q '^[1-9]' && ! grep -qE 'community\.docker\.docker_container:\s*$|state:\s*stopped' roles/garage/tasks/backup.yml && grep -q 'name: zstd' roles/garage/tasks/backup.yml && grep -q '{{ garage_s3_credentials_file }}' roles/garage/tasks/backup.yml && grep -q '{{ garage_meta_volume }}' roles/garage/tasks/backup.yml && grep -q '{{ garage_data_volume }}' roles/garage/tasks/backup.yml && grep -q 'mode: "0600"' roles/garage/tasks/backup.yml && grep -q 'mode: "0700"' roles/garage/tasks/backup.yml && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `roles/garage/tasks/backup.yml` exists and YAML-parses cleanly via `python3 -c "import yaml; yaml.safe_load(open(...))"`
    - File contains `ansible.builtin.command: "docker stop -t {{ garage_backup_stop_timeout }} {{ garage_container_name }}"` (or semantically identical command form). `grep -c 'docker stop' roles/garage/tasks/backup.yml` returns ≥ 1.
    - File contains exactly one `block:` opener and one `always:` opener at the same indent level (AN-2 wrapper). `grep -c '^  block:$\|^  always:$' roles/garage/tasks/backup.yml` returns 2.
    - File does NOT contain `state: stopped` anywhere (XP-1): `grep -c 'state: stopped' roles/garage/tasks/backup.yml | grep -v '^#'` returns 0.
    - File does NOT contain `state: absent` on `community.docker.docker_container` (AN-1): `grep -B1 'state: absent' roles/garage/tasks/backup.yml | grep -c 'docker_container'` returns 0.
    - `ansible.builtin.package: name: zstd state: present` pre-task present (OPS-V13-04): `grep -A2 'ansible.builtin.package' roles/garage/tasks/backup.yml | grep -q 'name: zstd'` exits 0.
    - 3-entry tarball: file references all of `{{ garage_meta_volume }}`, `{{ garage_data_volume }}`, AND `{{ garage_s3_credentials_file }}` (GP-3): each `grep -q` exits 0.
    - 3 `--transform` clauses for tar (one per entry): `grep -c '\-\-transform' roles/garage/tasks/backup.yml` returns ≥ 3.
    - Tarball destination mode 0600 set via `ansible.builtin.file` post-tar (XP-4): `grep -q 'mode: "0600"' roles/garage/tasks/backup.yml` exits 0.
    - Destination dir mode 0700: `grep -q 'mode: "0700"' roles/garage/tasks/backup.yml` exits 0.
    - Tags include both `garage` and `backup`: `grep -A1 'tags:' roles/garage/tasks/backup.yml | grep -c 'garage\|backup'` returns ≥ 2.
    - Container restart in `always:` clause: in the file, line numbers verify `docker start {{ garage_container_name }}` appears AFTER an `always:` line (`awk '/^  always:/{found=1} found && /docker start/{print NR; exit}' roles/garage/tasks/backup.yml` returns a line number > 0).
    - Inline healthy-poll uses `State.Health.Status == 'healthy'` (no `include_tasks: verify.yml` because Garage has none): `grep -q "Health.Status == 'healthy'" roles/garage/tasks/backup.yml` exits 0 AND `grep -c 'include_tasks: verify.yml' roles/garage/tasks/backup.yml` returns 0.
    - Header docstring cites at least D-176, D-178, and GP-1: `grep -cE 'D-176|D-178|GP-1|GP-3|AN-2' roles/garage/tasks/backup.yml` returns ≥ 3.
    - Total lines ≥ 80 (`wc -l < roles/garage/tasks/backup.yml` ≥ 80).
  </acceptance_criteria>
  <done>
    `roles/garage/tasks/backup.yml` ships the cold-quiesce 3-entry tarball pattern (meta + data + s3-credentials) using `ansible.builtin.command: docker stop` (NOT `state: stopped`), wrapped in block/always with inline healthy-poll restart guarantee. File parses cleanly, contains all locked decisions (D-176/D-178), and respects all pitfalls (GP-1/GP-3/XP-1/XP-4/AN-1/AN-2).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create roles/garage/tasks/restore.yml — confirm-gated wipe + 3-entry untar</name>
  <files>roles/garage/tasks/restore.yml</files>
  <read_first>
    - roles/garage/tasks/backup.yml (just created in Task 1 — symmetric inverse)
    - roles/garage/tasks/purge.yml (analog for D-159 WARN + `when:` gate idiom; 13-PATTERNS.md lines 282-301 has the WARN adaptation; lines 924-940 has the `ansible.builtin.fail` confirm-gate)
    - roles/garage/tasks/uninstall.yml lines 1-55 (analog for header narrative)
    - roles/garage/tasks/bootstrap.yml lines 21-35 (inline healthy-poll for the final post-restart verify)
    - .planning/phases/13-per-role-backup-restore-tasks/13-PATTERNS.md (Garage restore.yml walk-through lines 258-361)
    - .planning/phases/13-per-role-backup-restore-tasks/13-CONTEXT.md (D-179 latest-tarball discovery; `backup_restore_confirm=true` gate fires inside the role-task not just the orchestrator; carry-forward: Garage `s3-credentials` IS restored so D-146 self-healing branch does NOT fire)
    - .planning/research/PITFALLS.md (GP-1 cold-quiesce on restore too; GP-2 restore both volumes; GP-3 restore s3-credentials BEFORE container start; XP-1; AN-1; AN-2; AN-3 vault token rotation breaks bootstrap if `garage_admin_token` changed between backup and restore — document in header, no code change)
    - .planning/research/STACK.md § 3 (wipe pattern: `find -mindepth 1 -delete`, NOT `docker_volume state: absent`)
  </read_first>
  <action>
    Create `roles/garage/tasks/restore.yml` per `.planning/phases/13-per-role-backup-restore-tasks/13-PATTERNS.md` lines 258-361. The file MUST implement this task sequence:

    1. Docstring-style header comment block (40-50 lines): cite D-176 3-entry restore symmetry, GP-3 (s3-credentials restored before container start so D-146 recovery branch does NOT fire), AN-3 (vault token rotation between backup and restore breaks bootstrap verify with 401 — operator must keep `garage_admin_token`/`garage_rpc_secret` stable in `secrets.yml`), and the fail-fast confirm gate.

    2. `ansible.builtin.package: name: zstd state: present become: true` — first task, tags `[garage, restore]`. OPS-V13-04 idempotent ensure-present. Same shape as backup.yml.

    3. **Confirm gate (Pattern I — 13-PATTERNS.md lines 924-940):** `ansible.builtin.fail: msg: "Restore is destructive. Set --extra-vars backup_restore_confirm=true to confirm." when: not (backup_restore_confirm | default(false) | bool) tags: [garage, restore]`. Per CONTEXT.md, this gate fires inside the role-task — a standalone `include_role: tasks_from=restore` from a custom playbook is also blocked.

    4. **Latest-tarball discovery (Pattern G — 13-PATTERNS.md lines 304-320):** When `backup_restore_from | default("") == ""`, `ansible.builtin.shell: cmd: "find {{ backup_dest_root }}/garage/ -name 'garage-*.tar.zst' -type f -printf '%f\n' | sort -r | head -1"` registered as `garage_latest_tarball`, `changed_when: false`, `failed_when: garage_latest_tarball.stdout == ""`. Then `set_fact: backup_src_path: "{{ backup_dest_root }}/garage/{{ garage_latest_tarball.stdout }}"` (when latest path) OR `"{{ backup_dest_root }}/garage/garage-{{ backup_restore_from }}.tar.zst"` (when pinned).

    5. **D-159 WARN (Pattern E — 13-PATTERNS.md lines 295-301):** `ansible.builtin.debug: msg: "WARNING: irreversible -- garage restore: {{ garage_meta_volume }}, {{ garage_data_volume }}, {{ garage_s3_credentials_file }} from {{ backup_src_path }}"`. Per CONTEXT.md "Restore-side D-159 WARN must NOT echo the tarball contents or volume contents" — only the names of what will be replaced, NOT the data itself. This WARN does that correctly because it lists only the destination paths, not contents.

    6. **Integrity check (Pattern G — 13-PATTERNS.md lines 322-333):** `ansible.builtin.command: cmd: "tar --zstd -tf {{ backup_src_path }}"` with `become: true changed_when: false`. Fails fast if the tarball is corrupt — guards against the catastrophic case where we wipe `_data/` and only THEN discover the source is bad.

    7. Block/rescue/always wrapper opens (AN-2):
       - `block:` opens
         - `ansible.builtin.command: "docker stop -t {{ garage_backup_stop_timeout }} {{ garage_container_name }}"` with `become: true changed_when: true`.
         - `community.docker.docker_container_info` poll until `State.Running == false`, `retries: 30 delay: 2 changed_when: false`. Same shape as backup.yml.
         - **Wipe step (Pattern H — 13-PATTERNS.md lines 337-355):**
           - `ansible.builtin.command: cmd: "find /var/lib/docker/volumes/{{ garage_meta_volume }}/_data -mindepth 1 -delete"` `become: true changed_when: true`.
           - `ansible.builtin.command: cmd: "find /var/lib/docker/volumes/{{ garage_data_volume }}/_data -mindepth 1 -delete"` `become: true changed_when: true`.
           - `ansible.builtin.file: path: "{{ garage_s3_credentials_file }}" state: absent` `become: true`. (Note: `state: absent` on a single file is correct per AN-1 — AN-1 is specifically about NEVER applying `state: absent` to `community.docker.docker_container`; per-file removal is fine.)
         - **Untar step:** `ansible.builtin.command: cmd: "tar --zstd -xpf {{ backup_src_path }} -C /"` `become: true changed_when: true`. Note: backup.yml uses `--transform` to flatten the 3 entries to `meta/`/`data/`/`s3-credentials` in the tarball, so the restore must reconstruct the absolute paths. The `-C /` chdir + the tarball's relative `meta/`, `data/`, `s3-credentials` entries means we must instead use the corresponding `--transform` on extract OR keep the tarball entries as absolute paths.

           **DECISION POINT for executor (resolve before writing):** The backup.yml `--transform` rewrites paths to `meta/`, `data/`, `s3-credentials`. For restore to land them back at `/var/lib/docker/volumes/{{ garage_meta_volume }}/_data/...`, `/var/lib/docker/volumes/{{ garage_data_volume }}/_data/...`, and `{{ garage_s3_credentials_file }}` respectively, use TWO untar invocations with `-C` per-volume + a `--strip-components` + a third for the host file:
           - `ansible.builtin.command: cmd: "tar --zstd -xpf {{ backup_src_path }} --strip-components=1 -C /var/lib/docker/volumes/{{ garage_meta_volume }}/_data meta"` (extracts only the `meta/` entry, strips the `meta/` prefix, lands files at `_data/`).
           - Same shape for `data/` → `/var/lib/docker/volumes/{{ garage_data_volume }}/_data`.
           - Third invocation to extract `s3-credentials` to the host file location: `tar --zstd -xpf {{ backup_src_path }} -O s3-credentials > {{ garage_s3_credentials_file }}` won't work cleanly with Ansible's `command:` redirection. Use instead: `ansible.builtin.command: cmd: "tar --zstd -xpf {{ backup_src_path }} -C {{ garage_config_dir }} s3-credentials"` — extracts the `s3-credentials` entry directly to `garage_config_dir/`, which equals `garage_s3_credentials_file` (line 101 of garage defaults: `garage_s3_credentials_file: "{{ garage_config_dir }}/s3-credentials"`).
           - `ansible.builtin.file: path: "{{ garage_s3_credentials_file }}" mode: "0600" owner: root group: root` `become: true` — re-apply the per-file mode (XP-4: the host file holds the S3 secret key).

       - `always:` opens (AN-2):
         - `ansible.builtin.command: "docker start {{ garage_container_name }}"` `become: true changed_when: true`.
         - Inline `community.docker.docker_container_info` healthy-poll, same shape as backup.yml `always:` block.
       - `tags: [garage, restore]` on the block parent.

    Naming convention reminders: container `telemetron-garage` (hyphen, RFC 1123); vars use underscores; tarball filenames use hyphens. No `vault_*` prefix. English-only `name:` strings.

    Per Rock's memory ("`--syntax-check` skips role internals"): verify both via `--syntax-check` AND `python3 yaml.safe_load`.

    Use the Write tool, not heredoc. Do NOT use `ignore_errors:` anywhere — `state: absent` on the host file is already idempotent and the integrity check earlier in the file guards against bad tarballs.
  </action>
  <verify>
    <automated>test -f roles/garage/tasks/restore.yml && python3 -c "import yaml; yaml.safe_load(open('roles/garage/tasks/restore.yml'))" && grep -q 'backup_restore_confirm' roles/garage/tasks/restore.yml && grep -q 'ansible.builtin.fail' roles/garage/tasks/restore.yml && grep -q 'tar --zstd -tf' roles/garage/tasks/restore.yml && grep -q 'find /var/lib/docker/volumes/{{ garage_meta_volume }}/_data -mindepth 1 -delete' roles/garage/tasks/restore.yml && grep -q 'find /var/lib/docker/volumes/{{ garage_data_volume }}/_data -mindepth 1 -delete' roles/garage/tasks/restore.yml && grep -q '{{ garage_s3_credentials_file }}' roles/garage/tasks/restore.yml && grep -q 'WARNING: irreversible' roles/garage/tasks/restore.yml && grep -q 'name: zstd' roles/garage/tasks/restore.yml && grep -c '^  block:$\|^  always:$' roles/garage/tasks/restore.yml | grep -q '^2$' && ! grep -qE 'state:\s*stopped' roles/garage/tasks/restore.yml && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `roles/garage/tasks/restore.yml` exists and YAML-parses cleanly
    - Confirm gate present (OPS-V13-01 mirror, role-level): `grep -B5 'ansible.builtin.fail' roles/garage/tasks/restore.yml | grep -c 'backup_restore_confirm'` returns ≥ 1 AND `grep -A3 'ansible.builtin.fail' roles/garage/tasks/restore.yml | grep -c "when: not (backup_restore_confirm"` returns ≥ 1
    - Latest-tarball discovery via `find ... | sort -r | head -1`: `grep -q "find {{ backup_dest_root }}/garage/" roles/garage/tasks/restore.yml` exits 0 AND `grep -q "sort -r" roles/garage/tasks/restore.yml` exits 0 (D-179)
    - Tarball integrity check via `tar --zstd -tf` BEFORE any wipe step (verify line ordering): `awk '/tar --zstd -tf/{tf=NR} /-mindepth 1 -delete/{wipe=NR} END{exit !(tf < wipe && tf > 0)}' roles/garage/tasks/restore.yml` exits 0
    - D-159 WARN present: `grep -c 'WARNING: irreversible -- garage restore' roles/garage/tasks/restore.yml` returns ≥ 1
    - 3-entry restore: file references all of `{{ garage_meta_volume }}`, `{{ garage_data_volume }}`, AND `{{ garage_s3_credentials_file }}` (GP-3)
    - Two wipe steps (one per volume) AND host-file removal: `grep -c 'find /var/lib/docker/volumes/.* -mindepth 1 -delete' roles/garage/tasks/restore.yml` returns 2; AND `grep -B1 -A3 'state: absent' roles/garage/tasks/restore.yml | grep -c 'garage_s3_credentials_file'` returns ≥ 1
    - `docker stop` (NOT `state: stopped`) for cold-quiesce: `grep -c 'docker stop' roles/garage/tasks/restore.yml` returns ≥ 1 AND `grep -c 'state: stopped' roles/garage/tasks/restore.yml` returns 0
    - No `state: absent` on `community.docker.docker_container` (AN-1): `grep -B1 'state: absent' roles/garage/tasks/restore.yml | grep -c 'docker_container'` returns 0
    - block/rescue/always wrapper: exactly one `block:` and one `always:` at the right indent level (`grep -c '^  block:$\|^  always:$' roles/garage/tasks/restore.yml` returns 2)
    - `ansible.builtin.package: name: zstd` pre-task present (OPS-V13-04)
    - Restored s3-credentials file gets mode 0600 re-applied: `grep -B2 -A3 'mode: "0600"' roles/garage/tasks/restore.yml | grep -c 'garage_s3_credentials_file'` returns ≥ 1
    - Tags include both `garage` and `restore`
    - Inline healthy-poll for post-start verify (no `include_tasks: verify.yml`)
    - Header cites at least D-176, GP-3, AN-3: `grep -cE 'D-176|GP-3|AN-3' roles/garage/tasks/restore.yml` returns ≥ 3
    - Total lines ≥ 100
  </acceptance_criteria>
  <done>
    `roles/garage/tasks/restore.yml` ships the confirm-gated, integrity-checked, wipe-then-untar pattern restoring all 3 entries (meta + data + s3-credentials), with cold-quiesce + always-restart guarantee. File parses cleanly; all locked decisions (D-176/D-178/D-179) and pitfalls (GP-1/GP-3/XP-1/AN-1/AN-2) respected.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → Ansible | Operator runs `ansible-playbook` with `--ask-vault-pass`; vault secrets decrypted in-memory |
| Ansible → Docker daemon | `become: true` tasks invoke `docker` CLI as root on the host |
| Docker volume `_data/` → tarball | Root-owned host path read by tar; tarball written to `{{ backup_dest_root }}/garage/` mode 0600 |
| s3-credentials host file (mode 0600) → tarball | Secret S3 key inside the host file is captured in the tarball — tarball mode 0600 protects it at rest |
| Restore source tarball → volume `_data/` paths | Wipe + untar replaces existing volume contents irreversibly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-01 | Spoofing | `backup_src_path` from operator-supplied `backup_restore_from` | mitigate | Path is built by Ansible template substitution from a vetted prefix `{{ backup_dest_root }}/garage/garage-` + the operator-supplied timestamp + the literal `.tar.zst` suffix. The `tar --zstd -tf` integrity check before any wipe step ensures a path-traversal-style payload cannot smuggle in a different file (tar would fail to list a non-tarball). |
| T-13-02-02 | Tampering | Tarball corruption between backup and restore | mitigate | `tar --zstd -tf` integrity check BEFORE wipe step. If the tarball is truncated or corrupted, restore fails before any destructive operation. Out-of-scope for v1.3.0: SHA256 sidecar (deferred per FEATURES.md). |
| T-13-02-03 | Repudiation | Backup taken at wrong time / wrong key | mitigate | D-179 ISO 8601 basic UTC timestamp in filename + tarball perms 0600 + Ansible play-recap log all create an audit trail. No additional logging in v1.3.0 (manifest.json deferred). |
| T-13-02-04 | Information disclosure | s3-credentials secret key captured in tarball at rest | mitigate | Tarball mode 0600 (XP-4) + per-role subdir mode 0700 (XP-4). The default `/opt/telemetron/backups/` is root-owned. Encryption at rest is explicitly deferred to operator wrapping (FEATURES.md anti-feature). |
| T-13-02-05 | Information disclosure | WARN msg echoes tarball contents | mitigate | D-159 WARN msg names only the volume/file IDENTIFIERS (e.g., `{{ garage_meta_volume }}`), NOT their contents. Per CONTEXT.md, the restore-side WARN does NOT include `tar tf` listing or grep over `_data/`. |
| T-13-02-06 | Denial of service | Restore wipes volumes irreversibly on misfire | mitigate | Two-layer gate: (a) `backup_restore_confirm=true` `ansible.builtin.fail` gate at the start of restore.yml — fires BEFORE any destructive step; (b) `tar --zstd -tf` integrity check passes before any `find ... -mindepth 1 -delete`. Orchestrator-level gate (Phase 14) is a third layer. |
| T-13-02-07 | Denial of service | `docker stop -t 0` via operator-set `garage_backup_stop_timeout: 0` | accept | Operator footgun. Default 60s gives LMDB clean-close margin (GP-1). Phase 15 docs document the trade-off. No code-level guard. |
| T-13-02-08 | Elevation of privilege | `become: true` on tar/find tasks | accept | Required to read `/var/lib/docker/volumes/*/_data` (root-owned per Docker convention) and write to `/opt/telemetron/backups/` (root-owned mode 0700). Same threat surface as `deploy_docker.yml`. |
| T-13-02-09 | Tampering | `community.docker state: stopped` strips volume specs (XP-1) | mitigate | Explicit `ansible.builtin.command: docker stop` + `community.docker.docker_container_info` poll instead. AN-1 acceptance criterion enforces no `state: stopped` or `state: absent` on containers in either file. |
| T-13-02-SC | Tampering | npm/pip/cargo installs | n/a | Only OS package install is `zstd` via `ansible.builtin.package` — distro package manager, NOT npm/pip/cargo. No package legitimacy audit required per the planner rules (audit applies to npm/pip/cargo only). |
</threat_model>

<verification>
After both tasks complete, validate the 2 files and ensure they pass the `--syntax-check` gate:

```bash
# YAML parse cleanly
python3 -c "
import yaml
for f in ['roles/garage/tasks/backup.yml','roles/garage/tasks/restore.yml']:
    yaml.safe_load(open(f))
    print(f'{f}: parses OK')
"

# deploy_docker.yml still syntax-checks (it does not include these files via include_role at deploy time; this is a sanity gate that the YAML inside roles/ is still valid)
ansible-playbook playbooks/deploy_docker.yml --syntax-check -i inventory/example-homelab/hosts --vault-password-file <(echo dummy) > /dev/null 2>&1 && echo "Syntax-check: OK"

# Stand-alone include test: a tiny synthetic playbook that includes the backup task file must parse
cat > /tmp/gsd-test-include.yml <<'EOF'
- hosts: localhost
  gather_facts: false
  tasks:
    - ansible.builtin.include_role:
        name: garage
        tasks_from: backup
EOF
ansible-playbook /tmp/gsd-test-include.yml --syntax-check -i /tmp/gsd-test-empty-inventory.yml > /dev/null 2>&1 || echo "Include-role syntax probe failed (acceptable if inventory missing)"
rm -f /tmp/gsd-test-include.yml
```
</verification>

<success_criteria>
- `roles/garage/tasks/backup.yml` and `roles/garage/tasks/restore.yml` both exist and parse cleanly
- Backup: 3-entry tarball (meta + data + s3-credentials) via `--transform`, cold-quiesce via `ansible.builtin.command: docker stop`, AN-2 always-restart, inline healthy-poll (no verify.yml because Garage has none — D-178)
- Restore: `backup_restore_confirm=true` fail-fast gate, latest-tarball discovery via `find | sort -r | head -1`, `tar --zstd -tf` integrity check BEFORE any wipe, 3-step wipe + 3-step extract, cold-quiesce + AN-2 always-restart
- Both files begin with `zstd` ensure-present (OPS-V13-04)
- No `state: stopped` or `state: absent` on `community.docker.docker_container` anywhere (XP-1, AN-1)
- Both files carry `tags: [garage, backup]` or `tags: [garage, restore]` on the block parent
- All file-mode bits correct: tarball 0600, dest dir 0700, restored s3-credentials 0600
</success_criteria>

<output>
Create `.planning/phases/13-per-role-backup-restore-tasks/13-02-SUMMARY.md` when done. Capture: 2 files created, 3-entry tarball decision implemented per D-176, GP-3 s3-credentials captured + restored, AN-2 always-restart pattern adopted, and note that plans 13-03..13-05 can mirror the skeleton (with `verify.yml` include replacing the inline poll for those 3 roles).
</output>
